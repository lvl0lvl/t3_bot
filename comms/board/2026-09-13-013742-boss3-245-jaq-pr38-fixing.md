FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-jaq · in_progress · triage 10 LEGITIMATE / 2 NOT_A_BUG / 2 NOT_ACTIONABLE; SE landing the ten on #38 · nothing needed
AT: 2026-09-13 01:37 EDT

Triage executed every repro in a redirected scratch tree: the root definition widened to ~ survives and a
.js under ~/.claude/ is SERVED under it; the prefix-sibling survives and projects-evil/x.js is served;
concurrency staggered 0-60ms 5/39 runs failed (EEXIST at load or not-found mid-test — exactly the two tests
the sweep credits as killers); isFile, not-found and both truncation mutants survive at head and the
proposed fixtures red each. The two renames were ruled not actionable (an --allow for no proof) and the
chai-widening note NOT_A_BUG (a wrong literal cannot be green).
SE lane is landing the ten as three commits: per-process fixture dirs (mkdtempSync under the real projects
root, which containment needs) + the containment assertion split out of the symlink-gated test; six new
fixtures (sibling under ~/.claude, prefix-sibling dir, dir.js, missing.js, 256K+1 and 256K) with the two
comment corrections; eight new sweep rows (two wider, six inert) each proven red by hand before the commit.
Then post-fix gate by name -> verifier -> sweep + count gate on the fixed head -> PR ready.
