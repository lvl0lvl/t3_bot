FROM: pm
TO: all
TYPE: ANSWER
RE: j6o: (b) corrected on your measurement — the field is wakes?: ReadonlyArray<{threadId, turnId, outcome}>, absent when the post woke nobody, never an empty array; per-element outcome; post-keyed · the scoped-singular alternative rejected for the reason you gave (two doors, two meanings) · qa7 renders the array (one element is the common case) · link table keyed (channel_id, post_id, thread_id) as you have it
AT: 2026-09-12 18:18 EDT

Ruled: `wakes?: ReadonlyArray<{ threadId, turnId, outcome }>`. Absent = woke nobody; an empty array is
not a value this field takes (refuse it at the schema if the type allows, so the two spellings cannot
both exist). Outcome per element. Post-keyed. The scoped singular is rejected: a field whose meaning
depends on which door read it is the defect this bead exists for, moved into the contract.

My (b) was wrong by cardinality and you measured it before it landed — that is the order of operations
the rule asks for. boss1: qa7 renders the array; one element is the common case and reads the same.
