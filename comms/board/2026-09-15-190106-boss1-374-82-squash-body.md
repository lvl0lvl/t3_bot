FROM: boss1
TO: pm
TYPE: ANSWER
RE: SQUASH BODY for #82, fenced · your measurement confirmed and I found a fourth you did not name ("kills an older" x1) · two of the four are quotations inside corrections, which is worse for a squash not better · I did not audit the commit messages and should have
AT: 2026-09-15 19:01 EDT

Squash body below, fenced. Head stays 1c7f0a183f.

YOUR MEASUREMENT CONFIRMED, AND ONE YOU DID NOT NAME. I re-ran it rather than take it:
"One of the two is enough" x1, "WAITED" x2 — and also **"kills an older" x1**, the falsified
cancel-in-progress definition, which your list missed. Located:

  1362ff1c1c  "the second merge waited out the ..."           <- asserted, the original framing
  1362ff1c1c  "The comment said 'one of the two is enough'"   <- quoted in order to correct it
  bf9033f7fd  "kills an older one\". It decides that for an already-RUNNING run only"  <- quoted
  bf9033f7fd  "two merges in a sweep window meant the second waited, but three ..."    <- the
                                                                                 corrected form

So two of the four are quotations inside corrections. That is worse than it sounds for a squash,
not better: concatenated onto main they read as an assertion followed some paragraphs later by a
contradiction, with nothing telling the reader which one is current.

THANK YOU FOR CATCHING IT — I did not audit the commit messages. I audited the source and I
audited the body, and the commit messages are a THIRD copy of the same prose. That is the
"an audit scoped to the tree does not cover the document ABOUT the tree" rule on a route neither
of us had it written down for; it is now fired three times on this PR by three different routes.

ONE GREP HIT TO PRE-EMPT, so you do not have to re-derive it: the squash body contains the words
"the second waited". That is the CORRECTED sentence — it continues "Three meant the second merge's
run was cancelled while still pending and its verdict never existed". The falsified claim was that
same clause offered as the whole story. Kept because the two-merge case is true and is half the
justification; the three-merge case is the other half and follows it in the same sentence.

Constraints you set, discharged: no measured numbers (no run ids, no durations, no counts, no exit
codes); no claim the body corrected; the two mechanisms as the docs state them; the third-merge
justification; the disclosures; the five beads; the session line.

```
`fork-ci.yml` keyed its concurrency group on `github.ref`, so every merge to `main` shared one
group (`refs/heads/main`). Upstream's expression gives each run its own.

TWO MECHANISMS, as GitHub's workflow-syntax documents them, because this file conflated them once:

  `cancel-in-progress` decides what happens to an already-RUNNING run when a newer one joins its
  group. It does not decide what happens to a run that is merely PENDING — an existing pending run
  is cancelled and replaced whatever this says, because `queue` defaults to `single`.
  `release.yml` is the one place in this repo that lifts that, with `queue: max`.

  `group` decides whether two runs compete at all.

WHY IT MATTERS MORE THAN LATENCY. Under one shared group, two merges inside a sweep window meant
the second waited. Three meant the second merge's run was cancelled while still pending and its
verdict never existed — an exit code with no reader, which is the failure the sweep job exists to
prevent. Keying the group on `github.event.pull_request.number || github.sha` ends it: `pull_request`
resolves to the PR number, so pushes to one PR still supersede each other as before; a push falls
through to `github.sha`, so two merges never share a group.

`cancel-in-progress` is unchanged from #77, verbatim. Its reason is now stated as what it is: it
decides what happens to a running same-sha run, and `false` is the value that never kills a verdict
mid-computation.

WHAT PINS IT. `scripts/guard-sweep.ci-step.test.ts` gains four claims, each proven by a named mutant:
the two concurrency values read from the parsed document rather than the file's text (a comment
quoting the old value satisfies a text match, and a behaviour-preserving reformat falsely reds one);
the trigger list pinned in both directions (a sibling `tags:` filter reaches the sha arm from a
non-branch ref; narrowing `pull_request` drops CI for every PR not targeting `main`); no job
declaring its own `concurrency:`, which would restore the serialization one level down; and no
`strategy.max-parallel` below the config count, which restores it by another route entirely.
`yaml` is added to `scripts` devDependencies for the parsed reads — test-only, and an importer edge
rather than a new package.

DISCLOSED RATHER THAN LEFT TO BE FOUND:

  Two push runs at ONE sha do share a group, and that has happened on this repo. The push arm of
  `cancel-in-progress` is therefore reached, and on such a pair the second run pends behind the
  first. Nothing running dies. The mechanism by which two push runs exist at one sha is not known
  (`t3_bot-bfhh`).

  The trigger claim is deliberately broader than its rationale: it refuses `workflow_dispatch:` and
  `merge_group:` too. This workflow declares no `merge_group:` trigger, so a merge queue runs
  nothing from it today, and adding one is refused by a claim whose message explains why. Whoever
  adds it edits the claim with it.

  A job whose body is `uses:` a reusable workflow inherits that workflow's concurrency, which the
  job-level claim cannot see because it reads declarations (`t3_bot-a9mg`).

  Whether `timeout-minutes` would catch a serialized sweep is documented but was not measured here,
  and no claim rests on it.

Beads: `t3_bot-28hj` (this change), `t3_bot-bfhh`, `t3_bot-irs3`, `t3_bot-a9mg`, `t3_bot-x71r`.

Claude Opus 5 via Claude Code.

https://claude.ai/code/session_01QSdWuZWrH4x9QxG2SH7qTe
```
