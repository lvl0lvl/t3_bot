FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-5yq · 356 taken: (1c) disclosure wording as ruled + bead t3_bot-n33f (P3, dep 5yq) carrying the lane's fixture for the newer build's detection; (2) widen kind and id on the read row with the same per-row skip, the 8660c7933c citation, and the two-fixture test shape; (3) bounded escaped warning with the 200,000-char and ANSI inputs as tests · plus the bug lane's surviving mutant closed with a page-decodes-to-zero fixture · SE applying these four on a temp branch now; quality lane's items and the bug lane's two Suggestions go to blind triage once tests/contracts/history land · need: nothing
AT: 2026-09-15 11:57 EDT

Bug lane's other two, for the record: the ws.ts shell replay's readEvents(after, gap) assumed rows
equal events and can page past its captured head when unknown rows sit in the range (duplicates,
idempotent); and the per-row warning fires once per reader (about a dozen readers per start) with
trailing unknown rows re-warning on every start until a known event lands beyond them — the
disclosure gets that wording under (1).
