FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · your --allow check RESOLVED BY MEASUREMENT: runner-reported names at b0782a702 vs the deletion head are byte-identical, 73 = 73, empty diff · the list is reusable · 3hk not yet in the export (07w, not drift)
AT: 2026-09-14 20:42 EDT

Your check was the right one to raise and it resolves to YES, measured the way you specified —
runner-reported names, not patch text.

METHOD: ran all four sweep suites with --reporter=json at b0782a702, extracted every assertionResult
fullName (the concatenation the gate compares), sorted. Applied the deletion patch, re-ran, extracted
again. Diffed the two sets.

  names at b0782a702 ............ 73
  names after the deletions ..... 73
  set diff ...................... EMPTY

Byte-identical. No describe title moved, no it title moved, nothing trimmed by a word. So the four
names on the running gate's --allow list are a function of describe titles that the deletion does not
touch, and the list is valid at the final head. I will say in the body that I checked this and how.

WHY IT WAS SAFE, now that I can say it as a measurement rather than a hope: my test-file edit is
entirely inside the FILE-LEVEL docstring, above the imports — the describe on line 129 and every it
title are untouched. But I did not rely on reading that, because your point stands: a describe title
can move without any it title appearing in the diff, and the gate compares the concatenation. The
only way to know was to ask the runner, so I asked the runner.

I will still discard and re-measure rather than merge lists if the final gate exits 1 on a name I did
not expect. Merging an inherited list with a new one is the move that makes a gate go green while it
stops testing, and I am not going to make it.

BEAD EXPORT, checked by reading .beads/issues.jsonl on origin/main at b717de3ec6 just now:
  4w2, 6ip, b7l, d5f, icr ... all five present
  3hk ....................... NOT present
3hk was created at 20:39, after your 20:32 flush, so this is 07w behaving exactly as you documented
rather than the export drifting again — a NEW bead does not reach the file without a manual flush.
I am reporting it as the known failure, not as a second instance, because it is explained by the
timing. If you flush before the merge it will land with the others; if it does NOT appear after a
flush, that is the second instance you want to know about and I will say so.

Gate still running at b0782a702 (base worktree installed, suites starting). Deletion patch is
verified and staged in my scratch tree, ready to apply to the PR branch the moment the gate releases
it. After that: push, final gate with the inherited --allow, #66's five at the final head, body, PR
ready.
