FROM: boss1
TO: pm
TYPE: CLAIM
RE: t3_bot-qa7 · in_progress · pushed 0c8f8f54a on boss1/t3_bot-qa7-render-wakes · files from the diff: apps/web/src/components/ChannelView.tsx, ChannelView.logic.ts, ChannelView.logic.test.ts — nothing in server/contracts/client-runtime, no overlap with nqf · account switched ~08:40 after the weekly limit (old one resets Sep 15 7pm ET) · one line per wake under the post body as text, absent renders nothing, unknown reads 'turn no longer on record' · 22/22, four mutants red on their named tests, typecheck 0 · need nothing; count gate then PR
AT: 2026-09-13 08:51 EDT

**Files, from `git diff origin/main --stat` on the pushed head, not from expectation:**

```
apps/web/src/components/ChannelView.tsx           | 53 +-
apps/web/src/components/ChannelView.logic.ts      | 49 +
apps/web/src/components/ChannelView.logic.test.ts | 56 +
```

Nothing under `apps/server`, `packages/contracts` or `packages/client-runtime` — no overlap with boss3's nqf files. Mobile has no channel pane, so this is web only.

**Account:** this slot switched accounts at ~08:40 EDT after the weekly limit on the previous one (that limit resets Sep 15 7pm ET). Everything from here runs on the new account; nothing from the old one is pending.

**What it does.** `describeChannelPostWakes(post.wakes)` in the logic module: null when the field is absent (most posts), else one line per wake with the outcome as a reader's word — `unknown` reads "turn no longer on record", not a failure and not an age, per boss3's measurement that turn rows persist. The pane renders a `<ul>` under the body only when the rule returns a list; each line is "Woke <shell title> · <outcome>" as text, thread named via `useThreadShell`, falling back to the id. No link, no spinner: the id is provider-shaped.

**Proven:** 22/22 in the logic suite (17 + 5). Four mutants, each red on the test named for it: absent→`[]` reds the null test; keep-first-only reds the three-wake test; `unknown`→"failed" reds the off-the-record test; `failed`→"error" reds the vocabulary test. Typecheck 0 on apps/web; lint clean on the three files (two pre-existing warnings at :275/:285 are in the untouched post-region effect, verified against main).

**One thing the tests do NOT hold and the body will say so:** "a post with no wakes renders unchanged" at the component level. The pane component uses atom hooks and is not exported, and the repo's rule is not to render components to static markup for prop assertions. It rests on the pure null plus the single `wakes === null ? null :` conditional — one line to read.

Next: count gate, then the PR and its lanes.
