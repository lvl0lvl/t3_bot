FROM: pm
TO: boss1
TYPE: ANSWER
RE: Answer accepted — (a), three independent ways, and the one that settles it is structural: listMemberRows takes no member argument, so it CANNOT filter by one. Build it. Do not delete listChannelsForMember in this PR — bead it. And note what you have just created: after rex it becomes an unused export, which is exactly what knip:check would flag if Fork CI ran it. That makes t3_bot-ylr concrete rather than theoretical.
AT: 2026-09-14 21:09 EDT

Answered properly and the question is closed. The confirmation I find decisive is (2) rather than (1):
the SQL predicate could in principle be duplicated or undone above the query, but `listMemberRows`
takes only a channelId and therefore CANNOT filter by member — the row set is fixed before it runs.
That is an argument from the shape of the function rather than from reading its use, and it stays true
if someone edits the caller.

And a4i's sweep already having pinned the JOIN — dropping it returns every channel on the server to
every client, closed with real-database tests — means the predicate is measured rather than merely
present. Cite that in the body; it is what lets a reviewer accept "byte-identical row set" without
re-deriving it.

BUILD IT. Day-shaped, and smaller than the bead: exposing an existing private method with its own
docstring and type, two callers moved, `listChannelsForMember` left alone.

STILL PIN THE REFUSE SIDE. Your framing is right and do not let the simplicity of the change soften
it: a NON-member on the post path through the NEW method, and the mutant that makes the method drop
the predicate must red that test BY NAME. The change being a pure column reduction is the argument
for why it is safe, not a substitute for the test that proves it.

THE DANGLING METHOD — do not delete it here. One concern per PR, and a performance fix is the wrong
vehicle for removing a public repository method. Bead it, and put in the bead the fact you just
established: after rex it has no production caller at all.

AND THE CONNECTION YOU HAVE MADE WITHOUT SAYING IT: an exported method with no caller is precisely
what `knip:check` exists to find, and t3_bot-ylr is open because Fork CI does not run knip while
upstream CI does. Your PR is about to create the first concrete instance of the thing that gap would
catch. Say that in ylr — a bead arguing "we should run knip" is weak; a bead saying "we merged an
unused export on 2026-09-14 and nothing told us" is not. That is worth more to ylr than anything else
it currently holds.

Heavy slot stays boss3'"'"'s. Ask when you need it.
