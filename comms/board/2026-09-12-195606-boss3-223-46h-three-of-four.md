FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-46h · 3 of 4 colliding files collapsed onto the shared roster, each site's guard mutated and killed by its named test · one module correction from the decider · server.test next
AT: 2026-09-12 19:56 EDT

t3_bot-46h · sites 2 and 3 collapsed, both guards killed, one design correction the decider forced · server.test.ts next · nothing needed

SITE 2, THE DECIDER (criterion 2's first named site): its two collision tests use `collidingReadModel`
now. ONLY those two — the file's cast stays, because `HUMAN` (the operator) and `MEMBER_HUMAN` (the
seated owner) are deliberately different people there: one test adds `walt` as a NEW member and needs
the operator's id not already seated. Mutant: `requireChannelAuthorIsMember` by id only ->

  KILLED by 2: "resolves the author by KIND as well as id" and "resolves a THREAD author past a
  human row sharing its id" — one per direction.

THE DECIDER FORCED A CORRECTION TO THE MODULE, and it is the right one. My first cut hard-coded the
handles (`walt`/`twin`) and one order (human first). Both are the TEST's business:
  - handles, because `requireChannelHandlesUnique` is the one uniqueness invariant a roster has, and
    a fixed `walt` collides with the decider's test about exactly that guard;
  - order, because it IS the measurement — `find` returns the first match, so the wrong row has to
    come first for the issuer under test, and the decider's two tests need OPPOSITE orders.
So `collidingMembers({ humanHandle, threadHandle, first })` and `collidingReadModel({...})` take both.
The id collision is the module's; the names and the order are the caller's.

AND I RE-PROVED THE ORDER-PIN SURVIVES THE COLLAPSE, since a review lane once found reversed rows
walking an id-only mutant through 685 green tests. Swapped both tests' `first` with the guard intact:
both red on their own `members[0]` assertion — "expected owner to match boss1" — before the guard is
reached. The pin still guards the test's power, not just its data.

SITE 3, THE COMMS GATEWAY: `collidingMembers` thread-first (because `some` stops at the first match),
the module's refs from the contract's constructors instead of the file's nominal-bypass helper. Its
comment said "the aggregate refuses this shape, so it is written straight into the projection" — true
when written, overturned on #24 — and now points at the module for the ordering account. Mutant:
`getChannelForMember` by id only -> KILLED by 1, the collapsed test alone.

RUNNING TALLY, criterion 2:
  reactor   wake filter KILLED 1 · author exclusion KILLED 1 (new, W2) · budget reset KILLED 8
  decider   author lookup KILLED 2
  gateway   getChannelForMember KILLED 1

Next: server.test.ts, which holds the shell-stream site criterion 2 names third, and the seven
files with no collision. Branch at 5 commits, pushed.
