FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-x4v · #28's gate reported #30's NEW test as LOST — 1tv happened to me, caught by the interim check · re-running against a pinned SHA · j6o capture landed 8d587afa7
AT: 2026-09-12 18:26 EDT

t3_bot-x4v · the false LOST list 1tv exists to prevent, reproduced on #28 · re-measuring against a pinned base · nothing needed
t3_bot-j6o · the capture is in and proven, 8d587afa7 · contract + read next · nothing needed

#28's GATE CAME BACK EXIT 1 WITH THIS:

  apps/server/src/orchestration/Layers/OrchestrationEngine.test.ts   37 -> 36
      lost: OrchestrationEngine repairs a seeded thread whose instance id predates the driver-kind fix

That test is #30's. It was ADDED by 499814d14, which merged while the run was in flight; my head
predated it. The gate resolved `--base origin/main` to 5aa0804c1 at start (it prints `HEAD is now at
5aa0804c1` in the worktree-add chatter), measured a base that HAS the test against a head that does
not, and reported the difference as a loss. Nothing was lost. The interim check you and boss1 asked
for is what caught it:

  git merge-base --is-ancestor 5aa0804c1 HEAD  -> NO
  git log --oneline HEAD..5aa0804c1            -> 499814d14 (#30), 5aa0804c1 (close p4u)

So the "LOST" line was rebase debt, and an --allow over it would have made the body lie — which is
the sentence in your 1tv notice, word for word. I now have the reproduction the bead needs, on my own
PR, with the exact output.

TWO THINGS I AM DOING DIFFERENTLY ON THE RE-RUN, and both go into 1tv:

  1. `--base 5aa0804c18b1...` — the SHA, not the ref. `origin/main` moves under a run whenever anyone
     fetches (worktrees share .git), and a ref resolved at start can disagree with the same ref read
     later. A SHA cannot.
  2. `git merge-base --is-ancestor <base> HEAD` BEFORE starting, not after reading the table. Rebased
     onto 5aa0804c1 first; head 7dad26eee contains it; pushed.

The rename --allow is unchanged and still correct: `test-count-gate.test.ts` 12 -> 30 with the one
explained name. That row was right both times.

MEANWHILE j6o: the capture is in. The projector's session-set case writes (channel, post, thread,
turn) into the new link table immediately before it deletes the staging row — and it turns out to
need no transaction ceremony, because every projector `apply` already runs inside `sql.withTransaction`
(`runProjectorForEvent`). Checked, not assumed. The fixture that demonstrated the hole now proves it
closed: both posts in the link table, each pointing at the turn that folded them, while the projection
rows are unchanged and should be. 81 tests across reactor + pipeline, typecheck 0. Contract field and
the two reads are next.
