FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-9wj · 4 of 6 lanes read whole (contracts/sec/qual/hist) · security CRITICAL: gate exits 0 when the runner measured ZERO tests · the rule's count premise does not fit its own incident — 11 tests before, 11 after, the RENAME is the catch · scope ruling applied, fixes staged in 4 stages, waiting on bugs+tests
AT: 2026-09-12 13:54 EDT

t3_bot-9wj · PR #26 · FOUR lanes read whole, fix staged in four stages · waiting on bugs + tests · nothing needed

YOUR RULING IS TAKEN, both halves. Default scope is now the whole repo — and the default was worse
than "apps/server" sounds: the runner treats that positional as a FILTER, not a directory, so it was
a substring match that happened to look like a scope. The new default passes no filter at all, which
is the runner's own "everything". The table's first line reads "measured the whole repo against
<base>", or the narrowing when one is set.

READ WHOLE: contracts (8), security (5), quality (11), history (6). Bugs and tests are still out.

THE SECURITY LANE'S CRITICAL IS THE SAME HOLE AS MY PROBE, one door further in. SEC-26-01: the gate
exits 0 when the runner measured ZERO tests. A filter matching nothing gives valid JSON with an empty
testResults, `success: false`, exit 1 — three signals, and I read none of them. The lane replayed that
payload on both sides against a base that really had tests and got "No unexplained decrease, and no
test name lost." EXIT=0. My own line 77 states the principle it violates: "a suite that cannot RUN is
not a suite with zero tests." I guarded one form of it and not the other two.

WHAT THE LANES FOUND THAT I WOULD NOT HAVE:

  - MY BLIND-SPOT SECTION IS DISPROVEN BY ITS OWN EXAMPLE. The docstring says the gate cannot see an
    assertion removed from a test that still exists, and offers #13 as proof. Two lanes measured the
    actual commit: the block was RENAMED, so the name half flags it. The gate catches the example I
    gave as proof that it cannot. Worse, my own commit message says the opposite of my docstring.
  - THE RULE'S HEADLINE DOES NOT FIT THE INCIDENT IT CITES, and I built on the headline without
    checking. commsLive.integration.test.ts ran ELEVEN tests before bb3b61bb3 and ELEVEN after. The
    count never moved. A pure count gate — which is what the rule's sentence and the bead both ask
    for — would have passed the incident the rule exists for, clean. What catches it is the one
    changed NAME. That is a better argument for the name half than the anecdote I told, and it is a
    measurement rather than a story, so it replaces the anecdote.
  - I INVERTED TWO INCIDENTS. I wrote that the split-into-three prompted the rule. The rule was
    written at 08:02 citing #13's range-replace from 08:01; the split was found at 10:30, 2h28m
    later. Both are real, neither caused the other, and I credited the later one as the earlier's
    cause — in the PR body and in the tree.
  - "SIX HOURS" WAS 3h14m. Twice, in two files.
  - THE COUNT CLAUSE OF isRegression IS INERT. A lane deleted `row.after < row.before` and all six
    tests stayed green. The only input that reds it is a REPEATED test name — which is also a hole:
    names were compared as a SET, so a surviving twin masked a deleted one and base ["a","a","b"] to
    head ["a","b","c"] passed silently. Now a multiset, with the fixture that reds the clause.
  - MY LOGGER PRECEDENT IS NOT THAT PRECEDENT. I cited resolve-previous-release-tag.ts as writing a
    table to the stream for the same reason. It writes one key=value line for a machine to read and
    says nothing about logger formatting. The mechanism matched; the reason, which was the whole
    claim, did not.
  - THE TWELVE STANDING FAILURES: I asserted the symlink as their cause. Board 138 lists three
    suspected factors and narrowed to none. Softened to what the record supports.
  - "CI unavailable: billing" is stale on my own PR — Fork CI ran on e8e19d28c exactly and passed
    all four jobs at 17:28Z.

STAGED IN FOUR STAGES, none applied: the header rewritten (scope, the two incidents kept distinct,
the true floor — a test rewritten UNDER THE SAME NAME — and the exit codes); the parse seam extracted
as an exported pure `toSuite` so its refusals are testable without two suite runs; fail-closed on all
three measured-nothing signals; multiset comparison; the worktree leak and the stale registration in
the developer's own repo; unknown options, `--base` with no value, and an EMPTY `--allow` reason all
refused; per-test-name allow; import.meta.main; process.exitCode instead of process.exit (which can
truncate the table when stdout is a pipe, and this tool exists to be piped into a PR body).

FOUR NEW TESTS STAGED, including the two you asked for in both directions: a broken file in HEAD and
a broken file in BASE both refuse with the file named. One parser serves both revisions, so one guard
covers both — I have written that plainly in the test rather than implying two code paths, and the
second test exists so a future refactor that splits the parse must red.

ONE THING FROM THE HISTORY LANE YOU SHOULD KNOW, because it is mine and it touches every lane I ran
today: MY DISPATCH QUOTED A SUPERSEDED VERSION OF THE RULE. My worktree sits on the e60 branch, which
predates bd3939da8's 11:17 correction, so the CLAUDE.md text I pasted into six briefs was the old
one. The lanes worked against origin/main's current text and said so. No finding changes. But anyone
briefing from an old worktree inherits the wrong sentence, and I did.

Applying after bugs and tests land, then rebase onto c611bfb3f, re-run, CI, "PR ready #26".
