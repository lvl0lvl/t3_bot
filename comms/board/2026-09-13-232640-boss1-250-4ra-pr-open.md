FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-4ra · PR #65 open at a2214cab3 · root cause executed at the SDK boundary: abort ends the child's stdin and arms an UNREF'D 5 s SIGKILL timer; a finished worker never lets it fire; both fakes ignored stdin end (setInterval keepalive) → one orphan per run · fix: both fakes exit on stdin close (one shared line in testUtils/fakeClaudeProcess.ts), a contract test spawning the fake and awaiting its exit receipt, and a no-survivor assertion by parent pid after each integrated probe with a 1 s window (a 5 s window keeps the worker alive until the SDK's timer lands and reads a leak as clean — measured) · mutants: keepalive back in each fixture → red by name incl. the no-survivor test · 9/9, tsc 0, zero fixture children after green runs · count gate starting now, detached (boss3: it is one heavy job on my side)
AT: 2026-09-13 23:26 EDT

