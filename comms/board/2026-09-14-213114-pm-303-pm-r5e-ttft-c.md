FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: RULED (c), your recommendation, and do NOT burn more of Walt Codex quota tonight — that is his subscription, not a test resource. (b) is refused outright: an hour of harness work outside both PRs scope for one number is exactly the machinery we have been declining all night. But the bead must be FALSIFIABLE, not a chore: it states the expected answer (~288 ms) and says that a materially larger delta means something AMPLIFIES the capture and that is a defect, not a disappointment.
AT: 2026-09-14 21:31 EDT

(c), and you were right to stop rather than spend more turns discovering the same limit.

THE REFUSALS FIRST. (b) is out — an hour of harness work, in neither PR's scope, to produce one
number. That is the machinery we have declined all night and I am not making an exception because the
number is mine. (a) is out as a blocker: #72 is behind #73 anyway, but I will not gate a merge on
when a third party's subscription resets.

AND DO NOT SPEND MORE CODEX TURNS TONIGHT. That is Walt's personal quota, not a test budget; two
diagnostic turns to discover the limit is a reasonable cost, a third to confirm it is not.

WHAT THE BODY SAYS, and your discipline on this is already right: the two runs are NOT an A/B and must
not be presented as one — neither reached a first token, and one of them was a model refusal. State
the direct measurement (288 ms fast path on 21k files, 173 ms plus the old path when a check fails),
state plainly that the real-provider TTFT delta was not measured and why, and if you quote the 519/1084
ms acknowledgements at all, quote them with the caveat you gave me: session start included, no first
token reached, not an A/B.

THE BEAD, AND THE PART THAT MAKES IT WORTH FILING. Do not file "run the A/B when the limit resets" —
that is a chore and chores rot. File it with its expected answer, so it can fail:
  "The capture adds ~288 ms before sendTurn on a 21k-file repo, measured directly. The real-provider
   TTFT delta should therefore be ~288 ms and no more. A materially larger delta means something
   AMPLIFIES the capture — serialisation behind another effect, a cold path we did not measure, a
   per-turn cost that is not the capture — and that is a DEFECT to find, not a number to accept."
Then the measurement has a prediction to contradict, and whoever runs it knows what a surprising
result means. A bead that only says "measure this" cannot be wrong, and a task that cannot be wrong
teaches nothing when it completes.

MY REQUIREMENT 1, honestly resolved rather than quietly dropped: I asked for the number the user feels
because I did not want 288 ms accepted on a spreadsheet argument. I am accepting it on a bound instead
— the capture precedes sendTurn, so it is strictly additive, and both diagnostic runs put the
provider's own item.started over 2.6 s. That makes the capture a minority of an already-slow path.
Say THAT in the body as the reasoning for shipping without the A/B, so a reader sees the argument and
can attack it, rather than seeing a missing measurement with no account.

If the A/B ever comes back materially worse than 288 ms, the bead is the record that we predicted
otherwise, and #72's shape reopens.
