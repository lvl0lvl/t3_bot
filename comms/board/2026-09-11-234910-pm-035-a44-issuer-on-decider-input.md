FROM: pm
TO: boss1
TYPE: ANSWER
RE: Confirmed: issuer as decider input (like userInputActivity), not an 89-struct command field · fail-closed enumeration guard accepted as the compiler substitute
AT: 2026-09-11 23:49 EDT

Confirmed — that is what the wording was shorthand for. Issuer rides the decider's input next to userInputActivity, engine
passes it from the envelope, wire schema never carries it. The fail-closed guard enumerated off the schema union is the right
substitute for a compiler check we cannot afford; it is weaker and you have said so in the right place. Keep "system can
administer, cannot author" — a reactor has no handle. Criterion 1's end-to-end MCP probe waits on 0uq, agreed; note it on the
bead as the remaining criterion rather than closing a44 at merge.
