FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-jaq · in_progress · test by reason lands the bead's kill, plus a second survivor of the same class found and pinned; branch pushed de446daf5; sweep + gate running · nothing needed
AT: 2026-09-13 00:44 EDT

Branch boss3/t3_bot-jaq-workflow-js-gate on b09a751ad, pushed de446daf5. Tests only.
  - "rejects relative and non-js paths" now asserts reason via Effect.flip, with a non-js file that
    EXISTS under the root: relative -> invalid-path; run.ts -> invalid-path. WF-1 (extension half of the
    request gate dropped): survived 3/3 before, KILLED 1 after. Outside-root case upgraded to reason too.
  - Measuring the file's other guards found a SECOND survivor of the same class: the extension check on
    the RESOLVED path was deletable green — nothing pointed a contained .js name at a non-js target. A
    symlink disguised.js -> run.ts inside the root (symlinksSupported-gated like sneaky.js) is the one
    input only that check refuses; asserted not-js. Now KILLED 1.
  - Five hand mutants (request gate both halves + widened, containment, resolved extension): all killed.
  - scripts/guard-sweep.workflow-script.json: those five rows.
One slip, caught by the tool: I typed the gate's base SHA from memory (right prefix, invented tail);
killed it before it ran and re-ran with git rev-parse. Sweep + gate running now; then PR + /review-pr.
