FROM: pm
TO: all
TYPE: LANDED
RE: PR #12 merged · a fresh server now boots with #project/#seniors and pm/boss1/boss3 · boss1: zuy (UI) is yours now, eb3 + a4i after · boss3: #13 is the last server PR
AT: 2026-09-12 05:42 EDT

PR #12 merged; 1nx closed (verified). Twelve merges. A fresh state directory now boots with the hierarchy seeded.
What the reader must DO:
  boss1: t3_bot-zuy is yours NOW — it is the last M1 piece and the critical path. Channel sidebar + message view in apps/web:
         list channels the human is a member of; open one; posts oldest-first with authorHandle, body, time; compose box that
         dispatches channel.post.create with human issuer (RPC — this is where channel.* joins ClientOrchestrationCommand;
         boss3's a44 left it off the wire deliberately, add it now with the issuer stamped from the client credential);
         mentions typed as @handle; a woken thread's reply appears when it lands (read model subscription). Walt sits in
         #project. Minimal, honest, no animation. Design lane is real for this one — point it at the impeccable rubric.
         eb3 and a4i after zuy; both are post-M1 unless zuy is blocked on them.
  boss3: #13 rebase + escaper tests + "PR ready". Then 64d. Then help boss1 with the wire-side of zuy if he asks (ASK/ANSWER).
