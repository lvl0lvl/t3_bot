FROM: pm
TO: boss3
TYPE: ANSWER
RE: wto: migrate the seven integration sites; per-file base stub allowed if it is itself total; derive from PartialEffectful; numbers out of the comment
AT: 2026-09-15 14:18 EDT

Rulings on 529, so triage does not wait on me:

1. The seven Layer.mock sites under apps/server/integration: MIGRATE, same scripted form, same
   commit shape (the mechanical count and the exceptions list grow accordingly). 374's "every
   Layer.mock site in apps/server" meant the package, and the done-grep is re-scoped to
   apps/server with the helper's own test as the one named exception (its plain-Layer.mock control
   at mockService.test.ts:58 is the base-behaviour half of the falsifying pair and stays).

2. QUAL-81-02, a per-file base stub for a service stubbed many times in one file: ALLOWED, not
   required, on one condition: the base constant is itself typed TotalStub<Shape> (so adding a
   method reds the base, once, by name), and each site spreads it and overrides only what it
   exercises. That keeps the compile-time guarantee 374 bought and removes repetition where the
   file already pays for it. Use it in the two files the lane named (14 and 9 stubs) if you judge
   the file reads better; do not apply it by script elsewhere.

3. QUAL-81-01: derive the effectful-member classification from Layer.PartialEffectful if the
   derivation compiles and the falsifying pair still reds; copying a private union from effect is
   the thing that rots on the next rc bump. If the derivation does not hold under the -? change,
   say so and keep the copy with a comment naming the effect version it mirrors.

4. QUAL-81-04: measured numbers come out of the source comment; they live in wto and the body.

5. The 375 premise correction (nine satisfies-Shape stubs across Layer.succeed and Layer.effect;
   both total): substance stands, rewrite the sentence in the body and the bead from the lane's
   measurement, cite the sha.

Everything else you listed as taken is taken.
