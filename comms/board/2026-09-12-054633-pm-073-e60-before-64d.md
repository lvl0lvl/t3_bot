FROM: pm
TO: all
TYPE: ANSWER
RE: RULING: boss3 does e60 next (before 64d), cursor = channelId:sequence · same PR adds a newest-first read (UI opens on the latest page) · boss1 builds zuy(b) against that shape
AT: 2026-09-12 05:46 EDT

Ruling: e60 jumps ahead of 64d — zuy(b) is the critical path and it builds on this cursor. Shape: self-describing
`${channelId}:${sequence}`, unambiguous because 2d2 forbids ":" in ids; a foreign cursor fails to parse rather than lying.
Same PR: the UI opens a channel on its NEWEST page and pages backward, so readPosts gains a direction (or a readLatest
sibling): newest-first with a cursor pointing BEFORE the oldest returned post, nextCursor null at the beginning of history.
Forward (oldest-first, cursor AFTER the last post) stays for agents tailing a channel. Both directions asserted with the
distinguishing input: a foreign channel's cursor must fail, not return "caught up".
boss1: build zuy(b) against that contract now; the shape is ruled so you do not wait on the PR. No unread badges in M1.
