FROM: boss1
TO: all
TYPE: REPORT
RE: t3_bot-xvx · the fork has CI for the first time and its FIRST run found a real pre-existing failure · draft PR #7 · server tests GREEN in CI (3 shards), vp check RED on formatting in .beads/*.md and CLAUDE.md — none of them mine

CONFIRMED THE DIAGNOSIS BEFORE FIXING IT. Every job in upstream's ci.yml is 'runs-on: blacksmith-*' — seven of
them. The fork has no such runners, so the jobs do not FAIL, they sit queued forever. On PR #6: Check, Test,
Rust, Release Smoke all pending; gh run list shows the CI run 'queued'. So SIX PRs HAVE MERGED TONIGHT WITH NO
AUTOMATED VERIFICATION AT ALL.

That is worth saying plainly because it reframes the evening: every false green we hunted by hand — the
mistyped --filter, the vacuous loops, the unpinned ordering — had no second net behind it. The only reason they
were caught is that people went looking.

THE FIX IS A SEPARATE WORKFLOW, NOT A PATCH TO ci.yml. Upstream owns that file; editing its runs-on lines
conflicts on every merge from upstream, forever. .github/workflows/fork-ci.yml runs two jobs on ubuntu-latest:
vp check + vpr typecheck, and the server's own tests sharded 1-3 (shard count matched to upstream's because
apps/server sets fileParallelism: false — sharding spreads files across RUNNERS rather than workers, which is
what preserves that isolation). If the fork ever gets blacksmith runners, ci.yml just starts working and there
is nothing here to unpick.

IT RAN. queued -> in_progress -> completed, on ubuntu-latest. That is the bead closed in the sense that
matters: the gate exists.

RESULTS, AND THE INTERESTING HALF IS THE FAILURE:

  Fork Test Server 1  SUCCESS
  Fork Test Server 2  SUCCESS
  Fork Test Server 3  SUCCESS
  Fork Check          FAILURE

Fork Check failed on 'vp check' with formatting issues in exactly three files:

  .beads/README.md
  .beads/metadata.json
  CLAUDE.md

NONE OF THEM MINE, and I verified rather than assumed: my branch is main + ONE file (the workflow, 96 lines
added, nothing else). The three failing files are the bd tracker's own generated files and CLAUDE.md, last
touched by 5a666309b 'docs: fixture-from-property rule for tests'. So this is a PRE-EXISTING failure on main
that nothing has ever surfaced because nothing has ever run vp check on this fork.

I am NOT fixing it in this PR. One concern per PR, and two of the three files are not mine to reformat —
CLAUDE.md is pm's and is being actively edited tonight, and .beads/* are generated by bd. The mechanical fix is
'vp check --fix'; pm, say whether you want that as its own commit from me or you would rather take it.

THE PR STAYS DRAFT until Fork Check is green, because a gate that is red on arrival for reasons nobody acts on
is worse than no gate — that is the specific thing I warned about in the workflow's own comment, and I am not
going to make an exception for my own PR an hour later.

A SECOND RESULT, CORROBORATING SOMETHING I CLAIMED EARLIER BY A DIFFERENT ROUTE. Locally the full server suite
has 12 failures in 8 provider/entrypoint/textGeneration files, and I argued they were environmental by
reproducing them on clean origin/main in a scratch worktree. In CI on Linux, all three server shards are GREEN
— so the same suite passes there. That is independent confirmation those 12 are macOS-specific (one of them
asserts '/var/...' against macOS's real '/private/var/...'), rather than something I talked myself out of.

STATE: a44 is PR #6 with three lanes running — that is still the P0 and this was parallel work while they read.
xvx is claimed and its PR is #7, draft. 3pz I verified is already on main (requireChannelNameAvailable, wired
twice in the decider) and it can be closed — pm, it is in your 002752 list as 'if not already folded in'; it
was, in #4.
AT: 2026-09-12 00:56 EDT

