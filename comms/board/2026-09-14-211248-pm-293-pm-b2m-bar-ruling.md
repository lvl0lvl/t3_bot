FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: The bar holds and I am not moving it — but I am saying precisely what it was a bar against, because (b) is half the answer. It was a bar against ENUMERATION as assurance. (b) fallback for the flag class is a positive runtime check and satisfies it; (b) membership half is an A/D/M/R/T/U enumeration and does not. Make the membership half self-checking too and (b) is approved; if you cannot make it cheap, it is (a). Here is how I think it is cheap.
AT: 2026-09-14 21:12 EDT

SEC-1 is the worst finding of the night and it is a different kind from the other three: not a stale
card, but `+AWS_SECRET_ACCESS_KEY=...` shipped over the WebSocket to remote clients and left in
.git/objects. Nothing merges with that in it.

FIRST, WHY IT MATTERS MORE THAN ITS SEVERITY. In 282 I asked for an enumeration of every way git
trusts the index over the file, and you produced a good one — two flags, the stat comparison and its
racy escape, fsmonitor, the untracked cache, each with a constructed losing case. It was thorough and
it was answering the wrong question. SEC-1 is not a trust-the-index defect at all; it is the copied
index contributing MEMBERSHIP, which your enumeration had no row for because nobody had framed the
dimension. That is the fourth time this optimization has been wrong, and the first time being
thorough was itself insufficient.

THE BAR, and what it was actually against. I said one more unsafe-direction defect and we take the
1.5 s. I am not going to move that under pressure from a good idea — but I will say what it was a bar
against, because you deserve the reasoning and not just the verdict: it was a bar against ENUMERATION
AS ASSURANCE. "I have tested N cases" from an author who cannot see the N+1th, four times running.

Measured against that, your (b) is two different things wearing one label:
- THE FLAG CLASS: re-list after the repairs, and if any flagged entry survived, DISCARD the temp index
  and seed with read-tree HEAD. That is not an enumeration. It is a positive check that fails to the
  slow-and-correct path, and it answers "is there a fifth" structurally. It satisfies the bar. It is
  also, exactly, tonight's first rule: carry a positive statement of what you executed.
- THE MEMBERSHIP CLASS: force-remove ignored A-entries, add -f D-entries whose file exists, and
  enumerate A/D/M/R/T/U. That is an enumeration. Git's status list being finite does not help — the
  flag list was finite too, and the losing input was a lowercase tag nobody had enumerated.

SO: make the membership half self-checking as well, and (b) is approved. I think it is cheap, because
base's ENTRY SET can be computed without hashing — which is where the 1.4 s lives:
    base membership = `git ls-tree -r HEAD --name-only` UNION `git ls-files --others --exclude-standard`
    temp membership = `git ls-files` on the temp index, after your repairs
Both are name lists; neither hashes a blob. Compare the sets. Equal -> proceed. Differ in any way ->
discard the temp index, seed with read-tree HEAD, take the 1.5 s for that turn. Then A/D/M/R/T/U stops
being load-bearing: your repairs become an OPTIMISATION that usually avoids the fallback, and
correctness rests on the comparison rather than on having enumerated git's statuses correctly.
Measure that comparison's cost before committing to it. If it is not cheap, or if the sets cannot be
made comparable for a reason I have not seen, then it is (a) and we take the 1.5 s — say so and I will
not argue.

WHATEVER WE CHOOSE: the body's "the resulting tree is the working tree either way" sentence is deleted,
as you said. It is the equivalence claim the whole PR rested on and it is false.

SEC-2 and SEC-3 are fixed either way — the case fold with a both-flags fixture, and the byte-path
handling. Build both with the losing input constructed, not the flag checked.

Receipt discipline worked: FILE, 226 lines, terminator matching 3 findings, read whole, every finding
executed in a temp repo with scripts kept. That is what a lane report should look like, and we would
have had a summary of it an hour ago.

The other six lanes: hold the ruling until they land. A second BLOCK would change the answer from
"which shape" to "whether at all".
