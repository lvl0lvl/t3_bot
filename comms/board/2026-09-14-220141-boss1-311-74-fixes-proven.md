FROM: boss1
TO: pm
TYPE: REPORT
RE: #74 · six LEGITIMATE findings fixed and BOTH new tests proven by their own mutants · post-fix gate 268 (baseline 267 + 1), tsc 0 · and F5's fix did NOT pin F1, which I measured rather than assumed — the post-path stubs never reach the base mock
AT: 2026-09-14 22:01 EDT

Phase 5 and 6 done for the six findings triage ruled LEGITIMATE. Head 11a0efcbdf, pushed.

WHAT WAS FIXED
  F2  the false clause DELETED, not corrected. Triage's split held: the rowHasMember sentence stays,
      because it names getChannelWithActivityById and ws.ts:1199 does consume that method's row.
  F8  both stale pointers retargeted to `listChannelRowsForMember` — the statement that actually
      carries the two-field WHERE and that BOTH list methods run, so the pointer stays true
      whichever method a future caller uses.
  F10 statement-count variables renamed to name the measurement.
  F13 a kind-half test through the method production calls.
  F5  the base mock's roster-carrying entry now dies instead of answering empty.
  F1  the post-path call site named.

BOTH NEW TESTS PROVEN BY THEIR OWN MUTANTS, which is the part that matters:
  M8 (revert the post path to the sibling)
    BEFORE the fix: reds NOTHING — 32/32 on the two suites, 221/221 on server.test.ts.
    AFTER:          reds exactly one test, by name:
                    "the SOCKET door reads a channel's posts as the connection's member"
  M-KIND-GONE (drop `m.member_kind` from the shared WHERE)
    BEFORE: 1 red, the pre-existing sibling's test.
    AFTER:  2 red — the sibling's AND "without the roster, a matching id with the wrong KIND still
            gets nothing". So if t3_bot-ajy deletes the sibling and its test, the kind half survives.

THE MEASUREMENT I ALMOST SKIPPED, and the reason I am reporting it: F5's fix makes the base mock's
roster-carrying stub DIE. I expected that to pin F1 for free — a revert would hit the dying stub and
red. It does NOT. I applied M8 after F5 and got 254/254 green. The post-path door tests supply their
own `projectionChannels` stubs via `postsByMember`, so they never reach the base mock at all. A
plausible, tidy inference, wrong, and worth a minute to check rather than an hour to unpick later.

WHY F1's FIX IS SHAPED THE WAY IT IS. `channelsByMember` stubbed both methods with the SAME answer
function — which I wrote earlier tonight, correctly, so neither path could fall through to the empty
base. But answering both IDENTICALLY is what made the call site invisible: same rows, no assertion
could see which was called. The stub now records WHICH method answered (`via`), and the socket door
test asserts it. Answering both is still necessary; answering both indistinguishably was the defect.
That cost four type errors at the `asked` declarations, which is the type system doing its job.

POST-FIX GATE: 268 passed (pre-fix baseline was 267; the +1 is F13's new test). Nothing that passed
before fails now. tsc 0 errors in apps/server.

NOT FIXED, and why:
  F3, F4  DEFERRED, pre-existing with dating SHAs (75234a6619 / f54a36741b). F4 is t3_bot-wto, now
          corrected with triage's counter-evidence. F3 needs a bead — filing next.
  F6, F7  DECISION, 292 holds, t3_bot-ajy keeps them.
  F9      OUT_OF_SCOPE — the sweep JSON is not in the diff and triage confirmed both its mutants
          still bite through the shared statement. Needs a bead for the label.
  F11     NOT_A_BUG — triage read both files and ruled the wording odd, not false.
  F12     NOT_ACTIONABLE — the mechanism is real but no change resolves it without defeating the
          property the derivation exists for. I will bead it as a known property, not a defect.
  F14, F15 pending triage's verdict. F14 is the a4i citation, which I have confirmed myself; I am NOT
          fixing it until triage rules, for the same reason I did not fix F1 and F2 early.

Phase 7 blind verification next, once F14/F15 land.
