FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · in_progress · #66 mutants re-run at the widened suite: all 5 still red, NONE lost — the four renamed fixtures are each still load-bearing · claim audit found a THIRD and a fourth (fourth is pre-existing) · bugs lane still out
AT: 2026-09-14 20:12 EDT

Both required items are done. Nothing cut yet; waiting on the bugs lane per your item 3.

=== #66 MUTANTS AT THE WIDENED SUITE — NO PROOF LOST ==========================================
Re-run at 7d540e045, each mutant typechecked BEFORE being believed (my own harness lesson), each
checked for a suite-load failure, tree restored after. Suite was 4 tests at #66 and is 9 now.

  #66 mutant                                    | #66 claimed | now | tsc err | load fail
  gate result forced undefined                  |      3 red  |  7  |    0    |   no
  pre-flight skip removed                       |      1 red  |  1  |    0    |   no
  the 120000 test dropped                       |      3 red  |  4  |    0    |   no
  verdict push removed, log kept                |      3 red  |  7  |    0    |   no
  raw first record judged instead               |      1 red  |  1  |    0    |   no

ALL FIVE STILL RED. None dropped, none went green, every count is AT OR ABOVE what #66 claimed,
and none of the kills is a crash (0 tsc errors in the file, no suite-load failures) — so these are
behavioural kills, which is the distinction that made my earlier harness lie.

THE PART YOU ASKED FOR SPECIFICALLY — the four RENAMED fixtures are the four #66 tests, and each
is still the named killer of a #66 mutant at the new head:
  "reports the link row NOT RUN and measures the row after it on an untouched target"  (3 mutants)
  "names the link, not the anchor, when the link's target lacks the anchor"            (4 mutants,
       and it is the SOLE red for "pre-flight skip removed" — still the only pin for that site)
  "never writes to a file outside the tree that a link points at"                      (3 mutants)
  "judges a directory row by its own index entry, not by the link that sorts first…"   (sole red
       for "raw first record judged instead")
So the widening did NOT move a fixture past the guard it was written for. The --allow on those four
names was a rename, not a loss. That is measured, not argued.

=== THE CLAIM AUDIT — I FOUND A THIRD, AND A FOURTH THAT IS NOT OURS =========================
I walked every prose line the diff adds (refusal 7's header, linkRefusal's docstring, Mutation.file's
doc, exitCodeFor's enumeration, the pre-flight link-gate comment, the row-loop comment) and asked
each for its falsifying input.

THIRD — newly written by this PR, in 9e05357e3, and the same shape as the other two:
  "A directory's link count is above 1 with no inode shared — `.` and every subdirectory's `..`
   are names for it"
  FALSIFYING INPUT: btrfs, where a directory's st_nlink is always 1 regardless of subdirectories.
  MEASURED HERE (darwin/APFS): empty dir 2, one-subdir dir 3, /dev 4, / 22 — consistent with the
  claim, which is exactly the trap: one filesystem family agreeing is not a universal, and the
  sentence is written as one. I cannot measure btrfs on this machine and I am not going to assert
  it from memory as though I had.
  NOTE THE STRUCTURE, because it decides the fix: the CONCLUSION ("it says nothing about where a
  write lands, so it is not judged") is robust and true whether the count is 1 or 54 — a directory
  shares no inode either way. Only the PREMISE overclaims. So the fix is to drop the universal and
  keep the conclusion, not to hedge the whole sentence. Same one-line discipline as the other two.
  Grim detail: 9e05357e3 wrote this WHILE fixing the previous version of the same sentence, which
  said a directory's count is "its entry count" and was wrong on APFS and ext4. The commit replaced
  a false formula with a narrower false universal. Third instance, same file, same PR.

FOURTH — PRE-EXISTING, not this PR's to fix, flagged because it is the same class and because it
is my own bead's subject:
  exitCodeFor enumerates "a mutant that does not compile" as a NOT RUN class. There is NO compile
  or typecheck step anywhere in the tool — grep for tsc/compile/typecheck returns only comments.
  Kills are computed from vitest `assertionResults` where `status === "failed"`, keyed by
  `file.name > fullName` (lines 490-514). A mutant that stops the module loading produces NO named
  assertion failures, so the failed-set is empty and the row would be reported a SURVIVOR (exit 2),
  not NOT RUN (exit 3).
  STATED HONESTLY: that last step is REASONED FROM THE PARSING CODE, NOT EXECUTED. I did not build
  a non-compiling-mutant fixture; that is a real experiment and it is not #71's. The diff only
  widened this line to add "or hard-linked", so the compile clause is inherited, not written here.
  It fails SAFE either way (a false survivor is loud; exit 2 stops a merge), which is why I am not
  holding the PR for it. I am folding it into t3_bot-icr, which is already about a harness telling
  a kill from a crash — the tool makes the opposite error from the one I made, and both come from
  nobody checking whether the suite actually ran.

So: the answer to "what was the third" is the directory link-count universal, and where I looked is
the six sites listed above. Two of the six were already known (nlink platform, refusal 7), one is
new, one is inherited, and the remaining claims — the dangling-link ordering story, the pathspec
byte-exact equality, the stat-failure reachability list — I could not falsify and two of them I have
executed directly today.

=== STATE =====================================================================================
Sweep re-run: past baseline (741 tests, 0 already failing), table filling, exit code not yet printed.
Bugs lane: still out. Security lane: answered on remediation (realPath both sides, with a measured
macOS /var vs /private/var trap), still owes me its ship-here-or-bead call.
I have independently reproduced the security lane's route B mechanism myself: index says 100644,
stat-through-link says nlink 1, porcelain empty — all three gates admit — the write lands in the
outside file, and after `git checkout --` the outside file still holds `if (false) {`. I reproduced
the GATE ADMISSION AND THE WRITE, not the full end-to-end exit 0 (that needs a real suite harness;
the lane did that part). Saying which half I measured.
Commit will be: the call-site test + THREE narrowed claims now, not two.
