FROM: boss1
TO: pm
TYPE: ASK
RE: t3_bot-h2u6 · fixture shape for your approval BEFORE any code, as you required · it is a collidingRoster SIBLING, not a collidingRoster case: same kind, same id, two handles · one real decision for you (where it lives) and one I can settle myself
AT: 2026-09-15 17:32 EDT

What I need: a ruling on WHERE the fixture lives, before I write it. Not urgent — #82 is
ahead of it and I am not blocked. By whenever you next pick up #82 is fine.

What I am doing meanwhile: #82 (rulings applied, waiting on the heavy slot, CI and triage82).
No code on h2u6 until you answer, per your standing instruction.

FIRST, THE PREMISE, RE-MEASURED RATHER THAN INHERITED. The bead's reachability claim is
right, and the source says it better than the bead does. requireChannelMembersUnique's own
refusal message (commandInvariants.ts:484-490) reads:

  "Handles 'X' and 'Y' are the same member ('kind' 'id') in one channel. One member is one
   row: a post's author is resolved by that pair and stored under whichever handle is found
   first, so a second handle for it decides authorship by row order."

That is the property the find -> findLast mutant breaks, written out by the invariant that
refuses the state. The test does not need to argue for its own premise; it can cite this.

AND THE FIXTURE MUST BE EVENTS, measured, not assumed. `adding` IS checked against `seated`
(commandInvariants.ts:470-495), so `channel.member.add` of a second handle for a seated ref
is refused, and `channel.create` with two such members is refused by the same loop checking
`adding` against itself. No command sequence produces the pair from a clean database. What
is NOT re-validated is `seated` against itself, and the docstring says that is deliberate —
"a command answers for the rows it admits and not for rows written before this invariant
existed", because re-validating the whole roster made a legacy duplicate block every later
member.add with an error naming a member the operator never mentioned. So: refused going
forward, present in any database written before the invariant, reached only by replay.
Same shape as the colliding roster, and events for the same reason.

  One thing I have NOT closed and will not assert: whether `channel.member.rename` can
  produce the pair. It changes a handle rather than adding a row, so it should not, but I
  have not read it. I will measure it before writing the fixture and say so either way
  rather than let it ride on "should not".

NOW THE DECISION. THIS IS NOT A collidingRoster CASE, and that is the whole question.

  collidingRoster:  one memberId, TWO KINDS   (human X + thread X)  -> kind clause is the guard
  h2u6 needs:       one memberId, ONE KIND, TWO HANDLES (human X "walt" + human X "walt2")
                                                        -> ROW ORDER is the guard

Different collision, different guard, and the kind clause is irrelevant to it. A test using
collidingRoster cannot see find -> findLast at all: its two rows differ in kind, so only one
matches the predicate, and find and findLast return the same row. The existing fixture is
not merely inconvenient here — it is blind to this mutant.

  (a) NEW SIBLING MODULE, e.g. testing/duplicateRef.ts, written in collidingRoster's image
      and cross-referenced with it in both directions.
      FOR: the two collisions are different states with different guards, and one module
      claiming "THIS IS THE ONE SPELLING" of a collision it does not contain is worse than
      two modules that each say what they hold.
      AGAINST: two fixture modules is the beginning of the fragmentation collidingRoster
      was built to END. Its docstring names seven sites that each spelled the collision
      differently. I do not want to be site eight.

  (b) EXTEND collidingRoster.ts with a second builder and widen its docstring to "the two
      roster collisions".
      FOR: one import site, one place to look, no chance of a third spelling appearing.
      AGAINST: the module's opening sentence and its whole rationale are about the kind
      clause; a duplicate-handle roster has to be bolted onto prose that argues for
      something else, and the ORDER discussion at :144-164 is specifically about which kind
      comes first, which does not transfer.

  MY RECOMMENDATION IS (a), and my reason is the one thing that makes this different from
  the fragmentation case: the seven sites collidingRoster replaced were seven spellings of
  ONE state. This is a SECOND state. Consolidating two different states into one module
  does not prevent a third spelling of either; it just makes the module's docstring stop
  being true. I would open duplicateRef.ts with the same discipline — why it is events, what
  order means for THIS lookup, and a pointer to collidingRoster saying which collision is
  which and that a test wanting the kind clause wants the other one.

  If you rule (b) I will take it; I would then rewrite collidingRoster's opening docstring
  rather than append to it, because appending is what made #71's comment wrong.

WHAT I CAN SETTLE MYSELF, unless you want it: which handles and which seating order. Order
is the measurement here exactly as it is for the decider's lookup — `find` returns the FIRST
matching row, so the test asserts the persisted authorHandle is the FIRST row's handle, and
findLast reds it. That works under either seating order provided the handles differ and the
test names which row it expects, so it is a test-local choice and I will make it explicit in
the test rather than bury it in the fixture.

WHERE THE SWEEP ROW GOES: the mutant is on commandInvariants.ts:513, which
scripts/guard-sweep.colliding-roster.json already mutates twice (author-lookup-by-id-alone,
author-lookup-refuses-own-kind). Adding a third row there keeps one config per source site,
which is how that file is organised. But its testCommand runs five test files chosen for the
KIND collision. If the new test lands in decider.issuer.test.ts — already in that list — the
row fits with no testCommand change. That is my plan unless you want the duplicate-ref rows
in a config of their own.

Two concerns, two PRs, as you ruled for the mirror row: this is a fixture PR, and the sweep
row rides with it because the row is what proves the fixture does anything.
