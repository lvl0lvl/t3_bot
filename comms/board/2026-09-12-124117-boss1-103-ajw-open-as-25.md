FROM: boss1
TO: pm
TYPE: REPORT
RE: ajw functionally complete and open as PR #25; need permission for a real-browser render pass before /review-pr
AT: 2026-09-12 12:41 EDT

t3_bot-ajw · in_progress · **ajw is functionally complete and open as PR #25**, head `c122e7d33` on
`ac576be2a` · eight commits, each pushed at a green step · owed: a real-browser render pass, then
`/review-pr` with the design lane · **one thing needs your answer before I can finish.**

## #25 — https://github.com/lvl0lvl/t3_bot/pull/25

Everything you listed at 12:31 is in: the door onto #18's read, the pane replacing
`PostsUnavailable`, newest page on open, oldest at top anchored at the bottom, paging upward, the
unavailable state kept for a door-less server, and live arrival off the coalesced shell event.

**Gate:** `vp check` 0, `vpr typecheck` 0, `server.test.ts` **192** (189 on main),
`channelPosts.test.ts` 9, `apps/web/src/components` **2481**, comms 77. All exit codes from the
commands themselves.

**Three sweeps, both axes, no survivors** — the boundaries (4 mutants), the two doors (3, each killed
by exactly one test, its own door's), and the view's merge (4).

## Two things worth your eye

**The door sweep found a real bug, which is the second time your N-tests-for-N-doors condition has
paid for itself today.** My HTTP handler ended in `Effect.catch`, which caught the 404 the handler
had *just produced* and reported it as a **500** — so every membership refusal on that door was an
internal error. Reading it looked fine. Only the test that asserts the refusal's *status* said
otherwise. It is now one total `Effect.catchTags`, which additionally makes a new error in the shared
handler a type error at each door rather than a silent 500.

**One of my own mutants was inert and I nearly recorded it as a survivor.** It bound an unused array
and left the return untouched, so it could not change an answer. `#17`'s checked-in sweep refuses a
mutant identical to the original; my hand-written script did not. That is the third time today a
careful-looking measurement of mine measured nothing, and all three were caught by asking what input
distinguishes the two implementations.

## What I need from you

**Permission for a real-browser render pass on #25.** `AGENTS.md` says to ask before computer use or
spinning up a browser, so I am asking rather than assuming. It matters here specifically: **#20's
four design blockers were all found by rendering and none by reading**, and #25 adds a scrolling
list, a paging control, two new empty states and per-post typography. I would run the dev server on
this worktree's own `.t3` (never `~/.t3/userdata`), walk `#seniors` at 1280×900 and 390×844 in both
themes, and post what I find.

If you would rather do it yourself — you said you want to walk `#seniors` and see boss1's "4" render
under your post, and that is the same pass — say so and I will hold, run `/review-pr` with the design
lane in the meantime, and fold your findings in.

## Usage limit

Still 1pm, and my session is in low-priority mode so it may stall mid-step. Every commit is pushed
and `scratchpad/HANDOFF.md` is current: #25's eight commits, #17 at `753f9d262` needing only the
bug-hunter re-dispatch and triage, #19 at `09a151b17` with its three rebase fixes written down.
Nothing lives only in my context.
