FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · in_progress · #71 step 1 done: 4/4 review fixes load-bearing by mutation, ONE disclosed branch reds nothing (measured unreachable, no fix proposed) · sweep 18/18 killed, re-running for the exit code · 3 blind lanes still out
AT: 2026-09-14 20:04 EDT

Step 1 (prove each fix load-bearing by mutation) is done. Method: fresh worktree at 7d540e045
with its own node_modules, mutants applied from HEAD and restored with git checkout, tree
verified byte-identical to HEAD at the end. Baseline 72/72 green across the four sweep suites.

FOUR FIXES, FIVE MUTANTS, ALL LOAD-BEARING:

  a258fcb25 (pin the stat-failure branch it says is unreached)
    M-STAT-FAIL-REFUSES  Failure branch returns a reason instead of undefined
      -> RED: "hands a tracked file its stat cannot see to the read gate, and measures the next row"
    M-STAT-FAIL-REMOVED  Failure branch deleted
      -> RED: same test. Both directions, 1 test each, exit 1.

  48680c864 (pin the index-mode test before the stat)
    M-SWAP-ORDER  stat block hoisted above the 120000 test
      -> RED: "names a dangling link a symlink, not an unreadable file", and the observable is
         the wrong gate's reason verbatim: "through-the-dangle: NOT RUN — could not read
         src/dangle.ts" where "src/dangle.ts is a symlink in the index" was asserted.
         1 test red, 71 pass.

  4ada85ba9 (pin the in-tree twin by its porcelain)
    M-NLINK-GONE  `if (links > 1)` -> `if (false)`
      -> RED on 3 tests, incl. "refuses both tracked names of one inode and hands the tree back
         clean". HONEST QUALIFIER: this test is NOT the sole pin for that guard — the two
         outside-twin tests also red. What it alone measures is the operator's tree handed back
         clean (porcelain empty); the other two cannot see that.

  9e05357e3 (refuse a link count the layer did not fill)
    M-DIR-GUARD-GONE  the `type !== "File"` early return deleted
      -> RED: "judges a directory row by its own index entry, not by the link that sorts first
         under it". 1 test.

THE ONE THAT REDS NOTHING — reporting before fixing, per your instruction:

  M-NLINK-NONE-ADMITS  the `Option.isNone(nlink)` refusal deleted
      -> 0 red, exit 0, 72/72 green. A SURVIVOR.

  It is the second half of 9e05357e3, and that commit DISCLOSED it in its own message: "no
  admitted input reaches that branch and no mutant can turn it red; the posture is the pin."
  So this is a declared unpinnable branch, not a hidden gap. I measured the reachability claim
  rather than taking it from the comment (CLAUDE.md: a reachability claim is measured, not
  remembered): an Effect program against the real Node layer on this machine returns nlink
  Some for a File (1), a Directory (54) and a CharacterDevice (1) — never None. So no input I
  can construct reaches it. SCOPE LIMIT, stated plainly: that is darwin only. The code comment
  claims "every platform Node supports"; I measured one.

  MY RECOMMENDATION: no fix. The branch is a refusal (NOT RUN), not an admission, so if it is
  ever reached it fails safe, and the alternative — deleting it — would make an unreachable
  None silently admit. I would not hold #71 for it. Your call; say the word and I will either
  leave it or file a P3 to narrow the comment's platform claim to what was measured.

ONE ERROR OF MINE, CAUGHT AND CORRECTED, because it would have put a false line in the PR body:
my first harness applied M-SWAP-ORDER by string match, the anchor spanned a comment it did not
account for, and it INSERTED A DUPLICATE declaration rather than moving the block. That "killed"
20 tests including a file-level load failure — a compile crash reported as a kill, which is the
exact anti-signal the sweep tool exists to refuse. I only caught it because 20 reds did not match
the commit's claimed 1. Redone by line range, typechecked clean first (tsc no errors in the file),
then run: 1 red, the named test. The number in the table above is the corrected one.

SWEEP: done at 7d540e045, 18/18 killed on a Baseline of 741 tests, 0 already failing, 0 survivors,
0 NOT RUN. My nohup wrapper did not capture the exit code, and that is the number the merge record
quotes, so I am re-running it purely to measure the code rather than infer it from the table.
Heavy slot still mine until that lands.

Step 2 (3 blind lanes: bugs, security, tests) dispatched on 7d540e045, all three still out.
Nothing needed from you unless you want to rule on the M-NLINK-NONE-ADMITS branch.
