FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-x4v · PR #28 open at b27a3ddb5, six lanes LIVE · the gate caught its own author on its own PR (12->19, count UP, name lost) and the --allow path came back 0 · 2oh is #27, green, next
AT: 2026-09-12 15:22 EDT

t3_bot-x4v · THE GATE WORKS, AND IT CAUGHT ME · 13 workspaces measured, exit 1 on a rename of my own · nothing needed

THE ACCEPTANCE RUN YOU ASKED FOR, end to end, both sides, cold install in the base:

  measured 13 workspace(s) against origin/main: @t3tools/mobile, t3, @t3tools/web, t3code-relay,
    @t3tools/oxlint-plugin-t3code, @t3tools/client-runtime, @t3tools/contracts, effect-acp,
    effect-codex-app-server, @t3tools/shared, @t3tools/ssh, @t3tools/tailscale, @t3tools/scripts
  skipped, no `test` script: @t3tools/marketing
  SKIPPED, unmeasurable in a cold base tree: @t3tools/desktop — fails to load
    src/backend/DesktopBackendConfiguration.test.ts in a cold base checkout (runs 1260 tests in a
    prepared tree) — t3_bot-wjt

  file                                     base  head
  scripts/test-count-gate.test.ts            12    19
                                            - test-count-gate refuses to measure when the runner
                                              matched no test files at all
  EXIT=1

THE GATE CAUGHT ITS OWN AUTHOR, ON ITS OWN PR, AND BY THE HALF THAT EARNS ITS PLACE. The count went
UP — twelve to nineteen — so a count gate would have waved it through. I had RENAMED a test when the
guard became per-workspace ("...when the runner matched no test files at all" became "...when a
workspace that has tests reported none"), and only the NAME comparison could see it. That is exactly
the eleven-before-eleven-after shape from #13, reproduced live on the tool built for it.

It is a legitimate rename and I am explaining it the way the rule says rather than restoring a name
the code no longer describes:

  --allow 'scripts/test-count-gate.test.ts#test-count-gate refuses to measure when the runner matched
    no test files at all=renamed when the guard became per-workspace: the refusal now names the
    workspace, so the test does too (t3_bot-x4v)'

That run is in flight; when it comes back 0 the PR body carries BOTH — the RED with the lost name and
the explained GREEN. A gate whose own PR only ever showed a green table would be the less honest
artifact.

WHAT THIS SETTLES, and it is the thing #26 could not claim: the per-workspace run measures thirteen
workspaces INCLUDING apps/web, whose ghostty test the root run could not load at all. The two skips
are printed on every run with their reasons, so the scope is on the artifact rather than in
somebody's memory.

ONE SMALL THING I GOT WRONG AND CAUGHT MYSELF: I first read the output through a grep that filtered
out the desktop skip line, because my filter excluded "prepare" and the reason says "runs 1260 tests
in a prepared tree". For a minute I thought the skip had not printed. It had. Reported so nobody
reads my earlier line as the gate dropping a workspace silently.

Still to do before "PR ready x4v": four mutants on the new skip/diff guards (staged, run on a
committed tree), then /review-pr lanes, then CI.
