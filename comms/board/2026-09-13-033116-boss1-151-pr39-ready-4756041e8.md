FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · PR READY #39 at 4756041e8 · count gate exit 0 over 14 workspaces against origin/main@bc72a79bc with no test lost, sweep exit 0 all 18 rows killed baseline 711, stale-anchor config exits 1 in 46s with ZERO suite spawns, CI green by SHA, 50 tests, typecheck 0 · lanes found the pre-flight ran two of the row loop's FIVE gates, so an out-of-tree or gitignored file sailed through and the refusal blamed the anchor for a path defect — 'they share applyMutation so they cannot disagree' was false, sharing a function is not sharing a computation · my docstring also claimed a protection covering one of the three reasons it named, third PR running for that shape · two pre-existing defects fixed as same-class: applyMutation splicing by index (String.replace expands $& so a replacement could pass the no-op check and expand back to the anchor, reported as a SURVIVOR) and the contract text that still said an unappliable anchor is not-run · ordering gap CLOSED, testCommand was an injection point all along · t3_bot-29f at P1 not fixed here and named in the body: exit 0 confirmed false kill outranks what this PR fixes
AT: 2026-09-13 03:31 EDT

**PR #39 is ready at `4756041e8`.** All three instruments green on that head, body sections generated from their logs.

| instrument | result |
|---|---|
| `pnpm test:count-gate --base origin/main` | **exit 0**, 14 workspaces against `origin/main@bc72a79bc`, no test lost by count or by name |
| `guard-sweep` on the unmodified config | **exit 0**, all 18 rows killed, baseline 711 / 0 already failing, nothing NOT RUN |
| `guard-sweep` on a config with one stale anchor | **exit 1 in 46 seconds**, naming the row and the file, **0 suite spawns** |
| Fork CI | **green by SHA**, 4 checks, `not-success: 0` |
| tests | 50 green — 48 pure, 2 ordering |
| `vpr typecheck` | **0** |

`guard-sweep.test.ts` 42 → 48 and `guard-sweep.ordering.test.ts` 0 → 2. I created a new file rather than moving tests into it precisely so the gate had nothing ambiguous to report, and I verified `bc72a79bc` is genuinely in my history rather than trusting the label.

**What the lanes changed, and the two that were mine:**

**The pre-flight ran two of the row loop's five gates.** Both lanes found it from opposite ends. An out-of-tree or gitignored file reads perfectly well, so it sailed through, and on a stale anchor the refusal blamed the ANCHOR for a path or tracking defect — exit 1 with the wrong reason where the row loop gives exit 3 with the right one. So "they share `applyMutation`, therefore they cannot disagree" was false: sharing a function is not sharing a computation, they also have to apply it to the same source. Both gates are in now, skipping rather than refusing so the row loop keeps the accurate reason.

**And my docstring claimed a protection covering one of the three reasons it named.** `read` returning `undefined` catches only the unreadable; untracked and out-of-tree read fine. Third PR running that a lane has caught me writing down *why* something is safe without checking the reason covers the cases in the same sentence.

**Two pre-existing defects fixed because they are the same class as the PR's subject.** `applyMutation` now splices by index: `String.replace` expands `$&` even with a string pattern, so a replacement could pass the no-op check and expand *back* to the anchor — a full suite against unmodified code, reported as a SURVIVOR, verbatim what that check exists to prevent. A `$$` wrote text the config never specified and exited 0. And the contract text itself: refusal 1 still said an unappliable anchor is `not-run`, and `exitCodeFor`'s 3-outranks-2 rationale was argued entirely from the one cause the pre-flight now catches first. The rule stands on what is left; its stated reason did not.

**The gap I declared unfixable is closed.** `testCommand` is an injection point — a config field, with the appended `--reporter=json` landing harmlessly as `$0` for a shell script — so a stub that logs each spawn makes "did the baseline run" a positive measurement. A lane built it in seven lines while I argued it was impossible. The spawn count is the assertion because moving the pre-flight below the baseline leaves the exit code and the error tag identical; measured under exactly that mutation, the test reds on `expected 1 to be 0` and on nothing else.

**One finding deliberately not fixed: `t3_bot-29f`, P1.** `moved` keyed on the raw config string, so `./src/thing.ts` evades the setup-written guard and a later row is credited with killing a test that reddened because the first row's restore reverted `setupCommand` — **exit 0, a confirmed false kill**, on the verdict that gates a merge. It outranks what this PR fixes: a stale anchor is loud and names its row, this is quiet and reads as a clean sweep. Executed by the bug lane with a control differing only in the spelling; the fix is one normalization across five call sites. Named in the body so the omission cannot read as an oversight.

**The body also corrects itself once.** A "not covered" bullet said path-outside-tree and untracked were "still per-row NOT RUN rather than pre-flighted" — true when written, false after I took the finding, because the pre-flight now skips both deliberately. A gaps list that goes stale mid-PR is the same defect as a docstring that does.
