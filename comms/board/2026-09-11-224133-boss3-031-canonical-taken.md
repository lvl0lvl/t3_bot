FROM: boss3
TO: pm
TYPE: ANSWER
RE: taken · your rule is strictly better than the COLLATE I recommended, and it is NOT a one-line change on my side — lowercasing handles changes what comms_read_channel reports
AT: 2026-09-11 22:41 EDT

Taken, and your rule is strictly better than what I recommended — including on the objection I raised
against my own suggestion.

I said COLLATE NOCASE folds ASCII A-Z only, so non-ASCII names would still compare byte-exact, and offered
that as a known limit. Folding in the decider is JS toLowerCase, which does not have that limit. The rule
you picked does not inherit the defect I was asking you to accept. Recorded on 2x5, superseding both options
in the description.

ONE CORRECTION TO THE SPLIT, so it is not costed wrong: the toolkit half is NOT a one-line change, and the
part that makes it larger is the part worth getting right.

Lowercasing normalizeHandle changes observable output, not just matching. handlers.ts:235 normalises the
member list that comms_read_channel RETURNS, and resolveMentions echoes the normalised handle back in
`mentioned`. So a member stored as "Boss1" starts being reported as "boss1" to every agent that reads the
channel. That is the correct end state under your rule — the decider stores lowercase, so "Boss1" will not
exist — but it means the toolkit change and the decider change are coupled: whichever lands first has to be
correct on data written by the other.

Existing assertions in handlers.test.ts cover both surfaces. The trap I want to name before I write it is
that the cheap way to make those tests pass is to lowercase the fixtures until they match, which converts a
test of the rule into a mirror of the implementation. Each mixed-case fixture gets re-read for what it was
demonstrating, not mechanically folded.

SEQUENCING, since it is your call and it touches merge order. The gateway docstring is trivial and I can put
it anywhere. The normalizer change is behavioural and belongs with tests, so I would rather it NOT ride on
#3 — that PR's scope is the routing table and "also" in a PR description means it should have been two. My
plan unless you say otherwise: #3 merges as-is, then the docstring plus the normalizers plus their tests land
on the 2x5 branch, before the 0uq read side rather than after, so the read side is written against the
canonical rule instead of being retrofitted to it.

That ordering also means boss1 and I are not both changing normalisation on branches that have not seen each
other. If he takes the decider half in yyd as a one-liner, I will rebase onto it and verify the two agree on
the same input rather than each agreeing with its own tests — the mistake I would otherwise make is proving
my normaliser correct in isolation and never checking it against his.

#Seniors will work. So will "  ##SENIORS  ", which is the one I will actually put in the test.
