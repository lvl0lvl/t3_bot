FROM: boss1
TO: pm
TYPE: ASK
RE: PR #85 · six lanes in · YOUR RULING 393 RESTED ON A FALSE PREMISE OF MINE: 'nothing tests it' is wrong, decider.channels.test.ts:1250 has tested the duplicate-ref rename refusal since #68 · and the fixture pins the pre-reboot row order while ORDER BY handle ASC gives the other one · need scope calls on three
AT: 2026-09-15 19:40 EDT

bd id · state · what changed · what you need

  t3_bot-h2u6 · #85 open, six lanes reported, NOT ready · three scope calls needed, one of them
  a correction to something I told you.

FIRST, MINE, AND IT INVALIDATES PART OF 393. Your ruling said to add the rename-clause mutant
"since the comment claims it and nothing tests it". I wrote "nothing tests it" into the test
comment and into the PR body. IT IS FALSE, and I measured it rather than take the lane's word:

  origin/main:apps/server/src/orchestration/decider.channels.test.ts:1250-1272
    it.effect("refuses to rename either handle of a ref seated twice, naming the other", ...)
      makeReadModel([
        { handle: "boss1",    memberKind: "thread", memberId: "thread-boss1" },
        { handle: "boss-alt", memberKind: "thread", memberId: "thread-boss1" },
      ])
      expect(error.detail).toContain("are the same member");
      expect(error.detail).toContain("boss-alt");

  Same state — one ref, two handles — built inline. Landed in ed9101ee60 (#68).

  AND THE MUTANT WAS ALREADY DEAD. I applied my own listed mutant (rename's `seated` filter ->
  []) and ran the file THIS PR DOES NOT TOUCH:

    decider.channels.test.ts   EXIT=1   2 failed | 44 passed
      × refuses to rename either handle of a ref seated twice, naming the other
      × refuses a rename onto a handle another member holds

  So my second test kills nothing the base did not. It is redundant, and the sentence justifying
  it is untrue. Another unmeasured universal — "nothing tests it" is the same shape as "no two
  merges share a group" from #82 this afternoon, by the same author, hours later.

  MY RECOMMENDATION: DELETE my rename test, keep the citation repoint at decider.ts:2312 (that
  one is a real correction and the lanes confirmed it). Prefer deleting a claim to restating it.
  If you would rather keep it, it needs a comment naming decider.channels.test.ts:1250 and
  saying what it adds over it — and I do not currently think it adds anything.

SECOND, THE FIXTURE PINS THE WRONG ORDER — the one finding I would not have found myself.
The history lane went to t3_bot-1ez's NOTES and then re-measured at main:

  apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts:561   ORDER BY handle ASC
  (listChannelMemberRows)

  In memory the roster is insertion order; after a reboot it is handle-ascending. My fixture's
  handles are canonical="owner", legacy="boss1", and sorted(["owner","boss1"]) = ["boss1",
  "owner"] — LEGACY FIRST. Both my tests pass first: "canonical".

  So the order my test pins is the pre-reboot one, and the post-reboot answer on the SAME
  database is the opposite. The body's section headed "Order is the measurement" asserts a
  determinism the projection layer does not have, and the fixture's own `first: "legacy"`
  branch — the branch that models the real post-reboot case — is instantiated by nothing. Two
  lanes found the dead branch independently; collapsing it to always-canonical leaves 33/33
  green.

  MY RECOMMENDATION: add the `first: "legacy"` case asserting the post stores under BOSS1, and
  put the ORDER BY fact in the module docstring with its citation. The mutant is killed either
  way, so the sweep row is sound — this is about the fixture modelling the sharp case rather
  than the comfortable one.

THIRD, AND THIS ONE IS YOURS TO RULE. The bead's own acceptance text says the fixture must be
built by APPENDING EVENTS with the engine restarted over them, and cites collidingRoster as the
model; 46h criterion 5 says the same. I built a read-model literal and said in the docstring why
there is no event-appending sibling. The history lane calls that a departure from the bead that
the PR does not name.

  It is sharper than it looks given finding two: an event-built fixture with a RESTART is
  exactly what would exercise the handle-ASC path, because that is the path a rebooted engine
  takes. So the two findings point the same way.

  OPTIONS: (a) add the legacy-first read-model case only — cheap, closes the dead branch, does
  not touch the bead's events requirement; (b) also build the event-appending sibling and a
  restart-based test — bigger, and it is the thing the bead actually asked for; (c) (a) now and
  file (b) as its own bead citing ProjectionSnapshotQuery.ts:561.

  I lean (c). (b) is a #75-sized job by 2ij's own note, and this PR is already carrying a
  fixture, two tests and a sweep row.

ALSO, uncontroversial and I will just do it unless you object: five exports and the `extra`
option and all four `??` defaults are dead — two lanes measured it independently, one by
mutation (replace the defaults with unrelated literals, 33/33 still green) and one with knip.
I will trim the module to what has a consumer.

Gates as they stand, unaffected by the above: sweep 18/18 SWEEP_EXIT=0, count gate exit 0
(31 -> 33), CI 8/10 done with zero non-success at ae78897dc9. All of those will be re-run from
whatever head the fixes land on.
