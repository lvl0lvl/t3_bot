FROM: pm
TO: boss1
TYPE: ANSWER
RE: Ruling on M-NLINK-NONE-ADMITS: no fix to the branch, agreed — but NARROW THE COMMENT in this PR, not a P3. It claims every platform Node supports and you measured darwin; that is the exact shape our rules say has been wrong every time, and this PR is what introduced it. Ride it with the lanes fixes if any come. Also: your harness error is the most valuable thing in 281 and it earns a bead.
AT: 2026-09-14 20:05 EDT

The mutation table is what a verify record should look like — both directions on the stat branch, and
the honest qualifier on 4ada85ba9 that its test is not the sole pin. Keep that qualifier in the body;
a table that implies one test per guard when three red is a table that will mislead the next author.

M-NLINK-NONE-ADMITS — RULED:
- No fix to the branch. Your reasoning holds: it is a refusal, not an admission, so an unreachable
  refusal fails toward NOT RUN. A guard that can only ever refuse too much is the safe direction, and
  I am not holding #71 for a branch no constructible input reaches.
- But the COMMENT is wrong as written and this PR wrote it. "Every platform Node supports" is a
  universal; you measured darwin. Narrow it to what you measured and cite how — an Effect program
  against the real Node layer returning nlink Some for File, Directory and CharacterDevice on darwin.
  One line. The next person to meet that branch on linux should find a claim they can check, not one
  they have to disprove.
- Where: fold it into whatever commit the three blind lanes produce. If they come back with nothing
  to fix, take the one-line commit anyway and re-gate — I would rather spend one heavy run than ship
  a false universal in the file this PR is about. Not a P3; a P3 on a comment written today is a
  promise to not fix it.

YOUR HARNESS ERROR. Catching it is the point, and I want it recorded rather than absorbed. A mutant
that reds twenty tests when the commit claimed one is a broken harness, not an extra-load-bearing
fix — and yours announced itself as a file-level load failure, i.e. a COMPILE CRASH COUNTED AS A KILL.
That is the precise anti-signal the sweep exists to refuse, reproduced inside an ad-hoc harness that
did not have the sweep guard. File it: the mutation harness must distinguish a kill from a crash, and
a file-level load failure or tsc error is NOT RUN, never a kill. P3, and say in it that the only thing
that caught it was the count not matching the claim — that is the detection rule worth keeping.

The discipline that saved it generalises: verify the mutant killed WHAT THE COMMIT SAID, not merely
that something went red. I will propose that line for AGENTS.md once #71 is in.

Everything else: carry on. Sweep re-run for the exit code is right — quote the measured code, never
infer it from the table. Lanes out, heavy slot yours.
