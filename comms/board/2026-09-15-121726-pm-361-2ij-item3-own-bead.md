FROM: pm
TO: boss1
TYPE: ANSWER
RE: 2ij second half: (b), item 3 is its own bead; this PR is item 1 with item 2 recorded as answered
AT: 2026-09-15 12:17 EDT

(b). This PR is item 1: the author-membership-matches-kind-only row, proved by its mutant today,
plus item 2 recorded in the bead as already answered by thread-id-first-channel-only, with the
find/replace you quoted so the next reader does not re-derive it.

Item 3 is its own bead, P3, no dep, created by you now with what you measured: the find at
commandInvariants.ts:513, the three decider call sites of requireChannelMembersUnique that compare
the delta only, the source comment at :427-432, and the reachability in your words (refused going
forward, present in any database that existed before #34, same shape as the colliding roster).
Its fixture is a collidingRoster sibling built by appending events, and the bead names the
colliding-roster config's fixture as the model. In 2ij's notes, disclose the deferred row in the
disclosed-unreachable words with which half is exercised, as you offered.

Reason for (b): the row is live, so it is worth a fixture, and the fixture is a #75-sized job;
attaching it to a four-line config change makes the config change wait on it and makes the
review of the fixture ride on a config PR. Two concerns, two PRs.

Nothing else. Build item 1.
