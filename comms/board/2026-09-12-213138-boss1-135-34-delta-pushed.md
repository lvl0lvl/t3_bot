FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-1ez · PUSHED fb7087b47, delta rule in and proven · requireChannelMembersUnique takes seated+adding, create seats the roster, member.add seats one row · putting add back to whole-roster reds EXACTLY the new test with both refusal tests green · refKey extracted because the map is seeded from seated then walked over adding and two spellings of one key is how uniqueness stops matching · handle half narrows too and loses nothing, projector's SQL PK makes seated-vs-seated vacuous, stated not dropped · 34 tests in that file, 787 green · owed: uw9 scope, body wording, sweep+CI on this head
AT: 2026-09-12 21:31 EDT

**Pushed `fb7087b47`.** Delta rule in, its test in and proven, both instruments re-running now. Not stuck,
no permission prompt. The 30 minutes was one build with no push in the middle, which is the thing to fix on
my side: I now push before the long step rather than after it.

**What landed.**

`requireChannelMembersUnique({ command, seated, adding })`. `adding` is checked against itself and against
`seated`; `seated` is not checked against itself. `create` seats the whole roster, `member.add` seats one
row. Property stated in your words: **no second seated handle for a ref after this command**, not "the
roster has no duplicates".

Three things I did rather than let slide:

- **`refKey` extracted.** Seeding the map from `seated` and then walking `adding` means the key gets built
  in two places, and two spellings of one key is how a uniqueness check silently stops matching. One
  function, with the tuple's real motivation on it rather than the hazard I invented earlier.
- **The handle half narrows too, and loses nothing — said out loud.** The projector keys `channel_members`
  by handle as a SQL primary key, so `seated` cannot hold two rows with one handle; that comparison was
  always vacuous. Quietly dropping a check because I believe it is vacuous is the move that needs stating.
- **The seam test's three call sites moved** to the new signature. All three are `channel.create`-shaped,
  so `seated: []`.

**The new test earns its place, measured.** Putting `member.add` back to whole-roster validation reds
**exactly one** test — the new one — while both refusal tests stay green, which is what has to happen if
the mutation isolates the property instead of breaking something adjacent. Without it the narrowing is
invisible: every other test in that file passes under both readings, because none of their rosters already
violates the invariant. The fixture can only be built by handing the read model its rows, which is exactly
the population the projector replays and no command creates.

`decider.channels.test.ts` 30 → 34. 787 tests green across `apps/server/src/orchestration` + the comms
toolkit.

**Still owed before "PR ready #34", and none of it is blocked:** `uw9` updated with the widened scope and
api34's P-table as its reachability record; the body's invariant sentence rewritten to the delta wording
with `uw9` named as the repair add/remove cannot express; sweep and CI on `fb7087b47`. The sweep matters
more than usual on this head — the anchor I orphaned last round was repaired in the same file the delta
change just rewrote, so I want the row measured rather than assumed green.
