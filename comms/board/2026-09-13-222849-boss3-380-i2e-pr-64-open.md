FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-i2e · in_progress · #64 open at 7635f76ec: one testUtils helper for both turn-id pins, 16-row table with 5 named red mutants, 30 shape runs against the swapped pins all as expected; count gate running · nothing
AT: 2026-09-13 22:28 EDT

#61 torn down (wt-us8, fix tree, both branches). i2e claimed and built: https://github.com/lvl0lvl/t3_bot/pull/64 at 7635f76ec on main fbccc2a8e. `turnIdBrandSites(source)` under apps/server/src/provider/testUtils/ (fork-only; no upstream history) holds the strip and the match verbatim, with the strip's comment; both pins call it with their expected lists unchanged and keep their per-pin headers. The helper's own test is #54's fifteen-shape harness as a table over a synthetic two-site source, plus a bracket-access row. Mutants, all by name: #54's strip restored → the two block-close-with-code rows red; #53's strip (no lookahead) → those plus three more; no strip → the two comment-only rows; the previous match → the `?.`/`!.` rows; a wider match (`TurnId\W*make\W*\(`) survived the fifteen — the helper's comment claims a bracket access is not seen and nothing pinned it — so a sixteenth row now reds it. The adversarial question answered by execution: all fifteen shapes inserted into OpenCodeAdapter.ts and CodexSessionRuntime.ts one at a time against the swapped pins, 30 runs, every one as expected (eleven code shapes red on both pins, the two comment-only shapes and `.call` green) — no red the in-file pins produced is lost by the move. Four suites 156/156, fmt/lint clean, tsc alone exit 0. Count gate running detached (board 379; the count goes UP by 16 — a new test file, no decrease); rigor53 lanes next. Nothing needed from you.
