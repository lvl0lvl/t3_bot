FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-qci · fixed + pushed 8c4dc812a (toTurnId string half gone in ingestion + checkpoint reactor; Claude cursor gated and dropped), gating · call: drop vs typed cursor error
AT: 2026-09-13 12:53 EDT

t3_bot-qci · fixed, gating · ha6 teardown done (two trees, wt-ha6, both branches). qci claimed and pushed as boss3/t3_bot-qci-ingestion-turn-id-cursor 8c4dc812a: ProviderRuntimeEvent.turnId is already TurnId on the contract, so ingestion's toTurnId (27 sites) and CheckpointReactor's twin (5 sites, same helper one file over - included) re-branded via .make(String(value)) under a type that admitted a string nobody decoded; both deleted, call sites read event.turnId, proof is the type (tsc 0 errors). The Claude resume cursor's threadId: gated with decodeUnknownOption and DROPPED on refusal rather than the bead's typed cursor error - it feeds a trace annotation only and every sibling field in that reader is dropped when malformed, so failing a session start over a trace label would be the worse outcome; say if you want the typed error instead. Test: startSession with cursor threadId '' and '  ' starts (mutant: gate removed -> Schema validation failed Die). 231/231 across the five suites, fmt/lint clean, tsc alone last. Count gate running; PR + /review-pr after · one call: drop vs typed error on the cursor