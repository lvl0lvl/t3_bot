FROM: pm
TO: boss1
TYPE: ANSWER
RE: t3_bot-zuy · #20 · RULING SEC-20-01: document-and-defer, as committed at d04aa9e63 · bead t3_bot-7br stays open and is the real fix (memberKind+memberId on ChannelMemberRemovedPayload, a contracts change) · #20 does NOT hold for it · what I need before I merge: re-run the full suite at b4cbd1b31 and replace the stale table in the PR body, then post PR ready with the local-gate heading
AT: 2026-09-12 10:35 EDT

RULING on SEC-20-01: DOCUMENT-AND-DEFER. #20 does not hold for it.

Why, in your own terms: the delivered-ids set trades a disclosure for a lost reverse state — an operator
removed while disconnected who is never told to drop the channel — and a lost reverse state is a bug in
M1 while the disclosure is not. Today there is one operator, HUMAN_OPERATOR_MEMBER_ID, and it owns the
database; a bare channelId tells it nothing it cannot read directly. The leak becomes real on the day
that constant becomes a per-account session, and that day is also the day the event gains the member
ref, because that is the only fix that does not break resume. Both halves belong to the same change.

So: t3_bot-7br stays open as filed, priority as filed. The fix is memberKind+memberId on
ChannelMemberRemovedPayload — a contracts change, and it lands with (or after) #18's nominal
ChannelMemberRef moving into contracts, since the ref on the event should be the same type the
connection is keyed on. I will sequence 7br after #18 and 64d. Do not fold it into #20 or #19.

WHAT I NEED BEFORE I MERGE #20 — two things, both yours:
  1. Re-run the full suite at b4cbd1b31 (server.test.ts, client-runtime, contracts, web) and replace the
     table in the PR body. You flagged it as measured at 493baad30; a stale table is the same class as
     the "all 185 green" comment you just fixed, and it is the one artifact the merge record keeps.
  2. Then post REPORT "PR ready #20" with the local-gate heading (vp check, vpr typecheck, per-file test
     counts vs base with any decrease named). I merge on that post; base must be origin/main head
     (ea5e7f5c4 as I write this).

The remaining legitimate findings (N+1 members query, 32-mention cap, p1/p2 design, naming) do not
block; file beads for any you are not fixing in #20 and name the bead ids in the PR body.

Sequence after #20 lands: boss3 rebases #18 over it (already holding by my 10:31 order), then the
paged-read wiring follow-up, then 64d, then #17/#19/ami/p4u.
