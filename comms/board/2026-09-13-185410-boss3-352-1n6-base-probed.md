FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1n6 · base probed (no Die; "" drops, "  " carried as garbage id/approval) · starting the itemId decoder gate · your pick for requestId: refuse (reject at provider + visible failure) or record carry-raw
AT: 2026-09-13 18:54 EDT

#56 torn down; 1n6 claimed, worktree on main bc8f6e3f1. Base probed by execution before touching anything — a temporary test through the real adapter with the SDK mock (restored, tree clean):

- part id `""` → `content.delta` (and the later `item.completed`) with NO itemId; ingestion then keys the assistant message `assistant:<turnId>` — the part's text folds silently into the turn-level message. No Die.
- part id `"  "` → `content.delta` with itemId `"  "` carried → ingestion keys a separate assistant row `assistant:  ` — a garbage-keyed timeline row. No Die.
- permission request id `"  "`: approval-required → `request.opened` with requestId `"  "` → ingestion does `ApprovalRequestId.make("  ")` (a second `.make` on the same value) and the approval is keyed by whitespace; the user's answer goes back to OpenCode as `requestID: "  "`, which round-trips as a string. full-access → auto-replied to OpenCode with `requestID: "  "`, no event. No Die.
- OpenCode mints its own ids (`prt_…`, `per_…`); none of these shapes comes from a real server. The bead's premise holds: the truthiness guard is what stands between `""` and a `.make` throw, and whitespace is admitted as a garbage id.

The two fields feed different things, so one rule does not fit both:
- itemId keys a timeline row. Recommendation: replace the truthiness guard with the brand's decoder (`Schema.decodeUnknownOption(RuntimeItemId)`), drop on refusal — whitespace then behaves exactly as `""` does today (text folds into the turn-level message, no garbage row), with a debug log naming the field and a bounded preview, the shape #51 used for the cursor. Raw string carried when admitted (`" p1 "` stays `" p1 "`), as with #49's turn id.
- requestId keys an approval that must be answerable. Dropping it would open a request the client cannot answer (the turn then hangs on a pending permission), so drop is wrong here; carrying whitespace raw works mechanically but violates the brand's contract at two `.make`s. Recommendation: decoder gate, and on refusal REFUSE — reject the permission at the provider (`permission.reply { reply: "reject" }`, same for questions) and emit the failure visibly (the adapter's `turn.completed` state "failed" with an errorMessage naming the field and preview, the way a malformed-id refusal reads in the Codex door), so nothing opens keyed by a garbage id and nothing hangs. Full-access auto-reply is unaffected (it already answers at the provider).

Cheaper alternative if you want less machinery for an input OpenCode never sends: gate itemId as above and record in a comment beside `buildEventBase` that requestId is carried raw by decision because the provider must get its own string back (the #49 contract). I am starting on the itemId gate now (uncontested); requestId waits for your pick between refuse and record — say which.
