FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-rex · in_progress · built and pushed at 8fb4747222 · refuse side pinned on BOTH the repository and the post path, both directions, mutants named · 29 runtime failures a typecheck could not see, from Layer.mock taking a partial · not yet a PR
AT: 2026-09-14 21:19 EDT

Built, pushed, not yet opened as a PR — reporting before /review-pr so you can redirect if the shape
is wrong.

THE CHANGE: `listChannelActivityForMember` is `listChannelRowsForMember` exposed — the same statement,
the same predicate, without the per-row roster fetch. Both roster-discarding callers moved onto it.
`listChannelsForMember` untouched and left in place per your ruling (t3_bot-ajy holds the decision).

TWO THINGS I DID NOT EXPECT, both worth your attention:

1. THE TYPE SYSTEM CAUGHT A SIGNATURE THAT WAS ALREADY WRONG. `toChannelShell` declared
   `ProjectionChannelWithActivity` — it needs a roster it never reads. Narrowing it to the
   roster-less type is the signature stating what the function already did, and it compiles at the
   ws.ts call site unchanged because a value with members is assignable to a parameter without.

2. TWENTY-NINE TESTS FAILED AT RUNTIME WITH A CLEAN TYPECHECK. `Layer.mock` takes a PARTIAL, so three
   stubs of the old method kept compiling while the paths under test had moved to the new one — the
   mock had no `listChannelActivityForMember` and the calls died. tsc reported 0 errors before and
   after. This is the factory-to-call-site rule exactly: a new method on a shape is a new obligation
   at every stub, and nothing static says so. I only found it because I ran server.test.ts rather
   than trusting the typecheck, and I ran it because that rule says to.

MUTANTS — both directions on the new method, each red BY NAME, tree restored byte-identical after:

  M-PREDICATE-GONE      the membership WHERE removed (inert: the guard stops refusing)
    -> 10 red, including BOTH new tests and, as you required, the POST PATH:
         "without the roster, a non-member still gets nothing rather than everything"
         "gives a non-member ONE answer for absent and forbidden"        (post path)
         "checks membership BEFORE it looks at the cursor"                (post path)
  M-REFUSES-EVERYONE    the method returns [] to anybody (wider: the guard refuses too much)
    -> 10 red, including both new tests — this is the mutant that proves the ADMIT side is real,
       because the refusal alone passes for a method that answers nobody.
  M-PARITY-BROKEN       the roster-less method returns rows reversed
    -> exactly 1 red: "returns the same channels in the same order as the roster-carrying sibling".
       Sole pin, which is what makes it worth having as its own test.

So the refuse side is pinned at the repository AND through the post path, and the admit side is
pinned independently. That is your framing discharged rather than asserted.

TESTS ADDED (real-database, in ProjectionChannels.test.ts):
  - a non-member gets nothing through the NEW method, with the seated member's channel NAMED in the
    same test rather than counted — this file shares one database and the member is seated elsewhere.
  - the two methods agree on ids AND order, and on every non-roster field row for row, over a
    fixture whose activity order is opposite to its creation order so a stable accident cannot
    reproduce it.

STATE: 45/45 on the three affected suites, 221/221 on server.test.ts, tsc 0 errors in apps/server.
Branch boss1/t3_bot-rex-roster-less-list at 8fb4747222.

NOT DONE YET and I am not doing it without you: no PR, no /review-pr, no count gate, no sweep. boss3
holds the heavy slot for b2m and I am not taking it. Say when you want the PR opened and I will run
the review first per the merge gate.

ONE THING I WANT TO FLAG rather than decide: I have NOT measured the actual query-count reduction.
The bead argues it from the code shape and so do I, and after tonight I am uneasy shipping a
performance claim I have not measured. If you want a number in the body I will instrument it; if you
would rather the body say "one statement per channel removed, not benchmarked", that is honest too
and cheaper. Your call.
