FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-7iw · in_progress · decision: refuse at thread.create a threadId held by any seated HUMAN or equal to the operator constant; the charset cannot spell the difference · building now
AT: 2026-09-12 22:22 EDT

Took 7iw (released jaq, which I had self-claimed a minute before your ASSIGN; it is back in the pool).

THE INPUTS THAT DISTINGUISH THE TWO OPTIONS, written before picking:

  1. thread.create { threadId: "human-walt" } on a booted server (seeder ran).
     seated-check: REFUSED.   opaque charset: ADMITTED — "human-walt" matches [A-Za-z0-9_-]{1,64}.
     So the charset as proposed does not close the route at all. Only a NAMESPACE rule ("a thread
     id may not begin with human-") would, and that bakes a placeholder scheme into a wire contract
     (HUMAN_OPERATOR_MEMBER_ID's docstring: "replace it when accounts exist").
  2. Same command with noSeedHierarchy on, then seeding enabled on the next boot.
     seated-check alone: ADMITTED, and then the seeder's channel.create is refused by the shape guard
     (thread human-walt exists) — logged and ignored, operator never seated. That is the hole in the
     seated check, and the reason the constant is in the rule too: seated-check + constant: REFUSED.
  3. thread.create { threadId: "onboarding-agent-setup" } — an id the upstream web client mints as a
     literal (WelcomeWizard.tsx), alongside randomUUID and mobile's "showcase-pending-*".
     seated-check: ADMITTED. A UUID-only ThreadId: REFUSED — a breaking wire change for upstream clients.
  4. Thread X soft-deleted while a channel still lists thread member X; thread.create X again.
     Admitted today (requireThreadAbsent is live-only) and must stay admitted: the seated id is a
     THREAD's. This is the admit-side input for the new guard's wider mutant.

DECISION: refuse, at thread.create in the decider, a threadId that any channel holds as a HUMAN
member's memberId, or that equals HUMAN_OPERATOR_MEMBER_ID. Pure, read-model-only, reads the humans
the aggregate actually knows (so it extends to accounts for free), closes both orderings for commands
(the shape guard already refuses human-after-thread), and does not touch ThreadId's wire contract.
Replay of pre-invariant rows stays reachable, as 46h ruled.

CONSEQUENCE, and it is the bulk of the work: the aggregate now refuses the ordering, so 46h's
seedCollidingRoster cannot go through commands any more. Per 46h criterion 5 it is rebuilt from
EVENTS — three appended to the store, then a fresh engine bootstraps over them — which is also the
honest model of how a colliding row arrives (a database written before the invariant). The reactor
tests already restart runtimes over one database, so the shape fits. collidingReadModel and the row
consumers are unaffected.

Mutants planned: seated check dropped (inert), constant dropped (inert; input 2), guard widened to any
seated member kind (wider; input 4), plus the 17 colliding rows re-run over the events-built fixture.
Working on boss3/t3_bot-7iw-thread-id-is-no-human from main at 4ac476084.
