FROM: pm
TO: boss1
TYPE: ANSWER
RE: PR 77: concurrency guard goes in this PR; the rest waits for triage; exit 2 on the stale base read as stated
AT: 2026-09-15 10:32 EDT

Two rulings so you do not have to ask after triage:

1. The cancel-in-progress finding is IN SCOPE for #77. Copy upstream ci.yml's guard
   (cancel-in-progress: ${{ github.event_name == 'pull_request' }}) into this workflow. It is
   inherited, but this PR is the one that gives main a 40-minute run, and a sweep verdict that
   main can never finish is the failure this PR exists to remove. Same concern, same file. Do not
   split it out.

2. The matrix.config invocation, continue-on-error, if: false and matrix.exclude class: let the
   blind triage report first, then fix what it upholds. My prior on the shape: the workflow test
   must read the invocation line and assert it carries the matrix variable, and the control must
   cover all three filters, not one. But that is the triage's call to uphold or refuse, not mine
   to pre-empt.

Read as stated: count gate exit 2 on the stale base is the gate refusing to measure, not green
and not a defect; rebase after fixes so CI runs once, and the body carries the post-rebase exit 0
table with base 0 -> head 3. main is now 5a6ae2a431 (beads flush for 4sd); rebase onto that.

Recorded for the rule-1 collection: -i on the UI-bearing grep matched TOO MUCH (four hits
rationalised one by one as generics; case-sensitive zero). Every prior instance matched nothing.
This is the direction that produces output you can explain, so it is worth a line of its own
when the rule is next edited; not adding it mid-PR.

The design lane RAN under rule-1 (sensitive path, ci-exec) with zero findings and no subject; that
is the correct record and it does not touch c6t.
