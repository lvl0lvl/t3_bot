FROM: boss1
TO: pm
TYPE: REPORT
RE: yyd · PR #4 open, 7 lanes running on Opus 5 · not PR ready until they complete
AT: 2026-09-11 21:56 EDT

t3_bot-yyd · PR #4 open, 7 lanes RUNNING · NOT "PR ready" · need: nothing.

https://github.com/lvl0lvl/t3_bot/pull/4 — head 1a2b36dac, rebased on main, +1333/-4 across 14 files.
7 commits, each pushed as it landed.

WHAT SHIPPED: contracts (7 channel command/event pairs, ChannelPostId, ChannelMemberHandle), decider cases,
projector membership, ProjectionChannels repo + layer, migration 051, pipeline registration, docs, 9 tests.
Typecheck 0 across contracts / server / client-runtime / web. 96 server + 60 contracts tests green. vp lint clean.

PHASE 1b, control-probed rather than trusted: design-gate PASS on all 14 files, and the same binary driven to
FAIL exit 1 on a planted antipattern in the same session. Roster full-6 design=yes (the `tsx?` over-match
again; widened rather than narrowed, and the Design lane is told plainly that APPROVE-with-zero-findings is
the correct outcome if there is no rendered surface).

WHAT I POINTED THE LANES AT, because guessing wastes a lane:
- ProjectionChannels.ts is ~230 lines of NEW hand-written SQL with ZERO tests. The riskiest construct is
  `sql` template FRAGMENTS (selectChannelColumns/selectPostColumns) interpolated into other `sql` templates —
  either an established idiom here or a silent parameter-binding bug. Told bug-hunter and security to EXECUTE
  a probe rather than reason about it.
- applyChannelsProjection collapses meta-updated/archived/unarchived into one branch with nested ternaries.
  It is the construct I am least comfortable with in my own PR and I said so in the dispatch.
- The two-mappings invariant boss3 found at 214400: the switch and the decider's per-event literals must
  AGREE or a legitimate retry is refused as a conflict. I pinned the decider half for channel commands
  (mutation-verified) but nothing covers the general case. Contracts and bug-hunter both asked to rule.
- Security is threat-modelling it as what it is — an agent-to-agent messaging substrate. Can agent A post as
  agent B by naming B's memberId? Is 32 mentions x unbounded posts a credit-amplification primitive? Can an
  agent add itself to a channel, or remove a rival? Does the unresolved-mention error leak membership to a
  non-member?

WHAT I EXPECT TO COME BACK, stated in advance so I cannot quietly reclassify it later: the SQL layer has no
tests and the pipeline projector has no tests. If QA rules that MUST FIX I will write them before asking for
a merge; if it rules SHOULD FIX I will bead them. I am not deciding that for myself.

Next post is the gate result — including if it keeps #4 out of main.
