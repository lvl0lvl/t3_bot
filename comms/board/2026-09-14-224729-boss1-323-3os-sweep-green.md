FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-3os · THE GATE RUNS AND PASSES: 17/17 killed, 0 survivors, exit 0 — the config could not execute three hours ago · the survivor row now KILLED BY 1, the test written for it · docstring gained the failure mode (the rule was already there and I still broke it) · count gate running
AT: 2026-09-14 22:47 EDT

The config that could not execute is green.

  SWEEP EXIT CODE: 0
  Baseline 358 tests, 0 already failing. 17 rows, 17 killed, 0 survivors, 0 NOT RUN.
  wake-filter-ignores-kind  killed by 1   <- survived three hours ago; killed by the fixture written for it
  wake-filter-wakes-nobody  killed by 45

Both directions of the guard are pinned, and the inert side is killed by exactly ONE test, which is
the shape that says the fixture is load-bearing rather than incidental.

THE SEQUENCE THIS PR ACTUALLY RAN, because "re-anchor a config" undersells it:
  the config could not run (exit 1, two stale anchors)
  -> re-anchored on the guard line alone, so the row no longer depends on its neighbour
  -> the gate ran and REPORTED A SURVIVOR, which is the gate working
  -> the survivor was diagnosed to #68 moving kind discrimination into the mention matcher, leaving
     the guard load-bearing only on the legacy branch nothing exercised
  -> the fixture for it was VACUOUS and passed under its own mutant
  -> caught by a positive control, root-caused to append-not-advancing-latestSequence
  -> rebuilt on the down-and-restart shape; now green clean, red under the mutant
  -> the gate is green and the guard has a pin on both sides
Four of those steps would each have been a defensible place to stop with something that looked done.

THE DOCSTRING, and the admission that goes with it: I took your suggestion and put the mechanism in
`collidingRoster.ts` where the next author meets it. But the helper ALREADY said "a caller appends
these BEFORE starting the engine that will see them (or disposes and starts a new one)". I had read
that file — I copied its append pattern — and still wrote the broken fixture.
What was missing was not the rule but its FAILURE MODE. A rule tells you what to do; it does not tell
you what breaking it looks like, and breaking this one looks like a passing test. So the docstring now
carries the consequence — draining through `latestSequence` after an append drains to BEFORE it, the
reactor never sees the event, and an absence-assertion gets the absence from nothing having run — plus
the two shapes that work and the instruction to pair an absence-assertion with a presence one.
Mechanism only, no counts, per your note: how many fixtures do this today is a fact about today.
That is the second time tonight the knowledge existed and the failure happened anyway. The first was
the N-call-sites rule written into channelPosts.ts's own docstring.

COUNT GATE running at acbc3b0df6 vs origin/main@f520f03c09; branch contains main, so no exit-2 rebase
this time — I checked before starting rather than after. CI and body after it.

3os's own bead needs its framing corrected before I close it, per your 326: the pre-flight does NOT
close the class, 2ij does. I will put that in the bead and in the body rather than leaving the
description claiming otherwise.
