FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · two lanes dispatched, CI green by SHA on bf1ed68e2 (4 checks, not-success 0), waiting on lanes before PR ready · taken on the lane rule and you are right: I should have dispatched and SAID I expected low yield, not skipped and offered · bug39 has the moved-skip as its sharpest question — a row whose file setup wrote AND whose anchor is broken is admitted by the pre-flight and reported NOT RUN four minutes later, which is the exact outcome this PR exists to prevent, and I wrote two tests pinning that skip, so if it is reachable those tests pin a defect · api39 on the contract change: a stale anchor was exit 3 and is now exit 1, so anyone scripting against this sees a different code for the same input · generator now REFUSES a prose count that disagrees with the table beside it, per your instruction
AT: 2026-09-13 01:27 EDT

**Two lanes dispatched, CI green by SHA on `bf1ed68e2` (4 checks, `not-success: 0`).** Waiting on the lanes before "PR ready #39".

**Taken on the lane rule, and you are right.** I argued a two-file script change with unit-tested verdicts and both directions measured would get process rather than information from a lane. The rule has no small-PR exception, and the merge tooling refusing #22 is the evidence that the exception I was reaching for does not exist. What I should have done is dispatch and *say* I thought the yield would be low, rather than skip and offer.

**bug-hunter (`bug39`)** — the pre-flight's own fail-open shapes, with the named candidates plus the one I most want challenged. A `find` that matches inside a **comment** rather than the code the row is about: occurrence counting cannot tell the difference. A file the config names that moved. A config path resolving outside the swept tree. Two rows on one file where the first's mutation would change the text the second anchors on. And **the `moved` skip**, which is the finding I am most exposed on: a row whose file `setupCommand` wrote to AND whose anchor is genuinely broken is now silently admitted by the pre-flight and reported NOT RUN four minutes later — precisely the outcome this PR exists to prevent. I argued that skip is correct and wrote two tests pinning it. If it is reachable, those two tests pin a defect and my argument is wrong.

**contracts (`api39`)** — whether this **changes** the exit-code contract, because it does: a stale anchor was exit **3**, and is now exit **1**. Anyone scripting against this tool — CI, your merge step, a senior's habit — sees a different code for the same input, and "3 outranks 2" may be less coherent once the commonest cause of 3 exits 1 earlier. Also whether `GuardSweepConfigError` and `GuardSweepProcessError` stay crisp: that distinction is what told me tonight that my own change had not broken the tool when its scratch worktree vanished, which is the concrete case for `API-17-16`'s separation. And whether `unappliableRows`, newly exported from a script, promises something awkward — `read` returning `undefined` for "could not read" while `""` is a genuinely empty file.

Both lanes are told to check three of my claims in the reverse direction: that the pre-flight and the row loop "are one computation, so they cannot disagree" — find an input where they do; that "a config error is exit 1 before any measurement, which is what the contract already says", which is me quoting the contract at myself and may be a convenient reading; and that the baseline never ran, which I infer from the **absence** of a log line rather than from a test.

**Generator check added, per your instruction:** a count in prose must match the table beside it. That is the 12-vs-18 catch turned into a rule the script enforces rather than something I noticed once.
