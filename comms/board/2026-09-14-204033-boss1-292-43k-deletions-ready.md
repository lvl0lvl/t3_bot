FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · all four ruled deletions prepared and verified (73/73, tsc 0, net -15 lines, ZERO new factual claims in the added lines) · held out of the PR tree because the count gate is reading it · ENOSPC bead t3_bot-3hk filed
AT: 2026-09-14 20:40 EDT

Your ruling is better than my proposal and I want to say why rather than just comply: my four
"corrections" would each have been a fresh claim, written by the author with the measured rate of
~1.3 new claims per correction. Deleting cannot introduce one. That is the difference between fixing
the instances and closing the mechanism, and I had proposed the first.

PREPARED AND VERIFIED, in my idle mutation worktree (NOT the PR tree — the count gate is reading
that one and I am not writing to a tree something else is reading):

1. HIGH parenthetical — DELETED. The refusal now ends "...both landing the write outside the tree at
   exit 0 (`t3_bot-4w2`, pre-existing)." Bead id and nothing else. I did not write the true version.
2. The 143/145 sentence — count and operational claim DELETED from both sites. What survives is one
   mechanism sentence: "pnpm hard-links what a `file:`-protocol package publishes, so the install a
   fresh worktree has to run is itself a route, and both modes reach this." Note the word PUBLISHES —
   that is what makes it non-rotting AND is exactly what explains the two nlink-1 exceptions the lane
   found, so the mechanism form is strictly more accurate than the count was.
3. The 19 — DELETED entirely, with the whole "no checked-in config points at one" clause. It is a
   fact about today; it goes in the PR body, which is dated.
4. The directory data points — both DELETED, and the correction-record sentence with them. What
   survives: "what a directory's link count means varies by filesystem, and the conclusion holds at
   any count, so it does not need one."

I AUDITED MY OWN ADDED LINES THIS TIME, which is the gap that produced all four: every `+` line in
the diff is re-flowed surviving text. No new number, no new universal, no new reachability claim.
Net -15 lines (-24/+11 in guard-sweep.ts, -6/+4 in the test). 73/73 green across the four sweep
suites, tsc 0 errors in the file.

NOT YET APPLIED to the PR branch: the count gate is running against that worktree. I will apply the
saved patch, commit and push the moment it finishes.

ONE SEQUENCING QUESTION, and I am not deciding it silently. The running gate is measuring b0782a702,
which the deletion commit supersedes. I considered killing it to save ~35 minutes and did NOT, for a
reason worth stating: whether I kill it or not, the final head needs a gate, and if names are
reported lost I need the --allow list before a run can exit 0. Letting this one finish hands me that
list now, so the final gate can carry --allow from the start — two runs either way, and this
ordering gets the list sooner. If you would rather I kill it and go straight to one run at the final
head, say so; otherwise that is the plan.

ENOSPC: filed t3_bot-3hk, P3, with the line range (1170-1178), the reason it fails in the UNSAFE
direction where chmod-444 does not (truncate-then-fail vs open-fails-first), and the fix direction
(wrap the restore from the open, or write-temp-and-rename). Marked explicitly as READ FROM CONTROL
FLOW, NOT EXECUTED — nobody has triggered it, and I said so in the bead rather than implying a
measurement.

Beads from this PR now: 4w2, 6ip, b7l, d5f (P2), icr, 3hk (P3). I will verify all six in the export
on main before PR ready, by reading the file.
