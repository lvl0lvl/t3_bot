FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-wjt · PR READY #35 at c25db0bf7 · desktop measured on both sides per (b), the last measurement quoted per your addition, CI green — and CI's own prepare log shows the hook fetching on Linux · merge when ready
AT: 2026-09-12 19:41 EDT

t3_bot-wjt · #35 PR READY at c25db0bf7 · both sides measured, last measurement quoted, CI green on all four · merge when you are ready

THE BEAD'S ACCEPTANCE, as ruled (b)+(c):

  base  71642a423, hookless install + one hand prefetch, labelled with the command
        Test Files 98 passed | 4 skipped (102)   Tests 1260 passed | 43 skipped
  head  this branch's hook on a COLD tree — electron wiped, hook, suite, one run
        Test Files 98 passed | 4 skipped (102)   Tests 1260 passed | 43 skipped

@t3tools/desktop measured on both sides, no SKIPPED line, no failed-to-load. The head line is
deliberately NOT my working tree: that tree was installed hours before the hook existed and its
electron is warm from history, so a green run there proves nothing about prepare. I had written it
that way, caught it before publishing, and the body says which run is the proof and why.

THE LAST MEASUREMENT, per your addition — the unassisted run against the hookless base:

  COULD NOT MEASURE: apps/desktop/src/settings/DesktopSavedEnvironments.test.ts failed to load in
  @t3tools/desktop in .../t3-count-gate-9t2UaM/base, so its tests were never counted.
    Electron failed to install correctly. Please delete `node_modules/electron` and run
    "npx install-electron --no" manually.
  EXIT=2

A DIFFERENT file than the bead named — a different worker lost the race — with electron's own
message under it. The load-dependence in one line, and the gate cleaned up its temp root after its
own refusal.

ONE THING CI GAVE ME THAT I COULD NOT HAVE MEASURED HERE. Fork Check failed once, on a lint rule
(node builtins are namespace-imported in this repo; fixed, re-proven cold, pushed). But that same
log carried:

  . prepare: prefetch-electron: fetching Electron once, before any test worker can
  . prepare: prefetch-electron: done (electron)

The hook running on CI's OWN install, on Linux, in two seconds, writing `electron` where macOS writes
an .app path. That is the "CI gets it through the same install" claim measured rather than asserted,
and it is in the body.

Your other addition is in too: every senior's first install after this lands fetches Electron once
(~100 MB); every install after sees path.txt and skips. A slower first install is the fix working.

(c) STANDS AS WRITTEN: the next PR's unassisted gate — 1ez or 46h — is the confirmation, and its body
should say so. UNMEASURABLE_IN_COLD_TREE is `{}` with the mechanism kept and the reason recorded.

Then 46h. Scratch cleanup continuing between steps: three of thirteen removed.
