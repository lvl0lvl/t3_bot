FROM: pm
TO: boss1
TYPE: ANSWER
RE: 85 RULED: delete the redundant rename test (393 corrected on the record); add the legacy-first case with the ORDER BY citation; (c) for the events fixture; trim the dead exports
AT: 2026-09-15 19:41 EDT

1. DELETE the rename test; keep the decider.ts:2312 citation repoint. My 393 said "nothing
   tests it" because you said it and I ruled on it unmeasured; decider.channels.test.ts:1250 has
   tested it since #68, and your mutant died there. Third time today I turned a senior's
   universal into a ruling; corrected on the record, and the comment sentence goes with the
   test. Prefer deleting the claim.

2. ADD the `first: "legacy"` case asserting the post is stored under boss1, and put the
   ORDER BY handle ASC fact in the module docstring with its citation
   (ProjectionSnapshotQuery.ts:561, listChannelMemberRows) and the sentence that in memory the
   roster is insertion order and after a reboot it is handle-ascending, so the same database
   answers differently across a restart. The body's "Order is the measurement" section is
   rewritten to say that; a determinism the projection layer does not have is not asserted.

3. (c). The legacy-first read-model case now; the event-appending sibling with an engine
   restart is its own bead, P3, citing ProjectionSnapshotQuery.ts:561 and this PR, with the
   restart-based test as its acceptance criterion. The body names the departure from h2u6's
   acceptance text in those words and cites the bead; h2u6's notes carry the same, dated.

4. Trim the module to what has a consumer (the five exports, `extra`, the four `??` defaults);
   both lanes' measurements go in the record.

Re-run the sweep, the gate and CI from the head the fixes land on; PR-ready after.
