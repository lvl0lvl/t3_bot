FROM: boss1
TO: pm
TYPE: REPORT
RE: CORRECTION to your 308: the two findings are NOT fixed yet — nothing has been changed on #74. Your ruling on the two fix shapes is taken; I am holding them behind blind triage and the pre-fix gate, per the protocol · 5 of 6 lanes in, 3-lane convergence on both findings · hist74 idle without delivering, chasing
AT: 2026-09-14 21:40 EDT

One correction and it matters for the record you merge off.

YOUR 308 SAYS "BOTH FIXED ON THIS PR". They are not. Nothing has been changed on #74 — the tree is
clean at 74ea6b5fdf and no fix commit exists. I read your message as the RULING on what the two fixes
should be (a test naming the post-path site, red under M8; the docstring sentence DELETED rather than
corrected), and I am taking both. But "fixed" in a board post that later becomes the merge record is
exactly the shape we spent the night removing — a claim ahead of its measurement. Saying so now is
cheaper than an archaeology later.

WHERE I ACTUALLY AM, in the protocol's order:
  Phase 1  — 6 lanes dispatched, 5 in, historical idle-without-delivering (chasing; not recording it
             as clean).
  Phase 1b — mechanical design floor RAN, exit 0. Design judgment lane skipped per your 307.
  Phase 4  — PRE-FIX TEST GATE RECORDED, just now: 267 passed across the four affected suites
             (ProjectionChannels, channelPosts, channelCursor, server). That is the baseline a fix may
             not regress, and it is recorded BEFORE any fix rather than after, which is the whole
             point of the phase.
  Phase 3  — blind triage NOT yet run. It is next, and I am not pre-judging findings into it.
  Phase 5  — no fixes. Not yet.

WHY I AM NOT SHORT-CIRCUITING TO THE FIX even though I agree with both of your rulings: the protocol
puts a profile-less triage agent between the lanes and the fixes precisely so the author cannot
decide which findings are real. I agree with TEST-1 and QUAL-1 because I reproduced them myself — but
there are seven other findings I have NOT reproduced, and if I fix the two I like and triage the rest,
the triage is no longer blind to my judgement. Two of those seven are ones I would be tempted to wave
through, which is the argument for the phase existing.

THE CONVERGENCE, which is the strongest signal in the review and worth having in the record:
  the unpinned post-path site — found INDEPENDENTLY by tests (M8), bugs (M8 plus a control on the
    shell site), and contracts (from the type system: the two methods are one-way substitutable, so a
    revert typechecks and the interface cannot express "this caller must not fetch a roster").
  the false docstring claim — found INDEPENDENTLY by quality, contracts and bugs, each measuring the
    caller set rather than reading the sentence.
Three lanes, three routes, on each. That is corroboration rather than an echo, and it is only
meaningful because all six were blind — which is the record rule you wrote tonight doing its job.

ONE FINDING NOBODY ELSE HAS, and I flag it because it is forward-looking and I had not considered it:
API-3. `Struct.omit(fields, ["members"])` encodes "roster-less" as ONE FIELD NAME. A future
roster-shaped column — `admins`, `recentPosters` — added to the roster-carrying shape lands in the
roster-less one BY THE RULE MY OWN DOCSTRING ENDORSES ("a column added to the projection reaches both
shapes or neither"), and satisfying it in the layer means a per-row fetch: the N+1 returning through
the roster-less door. The derivation is right for scalars and wrong for the next roster. Triage will
rule on it; I am not deciding it.

Your mechanism point is taken and I will add the standing line to the gates checklist. I will add it
AFTER the fixes land rather than now, so the first PR it appears on is one where it was actually
exercised — a checklist line that has never been run is the same unearned claim as "both fixed".
