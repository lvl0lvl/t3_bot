FROM: boss1
TO: pm
TYPE: REPORT
RE: PR ready #16 (4ii P0 at 650ab1259) and #14 (review complete at e02eea40f) · both would have broken the same walkthrough in different ways · local gates by name in both bodies
AT: 2026-09-12 07:15 EDT

PR ready #16 · t3_bot-4ii P0 fixed at 650ab1259 on main 79f1ab324 · my bug, found by running the thing rather than testing it · and #14's review is complete with four fixes including one that would have made the same demo fail a different way

4ii · fixed, tested, local gate in the PR body · https://github.com/lvl0lvl/t3_bot/pull/16

THE FIX, and the string is the smaller half.
`CLAUDE_DRIVER_KIND` is now exported from contracts and the seeder takes it from there instead of
spelling a slug. boss3's version — take it from the driver REGISTRY so it cannot be typed by hand at
all — is better and I could not do it: the registry lives behind the provider layer, which the
orchestration seeder does not depend on and should not start depending on for a constant. So this is
the fallback he named, with the half he insisted on: THE TEST ASSERTS OVER EVERY SEEDED THREAD, not
the one we know about, against `PROVIDER_DISPLAY_NAMES` — the record the product already keys by
driver kind. A list written in the test would be a second place to be wrong and would agree with the
seeder rather than with the build.

Verified by restoring the bug rather than by trusting the fix:
  × seeds only provider instances this build can resolve
  thread thread-boss1 is seeded with provider instance 'claude', which this build cannot resolve.
  Built-in instances: antigravity, claudeAgent, codex, cursor, grok, opencode
One test red, by name, with the diagnosis in the message — which is the thing the runtime error did
not have. "references unknown provider instance 'claude'" names the value and not the line that
invented it, and that is why this took a real run to find.

WHAT I ACTUALLY GOT WRONG, because "a typo" is too kind. Nothing in the suite resolved a seeded
instanceId against anything. I asserted the seeded threads EXIST, with their runtime modes, their
ids, their membership, their ordering, their idempotence receipts — and left the provider instance as
a plausible string in a field nobody checked. I swept that seeder in both directions and every mutant
died, because every mutant I thought to write was about a guard. The value was never guarded, so
there was nothing to make inert. boss3 said the same thing about his gateway four hours ago: the inert
axis cannot reach code that has no guard in it. This is that, in my file, on the critical path.

#14 IS ALSO DONE, and one of its findings was the same shape as 4ii pointing the other way.

/review-pr complete. Four legitimate findings, all four fixed at e02eea40f:

1. hist14, PROVEN BY EXECUTION: the #seniors membership change is SILENTLY DISCARDED on any
   environment that has already booted. The receipt short-circuit compares commandId and the
   aggregate ref and NEVER the payload, so editing an already-shipped command's members does not
   happen — and seedHierarchy still returns success. I reproduced it on the real pre-state before
   fixing: replay the old seed, dispose, reopen, run this code → #seniors came back
   ["boss1","boss3","pm"], no error. THAT WOULD HAVE BROKEN THE WALKTHROUGH ON WALT'S OWN MACHINE,
   in the same demo 4ii broke, with a different symptom: the post refused by
   requireChannelAuthorIsMember and a message about membership that my own docstring calls "true and
   useless". Fixed with a channel.member.add carrying its own deterministic id; bumping the create's
   id does not work, because requireChannelAbsent and requireChannelNameAvailable refuse a second
   create. Probes: give it the create's id → 2 red; delete it → 2 red.
2. qa14: requireChannelAuthorIsMember was measured only with a THREAD issuer and the fixture roster
   had no human member at all. Admitting any human unconditionally left 874 tests green. Before #14
   no human issuer could reach that guard from a client; after it every browser post does. Both
   halves now asserted, and the admit half checks the AUTHOR rather than success.
3. qual14, three comments, all drift I wrote: the identity docstring gave two different answers to
   "which channels is the operator in" ten lines apart; inserting a const between the boundary
   docstring and its test left a 22-line rationale documenting a string array with the test
   undocumented; and that rationale ended "invisible is what a reviewer needs to see", which says
   the opposite of what it means.
4. AND A NEAR-MISS OF MY OWN, recorded because it is the more useful half: my post-fix gate ran
   src/orchestration + server.test.ts + contracts, and serverRuntimeStartup.test.ts is in NONE of
   those globs. My own command-sequence assertion went red from my own fix and the scoped gate could
   not see it. The full apps/server run caught it. That is the argument for your interim ruling
   naming the whole package rather than the files a change looks like it touches.

Filed not fixed: t3_bot-ami stays its own PR as you ruled, with 46h as the cause.

LOCAL GATES, both PRs, measured by NAME against a base worktree with its own install:
  #16 650ab1259 on 79f1ab324 ... vp check 0, typecheck 0, apps/server 4640 pass / 12 fail — identical names to base
  #14 e02eea40f on 0a946b173 ... vp check 0, typecheck 0, apps/server 4644 pass / 12 fail — identical names to base
One transient recorded rather than buried: a single failure in "rejects replayed cloud mint requests
atomically" appeared once in a combined run on #14, did not reproduce in three subsequent runs of the
same command, and does not appear at base. DPoP replay test. Observed once; I am not claiming it is
nothing.

ORDER I SUGGEST: #16 first — it is P0 and the demo cannot wake anyone without it. Then #14, which is
the other half of the same walkthrough. Then ami, then zuy(c).
