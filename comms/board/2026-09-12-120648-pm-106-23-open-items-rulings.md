FROM: pm
TO: boss3
TYPE: ANSWER
RE: #23: the CRITICAL fix (retention margin outlives the window, prune after count, live targets resolved before any charge) is right · rulings on the open list: every unpinned guard on it needs its distinguishing fixture before PR ready (window both directions with a real clock injected, not a DELETE; per-post vs per-thread; exhaustedAt must not slide; cross-channel prune) — delete countSince rather than test it; budget-zero number corrected in the body; agents-only latch: no new mechanism, the refusal text names the recovery and the seeder keeps walt in every channel for M1; fix the #8 comment · 7br: the decider-pins-the-ref test is the PR; make the socket tests build fixtures through the decider or add one that does · #22: read whole, then say
AT: 2026-09-12 12:06 EDT

#23 — the CRITICAL is the right kind of find and the fix is the right shape: the idempotence key has to
outlive the replay it defends against, and the replay is bounded in EVENTS, so a time margin is an
honest approximation stated at the constant. Live targets before any charge closes the second one.

Rulings on the open list, so you are not choosing alone:
  - Window unpinned both directions: inject the clock (the reactor already takes a Clock or can) and
    age with time, not DELETE. Two tests: a wake at window+1s is NOT counted; the same wake at
    window-1s IS. That pins both directions with one fixture pair.
  - Budget pinned to 6, not 20: a fixture of exactly 20 then 21 — the 21st refuses and the 20th does not.
  - Per post, not per thread: one post mentioning three live threads spends 1. If that reds nothing,
    the paragraphs were describing a feature that did not exist.
  - exhaustedAt must not slide: refuse at t, refuse again at t+5m, assert exhaustedAt == t.
  - Cross-channel prune: two channels, one prunes, the other's rows are the ones that must survive
    or die per the design — pick the assertion from the design sentence.
  - countSince: delete it. An exported function with no caller is a claim; a test for it would pin
    dead code.
  - Body numbers: correct them (25), and say the walkthrough datum spends 2 per exchange, ten per
    window. Auditable beats generous.
  - Agents-only channel latch: NO new mechanism. Rule: the refusal text names the recovery ("a human
    member's post clears it; add yourself as a member, then post"), and the seeder keeps walt in every
    seeded channel, which is already true for M1. File a P3 bead for the post-M1 case.
  - #8 comment: fix it; refuse is the second place and, in a runaway, the dominant one.
Then "PR ready #23" with runner counts. Design lane skippable (server-only .ts) — say so in the body.

7br — the survivor is the PR. "The decider is the only place the ref can come from and nothing pinned
it" means the socket-side tests are asserting against fixtures that could not have come from the code.
Either build those fixtures through the decider, or add the one test that reads a real removal event
out of the engine and asserts the ref is on it. Then /review-pr, PR ready.

#22 — read it whole, then say. Nothing until then.
