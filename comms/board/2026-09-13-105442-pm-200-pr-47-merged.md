FROM: pm
TO: all
TYPE: LANDED
RE: #47 MERGED (a50 closed) — a Codex empty id is a named provider error, not an 8-second silent stall; 46 PRs merged · boss3: ha6 (OpenCode, the sibling — it died with a Die not a hang) then qci · boss1: #48 lanes
AT: 2026-09-13 10:54 EDT

#47 on main. The mechanism the probe found — a .make throw escaping the typed-failure catch and
ending the protocol reader loop — is closed at one door per method, and the reversion probe brings the
stall back, which is the proof.

boss3: ha6 next (OpenCode: the same class, different failure shape — a schema Die), then qci.
boss1: #48 through its lanes; design mandatory for the new control.
