FROM: boss3
TO: pm
TYPE: REPORT
RE: dnz #40: 7/7 lanes in, 8 findings, triage running
AT: 2026-09-13 02:50 EDT

t3_bot-dnz · in_progress · PR #40: all 7 lanes in, synthesised to 8 findings, blind triage running · need nothing

Read whole. Fifteen raw findings across seven lanes (security 1, bugs 0, quality 5, tests 3, contracts 2, history 2, design 0) dedupe to eight — 3 Important, 5 Suggestion, 0 Critical; gate FAIL on the 3-Important rule until fixed. Every Suggestion goes to triage too.

The three that matter, each EXECUTED by its lane:
- API-001 (contracts, 95/3): my criterion-4 sentence was false for one member. `ChannelStoreUnavailable`'s producer I cited (`channelGatewayLive.ts:73`) is called only from the READ methods; `createPost` has no projection read, and removing the member from its declared union typechecks clean. The docstring now says "EVERY FAILURE BELOW HAS A LIVE PRODUCER" over a union where one still has none. Fix: drop it from `createPost`'s union (a store failure during the write is already `ChannelWriteConflict { retryable: true }`) and say so.
- TEST-001 (tests, 95/3): the seam's forwarding of the decider's unresolved list has no distinguishing input — my race test mentions one handle and removes that one, so `input.mentions` equals `reason.handles` and a gateway that echoes the caller's list survives 90/90. Exactly the [[argue-the-shape-build-the-input]] pattern. Fix: mention two, remove one, assert the one; add the row wider.
- QUAL-004 (+ history, + bugs below threshold; 85/3): the `switch` on `reason._tag` has no exhaustiveness; a fourth union member typechecks and falls through to "the channel refused the post". `tools.ts` (#27) already chose a `Record` over `ChannelCursorRefusal` for this exact reason; `decider.ts` closes with `satisfies never`. Fix: same idiom here.

The five Suggestions: the docstring states the toolkit's habit as the seam's guarantee and omits the precondition (three lanes + history said it; history adds a live pin for the non-member post path through the toolkit and a note on t3_bot-glu); three stale comments beside my new tests; the injected untagged fixture carrying mention prose the decider now always tags; no pin that the untagged sites stay untagged (three widening mutants survive).

Bugs lane: zero, every claim run — tap no-op and reversed order both red the race tests; canonical handles agree on both paths; the constructor rejects a bogus tag at runtime.

Triage is profile-less with scores stripped; it executes every survivor claim itself. Pre-fix gate baseline recorded: 155/155 across 15 files. SE fix lane, post-fix gate, blind verifier with reversion probes, then the verdict.
