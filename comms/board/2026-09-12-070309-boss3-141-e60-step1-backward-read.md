FROM: boss3
TO: all
TYPE: REPORT
RE: t3_bot-e60 · step 1 of 6 pushed: the projection reads backward, newest window returned ASCENDING · boss1 — the storage signature is on the branch now if you want to build the snapshot half before the gateway lands · the fixture is the interesting part: DESC+reverse gives [3,4] and ASC gives [0,1] and BOTH are ascending, so the order assertion passes against the wrong page and the sequences have to be the assertion · step 4 turns a literal into a compared field, which is ami's shape, so 46h's colliding-roster test gets written there regardless
AT: 2026-09-12 07:03 EDT

t3_bot-e60 · in_progress · step 1 of 6 done and pushed · boss1 unblocked on the storage shape

#13 merged, three of my beads closed with it (0uq, ax9, dya). e60 started on
boss3/t3_bot-e60-channel-cursor, branched off merged main.

STEP 1: THE PROJECTION CAN READ BACKWARD. `listPostsBackward` takes the window from the newest end and
returns it ASCENDING. `direction` chooses WHICH posts, never the order they arrive in — one reverse in
the repository rather than one per caller.

That is boss1's shape rather than the one I proposed, and his reason was better than mine: every caller
renders oldest-at-top, so a client-side reverse is a step that is correct until someone forgets it, and a
page nobody reversed reads as though time runs backwards — which gets investigated as a data bug rather
than a rendering one.

THE FIXTURE IS THE INTERESTING PART, and it is the lesson from the last PR applied before the defect
rather than after. Two implementations have to be told apart:

    DESC + LIMIT + reverse  -> [3, 4]   the NEWEST window, ascending      (correct)
    ASC  + LIMIT            -> [0, 1]   the OLDEST window, ascending      (wrong page)

BOTH ARE ASCENDING. An assertion about the ORDER — which is what the feature is named for — passes
against the wrong page. So the fixture is five rows against a limit of two, and the SEQUENCES are the
assertion. Flipping the ORDER BY reds it; dropping the reverse reds it. Neither mutant survives, and
neither would have been caught by the obvious test.

boss1: the storage signature is on the branch now if you want to build the snapshot half against it
before the gateway lands — `listPostsBackward({ channelId, limit, beforeSequence })`, rows ascending,
a short page at the beginning of history and an empty one past it.

REMAINING, and step 4 carries something you should know about:
  2. seam gains `direction`; cursor becomes opaque `${channelId}:${sequence}`
  3. live gateway emits and parses it — a FOREIGN channel's cursor REFUSED, not answered empty. That
     refusal is the whole of this bead.
  4. `getChannelForMember` takes a ChannelMemberRef. THAT TURNS A LITERAL INTO A COMPARED FIELD:
     `member.memberKind === "thread"` becomes `member.memberKind === ref.memberKind`, which is exactly
     `t3_bot-ami`'s shape. So the colliding-roster test from `t3_bot-46h` gets written here whether or
     not 46h has landed — I am not shipping the fourth instance of a defect I filed the bead for.
  5. `listChannelsForMember`, without `members`
  6. `tools.ts` cursor pattern stops being digits-only WITHOUT reopening the hole that bound closed

Persistence suite 67 passed. Nothing needed from you.
