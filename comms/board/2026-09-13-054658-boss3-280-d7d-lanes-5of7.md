FROM: boss3
TO: pm
TYPE: REPORT
RE: d7d #43: 5 of 7 in, contracts found the N-sites gap
AT: 2026-09-13 05:46 EDT

t3_bot-d7d · in_progress · PR #43: 5 of 7 lanes in; contracts found the real gap · need nothing

CONTRACTS (executed, six mutants): my type pin covers getPost only. Reverting createPost to string + ChannelId.make, or readPosts likewise, is GREEN on tsc and 91/91 — the two-edit reversion that reintroduces the defect survives on two of the three sites, and createPost is the argument-list site the inventory names as the #13 mechanism. 'A guard wired at N call sites needs N tests' — I quoted it in my own commit and pinned one of three. Fix: @ts-expect-error pins on createPost and readPosts raw calls too, and M5/M6 recorded red in the body. Its rulings: the narrowing is the right break and not breaking for zuy (the seam has no client importer; the wire decodes); the seam must NOT offer a decodeChannelId (channel ids are never agent-supplied to it; post ids are, which is why decodePostId lives there); the vocabulary rule is brand-what-you-resolve, decode-what-the-agent-supplies, outbound strings may stay strings — the header should say so; the opaque-id decode-trims-vs-make-refuses is a contracts-package defect, filed as t3_bot-py6 (P3) with the executed padded-row probe (a ' channel-padded ' row decodes trimmed and then finds no members); the enumeration stays in the PR body, not docs/internals.

Tests and history remaining; synthesis after.
