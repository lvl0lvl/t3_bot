FROM: boss1
TO: pm
TYPE: CLAIM
RE: t3_bot-dhg · in_progress · pushed 1c4d09a5d on boss1/t3_bot-dhg-return-to-newest · files from the diff: ChannelView.tsx, ChannelView.test.tsx — pane only, no overlap with #47 · shape: the newest-page atom stays mounted while paged up and live arrival is ungated (clearing the cursor double-reads past swr staleTime and re-walks held history); a paged-up reader gets a New posts control that scrolls, not one that re-reads · 36/36, three mutants red, one redundant conjunct found by a surviving mutant and removed (layout effect) · next render pass, gate, PR, lanes incl. design · need nothing
AT: 2026-09-13 10:22 EDT

**Files, from the diff on the pushed head `1c4d09a5d`:**

```
apps/web/src/components/ChannelView.tsx       | +~60 -15
apps/web/src/components/ChannelView.test.tsx  | +~115
```

Both in the pane; nothing under `apps/server`, `packages/`, or `scripts/`. No overlap with boss3's #47.

**The shape, and why not the bead's first suggestion.** The bead offers a jump-to-latest that clears the cursor. Measured against `Atom.swr` (client-runtime `runtime.ts:551-556`; effect's `Atom.js` swr): a remount re-reads only past `staleTime` (30 s), so within it the effect would have to force the read, and past it the remount AND the effect both read — the open-ran-twice defect the effect's own comment exists to keep out. Clearing the cursor also sends the pager back through history the region already holds. So instead the newest page's atom stays mounted while the reader pages up (it is the same atom as the pager's until the first click), the live-arrival effect refreshes it with no cursor gate, and a reader who is up in history gets a `New posts` control that scrolls — the posts are merged already — rather than one that re-reads. One subscription, no extra read.

**One thing a mutant found in my own first pass:** the control's condition had a `cursor !== undefined` conjunct that a mutant deleting it survived — the newest page marks each post seen, so the conjunct only prevented a one-frame flash a passive effect allows. The scroll-and-mark is now a layout effect (before paint, which is also the right time to scroll) and the conjunct is gone; the replacement mutant (never mark seen) reds.

**Proven:** 36/36 across both ChannelView suites (+2). Three mutants red on their named tests: restore the cursor gate; remove the control; never mark seen. Not pinned and said so: "does not scroll while paged up" — react-test-renderer has no DOM, so that half rests on the render pass. Typecheck 0. Two new `set-state-in-effect` lint warnings of the class the file already carries (state synced from an atom value).

**Next:** render pass in the real client — 60 seeded posts, page up in one tab, post from a second, watch the control light without a scroll — then count gate, PR, lanes (design mandatory: it is a new control).
