FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-rex · in_progress · bead VERIFIED against main before building: premise holds and is STRONGER than written — it says 'its only production caller', there are now TWO and neither uses members · line numbers and one symbol stale · heavy slot is boss3's, my work is light
AT: 2026-09-14 21:07 EDT

#71 merged, thank you. rex claimed and verified before I touch anything — the bead is from 2026-09-12
and ~70 PRs have landed since, so I read its every named symbol against main at 981a92b440 rather
than trusting it.

WHAT THE BEAD SAYS vs WHAT IS TRUE TODAY:

  "Its only production caller is withMemberChannels"
    STALE, and in the direction that STRENGTHENS the bead. There are now TWO production callers of
    listChannelsForMember:
      apps/server/src/orchestration/channelShell.ts:97   (withMemberChannels -> toChannelShell)
      apps/server/src/orchestration/channelPosts.ts:130  (membership check before the cursor)
    NEITHER uses members. channelPosts reads only `row.channelId`
    (`channels.some((row) => row.channelId === channelId)`) — it is a membership existence check, so
    the roster is fetched and discarded there too. So the N+1 is paid on the POST-PAGE path as well
    as on shell bootstrap, which the bead does not know.

  "ProjectionChannels.ts:314-325"
    STALE line numbers. The Effect.forEach is at 334-345 today. Same shape.

  "toChannelShell DROPS members / OrchestrationChannelShell has no members field"
    HOLDS, measured: toChannelShell returns id, name, archivedAt, latestPostAt, createdAt, updatedAt
    and nothing else.

  "getChannelWithActivityById DOES need members — rowHasMember reads them on the live-stream path"
    HOLDS, measured: ws.ts:1199 calls rowHasMember(row, connectionMember) on the row that came from
    getChannelWithActivityById. So the two callers genuinely want different shapes and the bead's
    argument for TWO METHODS rather than a flag survives.

  withMemberChannels itself: still exists, channelShell.ts:87, called from http.ts:69 and ws.ts:1951.

SO THE BEAD IS SOUND AND UNDERSTATED. I will correct its description with today's facts before I open
anything, so the next reader does not re-derive this.

SCOPE, and I will say now rather than after building: this looks day-shaped to me — a second
repository method returning rows without members, two call sites moved onto it, and the existing
real-database tests extended. But the membership check in channelPosts is a SECURITY-relevant read
(the disclosure the channelPosts docstring is about), so changing what that path fetches needs the
membership property pinned independently of the roster shape. If that turns out to need more than a
day's PR I will say so and it goes to Milestone 2 rather than being built, per your standing rule.

HEAVY SLOT: boss3 has it for b2m, noted. My rex work is reading, a repository method and targeted
suite runs — nothing that needs the slot. I will ask before anything heavy.

WORKTREES: #71's two removed, pruned, and I checked for surviving node children with their cwd
inside them before and after. None.
