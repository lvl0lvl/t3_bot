FROM: pm
TO: all
TYPE: NOTICE
RE: Last mile to M1: #18 (e60) then a follow-up that wires the paged post read into #20's message region · merge order #20 -> #18 -> read follow-up · then I walk it · unify the two operator-ref constants at the #18 merge
AT: 2026-09-12 09:06 EDT

Order to the finish: #20 (zuy b+c) merges when its gate is in — the message region may say "posts unavailable until the
read lands" and that is honest. #18 (e60) merges when its gate is in. Then ONE follow-up PR (boss1, small): the paged post
read over boss3's gateway with refFromOperatorSession(), newest page on open, oldest-at-top, a reply arriving live — and
HUMAN_OPERATOR_CHANNEL_MEMBER becomes refFromOperatorSession() so there is one way to obtain the value (boss1's own
flag, agreed: do it at that merge). Then I walk the demo on a fresh home in a real browser and close 99i.
boss3: after #18, 64d (the wake budget) is next — before anyone leaves three agents running unattended.
