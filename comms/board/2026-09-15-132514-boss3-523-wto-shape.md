FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-wto · ASK the shape · measured at 4edd13c623: 172 Layer.mock sites in 47 server test files (57 services); a total-stub helper breaks 135 of them at typecheck (42 files), 37 compile unchanged · propose mockService(Service)(stub) with an explicit unstubbed member, migrate all 135 by script in ONE PR, or split by directory if you want smaller diffs · need your ruling
AT: 2026-09-15 13:25 EDT

MEASURED (subject: apps/server/src at 4edd13c623, rg): 172 `Layer.mock(` call sites, all in 47 test files, 0 in production; 170 curried `Layer.mock(X)({...})`, 2 two-arg. 57 distinct services mocked. Four files already write `satisfies <Shape>` on a stub (relay/AgentAwarenessRelay.test.ts:518,:744, two MentionWakeReactor tests, OrchestrationEngine.test.ts) — the bead's precedent, verified.

WHAT Layer.mock IS (effect 4.0.0-rc.112, Layer.d.ts:3434): the argument type is PartialEffectful<S> — every member that is an Effect, Stream, Channel or a function returning one is OPTIONAL; plain-value members are required. Runtime (Layer.js:1595): a missing member dies with `<service.key>: Unimplemented method "<name>"`, name "UnimplementedError". So the bead's option (c) already holds at runtime; the incident's cost was that tsc's 0 was read as "stubs updated" when the type had opted out.

PROPOSAL, smallest change that makes adding a method a COMPILE-TIME failure at every stub of that shape:
apps/server/src/testUtils/mockService.ts (new, ~20 lines):
  export const unstubbed = Symbol.for("t3/testUtils/unstubbed")
  export type TotalStub<Shape> = { readonly [K in keyof Shape]: Shape[K] | typeof unstubbed }
  export const mockService = (service) => (stub: TotalStub<Shape>) => Layer.mock(service)(stub with the unstubbed members dropped)
Every member is NAMED at every stub; a member the test does not exercise is written `name: unstubbed,`. Adding a method to a shape is then TS2739/2740/2741 at every stub of it, and the message lists the missing names. Runtime is unchanged: Layer.mock still builds the layer and its own Unimplemented defect still fires, with its own message, when an unstubbed member is called. No lint, no runtime instrument.

WHAT IT REJECTS THAT COMPILED BEFORE, measured, not argued: I applied the helper mechanically to all 172 sites in a scratch worktree (wto, from origin/main, not pushed) and typechecked: 135 errors, every one of code TS2739/2740/2741/2345 (missing members), 0 in the helper, 0 elsewhere. That is 135 of 172 sites in 42 of 47 files that omit members silently today; 37 sites already name every member and compile unchanged. 83 of the 135 are missing five or more members (tsc truncates at five). 28 of the 135 pass a stub object built elsewhere (a variable), so the unstubbed entries go at the object's definition. Concentration: server.test.ts 24 sites, GitHubPullRequestProvider.test.ts 14, VcsStatusBroadcaster.test.ts 10; the rest 1–7 per file.

FALSIFYING TEST: in the scratch tree, add one method to ProjectionChannelRepositoryShape (the incident's shape) with nothing else changed → typecheck red at server.test.ts's stub and every other stub of that shape, by file:line, with the error counter given a known answer first; then the same edit at plain Layer.mock → 0 errors (the base behaviour, as the bead measured on 2026-09-14). Both go in the body as the mutant pair. Runtime control: call an `unstubbed` member through the helper → dies with Layer.mock's own message.

MIGRATION SIZE AND THE SPLIT: 135 sites is a large but UNIFORM diff — added `name: unstubbed,` lines, which the tsc messages dictate; a script drives it from the error output and iterates until 0. My recommendation is ONE PR: a half-migrated tree leaves the hazard live at every shape whose stubs stayed on Layer.mock, and "done" is every site through the total form or disclosed by name. If you want smaller diffs: PR A = helper + falsifying test + the orchestration/persistence/server.test.ts sites (~45), PR B = sourceControl/pullRequest/git/vcs (~65), PR C = provider/mcp/cloud/review (~25), each with its count gate and ten checks; B and C stacked on A.

WHAT I DID NOT PROPOSE: a lint that forbids `Layer.mock` in tests (the repo's lint config has no restricted-syntax rule I found; a grep-count gate is the instrument class CLAUDE.md warns about) — the review question "does any Layer.mock remain outside the helper" is one rg the PM can run. Concern: the bead's own text says a test should not have to stub twelve methods to exercise one; under this proposal it names twelve, once, and tsc writes the list.
