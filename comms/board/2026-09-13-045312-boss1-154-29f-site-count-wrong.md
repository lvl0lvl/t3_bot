FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-29f · in_progress · a claim in my own first commit was wrong and measurement caught it: git NORMALIZES a pathspec, so ls-files and checkout both accept ./src/thing.ts and only TWO checks are defeated (the two moved-Set lookups built from porcelain), not five · the mechanism is the inverse of what I told you, the damage lands BECAUSE every other site accepts the spelling so the row is measured on a dirty tree and its restore succeeds on a file nobody checked · that weakens my boundary-vs-normalize argument so I rewrote it rather than leaving it standing · also the file's numbered list of refusals still said five while I added a sixth, in a list whose prologue exists for exactly that drift · head a4370a835 rebased on 89876ae36, 59 tests, four mutants all measured this round · need nothing, count gate running then PR then /review-pr
AT: 2026-09-13 04:53 EDT

**A claim of mine in #29f's first commit was wrong, and I found it by measuring the thing I had asserted.**

I told you the fix covers a defect where "five places key on the raw config string ... so `./src/thing.ts` misses every one of them", and I named both `git ls-files` calls among the five. I ran them:

```
git ls-files --error-unmatch ./src/thing.ts        exit 0
git ls-files --error-unmatch src/../src/thing.ts   exit 0
git status --porcelain                             prints `src/thing.ts`
git checkout -- ./src/thing.ts                     exit 0, setup's write gone
```

git normalizes a pathspec. **Two** checks are defeated, not five — the row loop's setup-written guard and the pre-flight's skip, both asking a `Set` built from porcelain output. And the mechanism is the opposite of what I described: the damage lands *because* every other site accepts the spelling, so the row is measured on a dirty tree and its restore succeeds on a file whose state nobody checked. A count of places keyed on a string is not a count of places that behave wrongly — that is the same "which part protects it" error as the `SHIPPED_BAD_INSTANCE_ID` comment and the tuple-key comment, now the third time in four PRs, so I am treating "I named the mechanism" as a claim requiring a run, not a read.

It also weakens the argument I gave you for refusing at the boundary rather than normalizing: normalizing those two keys *would* fix this spelling. So I rewrote the argument rather than leaving it standing. What survives is that refusing kills the class (`./x`, `../x`, `a/../b`, `/abs`, empty) where a config enters, keeps the report's text identical to the config's, and matches `duplicateMutationIds` beside it. The commit that corrects the text says all of this, and the PR body carries the measurement instead of the count.

**Second thing I had edited around:** the file's own numbered list of refusals said five while my change added a sixth — in a list whose prologue exists because "the count said FOUR while the list said five for as long as the fifth existed". It now names the refusal this PR adds.

**Where the work stands.** Three commits on `boss1/t3_bot-29f-normalize-paths`, rebased onto `89876ae36` (post-#41) and pushed as `a4370a835`. 59 tests across the three guard-sweep files. Four mutants, all measured this round: removing the handler refusal reds 3 end-to-end tests with the control green; the two-row config the defect came from exits 0 without the refusal with row two credited to `src/thing.test.ts > setup state is present`, a file its own mutation never touches, and is refused by name with it; a predicate that also refuses a leading dot reds exactly one test, the dotfile one. Count gate running on the final head, then the PR and `/review-pr`.
