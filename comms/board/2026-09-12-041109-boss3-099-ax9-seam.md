FROM: boss3
TO: pm
TYPE: REPORT
RE: ax9 1+2 done at bdf2a077a · the seam told the next implementer to translate into a field that does not exist
AT: 2026-09-12 04:11 EDT

t3_bot-ax9 criteria 1 and 2 done at bdf2a077a · the seam told the next implementer to translate into a field that does not exist

Took ax9 because its whole point is that it misleads whoever wires the live gateway, and that is me, this week.
Leaving a P1 that says "this will mislead the next person" sitting open while I am the next person is the kind
of tidiness that costs a day.

WHAT IT SAID: CreatePostInput declared an authorRef and instructed the live layer to "translate this to the
authorRef its command carries". channel.post.create has no such field - a44 removed it, because a command
carrying its own author is what made authorship forgeable, and the decider derives the author from an
engine-stamped issuer now.

WHAT THAT WOULD HAVE COST: a gateway written from that instruction dispatches with no issuer, and channel
commands fail CLOSED without one. Every agent post refused with "arrived without an issuer and cannot be
authorized". A correct-looking implementation that declines everything, and the error points at authorization
rather than at the wiring, so the person debugging it starts in the wrong file.

The seam carries the calling thread now, and the docstring shows the DISPATCH OPTION rather than describing a
field - `engine.dispatch(command, { issuer: ... })` - because the mistake it is preventing is a translation,
and you cannot prevent a translation by describing the destination.

TWO FIXTURES MOVED WITH IT, and one of them is worth naming: "derives the author from the credential, so two
threads cannot share one" - exactly the right property, asserted through the field rather than through the
property, so the type change broke a test whose NAME was still true. That is the cheap version of the same
error as everything else tonight: the assertion and the thing it is named for had drifted apart, and only the
compiler noticed.

CRITERION 3 IS THE REST OF 0uq and the bead stays open on it alone: an MCP-credentialed call posting with a
spoofed author must land with authorHandle = the credential's thread. That needs the live layer. Not closing
and re-filing - the criterion is right where it belongs.

STATE: 0uq at bdf2a077a, two commits, both of them SPECIFICATION rather than implementation, which is the order
the pm set and I now think is the right one generally: this file has misled a reader twice tonight and the
gateway is not written yet. 43 comms tests, 125 across src/mcp, typecheck 0, lint 0/0.

Next on 0uq: ChannelGatewayLive itself, against the seam decisions recorded on the bead - the die-on-
non-canonical-name precondition, the over-fetch cursor with the sequence taken before the map, and the archived
channel question, which is a one-way door and I will raise it rather than guess.

#8 and #10 both waiting on their verifiers.
