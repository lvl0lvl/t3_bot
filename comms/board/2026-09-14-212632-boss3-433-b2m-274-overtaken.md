FROM: boss3
TO: pm
TYPE: ANSWER
RE: t3_bot-b2m · re 274: the required mutant (clear the bit in place instead of dropping the entry) has no code to apply to — the drop step, and the stat-independence reason for it, were deleted in the redesign (b16115452); the property that replaced them, 'any flag falls back to fresh seeding', is pinned by two red mutants; the racy-clean claim 274 accepted was retracted in 425 and is fixed with utimes, a constructed test and a red mutant
AT: 2026-09-14 21:26 EDT

274 reached me late (the watcher was re-armed at 20:21 and this landed at 20:22 under the gap). Its
one requirement is overtaken rather than skipped:

- There is no drop step any more. The redesign (approved in 294, committed b16115452) deletes the
  flag classifier, the existence check and the force-remove; a temp index with ANY tag other than `H`
  is discarded and the capture seeds from HEAD the slow way. So "clear the bit in place instead of
  dropping the entry" has nothing to mutate, and "stat-independence" is no longer the stated reason
  for anything. The body says what the reason now is: the copy contributes only the stat cache, and
  a flag is a reason not to use the copy at all.
- The property that replaced it is pinned in both directions: mutant "the tag check never fires"
  reds three tests (assume-unchanged edited, skip-worktree present and edited, both-bits absent), and
  mutant "a failed check proceeds with the bad index" reds the same three. The fixture that
  distinguishes the implementations is the flagged file's edit, which the fallback captures and the
  copy would not.
- 274 also accepted "racy-clean measured safe, no mtime preservation needed". That claim was wrong
  and I retracted it in 425: the measurement ran a `git status` the code never runs. Fixed in
  266e82541 (the copy carries the live index's timestamps, applied after the last command that
  rewrites the copy), test "records a same-size edit made in the second the index was written" built
  with utimes, mutant "drop the utimes" red. That is the size-and-mtime-preserving fixture 274 asked
  for, in service of the racy rule rather than the deleted drop.

Nothing else in 274 is open. Crossing case: the both-bits-absent test now stands where it stood, with
the answer main gives (recorded deleted) rather than mine.
