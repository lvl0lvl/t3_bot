FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh · CI caught a regression I shipped — the same defanging, a third file out · fixed b84648ed3
AT: 2026-09-12 17:17 EDT

t3_bot-2oh · #27 CI caught a regression I shipped, and it is the SAME defanging, a third file out · fixed as b84648ed3 · nothing needed

FORK TEST SERVER 2 FAILED and it was mine:

  × the HTTP door carries the direction and the cursor it was given
    AssertionError: expected 400 to equal 200

`server.test.ts` carries both client doors' cursor fixtures and every one of them was two-segment.
That one went RED — `channel-project:1` is no longer a cursor, so the door refused the read the test
exists to make. TWO MORE STAYED GREEN AND STOPPED TESTING: the HTTP and SOCKET "refuses a cursor
another channel issued" pair. Both assert a refusal, and a foreign cursor is refused either way — now
at the segment boundary rather than as the PROVENANCE refusal each test is named for.

MEASURED BOTH WAYS, because "it would have been defanged" is the kind of claim I have been wrong about
today:

  three-segment fixtures, channel guard deleted -> both door tests FAIL
  two-segment fixtures,   channel guard deleted -> both door tests PASS

So the pair was carrying no weight at head, and does again.

WHY I DID NOT SEE IT: my local run was scoped to the comms toolkit, the codec and the paged read —
110 tests — and `server.test.ts` is none of those. I have now run the whole of apps/server (4745
passed, 12 failed, all of them the symlink/provider set this repo fails on a developer machine; none
touch channels, cursors or comms) and the whole of apps/web (4612 passed). The count gate re-running
now is the rigorous version of that claim, since it compares base and head by test NAME.

THAT IS THREE FILES FROM ONE CAUSE TODAY — fifteen fixtures in the comms tests, four in #25's codec
tests, three at the doors. The pattern is worth the team having in its head:

  ADDING A REQUIRED SEGMENT TO A FORMAT SILENTLY MOVES EVERY EXISTING FIXTURE PAST THE GUARD IT WAS
  WRITTEN FOR — and the ones that go red are the lucky ones.

The tell is that nobody edits a test for it to happen, so a review of the diff cannot see it. What
found it each time was re-running the PREVIOUS mutants on a file the change touched, which is the
count gate's own closing line, and this third one was found by CI rather than by me doing that.

Guard sweep is still exit 0, 18/18, both axes wherever both exist. Pushed as b84648ed3. CI re-running;
count gate re-running on the fixed head. "PR ready #27" follows both.
