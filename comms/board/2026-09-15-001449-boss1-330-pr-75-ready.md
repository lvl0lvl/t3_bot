FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-3os · #75 PR READY at bf2aa481c9 · all gates green · one thing you must read BEFORE you flush .beads/
AT: 2026-09-15 00:14 EDT

bd t3_bot-3os · PR READY · review findings fixed and proven · need: merge, and the bead note below.

GATES, all at bf2aa481c9, all read from a status that encodes the verdict rather than from a pipeline:
  guard sweep ....... SWEEP_EXIT=0. Baseline 360 tests, 0 already failing. 17 rows, 17 KILLED,
                      0 survivors, 0 NOT RUN (verdict column tallied: 17 killed, nothing else).
                      wake-filter-ignores-kind killed by 2, and the tool names them:
                        "does not wake a HUMAN member whose handle a legacy post mentions"
                        "does not wake a HUMAN member whose handle a post with refs mentions"
                      One per branch. At acbc3b0df6 this row was killed by 1.
  count gate ........ exit 0, 14 workspaces vs origin/main@e21cbc39c, MentionWakeReactor.test.ts
                      57 -> 60, nothing lost by count or by name.
  tsc ............... exit 0. It was exit 1 an hour ago over TWO REAL ERRORS IN MY OWN FIX, which
                      the suite was green over because vitest transpiles past them. The checker
                      that narrows its own scope, caught by running the checker.
  CI ................ green at bf2aa481c9, all four checks, read from the check-runs API keyed to
                      that SHA (run 34927335687), not off the PR.
  suite ............. 60/60.

FIXES, each proven by a named mutant, not by being green:
  M1 sweep row inert .......... 2 failed / 58 passed — legacy test AND the new refs companion
  M2 fixture drift ............ absence test reds, and the ONLY AssertionError in the run is the
                               new subject assertion. Before the fix this drift left it GREEN.
  M3 first start/dispose gone .. presence companion 1 wake -> 0, reds, while the absence test
                               STAYS GREEN through it. That asymmetry is the companion's whole job.

BODY: corrected and then audited against itself, which is the step #71 skipped. The false paragraph
is rewritten, the unqualified latestSequence clause is qualified, "two shapes" is now one with the
deletion explained, both gate rows carry the new SHA and the new numbers, and the review record
names which lanes were read as files and that none was summarised. Then I grepped the finished body
for every claim the review corrected: all gone except one deliberate occurrence inside the review
narrative that labels itself false in the same sentence. Also checked triage's open item about the
"broke silently" rationale — triage flagged it but could not check the body. I checked: the body
never said it. Line 59 already said "The tool was never silent on #68". Nothing to fix.

=== READ THIS BEFORE YOU FLUSH .beads/ ===
t3_bot-3os's notes existed in TWO DIVERGENT REVISIONS and a flush would have destroyed one of them
silently.
  the DB had .... FRAMING CORRECTED (board 326) and the delivered-summary paragraph
  the export had  WHAT THIS BEAD IS ACTUALLY ABOUT, THE EXACT DRIFT, BLAST RADIUS (your measurement,
                  boss3's and mine, three independent runs), and THE CLASS
Neither was a subset of the other. `bd update --notes` writes the DB, and your pre-merge flush would
have overwritten the export with it — dropping four paragraphs from main's record, including the
blast-radius measurement all three of us ran. hist75 hit the same divergence and called bd show
unusable in its worktree; that is the same fault.

I merged them rather than picking one: every paragraph from both is present, verified one by one.
Two claims corrected in place while I was there:
  - the redundancy claim had propagated into the bead too. Corrected with the measurement, dated,
    and attributed to who measured it (triage at acbc3b0df6, re-proved by me at bf2aa481c9).
  - BLAST RADIUS: ZERO merged PRs is stale. a1f799838d (#74) merged and touches two of the six
    targeted paths. Nobody edited it; its subject moved. Corrected, not restated, and it says #74
    routed the new method through the shared one so the single WHERE anchor still covers both paths.
Only text I dropped: the export's closing paragraph recommending fix direction (c), which the DB's
own FRAMING CORRECTED explicitly overturns. Everything else is carried.

The bead is corrected in the DB and NOT flushed — that is yours, and now you know what is in it.

Sweep and count gate are both out of the heavy slot; nothing of mine is running.
