FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · PR #39 OPEN at bf1ed68e2, CI running · both directions measured: bad config exits 1 in 46s naming the row with NO baseline line, good 18-row config exits 0 with every row killed baseline 707 — and the good run includes the six rows #37 added which I did not write · count gate exit 0 over 14 workspaces · caught my own defect first: the body quoted '1 of 12 mutations' beside an 18-row table because the probe predated the #37 rebase, which is the #17 ledger problem in miniature — re-ran on the current config, 44s became 46s · body names three things it does NOT prove, ordering untested chief among them · no lanes dispatched, say if you want QA on the ordering gap · PR ready follows CI green by SHA, then qa7
AT: 2026-09-13 01:23 EDT

**PR #39 is open for `t3_bot-2wm`**, head `bf1ed68e2`, CI running (four checks in progress — it starts when the PR exists, as with 7rj).

**Both directions measured, on the head the PR carries:**

| direction | result |
|---|---|
| the config with one row's anchor replaced by a string that does not exist | **exit 1 in 46 seconds**, naming the row and the file, **no `baseline…` line** |
| the checked-in config, unmodified, 18 rows | **exit 0**, every row killed, baseline 707 / 0 already failing |
| `pnpm test:count-gate --base origin/main` | **exit 0**, 14 workspaces against `origin/main@b09a751ad` |
| unit tests | 6 for `unappliableRows`, 48 in `guard-sweep.test.ts` |

A gate that refuses bad input is half proven; the other half is that it admits good input, and the good-input run includes **the six rows #37 added**, which this branch did not write.

**One defect I caught in my own generated body before you read it.** The first version quoted the refusal as "1 of **12** mutations" beside a table listing **18 rows** — the probe had run before the rebase over #37, when the config had twelve. A reader hits that and cannot tell which number is real, which is the #17 ledger problem in miniature: a figure carried across configs. Re-ran the probe against the current 18-row config, which also moved the measurement from 44 to 46 seconds. The generator protects the figures it generates; the consistency *between* two generated blocks was still mine to check, and I nearly shipped two adjacent measurements disagreeing about the size of the config.

**What the body says it does not prove**, and this is the part worth reading first:

- **The ordering is not tested.** That the refusal precedes the baseline is shown by the probe and by line numbers — refusal at :795, `runSuite` at :801 — not by a test, because the sweep has no injection point for its test command.
- **Path-outside-tree and untracked-file are the same class as a stale anchor**, and the sweep's own docstring says so. They remain per-row `NOT RUN`. Promoting them is a question I am raising rather than a change I made quietly — it would widen this from anchors to paths.
- **`NOT RUN` is not retired.** It stays for what the pre-flight cannot see: a `setupCommand` that writes a target, a mutation that does not compile, a baseline reporting no tests.

**Lanes:** none dispatched. This is a 2-file change to a script, its verdicts are unit-tested, and both directions are measured against the real tool — my read is that a lane would add process rather than information. Say if you want one and I will send contracts or QA at it; QA would be the one, on the ordering gap.

"PR ready #39" follows CI green by SHA. Then `qa7`.
