FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · MEASURED: the pre-flight fires, exit 1 config error naming the row and file, and the baseline NEVER RUNS — the log goes setup then refusal with no 'baseline…' line, which is the observable form of the assertion the bead demanded · 44 seconds start-to-finish against ~4-5 min for a full 12-row sweep · correcting myself twice: 'about thirty seconds' was unmeasured (44 is the number), and then I inferred from a SILENT probe that the pre-flight was not firing, which was a second unmeasured claim made while retracting the first — the probe captured output instead of streaming it so it could not have told me either way · next: commit the tests, sweep and both gates on the final head, then the PR
AT: 2026-09-13 00:19 EDT

**Measured. The pre-flight fires, the baseline never runs, and the number is 44 seconds.**

Probe: the real sweep against a config copy with one row's anchor replaced by a string that does not exist.

```
sweeping a worktree of .../2wm at /var/folders/.../guard-sweep-nnQ4on/tree
setup: pnpm install --frozen-lockfile
ERROR (#1): GuardSweepConfigError: This config cannot be swept: 1 of 12 mutations could not be
applied, so the sweep would report them as NOT RUN after measuring everything else:
  issuer-required-fails-open: anchor not found in apps/server/src/orchestration/commandInvariants.ts
PROBE_EXIT=1
```

Start `1789271293`, end `1789271337` — **44 seconds**, against roughly four to five minutes for a full twelve-row sweep. Exit **1**, a config error, which is what the exit-code contract already says a config failure is.

**And the baseline never ran, which is the assertion the bead demanded and I can now show rather than argue.** The log goes `setup:` → refusal, with no `baseline…` line. That line is unconditional immediately before `runSuite`, so its absence is the observable form of "the test command was never invoked". It is evidence from the real tool rather than a unit test — there is no injection point for the test command — and the PR body will say exactly that rather than implying test coverage.

**Correcting my own numbers, twice over.** I first wrote "about thirty seconds" with nothing behind it; the measured figure is 44. Then I inferred from a silent probe that the pre-flight was not firing — which was a second unmeasured claim, made while retracting the first. The probe could not have told me either way, because it captured output instead of streaming it. Retracting an unmeasured claim was right; replacing it with a different unmeasured inference was not, and that is the part worth remembering.

**Where 2wm stands.** Pre-flight in `sweep()` before the baseline; `unappliableRows` pure and exported with six unit tests (48 in `guard-sweep.test.ts`); `vpr typecheck` 0. Committed at `9332497a9` and pushed — the tests are not committed yet, that is next, then the real sweep and both gates on the final head, then the PR.

Three things the body will carry as gaps rather than let a reader assume:
- the ordering is shown by a probe and by line numbers, **not by a test**;
- rows `setupCommand` wrote to are deliberately not pre-flighted, and `moved` is empty after an install (measured: 0 porcelain paths before and after), so that skip cannot swallow the case this exists for;
- path-outside-tree and untracked-file are the same class as a stale anchor — the sweep's own docstring says so — and they are still per-row NOT RUN rather than pre-flighted. Moving them is a question, not something I did quietly.
