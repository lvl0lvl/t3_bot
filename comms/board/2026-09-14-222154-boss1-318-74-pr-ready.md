FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-rex · PR READY · #74 at beaeb8a01f — every gate measured, MERGEABLE, body published · one gate recorded as COULD NOT RUN (pre-existing, t3_bot-3os) · 3os is NOT yet in the export, needs your flush
AT: 2026-09-14 22:21 EDT

t3_bot-rex · PR ready · #74 at beaeb8a01fc074d380f89b5a7096553890bb78c1 · need: merge

GATES, every one measured:
  count gate .......... exit 0 at beaeb8a01f vs origin/main@3a1eb64d2. 14 workspaces, NO test lost by
                        count or by name, ProjectionChannels.test.ts 19 -> 23. An earlier run exited
                        2 (COULD NOT MEASURE, stale base) — rebased, not explained.
  base drift .......... re-measured for the NEW subject after main moved twice more: 3a1eb64d21 vs
                        779bf39fe4 is CLAUDE.md only, test-file blob md5 identical both sides. Gate
                        stands without a third rebase.
  sweep, channel-post-wake .... 17/17 killed, exit 0, 158-test baseline, 0 survivors, 0 NOT RUN.
  sweep, colliding-roster ..... EXIT 1, COULD NOT RUN. Recorded as not-run, never as passed or
                        inapplicable. Pre-existing, t3_bot-3os, not caused by #74 (measured: this
                        branch does not touch MentionWakeReactor.ts, blob identical to origin/main,
                        both anchors absent there too).
  /review-pr .......... six blind lanes + design lane recorded as NOT dispatched with its
                        measurements; 19 raw findings -> 15 deduped; all 15 dispositioned by blind
                        triage; 7 LEGITIMATE fixed; post-fix gate 268 vs a 267 pre-fix baseline.
  CI .................. Fork Check + Test Server 1/2/3 all completed/success, read PER-SHA from
                        check-runs on the full sha.
  mergeable ........... MERGEABLE, state OPEN — checked after GitHub finished recomputing, not while
                        it still said UNKNOWN.

BOTH NEW TESTS PROVEN BY THEIR OWN MUTANTS, before/after:
  revert the post-path call site: reddened NOTHING anywhere -> now reds one named test.
  drop m.member_kind:             reddened 1 (the sibling's) -> now reds 2, so the kind half keeps a
                                  pin if t3_bot-ajy later deletes the sibling and its test.

YOUR 323 CONFIRMED, independently and before I read it. I ran the six-path log with CONTROLS, because
an empty result is the one shape that cannot distinguish "nothing matched" from "the query was
broken": the same range without the path filter returns commits, and with a path that did change
(CLAUDE.md) returns commits, and with the six returns nothing. ed9101ee60 is an ancestor, 15 commits
in range. Three computations, three methods, same zero.
I also confirmed #68 touches two of the six (git show --stat: MentionWakeReactor.ts,
commandInvariants.ts), and reconciled the two descriptions of the drift, since both were half right:
the SET `mentioned` was renamed `mentionedHandles`, AND the name `mentioned` was reused for a new
predicate function, so the anchor's second line went from `mentioned.has(member.handle)` to
`mentioned(member)`. Both are in 3os with the class you asked for — a gate whose exit code nobody
reads has not run.

BEADS CITED IN THE BODY, verified by READING .beads/issues.jsonl on origin/main@779bf39fe4:
  t3_bot-rex, ajy, wto, c6t ... in export
  t3_bot-3os .................. NOT in export — created at 22:18, after your last flush. 07w behaving
                                as documented. It needs your flush before merge; I am flagging it
                                rather than letting you find it, and I am not claiming it resolves.

WHAT THE BODY CARRIES: the review record with every lane's INPUT named (blind, against which sha) and
the design lane recorded as not-dispatched with the measurements; all 15 dispositions including the
rejections, deferrals and the two DECISIONs your 292 holds; the three prose lines that failed the
falsifying-input test and what happened to each; the a4i correction pointing at c38963c12f and
channelPosts.test.ts:18-26 with NO count and why the count is deleted rather than corrected; the wto
corrections triage measured; F12 recorded as a known property rather than forced into a change; and
the two new checklist lines, both ticked, both on the PR that first exercised them.

NEXT AFTER MERGE, per your ruling: t3_bot-3os ahead of the P3 backlog — a merge gate that cannot fire
outranks everything in ready.
