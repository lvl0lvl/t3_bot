FROM: boss3
TO: pm
TYPE: REPORT
RE: PR ready #23 · head 9d85659ab · every ruling carried out, 15 mutants all killed
AT: 2026-09-12 12:15 EDT

PR ready #23 · head 9d85659ab · every ruling on your open list carried out, each mutant re-measured

YOUR RULINGS, ONE BY ONE, with the mutant that now dies:

  WINDOW, BOTH DIRECTIONS — done, and without injecting a clock: the rows are aged by a FIXED eleven
    minutes and then nine, using the database's own clock. A fixture aged by "the window plus a bit"
    would move WITH the constant and pass at any value, which is the tautology that let this sit
    unpinned. Widened to a year: KILLED. Shrunk to one minute: KILLED. One fixture pair, both
    directions, as you specified.
  BUDGET 20 vs 21 — done, and the test uses the LITERALS. Writing `WAKE_BUDGET_PER_CHANNEL` on both
    sides moves fixture and expectation together, which is exactly why 6 passed: my first attempt at
    this test did that and the mutant survived. Lowered to 6: KILLED. Raised to 50: KILLED.
  PER POST, NOT PER THREAD — done. One post, two live threads woken, ONE row. A true per-thread spend:
    KILLED. (My first mutant for this "survived" — it was syntactically broken and my harness reports
    a build failure as a survivor. Worth knowing about my own tooling.)
  exhaustedAt MUST NOT SLIDE — done, refuse then refuse again, timestamp held and count moved.
    Sliding: KILLED.
  CROSS-CHANNEL PRUNE — done, assertion taken from the design sentence as you said: a quiet channel's
    stale rows must be gone when a DIFFERENT channel spends. Scoped to the writer's channel: KILLED.
  countSince — DELETED, not tested.
  BODY NUMBERS — corrected. Budget-zero reds 25, not 23. And the walkthrough datum is 2 posts/3 wakes
    while the budget charges per POST, so that exchange spends 2 and twenty is TEN exchanges per
    window, not the six or seven I claimed. I also removed the sentence saying "per post" is the unit
    the walkthrough's "3 wakes" was measured in — it is not, and that was the error underneath the
    arithmetic.
  AGENTS-ONLY LATCH — no new mechanism. The ERROR line now names the recovery: "A HUMAN member's post
    clears it; if none is a member, add yourself (channel.member.add) and then post." Filed
    `t3_bot-b4t` (P3, depends on 64d) with the three real options and the one I would argue for, plus
    a DO-NOT: closing it by weakening the membership check on posts would undo `t3_bot-a44`.
  #8 COMMENT — fixed. `refuse` is the second place a mention is knowingly dropped and, in a runaway,
    the dominant one.

FIFTEEN MUTANTS NOW, all killed. The six added since the lanes ran are each a case where an argument
had no fixture behind it — which is the same sentence I have written three times today, and the
reason I now write the mutant before I trust the paragraph.

GATE on 9d85659ab over 06543c619: vp check 0 errors, vpr typecheck 0, apps/server 4691 passed /
12 failed, identical by NAME to baseline. Runner counts: MentionWakeReactor.test.ts base 30 -> head
47, no other test file touched, no decrease. Design lane skipped and said so in the body: server-only
.ts, no UI surface.

NEXT: 7br's survivor, per your ruling — the socket-side fixtures assert against events that could not
have come from the code, so I add the test that reads a real removal event out of the ENGINE and
asserts the ref is on it, then /review-pr. And #22's verifier report, which I still have not read and
will read whole before saying anything about it.
