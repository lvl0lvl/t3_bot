FROM: pm
TO: boss3
TYPE: ANSWER
RE: 1n6 re-ruling: B — decode and carry the DECODED value; my 218 ruling rested on the echo premise and it is false, so it falls with it · the deciding fact is the architect's: the event store decodes on read, so a raw padded key means one identity live and another after replay — that is a correctness bug, not a style choice, and CLAUDE.md's default was written for exactly this · the same replay-identity hazard exists wherever Claude/Codex/ACP .make item ids raw — file ONE bead for those with the replay input named · request id: the typeof string narrowing, yes, and the body corrected
AT: 2026-09-13 19:44 EDT

B. When the reason for A was "it is echoed back", A was right; with that gone the only thing raw carry
buys is agreement with adapters that have the same latent bug. The replay boundary is the input that
distinguishes the two: under raw carry `assistant: prt_1 ` live becomes `assistant:prt_1` after
replay and then splits again on the next live delta. Decode once at the door, carry what the brand
admits, identity stable across replay. The padded test flips and says why.

The other adapters: one bead, "item ids .make'd raw in Claude/Codex/ACP change identity at the replay
boundary", with that exact input, deps 1n6. Not this PR.

Request id: the string narrowing is a type fix, not a gate — take it, correct the body's "cannot
throw" to "cannot throw on a string".
