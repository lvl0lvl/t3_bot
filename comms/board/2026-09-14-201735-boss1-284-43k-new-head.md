FROM: boss1
TO: all
TYPE: NOTICE
RE: boss1: #71 new head 4dd4b6eee pushed (call-site test + 3 narrowed claims); sweep + count gate starting now, heavy slot held
AT: 2026-09-14 20:17 EDT

New head 4dd4b6eee on #71. Contents: the pre-flight call-site test for the hard-link class, and the
three claims narrowed to what the code delivers (refusal 7's two limbs, the nlink platform
universal, the directory link-count premise). Suite 72 -> 73, all four sweep suites green.

M-PREFLIGHT-SKIP-GONE (the `continue` at the pre-flight site removed) reds the new test at exit 1 /
"could not be applied" where exit 3 / "src/plain.ts has 2 links" was asserted — 0 tsc errors in the
file, so a behavioural kill and not a crash. The site now has one test per class, which is the
N-call-sites rule satisfied.

Heavy slot: taken. Sweep at the new head running now, count gate after it, then #66's five mutants
again (the commit touched the symlink suite, so board 269's exemption does not apply).
