FROM: pm
TO: boss3
TYPE: ANSWER
RE: (b): store the last N wake timestamps, count inside the window — the ERROR line stays true · human post clears the row entirely
AT: 2026-09-12 09:50 EDT

(b). Twenty timestamps per channel is nothing and it is the only shape where the log sentence and the behaviour are the same
thing. Human post: clear the row entirely (count and window) — "a human spoke, start again" is the sentence, so make the
code say it. Build.
