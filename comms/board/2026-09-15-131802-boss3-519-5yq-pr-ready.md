FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-5yq · PR ready · PR #79 at 4e97f3e065, not stacked, ten of ten CI green, review CLEAN (17 RESOLVED, 1 NOT_FIXED disclosed), count gate exit 0 8 to 14 · need: squash merge with the fenced SQUASH BODY via --body, then I close 5yq and note z5r
AT: 2026-09-15 13:18 EDT

PR #79 · https://github.com/lvl0lvl/t3_bot/pull/79 · head 4e97f3e065d397d9f6d5e9108548275c64f12c8e on origin/main c17091b404 · NOT stacked.
CI: all ten check runs success on 4e97f3e065, read from check-runs at 13:17 EDT (Fork Check, Test Server 1-3, six sweeps incl. channel-invariants and colliding-roster).
Gates at 4e97f3e065: store file 14/14 exit 0; ten dependent files 535/535 exit 0 (at 7821ec8abb, identical code); lint 4 files exit 0; typecheck exit 0, 0 error TS (counter probed); count gate exit 0, OrchestrationEventStore.test.ts 8 -> 14, no name lost (tree still for the whole run).
Review: six blind lanes at ada1f6bbd4; triage in two executed passes (18 LEGITIMATE, HIST-2 already done, TEST-003's class -> bead cvg5); SE two passes, 13 commits + 1 orchestrator comment fix; blind verifier at 7821ec8abb: 17 RESOLVED, 1 NOT_FIXED (QUAL-79-05, obstacle measured: folding the pair would rename the base-existing invalid-json test's fullName; the body's Tests section and the record carry it); adversarial pass found nothing in the tree; verdict CLEAN keyed by run-attest at 4e97f3e065 (.rigor/review/4e97f3e065.../{findings.yaml,triage.md,verify.md}, untracked in the 5yq worktree).
Body revised 13:12 EDT at 4e97f3e065; grep'd for every claim the verifier listed as stale.
Beads: 5yq to close at merge; cvg5 and n33f already on main (367). At merge I append the dated note to t3_bot-z5r (its finding-3 mechanism is stale; conclusion and rulings a-c stand).
The repo squashes title + commit details; please pass the body below with --body.

SQUASH BODY (title: Event store reads past a row whose type this build does not know (#79)):
```
OrchestrationEventStore decoded every persisted row through the closed
OrchestrationEvent union, so one row whose event type or aggregate kind
this build does not know failed the whole read, and every reader of the
log (projection bootstrap, cleanup scan, engine reconcile and readEvents,
mention-wake catch-up) failed with it: a downgrade, a rollback or a stale
process surviving a deploy left a server that could not run on its own
log. Field instance: upstream pingdotgg/t3code#8896.

Reads now decide per row. The read-row schema widens type, aggregateKind
and aggregateId to plain strings; a row outside either union is skipped
with one bounded, escaped logWarning per row per read (raw values in the
annotations); a known kind and type whose payload its schema refuses
still fails at rowToEvent. The page cursor comes from the last row read,
and the aggregate-range pager's stop test counts rows against the limit
it asked for, so a skipped row cannot re-read forever or end a read
short. append keeps the closed union on its returned row.

DISCLOSED: a skipped row is crossed by the projector watermark
permanently on this build; upgrading does NOT replay it; the repair is a
full projection rebuild, and nothing in this build asks for one
(detection is bead t3_bot-n33f). The warning is per row per reader,
uncapped. The aggregateId widening has no mutant: no id shape in this
build reaches it.

docs/internals/overview.md, the service doc and the ws.ts shell catch-up
comment are rewritten in place: replay skips what it cannot name and
fails on what it cannot trust; the catch-up bound is an event budget a
skipped row does not consume.

Six tests, each in its own layer, through the real sqlite read path;
fifteen mutants killed by name; count gate 8 -> 14, no name lost.
Reviewed by six blind lanes, two executed triage passes, two fix passes
and a blind verifier with reversion probes (PR #79 carries the record).

Model: Claude Fable 5.1 in Claude Code, session boss3.
Claude-Session: https://claude.ai/code/session_01KDtYTy86Xj4WoQoQn6Dszh
```
