FROM: boss3
TO: pm
TYPE: REPORT
RE: #22 v4 at ee4faeff7 — verifier read whole, found three more · #24 (7br) open with six lanes out
AT: 2026-09-12 12:24 EDT

#22 v4 pushed at ee4faeff7 — I read the verifier whole and it found THREE more · #24 (7br) open with six lanes out

I READ verify22.md WHOLE, as you said and as I owed. It is 301 lines and worth the time: seven of nine
claims VERIFIED, two qualified, five adversarial findings. The three that mattered:

  1. v3 COMMITTED THE EXACT FAULT IT NAMED. Line 1864: "it is empty for every RUNNING turn". Line
     1890: "this row links post B". Post B is STAGED while turn-1 runs — the verifier printed the row.
     Both sentences in one block, twenty lines apart, and the second is the whole point of the
     paragraph. I wrote "two statements of opposite sign in one comment block is worse than either
     alone" and then did it.
  2. v3's CORRECTION OF v1 IS FALSE ABOUT THE FIXTURE ON SCREEN. I said v1's "two posts leave the
     first post with nothing to find" was false because the first post's key is on the turn row. For
     THIS test there IS no turn row — it starts no turn, `turnRowCount = 0`, and the first post's key
     really is nowhere. v1 was right about the code in front of the reader and I "corrected" it into
     a falsehood. I also quoted v1 with its qualifier ("inside a turn") stripped, which is what made
     the difference.
  3. THE GAP IS WIDER THAN I STATED. I framed it as a cancellation problem. An ordinary second
     `session-set` on a still-running turn deletes the staged row too — no cancellation involved. A
     criterion-3 builder reading me would scope their fix to cancellation paths and miss the ordinary
     one.
  Plus: "two of the five triggers appear in neither bead" is THREE, in a paragraph whose subject is a
  previous miscount. And nine bare line numbers that turn into confident wrong pointers on any
  insertion above them.

v4 TAKES YOUR RULING: SAY LESS. Four numbered facts, each one a lane executed, and an explicit line
saying nothing in the file demonstrates them. Gone: the five-trigger list and its count, the bare line
numbers, the cancellation framing, and every claim about what this test stages. The title also went —
it promised "can find the turn" about the one row the block below says must not be used to find it.

Comment-only confirmed mechanically against the new base: one line differs, the title. `expect(`
multiset identical. vp check 0, vpr typecheck 0, 47 passed.

#24 IS OPEN (t3_bot-7br), head 664b32fe7, rebased onto e46b0ee76, six lanes dispatched. Four mutants,
each killed by the test that names it — and the fourth SURVIVED the first pass, which is the finding
worth your attention: every socket-side test builds its own event fixture with the ref typed in by
hand, so deleting the two payload fields from the decider left all of them green. A fixture that can
only be hand-built cannot distinguish "the code produces this" from "I typed this". I took your first
option and pinned it through the decider.

Gate on 664b32fe7: vp check 0, vpr typecheck 0, apps/server 4694 passed / 12 failed identical by NAME
to baseline. Runner counts: server.test.ts 189 -> 191, decider.channels.test.ts 28 -> 29. Design lane
skipped, said so in the body.

NOTHING NEEDED FROM YOU. #22 v4 is yours to merge or to send back; #24's lanes will take a while.
