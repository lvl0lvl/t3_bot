FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh + t3_bot-x4v · both rebased on 7f1996566, both CI green · #27's gate in its base run, #28's queued behind it
AT: 2026-09-12 17:44 EDT

t3_bot-2oh · #27 CI green on the rebased head, gate in its base run · nothing needed
t3_bot-x4v · #28 CI green on the rebased head, gate queued behind #27's · nothing needed

BOTH BRANCHES ARE ON 7f1996566 NOW. #27 is 2d08827b0, #28 is 2a0fedfa8, both force-pushed, both green
on Fork CI (#27's three server shards included, which is the evidence that the twelve failures my local
apps/server run shows are this macOS machine's symlink/provider set and not the change).

#27's final gate is measuring with the rename explained. I checked the --allow key against BASE rather
than head before trusting it — `git show origin/main:…/channelCursor.test.ts` has `describe("channelCursor")`
and `it("refuses a cursor another channel issued")`, so the full name the gate lost is the one the
--allow names. A mistyped --allow is exit 2 and another twenty-five minutes.

THE GATE IS SLOW BECAUSE THE MACHINE IS SHARED: there is a `guard-sweep-A1K3Ec` worktree running
alongside it, presumably boss1 on 7rj. Not a conflict — separate temp roots — but it is why one cold
base install plus thirteen suites is taking longer than the earlier run. I am running one heavy
measurement at a time for the same reason, so #28's gate starts when #27's finishes.

ALSO WORTH KNOWING: #28's injective report filename is live in this run and works end to end — the
report for the mobile workspace is `%40t3tools%2fmobile.json` rather than `-t3tools-mobile.json`. The
collision that used to be caught downstream by deleting the file cannot be constructed now.

I verified the guard sweep's 18 anchors still match the rebased head (18 mutations, 0 anchors wrong), so
the re-run will MEASURE rather than report "not run" — exit 3 outranks exit 2 for a reason and a sweep
that did not run is not a sweep that passed.

Order from here: #27 gate table -> re-run the sweep at the final head -> PR body -> "PR ready #27".
Then #28's gate -> its table -> "PR ready #28".
