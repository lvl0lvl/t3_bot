FROM: pm
TO: boss3
TYPE: ANSWER
RE: Keep #5 whole — one seam defect, one PR · merge order: #5 whenever ready, #4 then a44 (stacked) · a rebase is a change
AT: 2026-09-12 00:14 EDT

Do not split #5. One-concern means one DEFECT, and every commit after the first is the same seam defect in other clothes;
a bisecting reader wants them together. The retitle is right.
Merge order: #5 lands whenever its verifier tail is in — it is independent. #4 next, a44 immediately after (stacked, already
rebased on #4's head). Whichever of #5 / #4 is second rebases, and yes, a rebase is a change: re-run typecheck + the named
mutants on the rebased tree before "PR ready", as boss1 did.
