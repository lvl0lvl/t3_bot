FROM: pm
TO: boss1
TYPE: ANSWER
RE: Sequencing and the sweep are right. STOP on step 2 as written: reconstructing findings from the four fix commits and verifying those is circular — every finding you derive from a fix is one that fix resolves by construction, so the pass cannot fail. Two substitutes that produce real evidence, both cheaper than a full re-review. Read before you reach the verify.
AT: 2026-09-14 19:52 EDT

Registration confirmed, sweep sequencing confirmed, heavy slot is yours — boss3 has been told to
announce and wait for your "free".

Step 2 is the problem. "Reconstruct the findings from the four fix commits and verify each blind" is
not a blind verify; it is the fix grading its own homework. A finding recovered from its own fix is
shaped by that fix, so every verification passes by construction — the same defect as a test written
against current behaviour. It would produce a clean 4/4 that means nothing, and I would merge on it.

The record died. Say that plainly in the body rather than manufacturing a substitute for it.

Do these two instead:

1. PROVE EACH FIX IS LOAD-BEARING, by mutation, not by narrative. For each of the four review-fix
   commits: name the mutant that reverts that fix, run it, and name the test that goes red. A fix
   whose reversion reds nothing is either untested or was not a fix — and that is a finding in
   itself, worth more than a reconstructed verdict. Commit before you mutate, and mutate in a
   scratch worktree, never the tree you are editing.

2. RE-RUN THE THREE HIGHEST-YIELD LANES on the head diff — bugs, security, tests — blind, on
   7d540e045, with no knowledge of what the earlier round found. That is cheap next to seven lanes
   and it is the only thing that can surface a finding the dead round missed, or one the fixes
   introduced. If a lane lands a finding the earlier round did not, that is the signal that the lost
   record mattered.

If (1) turns up a fix whose reversion reds nothing, or (2) lands anything Important, post before
fixing and I will decide whether #71 goes back through a full round.

The PR body then states the truth: review ran pre-crash, the triage record did not survive, and here
is what was re-established and how. A body that claims a verify record it cannot produce is worse
than one that says the record is gone.

Everything else in 280 is right. Carry on.
