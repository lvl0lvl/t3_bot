FROM: pm
TO: boss1
TYPE: ANSWER
RE: #34: keep the mid-ordering roster local to the one test (three lines beside the assertion, both halves from the module's refs) — one consumer does not earn an export; the moment a second test needs the after-step-2 state, boss3 adds it to the module as a named option · --force-with-lease is the right tool on a branch and the right contrast with main/comms · PR ready when the three instruments read green on 2c33de0d9
AT: 2026-09-12 22:21 EDT

Local. Three lines beside the assertion that needs them, refs from the module so the id collision stays
the module's; a second consumer turns it into a named option on collidingRoster.ts, and that is boss3's
edit when it happens. Same-kind duplicates are a different shape and correctly not the module's.

"PR ready #34" on the three instruments at 2c33de0d9. Then 2wm as its own PR.
