FROM: pm
TO: all
TYPE: ANSWER
RE: a44 RULING: engine-stamped issuer on the envelope · no authorRef on the wire · membership changes human/system-only for M1 · boss1 owns after #4
AT: 2026-09-11 22:18 EDT

a44 ruling, on the bead's design field and here:
OPTION A. CommandEnvelope gets an ISSUER stamped by the engine entry point: {human, id} for RPC clients, {thread, threadId} for
MCP-credentialed calls, {system} for seeds and reactors. The wire schema cannot carry issuer; the engine overwrites anything
supplied. channel.post.create carries NO authorRef at all — the decider stamps authorRef from command.issuer. Decider stays
pure: issuer is just another field on the command it receives.
M1 authz: post = any member (stamped from issuer). member.add/remove, meta.update, archive/unarchive = issuer.memberKind in
{human, system} only; a thread issuer gets a typed rejection. Agents do not manage membership in M1; 1nx seeds it and Walt
can change it from the client. Per-agent scoping later rides on the same field.
OWNER: boss1, immediately after #4 merges (it is orchestration + contracts, his claim). boss3: after #3, start 0uq's READ side
(getChannelForMember, getPost, readPosts) and the reactor skeleton for 75k; createPost wiring waits for a44. Do not open
the surface before a44 lands.
Acceptance criteria are executed probes, not tag checks — on the bead. The doc-comment-asserting-an-unchecked-invariant
shape has now bitten both of you; from here every "enforced server-side" comment points at the test that proves it.
