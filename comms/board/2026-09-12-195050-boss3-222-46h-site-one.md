FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-46h · shared roster built; reactor collapsed onto it; its 3 memberKind guards mutated, one SURVIVED that the bead did not list, now killed · 3 sites to go
AT: 2026-09-12 19:50 EDT

t3_bot-46h · fixture module built, site 1 of 4 collapsed, a fourth guard found and pinned · nothing needed

MEASURED FIRST, on the tree I am changing: 144 (memberKind, memberId) pairs across 11 test files;
4 files hold a collision, each spelled differently — `shared`, `TWIN_MEMBER_ID`, `"thread-boss1"`,
`HUMAN_OPERATOR_MEMBER_ID`. Two of the four had picked the operator's id by accident. That is the
"seven sites": four spellings of one fixture plus seven files with none.

THE ONE SPELLING: `orchestration/testing/collidingRoster.ts`, following the vcs/testing convention.
  - The id is HUMAN_OPERATOR_MEMBER_ID. Both refs come from the contract's own makers —
    `refFromOperatorSession()` IS ("human", operator id) by definition, and `ChannelMemberRef` is
    nominal on purpose, so a literal cannot become one without visibly going around them.
  - TWO CONSUMERS, because tests drive the aggregate two ways: `seedCollidingRoster` does the three
    dispatches in the only order the shape guard admits (human seated while no thread carries the id,
    thread created after, thread member added last — my criterion-4 proof from #24, verified again
    against `requireChannelMemberShape`'s source: "is a thread id" means a thread with that id EXISTS
    at add time, not a string shape); `collidingReadModel` hands the decider's pure tests the roster
    as events leave it, which is also the pre-invariant path.

SITE 1, THE REACTOR: its inline three-dispatch seed was the model for the shared call and is now that
call. Local spellings gone. Its budget test already carries criterion 3's assertion — the twin thread
does not reset the budget, the human of the same id does.

THEN I MUTATED ALL THREE memberKind GUARDS IN THE REACTOR, not just the one the bead names:

  W1  wake filter, kind dropped          KILLED by 1 (the pre-8i2 fake-row test)
  W2  author exclusion, kind dropped     SURVIVED
  W3  budget reset, kind inverted        KILLED by 8

W2 is a guard the bead does not list. `!(authorRef.memberKind === "thread" && authorRef.memberId
=== member.memberId)` keeps an agent from waking on its own post; with the kind clause gone it reads
"not anyone with the author's id" — so on the colliding roster a HUMAN can never wake the thread that
shares their id. No fixture had a human author whose id matched a mentioned thread. Now one does,
through the shared roster, both directions: the human's mention wakes the twin, the twin's own mention
does not. W2 dies to that test alone. 54 tests in the file.

This is the bead's thesis confirmed one more time: the fourth site was there to be found the same
way, and only the fixture that can distinguish the two implementations found it.

Next: commsLive (`shared`), decider.issuer (`"thread-boss1"` — the needle: its collision is spent on
the shape guard and never reaches the membership guard), server.test (`HUMAN_OPERATOR_MEMBER_ID`),
and then the seven files with no collision that test a membership comparison. Criterion 2's three
named sites get their kill counts recorded as I reach each. Branch boss3/t3_bot-46h-colliding-roster
at 2 commits, pushed.
