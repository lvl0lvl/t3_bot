FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-eb3 · PR #15 open at aca1335be, gating · the startup generator runs in a test and all eleven mutants die · eight phases verified by deletion, four named as unpinned
AT: 2026-09-12 06:36 EDT

eb3 · PR #15 open at aca1335be · the startup generator now runs in a test, and every one of the eight phases I claim dies when deleted

t3_bot-eb3 · PR open, gating · the survivor I reported on #12 is closed · nothing needed from you.

PR #15: https://github.com/lvl0lvl/t3_bot/pull/15 · rebased on afb1dc782 · gate running on aca1335be.

WHAT MADE IT CHEAP, and it is the part worth reusing: `runStartupPhase` ALREADY names every phase in a
span — `server.startup.<phase>`. A recording tracer turns the phase list into an array and ONE assertion
pins all of it. The alternative I started with was a double per phase recording that it was called, which
measures the doubles. The tracer pattern was already in this repo, in RpcInstrumentation.test.ts; I did not
invent it and I should not have needed to look for it twice.

Sixteen collaborators are doubles. The orchestration stack is NOT: real engine, real sqlite file, real temp
directory, and the hierarchy read back through getCommandReadModel. A seed asserted against a fake engine
passes for a seeder that dispatched nothing, which is the whole reason this bead exists.
The boundary is `awaitCommandReady`, which settles at signalCommandReady — so it waits on the contract, not
on a sleep and not on a span count.

THE SWEEP IS THE BEAD'S OWN ACCEPTANCE CRITERION RUN AS AN EXPERIMENT. Baseline 2. One mutant per phase:
  keybindings.start ............. 1 red      provider-sessions.reconcile ... 1 red
  settings.start ................ 1 red      projects.auto-pull ............ 1 red
  reactors.start ................ 1 red      hierarchy.seed ................ 2 red
  auxiliary-roots.parked ........ 1 red      welcome.publish ............... 1 red
  hierarchy.seed moved after the bootstrap fork ..... 2 red
  opt-out never fires .............................. 1 red
  opt-out always fires ............................. 1 red
Eleven mutants, eleven dead, each by name.

THE REORDER ROW IS THE ONE I CARE ABOUT. Seeding after the bootstrap fork is the race the phase placement
exists to avoid, and until now that was a comment nobody could check. It is now a failing test.

AND WHAT IS NOT PINNED, because "twelve phases" would be the easy claim and it is false. NINE are in the
assertion; FOUR are outside the boundary and I am naming them rather than letting the count imply coverage:
  welcome.autobootstrap, heartbeat.record, headless.output / browser.open — all inside forkParked, so
    startup does not await them and they may not have run when the assertion reads the spans.
  ready.publish — it runs AFTER signalCommandReady, which is the boundary the test waits on. Catching it
    means waiting on something later, which is waiting on the implementation again.
`http.wait` is in the list but not in the sweep: deleting it removes the Deferred the harness satisfies by
calling markHttpListening, so that mutant changes what the harness DRIVES rather than what it MEASURES.
So: eight verified by deletion, nine pinned by the list, four unpinned and named.

I also distrusted it for passing on the first boot. Two tests green immediately, one of them asserting an
exact nine-element list, is likelier vacuous than right — and the second test asserts three empty
collections, which is also what you get if the generator never ran at all. The sweep is what separates
those two worlds, and it is why I ran it before reporting rather than after.

STATE, all of it:
  #14 (zuy a) ..... Fork green, six lanes in, five returned, synthesis next. Pre-fix gate recorded.
  #15 (eb3) ....... open, gating. Closes the #12 survivor.
  zuy (b) ......... 28b9cb450 pushed, unPR'd. Stream half done and swept; snapshot half blocked on e60 and
                    staying blocked rather than growing a second channel read path.
  a4i ............. scripts/guard-sweep.ts pushed on boss1/t3_bot-a4i-guard-sweep, 15 tests, lint 0, unPR'd
                    because you said post-M1. Say the word and it is a PR in a minute; it touches no server
                    code so it is orthogonal to the merge order.

NEXT: #14 synthesis and blind triage when the last lane lands.
