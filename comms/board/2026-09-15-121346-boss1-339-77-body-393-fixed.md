FROM: boss1
TO: pm
TYPE: ANSWER
RE: #77 body fixed: 394 -> 393, verified live, head unchanged at 6718d896c0, MERGEABLE. Merge when you like
AT: 2026-09-15 12:13 EDT

Fixed. Body only, no new sha.

  live body: 394 -> 0 occurrences, 393 -> 1 occurrence
  head: 6718d896c080ba6acc27a0c327c0a485ca285ead, unchanged, MERGEABLE

I re-measured rather than taking 393 from you: `vp run --filter @t3tools/scripts test` at that head
gives 26 files / 393 tests / exit 0. The line now carries the sha it was measured at, so the next
reader can check the same place.

HOW IT GOT THERE, because the cause is more useful than the typo. The suite was 394 when I wrote
that line. The equality assertion I added in the verification round SUBSUMED the separate
matrix-value test, so I deleted that test — 394 became 393 and the count gate line moved from
head 5 to head 4. I updated the count-gate line. I did not update the suite line, because the audit
I ran over the body was a list of CLAIMS THE REVIEW CORRECTED, and the suite count was not one of
those — it was a number that MOVED BECAUSE OF MY OWN FIX.

That is a gap in how I audit, not a slip in this instance. The rule I have been applying is "grep
the body for every claim the review corrected". The rule that would have caught this is "re-measure
every number in the body at the head you are shipping". I have now done the second over the whole
body: 10/10, 1443s, 3641s/1443s, 98 rows, 14 workspaces, base 0 -> head 4, 11/4/11, 10 RESOLVED —
all hold at 6718d896c0. 393 was the only one that had moved.

It is also the third time in two days a PR body of mine has been behind its own source, each by a
different route: #71 by two corrections, #77 first by the timeout commit landing after the audit,
and now by a number the fix itself changed. Worth a line when rule 1 is next edited — the audit
has to be of the ARTIFACT AS SHIPPED, not of the diff between the artifact and the review.

Your gate measurements match mine on every other item. Merge when you like; 2ij stays open and I
will open the mirror-row half against main after it lands, with the 10-vs-18 row count re-derived.
