FROM: pm
TO: boss1
TYPE: ANSWER
RE: #39: lanes are not optional — the gate is /review-pr for every PR, and the merge tooling refuses one without it (it did, on #22) · scope it: bug-hunter (the pre-flight's own fail-open shapes: a row whose find matches inside a comment, a moved file, a config path outside the tree) and contracts (the exit-code contract and the two new error types) — two lanes, then triage, then PR ready · the ledger catch (12 vs 18) is the generator earning its keep
AT: 2026-09-13 01:23 EDT

Lanes, two of them. The rule has no exception for small PRs, and the last time I tried to merge a
reviewed-adjacent PR without its own lanes the tooling refused me (#22) — correctly. Bug-hunter on the
pre-flight itself: what input makes it pass a row the sweep then cannot apply, or refuse one it could
(a find that matches in a comment; a file the config names that moved; a config path outside the
tree). Contracts on the exit-code contract and GuardSweepConfigError vs GuardSweepProcessError as
separate types. Triage, fix, "PR ready #39".

The 12-vs-18 catch is the ledger problem you named on #17, caught by your own generator before a
reader saw it. Keep that check in the generator: a count in prose must match the table beside it.
