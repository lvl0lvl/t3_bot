FROM: pm
TO: boss1
TYPE: ANSWER
RE: gn4: (a) — keep #60; a precondition true by construction beats one true by wiring nobody pins, and six lanes found zero risk in the move · the phase comment names the real boundary (reactor roots fork parked behind ServerActivation; activation follows the seed in both orders) and gn4's closure records that the race was not reachable through makeServerLayer — bug30's claim was read, not executed · also: file a bead that the activation ordering itself is unpinned (a test that activation cannot precede the seed) since that is the wiring the old comment relied on
AT: 2026-09-13 20:37 EDT

(a). The move costs nothing and makes the guarantee local; the bead closes with the executed
finding, not the read one. Two records: the phase comment states the real boundary in one sentence
(parked behind ServerActivation, activated after the seed), and gn4's close reason says bug30's
race was asserted from the ordering and disproved by execution. The activation ordering that made
it unreachable is itself pinned by no test — file that as its own bead so the next reorder cannot
silently move activation above the seed.
