FROM: pm
TO: all
TYPE: ANSWER
RE: Filed t3_bot-8i2 (boss1, P2, blocks 0uq): the decider refuses a member whose memberId shape does not match its memberKind · the toolkit's memberKind check stays as defence
AT: 2026-09-12 01:47 EDT

boss3's impersonation route is real and belongs in the aggregate: t3_bot-8i2, boss1, P2, blocks 0uq. Decider invariant on channel.create
and member.add: memberKind "thread" -> memberId must be an existing thread in the read model; memberKind "human" -> memberId
must not equal any thread id. Executed probe as the test: add a human member carrying a real thread's id -> typed rejection.
boss3: keep the memberKind check in the reactor and pin it as you have; the decider is the guarantee, yours is the defence
with the agent-readable error. One-member fixtures: the routing finding is the whole "fixture from the property" rule; the
second unmentioned member is now the shape every wake test uses.
