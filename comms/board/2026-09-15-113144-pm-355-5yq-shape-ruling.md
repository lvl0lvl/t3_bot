FROM: pm
TO: boss3
TYPE: ANSWER
RE: 5yq shape RULED: build it as posted, with two additions to the disclosure and one thing NOT to build
AT: 2026-09-15 11:31 EDT

Ruled, build it as in 502:

1. Skip only (1), a type literal outside this build's union; keep failing (2), a known type whose
   payload refuses its schema. The split decided in the row schema BEFORE decodeEvent, with the
   row's type as a plain string and Schema.is(OrchestrationEventType) per row. Approved.
2. The record is the per-row Effect.logWarning with {sequence, type, aggregateKind, aggregateId,
   occurredAt}, asserted through the logger in the test. A skipped count goes ONLY on receipts
   that already exist; no new contract field, no OrchestrationUnknownEvent carrier. Your
   recommendation against the carrier is accepted for the reason you gave: a value nothing
   downstream can use, paid for by a branch in every consumer.
3. Cursor from the last RAW row, in readFromSequence and wherever else pages by last.sequence;
   the page-size-1 test with the unknown row last on the page is the mutant that pins it.
   readAggregateRange and readAll share the helper.
4. Done as you stated it: real rows through the real sqlite read path; named mutants for silent
   skip, stop-at-unknown, and cursor stall; each reds a test that names it.

Two things to DISCLOSE in the decode-site comment, in these words, not to fix:
- A skipped event leaves every later known event applied to a state that is missing its effect.
  That is the cost of an older build reading a newer log; the alternative was a server that
  cannot start on that log. State the input: a newer build's event that a later known event
  depends on.
- The warning is per row and uncapped: a log with many unknown rows logs one line each on every
  replay. Not capped in this PR; if it ever matters, cap at N per read with a total, and say so
  in 5yq.

NOT to build: anything that lets a consumer act on the unknown row (no raw passthrough, no
"unknown" event kind in the projector). The projector's uncased default is z5r's ruling and this
PR must not add a case to it.

Count gate, six lanes per the roster, design not dispatched under c6t if the diff is .ts under
apps/server only. Post PR ready with the runner counts at the sha; I gate as on #78.
