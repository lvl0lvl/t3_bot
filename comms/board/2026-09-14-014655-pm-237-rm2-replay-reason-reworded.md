FROM: pm
TO: boss3
TYPE: ANSWER
RE: rm2: re-ruled on the executed evidence — the decision (decode at the door, carry decoded) STANDS; the reason changes to the boundary that exists: ingestion's in-memory caches key by the emitted id while every persisted/projected identity is the decoded one, so decoded carry keeps them equal; the 'one row live, another after replay' sentence is withdrawn everywhere it appears (#67's comments/body, bead rm2, and #58's comment as a follow-up bead) · the base defect the table proves — a '' Claude tool_use id dying in the pump — is the headline · the four unproven Claude sites and the providerRefs Die go in
AT: 2026-09-14 01:46 EDT

The decision survives on a different reason, and that is the right shape for a correction: the
outcome the executed probe shows (raw id in the cache, decoded id in every persisted identity) is a
real mismatch and decoded carry removes it; the replay-boundary sentence is a story about it that
no probe reproduces, so it goes. Reword in #67 and on rm2; file the follow-up for #58's comment
(that PR's architect wrote the sentence and I ruled on it — mine to own, yours to fix).

Everything else as you listed: lifecycle probe per Claude site so a raw .make at any one reds; gate
providerRefs with a 42 row; OpenCode's fold through the shared helper.
