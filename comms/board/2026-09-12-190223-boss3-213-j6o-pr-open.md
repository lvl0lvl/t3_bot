FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-j6o · PR #33 open at a37604ec8 · sweep 17/17 exit 0 at that head · 184 tests · gate queued behind #32's
AT: 2026-09-12 19:02 EDT

t3_bot-j6o · PR #33 open, sweep artifact in the body from the run's own output, gate queued · nothing needed
t3_bot-1tv · #32 gate re-running with a rename explained; #28's mutants re-run on that file, 4/4 still die · nothing needed

#33 IS OPEN at a37604ec8. Everything in the body but the gate table is measured at that exact head:

  guard sweep     17 mutations, 17 killed, exit 0, baseline 150 tests 0 already failing
  tests           184 across comms, the paged read, the reactor, the pipeline, migration 053
  typecheck       0 in apps/server, packages/contracts, apps/web, packages/client-runtime

THE FIVE ONE-AXIS GUARDS THAT REMAIN ARE THE FIVE WITH NO OTHER SIDE, and the body says which and why:
"wakes attached" and "wakes forwarded" cannot be made wider than attaching the field; the outcome
mapping is total; the two absent-not-empty guards have no inert form because absent is what happens
when nothing is done. The four that DID have a second axis are the last four rows, and one of them
found the human-turn survivor from board 212.

#32'S GATE CAUGHT A RENAME OF MINE — the third today. `refuses a base ref it cannot diff` became
`refuses a base ref that does not exist, before it diffs` when the refusal moved earlier; count went
30 -> 32 with one name gone. Base checked as contained BEFORE reading the LOST line, which is the
discipline #32 makes the tool enforce. Re-running with the rename explained.

AND THE GATE'S CLOSING LINE WAS DISCHARGED, not waved: #28's four mutants on test-count-gate.test.ts,
re-run on #32's head. Three die outright; the fourth anchor-missed because #28 itself refactored
`splitScope` to one filter after the mutant was written — re-anchored against the new text, it dies
to the same named test. The body records all four rather than "3 of 4, one not run".

ORDER FROM HERE: #32's gate lands -> its table into the body -> "PR ready #32". Then j6o's gate on
the freed machine -> its table -> "PR ready #33". Both PRs' surfaces walks are in their bodies; the
j6o one names qa7 as the pane half and says why the pane was split off rather than built here.
