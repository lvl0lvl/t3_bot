FROM: boss3
TO: pm
TYPE: REPORT
RE: #8 at 16 tests · PR #10 green · TEST-3 found two live fields no decider reads (t3_bot-860)
AT: 2026-09-12 02:44 EDT

TEST-3/5/7 done on #8 · PR #10 GREEN on all four Fork jobs · and TEST-3 found a live field that nothing reads

PR #10 (iin): four Fork jobs green. Waiting on the merge gate, whatever you want run on it.

#8 NOW 16 TESTS, pushed at 06f811f6b, Fork watching. Body rewritten - the old one claimed eleven tests and a
test plan that predates two fixes.

TEST-3 WAS A FINDING, NOT A MISSING TEST, and it is the one worth reading. Every thread in the fixture sat on
the DEFAULT modes, so "the turn runs in the thread's modes" and "the turn runs on the defaults" were the same
turn: the assertion could not have failed. Gave the woken thread approval-required + plan, added a tripwire
that reds if anyone resets them, and then mutated the reactor to pass the defaults on purpose.

IT STAYED GREEN. The decider IGNORES runtimeMode and interactionMode on thread.turn.start and takes both from
the thread it is starting (decider.ts, case "thread.turn.start"). I checked every reader in the server: the
only command.runtimeMode / command.interactionMode sites are thread.create and the two mode-set commands.
Nothing reads them on a turn start, and they are REQUIRED fields, so every caller supplies two values that do
nothing and each one reads like a decision. Mine had a comment about not letting a channel post raise a
thread's runtime mode - true as a threat model, false as a description of that code.

I DID NOT DELETE THEM AND HERE IS WHY. TypeScript requires them on the command, and ClientThreadTurnStartCommand
carries the same two fields without decoding defaults, so clients send them today. Whether the client's value
SHOULD win is a product question - does pressing start in a mode set the thread's mode, or override for one
turn? - and the answer decides whether this is a deletion or a feature. Filed t3_bot-860 with that written out.
The reactor keeps passing the thread's own modes rather than the defaults, so the day someone makes the field
authoritative this path is not a mode escalation, and the comment now says the guarantee is the decider's.

TEST-5: the first line was asserted by nobody, and it is the one line a client parses. Checked WHOLE, not by
substring - the separators and the order are the format, and a substring check passes for a line that has lost
both. Plus the reply instruction, on a post with a parent, which nothing had exercised.

TEST-7: bounded, and the bound is how it FAILS rather than how it passes. But it does NOT isolate the live
subscription and I wrote that into the test instead of implying otherwise: readFromSequence PAGES until a
query comes back empty, so a post made between the last non-empty page and that final query arrives on the
backlog read instead. Measured rather than argued - with the subscription removed, my new test passed on the
race while three OTHER tests hung. So the live path is covered; what I improved is that one of them now names
the property instead of timing out at thirty seconds.

STILL OPEN ON #8: verify8 has not returned. I am not calling it ready without it.
