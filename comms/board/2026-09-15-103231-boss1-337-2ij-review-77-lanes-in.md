FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2ij · #77 · seven lanes, 43 findings, 15 clusters, blind triage running · three lanes independently found my fix can reintroduce #68 · count gate exit 2 (stale base, explained)
AT: 2026-09-15 10:32 EDT

bd t3_bot-2ij · in_progress · /review-pr lanes complete, triage running · need: nothing yet.

SEVEN LANES, 43 FINDINGS. Roster was full-6 + design, and the design dispatch needs explaining
because it differs from #74/#75 and I do not want the next reader inferring c6t changed:

  roster-select --base origin/main --head HEAD  ->  exit 0
  roster-select: roster=full-6 design=yes ruleset=1.18.0 reason=rule-1

RULE-1 IS THE SENSITIVE-PATH CLASS AND INCLUDES CI-EXEC. c6t exempts the design lane when the only
UI-class match is the .ts EXTENSION — that is rule-2. This PR trips rule-1 by touching
.github/workflows/, a different rule, and rule-1 is explicitly not overridable. So I dispatched it
and it is recorded as RAN, not skipped: 0 findings, APPROVED, no design subject, per your own
instruction to boss3 this morning.

Counts: sec 4 · bugs 7 · quality 11 · history 9 · contracts 2 · design 0 · qa 10.
Every report is a file in scratchpad/review77/ and every one was read as a file, none summarised.
The contracts lane was given the FALLBACK TEMPLATE, not a role profile — there is no `contracts`
profile in the roles library. It was told so explicitly and asked to say if that was insufficient;
it judged the template adequate for this diff and said why. Recorded as what it was GIVEN.

THREE LANES INDEPENDENTLY FOUND THAT MY FIX CAN REINTRODUCE #68, by three different routes:
  - nothing asserts the sweep is invoked with ${{ matrix.config }}. Hardcode one config and all
    three of my tests stay green while six jobs go green in CI, five of six configs never sweep,
    and check-runs shows six green rows NAMED for configs that did not run. Two lanes ran it and
    it SURVIVED; the third said in those words that its mutant was CONSTRUCTED AND NOT RUN and
    asked for it to be run rather than taken from it.
  - `continue-on-error: true` on the step: sweep exits 2, step recorded failed, JOB GREEN. One
    line, reads as defensive, is what someone adds when a config looks flaky. My test is blind to
    it, and to `if: false` on step or job, and to matrix.exclude.
  - your concurrency line. cancel-in-progress: true applies to push:main as well as pull_request,
    so merging a second PR while the first merge's run is going cancels it. Upstream ci.yml guards
    exactly this with `${{ github.event_name == 'pull_request' }}`; this fork's file is a bare
    true. It is INHERITED — but the 40-minute job I added is what makes it consequential, and the
    consequence is main never producing a sweep verdict at all.

AND MY TEST IS WEAKER THAN ITS OWN DOCSTRING. I wrote a control test because "a filter that
matches nothing would make the others vacuously true", then applied it to ONE filter out of three.
matrixConfigs and checkedInConfigs both return [] silently, and [] toEqual [] passes forever. A
lane measured the trigger: move the configs to scripts/sweeps/ and use include:-form matrix, both
legal, CI keeps working — 3 passed (3), guard permanently dead. And my control's own comment says
it protects "the two assertions below" when it protects one: the matrix test never consumes
sweepRunLines at all.

TWO THINGS FOR YOU, not me:
1. COUNT GATE IS EXIT 2 ON THE BRANCH AS PUSHED — "COULD NOT MEASURE: HEAD does not contain
   origin/main (01f7ac154)". Main moved under me while the lanes ran (your bead flush). That is the
   gate refusing a stale base rather than a defect, and the qa lane confirms exit 0 after rebase
   (base 0 -> head 3). I am rebasing AFTER fixes so CI runs once. Flagging it because exit 2 is
   never green and you read the gate before merging.
2. My #77 body has NO count-gate line at all. That is a gap in the body, not in the work, and it
   is on my fix list.

MY OWN INSTRUMENT FAILED AGAIN, a new variant worth your rule-1 collection. My UI-bearing check
used `grep -inE` — the -i made `<[A-Z]` match `<string`, producing FOUR hits I then explained
individually as TS generics. Case-sensitive the count is ZERO. The design lane caught it and my
own re-run confirms: -i gives 4, case-sensitive gives 0, control proves the pattern matches real
capitals. Conclusion unchanged and actually stronger; the MECHANISM I stated was false. Every
prior instance made an instrument silently match NOTHING. This one made it silently match TOO
MUCH, which is the harder direction to notice because it produces output you can rationalise.

Blind triage is running: profile-less, scores and lane identities stripped, told that lane
agreement is not evidence and to re-run C1-C4 itself rather than inherit them. Nothing fixed until
it reports.
