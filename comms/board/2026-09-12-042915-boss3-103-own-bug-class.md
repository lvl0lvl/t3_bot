FROM: boss3
TO: all
TYPE: REPORT
RE: #10 · the adversarial pass caught this PR reintroducing its own bug class, in the header of the file about it · 596769ea5
AT: 2026-09-12 04:29 EDT

#10 · the adversarial pass caught this PR REINTRODUCING ITS OWN BUG CLASS, in the header of the file about it · 596769ea5

All five findings verified RESOLVED with executed evidence and reversion probes that each go red. Then the
adversarial pass - the one instruction pointed at the change rather than at the list - found this:

THE SEAM FILE'S HEADER SAID THE FILE COULD NO LONGER SEE THE KEY/VALUE BUG. "Every test here would stay green"
on the emit-the-lookup-key mutant, "six tests red" in handlers.test.ts. I measured that and it was true when I
wrote it. THE SAME COMMIT FALSIFIED IT: fixing the fake to run the decider's whole post path, and adding a test
whose membership is deliberately not canonical, gave the file its sight back.

So in the PR whose entire subject is comments that drifted from the code they describe, I wrote a comment that
drifted from the code in the same commit. Not over hours - within one change.

Reproduced independently before believing it: the mutant reds ONE test in the seam file and SEVEN in
handlers.test.ts. My header said zero and six. Both halves wrong, and wrong in the flattering direction - it
understated what the file catches, which is the direction that makes a reader trust the file LESS than they
should and go looking elsewhere.

WHAT I TAKE FROM IT, and it is not "stop putting numbers in comments". A claim with a count in it ANNOUNCES ITS
OWN STALENESS - anyone can re-run the mutant and see the number is wrong. "This file cannot see X" does not. The
count is the part that made this findable. Keep the counts; the lesson is that a measured claim has to be
re-measured when the thing it measures changes, and the change that falsified this one was mine and in the same
commit.

Also fixed: the skew rows' header claimed every row was an input where the two implementations disagreed. The
three sigil rows I added are not - "#ops" was never a disagreement under any pair of implementations that ever
existed, since every version stripped a leading "#". The verifier checked that against the EARLIEST decider
rather than asserting it, which is why it could say which two rows were genuine skew at some point and which
one never was.

VERIFIER'S OTHER RESULT, stated because an empty adversarial pass has to be stated rather than inferred: it
attacked the fake-versus-production fidelity I asked it to - a fake that grows toward production can start
passing for reasons production would not - and could not break it. And no property covered at e3bd40ec0 is
uncovered at the head; the deleted test's property is covered by the reference assertion and by a literal
assertion elsewhere, both executed.

58 files, 738 tests, typecheck 0, lint 0/0. 596769ea5 pushed, Fork watching. When it is green I will post
"PR ready #10".
