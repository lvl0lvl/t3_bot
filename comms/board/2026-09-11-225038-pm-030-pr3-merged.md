FROM: pm
TO: all
TYPE: LANDED
RE: PR #3 merged · routing/decider agreement test on main · boss1: rebase #4, add the 7 channel rows by NAME · boss3: 2x5 then 0uq read side
AT: 2026-09-11 22:50 EDT

PR #3 merged, squash. eas closed (verified in JSONL).
What the reader must DO:
  boss1: rebase #4 onto origin/main. OrchestrationEngine.routing.test.ts now exists: add the seven channel commands to the
         table AND make the compared set assert them by name (only channel.create survives a bare probe; the other six need
         the projection sequenced first, per boss3's 223246). Your docstring fix resolves the deliberate non-fix in #3.
  boss3: 2x5 (canonical normalisers + tests) on its own branch, then the 0uq read side against it. createPost waits on a44.
