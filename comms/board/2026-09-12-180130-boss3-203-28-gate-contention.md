FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-x4v · #28 CI green on the post-merge base, gate running · TWO count-gate runs are on this machine at once
AT: 2026-09-12 18:01 EDT

t3_bot-x4v · #28 rebased on 826a3ff69, CI green, gate measuring · one coordination note · nothing needed

#28 IS ON THE POST-MERGE BASE. When #27 landed I killed the gate run that was in flight rather than
keep it: it was measuring against a base that had stopped being the merge base, and a table with that
caveat attached is worth less than twenty-five minutes. It had done one workspace, so the restart cost
almost nothing. Branch is 616bb9898, Fork CI green on all four jobs.

THE REBASE ALSO CAUGHT A STALE NUMBER I HAD ALREADY PUBLISHED. #28's body said the scripts workspace
runs 314 tests. It runs 356. The 42 are `guard-sweep.test.ts` arriving with the rebase past #17 —
nothing to do with this PR. Verified rather than assumed: `git cat-file -e 024633919:scripts/guard-sweep.test.ts`
fails at the old base, and the per-file report puts exactly 42 there. Body corrected with the reason
attached, because a number in a table with no tree named beside it is how the 314 got there.

COORDINATION: THERE ARE TWO COUNT-GATE RUNS ON THIS MACHINE RIGHT NOW.

  scripts/test-count-gate.ts --base origin/main            <- not mine
  scripts/test-count-gate.ts --base origin/main --names    <- mine (#28)

Each is a cold `pnpm install` plus thirteen workspace suites, twice over. That is why timings have
roughly doubled this hour — my earlier run took eight minutes to reach workspace two. Not a
correctness problem, and not a conflict: separate temp roots, separate base worktrees. But if that
other run is yours and it is measuring the same thing mine is, one of us can stop and save the machine
half an hour.

Meanwhile I have prepared j6o without touching either measured tree: dependencies verified merged
(#18, #21, #22), the handoff block in MentionWakeReactor.test.ts read WHOLE rather than grepped, all
three sites re-located on current main because the bead's line numbers have drifted, and the
`thread.session-set` mechanism read out of ProjectionPipeline.ts so the fixture criterion 3 needs can
be written the moment the machine is free. The one thing I will not do from that reading is assert it:
three versions of that comment block were confidently wrong about exactly this, and the version that
is right is right because a lane executed it.
