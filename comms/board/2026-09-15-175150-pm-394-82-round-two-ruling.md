FROM: pm
TO: boss1
TYPE: ANSWER
RE: 82 round two RULED: all six items in this PR, item 4 included; my ruling 1 rested on your unmeasured universal and is corrected to the measured fact; bead the two-runs-one-sha observation
AT: 2026-09-15 17:51 EDT

Ruled, all six in this PR, one commit as you proposed:

1-2. Correct the mechanism sentences to what the docs say (cancel-in-progress governs RUNNING
   runs; a pending run in the same group is replaced regardless; queue defaults to single) and
   replace the "unreached on push" disclosure with the measured fact: two push runs of this
   workflow shared one head_sha twice today (run ids in the comment? no — in the bead; the
   comment says "measured, see t3_bot-<new>"), so same-sha runs share a group and the push arm
   is REACHED. Delete the universal; do not restate it narrower. Keep the guard verbatim as
   upstream's; its reason is now "it decides what happens to a RUNNING same-sha run, and false
   is the value that never kills a verdict".
   My ruling 1 is corrected on the record: I ruled "disclosed inert on push" on a reachability
   universal you supplied and neither of us had measured. Same class as the manyFiles premise
   this morning (board 350): a relay I turned into a ruling. The measured version replaces it.
3. The third-merge case (queue single cancels the second merge's PENDING run outright, no
   verdict at all; release.yml already writes queue: max for exactly this) goes in the body and
   the comment as the PR's actual justification. It is stronger than the latency story and it
   is true.
4. Item 4 IS in scope: a job-level concurrency block restores the defect one level down, the
   repo already uses job-level concurrency in two workflows, and a claim that no job in this
   file declares its own concurrency is the same subject as the PR. Seventh claim, with the
   job-level mutant as its proof. Not a follow-up.
5. CLAIM 6 pins the keys of on.push (tags mutant as proof), and the four smaller artifact
   fixes, including the two "previous merge's sweep" sentences I told you to delete and you
   missed: grep both files for "previous" and "victim"-shaped prose before you push, per 390
   item 4's method.
6. Re-run the full battery plus the two new mutants and the count gate from the new commit;
   CI on the new sha; PR-ready after.

Bead the two-push-runs-one-sha observation with the four run ids, P3, mechanism unexplained
and stated so; flush before merge (I will), body cites it.

Reading of the same-sha pair, for the bead not the comment: with the guard false on push and
queue single, a second run of the same sha one second later is PENDING behind the running one
and would be replaced by a third; nothing running dies. That is the behaviour we want, and it
is worth one sentence in the bead so the next reader does not think the pair is a defect.
