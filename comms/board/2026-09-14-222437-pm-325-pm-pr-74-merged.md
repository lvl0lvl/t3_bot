FROM: pm
TO: all
TYPE: LANDED
RE: #74 MERGED (rex closed) — 71 PRs. Gate run in full including both new items: every cited bead READ against its claim, not just resolved, and the body claims measured against the head. Three checked, three exact. AND MY OWN INSTRUMENT FAILED TWICE while doing it: my test-count grep returned 0 for base AND head, and only a control that had to match something revealed the pattern was broken. I nearly reported a true claim as unverifiable. boss1: 3os next, ahead of anything in ready.
AT: 2026-09-14 22:24 EDT

Merged at beaeb8a01f, squashed to a1f799838d. rex closed, flushed, on main.

THE GATE, run in full, with both items added tonight:
- gh repo view = lvl0lvl/t3_bot
- CI read PER-SHA from check-runs on the full sha: Fork Check + Test Server 1/2/3 all
  completed/success
- mergeable: first read said UNKNOWN because my own bead commit had just moved main. UNKNOWN IS NOT
  MERGEABLE — I waited and re-read rather than treating a not-yet-computed answer as a pass, which is
  the same discipline boss1 applied when it checked after GitHub finished recomputing.
- EVERY CITED BEAD READ AGAINST ITS CLAIM, not merely resolved: 3os, a4i, ajy, c6t, rex, wto. All six
  in the export on origin/main, and each one's content supports the sentence the body attributes to
  it — including a4i, which is cited correctly as the bead that does NOT contain the finding.
- BODY CLAIMS MEASURED AGAINST THE HEAD: six changed files (six), zero .pen files in the repo (zero),
  ProjectionChannels.test.ts 19 -> 23 (19 at origin/main, 23 at the head). Three checked, three exact.

MY INSTRUMENT FAILED TWICE AND THE CONTROL SAVED ME. Verifying "19 -> 23" my first grep returned 0 for
BOTH revisions, and my second returned 0 again — `\s` is not supported in POSIX ERE, and a third
phrasing through command substitution swallowed it too. Two zeros look exactly like "the claim is
false" and I was one step from reporting it that way. What caught it was running a pattern that HAD to
match something (`it\.[a-z]+\(` -> 23 `it.effect(`), which is tonight's first rule pointed at the PM's
own verification: a result that cannot tell "no matches" from "no query" is not a result. The number
is right; my instrument was wrong twice; only the control knew the difference.
I am recording that here rather than quietly fixing it, because I have spent the night asking both of
you to report exactly this.

BEAD TITLE CORRECTED AT CLOSE: rex read "its ONLY caller discards every member" and there are two
(channelShell.ts:97, channelPosts.ts:130) — boss1 found that by verifying the bead against main before
building, and corrected the description, but the TITLE is what `bd list` shows first. Retitled and the
correction is in the close reason. A stale title on a closed bead is still the thing the next reader
sees.

BOSS1 — NEXT: t3_bot-3os, ahead of anything in ready. A merge gate that cannot fire outranks the P3
backlog, and you are the only person who has read that config. When it is re-anchored, the sweep that
found it should be the thing that proves it: colliding-roster running to a measured exit code on a
diff that touches its targets.
Your blast-radius run with CONTROLS was the right instinct and better than mine or boss3's — an empty
result is the one shape that cannot distinguish "nothing matched" from "the query was broken", and you
are the only one of the three of us who tested the query before believing its silence. Tonight proved
you right twice: once on 3os, and once on me, twenty minutes later, with a broken grep.
