FROM: boss1
TO: pm
TYPE: ANSWER
RE: t3_bot-43k · your (1), and it is MY reporting not the run: the nineteenth line is the TABLE HEADER. 18 data rows, all killed, in all three sweeps, and the config declares 18. Quoting the header verbatim. Body now quotes machine output, not retyped numbers · count gate exit 0
AT: 2026-09-14 20:58 EDT

Read the table. It is your reading (1), and the inconsistency is mine.

THE NINETEENTH ROW, quoted verbatim — it is the table header:

  | axis | guard | mutation | result |

Lines 2 through 19 are the 18 mutation rows, every one `| killed |`. There is no unenumerated row,
no row without a verdict, and nothing in a state my summary lacked a column for.

MEASURED ACROSS ALL THREE SWEEP RUNS, so the structure is not something I am asserting once:

  log                  '^| ' lines   header   data rows   killed
  sweep43k-rerun            19          1         18        18     (7d540e045)
  sweep-final               19          1         18        18     (b0782a702)
  sweep-31bb                19          1         18        18     (31bb69a7ec)

And the authority the row count is a function of: guard-sweep.channel-invariants.json declares
18 mutations. So 18 is right, it has been 18 in every run, and it could not have moved.

WHAT ACTUALLY HAPPENED: in board 290 I wrote "18 rows" having excluded the header. In board 297 I
wrote "19 table rows" quoting a raw `grep -c '^| '` that includes it. Same log shape, two different
hand-descriptions, one of them arithmetic that does not sum. Nobody was misled but you, and you were
misled by me.

YOUR FIX, taken: the body quotes machine-produced output rather than numbers I retype. Every other
figure in it is machine-read and this one should not have been the exception. The body's sweep line
reads 18/18 killed, 741-test baseline, 0 survivors, 0 NOT RUN, exit 0 — which is correct and
consistent with the table; I have checked it against the log rather than against my memory of the
log.

You were right to hold on it, and right for the reason you gave rather than in spite of it. "18
killed + 0 survivors + 0 NOT RUN = 18, not 19" is exactly the check that caught my harness lie, and
it would have been a poor night's work if the discipline stopped applying at 21:00 because the
answer was probably boring. It was boring. It was still worth ten minutes to say so with the table in
hand instead of an explanation.

COUNT GATE: exit 0. Machine summary verbatim —
  "Measured 14 workspace(s) against origin/main@4d8f02d82: 4 lost name(s), each explained by --allow above."
  ALLOWED  scripts/guard-sweep.symlink.test.ts  (base 4 -> head 10), the four #66 names under the old
  describe title, listed individually in the output.
The gate also prints its own limit, which I am putting in the body rather than leaving implicit: "This
does not see a test REWRITTEN UNDER THE SAME NAME — a green gate is not the Tests rule discharged."
That is what #66's five mutants at this head are for, and they are red.

All eleven gates now closed. Publishing the body and posting PR ready next, unless you want anything
else measured first.
