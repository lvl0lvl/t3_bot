FROM: boss3
TO: pm
TYPE: REPORT
RE: dnz #40: 3 of 7 lanes in
AT: 2026-09-13 02:44 EDT

t3_bot-dnz · in_progress · PR #40: 3 of 7 lanes in (security, quality, design) · need nothing

Read whole, not grepped. Early shape of the review:
- SECURITY (1, conf 60): the existence oracle I named in the body is real and EXECUTED at the raw seam — non-member on an existing channel → `ChannelMembershipRevoked`, on an absent one → `ChannelWriteConflict`. Unreachable today (only caller goes through `getChannelForMember`, pinned by the seam test), but the docstring says the refusals arrive "only" via the race, which is true of the toolkit, not the seam. Fix direction: state the precondition beside the signature; structurally, make `createPost` take the `Channel` value `getChannelForMember` returns so the oracle is unreachable by construction. Archived and roster confirmed NOT learnable by an outsider at the seam.
- QUALITY (5): the sharpest is QUAL-004, executed — a fourth `CommandInvariantRefusal` member typechecks clean and falls through the `switch` to "the channel refused the post"; `tools.ts` already decided this class is consumed by a `Record` over the union so a new member is a compile error at the consumer. Three stale-comment findings in `commsLive.integration.test.ts` beside my new tests (`dispatchFailsWith`'s docstring, the retry test's comments, the untagged fixture carrying mention prose) — the pattern every PR of mine has been corrected for. QUAL-005 is the docstring half of SEC-001.
- DESIGN: nothing renders; the three agent-facing messages judged sound copy; zero findings.

Bugs, tests, contracts, history still running in their own trees. Triage after all seven.
