FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-a44 · PR #6 open at https://github.com/lvl0lvl/t3_bot/pull/6 · rebased --onto so the squashed #4 is not replayed · all TEN named mutants replayed RED on the rebased tree, not assumed · /review-pr lanes running with the ref-naming fix that would have prevented my bad HIGH

REBASE: used 'git rebase --onto origin/main 28a97cedc' rather than a plain rebase. #4 was SQUASH-merged, so its
individual commits no longer exist on main; a plain rebase would have tried to replay six commits of mine plus
twenty-three of #4's that are now one squashed commit. Result is six commits on top of main, no duplicates,
verified by reading the log rather than trusting 'Successfully rebased' — which has already lied to me once
tonight.

VERIFIED ON THE REBASED TREE, per your 002752 instruction, not assumed to survive:
  typecheck 0 on t3 and @t3tools/contracts
  774/80 server orchestration+persistence+mcp, 654 shared, 387 contracts
  ALL TEN named mutants replayed, every one RED:

    issuer check fails open                      1 failed
    administration guard allows everyone         1 failed
    system issuer may author                     1 failed
    fixpoint back to a single pass               4 failed
    NFC pass dropped                             4 failed
    invisible-character gate inert               2 failed
    mentions folded before the author check      1 failed
    member.add drops the handle fold             3 failed
    mention dedupe removed                       1 failed
    archived channel accepts posts again         1 failed

  Scripted so it restores each file and prints the tree state at the end; tree clean apart from the known
  pnpm-lock.yaml drift, which I reverted.

/review-pr DISPATCHED: security, bug-hunter, QA, each in its OWN scratch worktree with its own install and the
node_modules symlink check. Three changes to the prompts, all from tonight's failures:

1. EVERY LANE IS GIVEN ALL THREE REFS BY NAME — a44, origin/main, and origin/boss3/t3_bot-2x5-canonical-names —
   and told that any cross-component claim MUST say which it ran. That is the fix for my bad HIGH: a lane
   pointed at my worktree read main's toolkit and I passed it on as a divergence between boss3's work and mine.
   The prompt now says so explicitly, including that the answer differs (0 of 7 vs 7 of 7).
2. THE TEN KILLED MUTANTS ARE LISTED with an instruction not to re-derive them but to attack what they do not
   cover. Last round two lanes spent effort confirming things I had already mutation-proven.
3. A LENGTH BUDGET. Three of four reports last round were truncated in transit and had to be re-requested,
   which cost a full round trip each.

WHAT I ASKED THEM TO ATTACK, since it is where I am weakest rather than where I am confident: the
overwrite-never-reject rule (is a supplied issuer ever distinguishable?), whether a thread issuer can obtain an
effect via ordering, the fixpoint's termination on pathological input, whether stripping a sigil can create a
new composition so the stored value is NOT in NFC, and whether pre-PR single-pass names on main can collide
with new canonical ones in the UNIQUE index but not in the in-memory check — the inverse of the bug that check
was added for.

3pz IS FOLDED IN: the archived-channel decider invariant is in this PR (requireChannelNotArchived on post,
member.add, member.remove, archive; requireChannelArchived on unarchive), with the rename deliberately still
allowed and the docstring saying why.

Will post 'PR ready #6' or a specific reason it is not. Not calling it ready on my own mutants — every serious
finding tonight came from a lane, not from me.
AT: 2026-09-12 00:39 EDT

