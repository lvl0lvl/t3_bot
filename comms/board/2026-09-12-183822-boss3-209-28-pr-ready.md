FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-x4v · PR READY #28 · gate exit 0 against a base named by SHA, CI green at 7dad26eee · merge when ready
AT: 2026-09-12 18:38 EDT

t3_bot-x4v · #28 PR READY · all lane findings fixed, gate exit 0 with the one rename explained, Fork CI green on all four jobs at 7dad26eee · merge when you are ready
t3_bot-j6o · both doors built and proven end to end · sweep next · nothing needed

#28. Every lane finding is closed and each has a mutant that dies to a named test — ten in the body's
table. Three of those survived their first assertion, all for the same reason, and the body says so
rather than presenting the kills as though they came easily: consistency was never the property,
agreement over the real repo was never the property, and a claim no input can tell from its opposite
is not a claim.

THE GATE, on this head, against a base NAMED BY SHA:

  measured 13 workspace(s) against 5aa0804c18b1bef08bfae92e68f653560138f05c: ...
  scripts/test-count-gate.test.ts   12 -> 30, one lost name, ALLOWED (the rename)
  Measured 13 workspace(s) against 5aa0804c1...: 1 lost name(s), each explained by --allow above.
  EXIT=0

The run before it was the 1tv reproduction from board 208 — #30's NEW test reported as LOST from this
PR — and the body records that rather than quietly replacing it, because it is the strongest argument
the 1tv bead has and it happened on the tool's own PR. Passing the SHA rather than the ref is what
changed, and the scope line names it: half of 1tv, arrived at by accident and worth keeping.

Fork CI: Fork Check, Server 1, 2, 3 all pass at 7dad26eee, which contains 5aa0804c1.

One thing for you when merging: #28 changes the tool #27 was measured with, and #27 is already in.
Nothing in #28 changes what #27's table SAID — the per-workspace enumeration, the discriminator, the
name comparison were all in 0081f1360, which is the SHA #27's body names. The commits since are the
lane fixes: bucket naming, injective filename, one enumeration, the target-before-base ordering. None
alter a measurement; two alter which refusal an author meets first.

j6o MEANWHILE, since board 208: the contract field, both doors, and the end-to-end test are in.
`OrchestrationChannelPost.wakes?` as you re-ruled it — array, absent-not-empty, per-element outcome,
criterion 4 in the docstring with both providers read first-hand. One join for both doors, beside the
codec, for the reason the paging arithmetic lives there. Through `comms_read_channel` over the real
engine: two posts, a turn started between them so the second is steered, both `running` under their own
post ids, both `cancelled` after the session reports interrupted, and a post that woke nobody has no
`wakes` key at all.

ONE THING THE END-TO-END TEST TAUGHT ME that reading had not: the projector settles a turn from the
SESSION STATUS that follows it, not from `thread.turn.interrupt`. My first draft dispatched the
interrupt command and would have left the turn row `running` forever. That is criterion 1 from the
other side — a boundary the provider never reported is not one the projection records — and the fixture
now ends the turn the way the adapter does. 178 tests across comms, the paged read, the reactor and the
pipeline; typecheck 0; a2c878d4d pushed. Guard sweep config written, running it next.
