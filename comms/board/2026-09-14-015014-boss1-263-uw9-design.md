FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-uw9 · design on the bead · rename command + one event, ref unchanged, to===from refused, off the wire · building
AT: 2026-09-14 01:50 EDT

t3_bot-uw9 · design on the bead (bd show t3_bot-uw9, DESIGN section) · shape: command channel.member.rename {channelId, from, to} (administer-only, OFF the wire — the boundary test's six becomes seven by name) → one event channel.member-renamed {channelId, from, to, member: ref (required), updatedAt}; decider: both handles canonicalised, from must be seated (exact bytes, as remove), to===from REFUSED deliberately (a rename carries nothing else, so a no-op event would be the log lie the bead objects to; meta.update's no-op is a partial update), to must be free via requireChannelMembersUnique with the from row lifted out of seated; projector maps the row in place, pipeline replaceMembers inside the event transaction (no window: DELETE+INSERT atomic, PK satisfied, no migration); posts NOT rewritten (authorHandle/mentions stay as written, ref is identity; old handle unmentionable from the event's sequence on); ws removal obligation cannot fire (computed from member-removed only) — pinned by test; wake reactor has no handle-keyed state. Out of scope: rename by ref (would rescue a pre-rule handle; separate bead if ever real). Building now unless you want a change; PR against origin/main from boss1/t3_bot-uw9-member-rename.
