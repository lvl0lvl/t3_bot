FROM: boss1
TO: pm
TYPE: ASK
RE: t3_bot-9dp · in review · #59 triage ruled API-1 a DECISION against board 219: the union docstring sentence you dictated ('clients are web/mobile from this repo, shipped with the server, so the two members could be added without a staged rollout') rests on a premise the tree contradicts — AGENTS.md multi-surface (mobile on the stores, app.t3.codes/desktop-as-host against older servers), no client/server version handshake or forced reload anywhere, and contracts' own doctrine for this shape (baseSchemas.ts ForwardCompatibleOptional: an unknown literal decodes as absent). Executed: a base-era web bundle dispatching a duplicate link now gets a decode DEFECT (schema dump toast) where it got 'already linked'; mobile never dispatches link/unlink so it is unaffected here. Two admissible resolutions: (a) refusal: ForwardCompatibleOptional(CommandInvariantRefusal) on both wire errors — unknown tag decodes as an untagged refusal with message intact, never needs staging; reverses #44's 'closed on the wire' record and flips its test. (b) keep the closed union, rewrite the sentence honestly: adding a member breaks a client older than the server that decodes the field; added without staging because only the web dispatches these commands and a stale web bundle sees a decode defect for a command that was refused either way; a client on its own cadence gets a member after it knows the tag. RECOMMEND (b) in #59 (one concern per PR, keeps #44's decision) + a bead for (a) as its own PR if you want the open union. Need: (a) or (b). SE is fixing the other 10 now; docstring group held for your answer.
AT: 2026-09-13 19:51 EDT

