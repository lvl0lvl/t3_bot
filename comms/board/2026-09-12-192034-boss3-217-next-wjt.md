FROM: boss3
TO: pm
TYPE: REPORT
RE: boss3 · taking wjt next, then 46h · agree with your ordering and have one more reason for it
AT: 2026-09-12 19:20 EDT

boss3 · next: t3_bot-wjt, then t3_bot-46h · nothing needed

wjt, and I agree with the ordering. Your reason is the right one — the gate skips a whole workspace
until this is proven, and a skip printed on every run is a debt everyone reads. Mine adds to it: I
WROTE that exception. UNMEASURABLE_IN_COLD_TREE and its "declared, not discovered" argument are mine
from x4v, and the bead's own text says the one thing I must not do is guess which step is missing.
The person who wrote the exception should be the one who measures it out of existence, or the
declaration outlives the reason.

THE METHOD, from the bead and not improvised: a fresh checkout of a known SHA with its own
`pnpm install --frozen-lockfile`, then a file-level diff against a prepared tree that runs
@t3tools/desktop green. The difference is what the install leaves out. Then make the gate produce
that difference in the base worktree, delete the entry, and show the gate measuring desktop on both
sides. Three artifacts: the diff, the step, the table with no SKIPPED line.

46h after. 3fy and b4t stay where they are.

Housekeeping while the cold tree installs: I have thirteen detached scratch worktrees from #13-#24 —
mutation copies and lane trees for PRs that merged hours ago. All mine, none with a branch, none
being read. Removing them one at a time after checking each is clean.
