FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-yyd · PR #4 READY at c853bbb75 · rebased on main 5a666309b, 639/67 green, typecheck 0 both packages · /review-pr run, all findings from four lanes answered · THREE MEDIUMs knowingly NOT in #4 and closed by a44 — listed so you merge with eyes open, not so you discover them

PR #4 IS READY. Rebased onto main (5a666309b) at c853bbb75 and verified AFTER the rebase rather than trusting
the exit code: 639 tests / 67 files green, 0 type errors in both t3 and @t3tools/contracts.

REVIEW GATE: /review-pr run, four lanes, every finding answered. The three that survived the whole suite and
are now fixed and mutation-replayed on the rebased tree:

  mentions canonicalised BEFORE the author check   636/636 green as a mutant. The membership-oracle ordering
                                                   I had claimed was pinned and was not. Now red by name.
  channel.member.add drops the handle fold          636/636 green. The create-path test said nothing about
                                                   that branch.
  meta.update drops the name-availability check     636/636 green. Both refusal tests went through create and
                                                   the rename test asserted only the permissive direction.

Plus two conditionals that narrowed on an event type without asserting it, and two table loops with no
tripwire — either could pass green having asserted nothing.

THREE MEDIUMs ARE NOT IN #4, DELIBERATELY, AND a44 CLOSES THEM. I am listing these because you should merge
knowing them rather than find them later:

  1. Invisible/control characters in names and handles. 'Non-empty after trim' admits a handle of one
     zero-width space and an invisible twin of 'boss1'; the same gate admits ANSI escapes into stored names,
     which are echoed to agent and CLI output.
  2. Folding lets three spellings of one member land as three identical mentions in the persisted event.
  3. An archived channel accepts posts, membership changes, and re-archiving (which overwrites archivedAt and
     destroys 'when was this retired'). The archived ruling came after #4 was built.

All three are fixed on a44 (559a49a35). They are unreachable on main for the same reason you gave at 002231 —
the toolkit is not registered until 0uq, channel.* is not on the wire, createPost dies notWired — so no caller
can reach any of #4's channel code. I am NOT asking to fold them into #4: your reason for keeping a44 separate
(a big PR gets a shallow review) applies to these too. But the constraint on the bead should be the one you
already recorded, and it is doing more work than it looks like: a44 BEFORE 0uq is what makes these three
unreachable-forever rather than unreachable-for-now.

ONE CORRECTION TO MY OWN EARLIER REPORT, since it bears on the merge order you set: my HIGH about the
toolkit/decider divergence compared a44's decider against MAIN's toolkit, not against #5's. Executed the seven
rows against all three — 0 of 7 disagree with #5, 7 of 7 with main. So that finding is true of main today and
false from the moment #5 lands, which means your order is not a convenience, it is the control that makes it
unreachable. I did not understand that when I agreed to it.

WHAT I AM NOT CLAIMING: no end-to-end test across the decider/toolkit seam. It needs both halves and the one
that exists is saved on the comms branch to land with iin. Every test in #4 is decider-side, and a fake of that
seam cannot catch a disagreement with the aggregate by construction.

bd: yyd ready to close on merge. iin's decider half is on a44, so iin closes with a44 rather than #4. a44
carries criterion 1's remaining half (the MCP probe) which waits on 0uq, per your 234910.
AT: 2026-09-12 00:27 EDT

