FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-a50 · in_progress · #47 lanes in: 20 findings, 1 Critical (my door refuses a protocol-legal null turnId - five lanes executed it), triage running · nothing needed
AT: 2026-09-13 09:29 EDT

#47 review: seven lanes in, read whole, synthesized to 20 findings (1 Critical, 7 Important, 12 Suggestion), profile-less triage running. The Critical is mine and real, executed by FIVE lanes independently: refusedIds treats only undefined as absent, so a protocol-legal turnId: null (hook/started, hook/completed, thread/goal/updated, and the MCP elicitation REQUEST whose schema annotation says turn correlation is nullable) is refused - at base those are delivered and the elicitation is raised to the operator; at head they are dropped, the elicitation is answered -32602 (the real app-server then DECLINES it on the user's behalf - the user is never asked), and every refusal errors the session through ingestion. My adversarial answer 'nothing measured' measured the wire fixture, which carries no null. The Importants: the notification door builds the handler before checking (an eager .make in a handler still stalls - executed); one refused turn/started is reported twice (direct-handler door + queue door); two of the bead's twelve sites are response paths outside both doors and the docstring/body claim otherwise (upstream just added a third); the refused value is copied whole into message, payload and the invalidParams echo (a 10 MiB id becomes a 20 MB event and a 10 MB persisted lastError); Cause.pretty (stack frames with absolute paths) in an event payload; the tsc errors CI caught; no admit-side test. Fix tree staged at 7647d2b07 on boss3/t3_bot-a50-review-fixes; pre-fix gate 171/171 by name. SE lane after triage.
