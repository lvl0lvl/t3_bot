FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-0uq · rebased onto afb1dc782 and pushed at eddfd9c8a because my base claim went stale while I was writing it — the exact failure your standing correction exists to stop · 788 green, tsc 0 · gate running, re-verification out · boss1 unblocked on listChannelsForMember
AT: 2026-09-12 06:25 EDT

t3_bot-0uq · rebased onto afb1dc782 and pushed at eddfd9c8a · gate re-running · re-verification out · still not "PR ready"

REBASED BECAUSE MY BASE CLAIM WENT STALE WHILE I WAS WRITING IT. The PR body said "rebased on
245b65c76" and by the time I saved it origin/main was afb1dc782 — your #14 merge plus the 2d2 criterion
amendment. Under your standing correction that claim is the thing you check at merge, so a body asserting a
sha that is no longer main's head is exactly the failure the correction exists to stop, and I had just
written one. Rebased, 788 tests green, tsc exit 0, pushed, body corrected to afb1dc782.

Worth noting the two commits I rebased onto, because one of them is mine coming back at me: 4b5f96450
"docs: decode, never .make, external ids" is the CLAUDE.md rule that came out of d7d — and I then had to
check my own diff against it rather than assume I was already compliant, since I wrote the bug that
produced the rule. The remaining `.make` in `createPost` is the one exception, documented at the site and
pointed at d7d.

ONE SMALL THING, recorded because it is the same defect class in a different medium: the PR body I wrote
explaining that U+2028 survives JSON.stringify contained a LITERAL U+2028 rather than the text " ", so
the sentence rendered as `JSON.stringify("a b")` and demonstrated nothing. An invisible character in the
document about invisible characters. Fixed, and the check is now an assertion in the script that writes it
rather than my eye.

STATE: gate running on eddfd9c8a. Re-verification of the four adversarial findings is out with verify13,
along with two questions I asked rather than answered — whether four was the count or the budget, and
whether the remaining `.make` coupling must be decoded in this PR rather than written down.

boss1 is unblocked: answered listChannelsForMember (yes, in e60, mine — but the gateway method drops
`members`, because shipping every channel's full membership to every client on every shell refresh is the
websocket-payload regression AGENTS.md names). Flagged that his deleted post-appended event now rests on a
projector behaviour he should ASSERT rather than assume: if a post does not move the channel row, no
channel-upserted fires and the message view goes quiet, which looks exactly like the other agent never
replying.

Nothing needed from you. Next message is "PR ready #13" with the base sha, or another finding.
