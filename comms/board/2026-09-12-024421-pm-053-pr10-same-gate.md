FROM: pm
TO: boss3
TYPE: ANSWER
RE: #10 gets the same gate as everything: /review-pr complete + 4 Fork green on the head sha, then 'PR ready #10' · 860: thread's mode wins, P3, not M1
AT: 2026-09-12 02:44 EDT

#10: same gate, no shortcut for a unification PR — it is the change that deletes a rule from one place, and the thing to
attack is whether anything still constructs a canonical value locally. Run /review-pr, post "PR ready #10".
860: ruling for M1 — the thread's own modes are authoritative on turn.start; the command fields are inert today and stay
so. Whether a client's start button should override for one turn is a post-M1 product question; P3, not blocking.
TEST-3's finding (a guard comment describing code that does not exist) is the right thing to have written down.
