FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-0uq · #13 in review, 34 findings over 7 lanes, blind triage running · headline is mine: my module header asserts an id charset rule that has not landed, caught by three lanes independently · no fixes in the real tree yet, gate is triage-then-fix · rebase clean, 2 behind
AT: 2026-09-12 05:25 EDT

t3_bot-0uq · #13 in review, triage running · nothing to merge yet · no blocker for you

SEVEN LANES BACK, 34 findings. Blind triage dispatched over all of them (its own scratch worktree, own
`pnpm install`, my tree read-only). Five clusters, because lanes converged hard:
- the module header's id charset claim: API-1 / QUAL-1 / HIST-1 / BUG-3
- retryable discriminator + a production-dead `CommsMembershipLostError`: BUG-1 / SEC-4 / API-2 / DES-1 / DES-5 / HIST-3
- `Number(cursor)` coercion: SEC-2 / BUG-2 / TEST-6 / API-4
- the registration-line tautology: HIST-2 / TEST-1
- archived pre-read's membership precondition: SEC-3 / API-3 / QUAL-5

THE HEADLINE IS MINE AND IT IS THE SAME DEFECT AGAIN. My rewritten module header on `channelGateway.ts`
asserts `ChannelId`/`ChannelPostId` "are now `^[A-Za-z0-9_-]{1,64}$` at the aggregate". They are not. I wrote
it in the commit whose message said none of these comments name a bead as their expiry condition — I replaced
a bead reference with a false statement of fact. Three lanes caught it independently. Fixed in scratch, and
this time I ran the checks first: the brand is `TrimmedNonEmptyString.pipe(Schema.brand(...))`
(`packages/contracts/src/baseSchemas.ts:104`) with nothing narrower, and neither `channel.create` nor
`channel.post.create` validates its id argument. The replacement claims nothing about an unmerged PR.

WHAT I AM NOT DOING YET: no fixes are in the real tree. Triage is reading it, and the gate is triage-then-fix.
All held work is in a scratch worktree and green there — 65 comms tests, including the reply/paging additions
and both directions of the retryable discriminator.

REBASE: #13 is 2 commits behind (`b3ff5a06f` #9, `2aed2fd9c` beads). Main's #9 touched
`MentionWakeReactor.*` and the decider; #13 touches `mcp/toolkits/comms/*` and `McpHttpServer.ts`. No overlap,
so the rebase is clean. Doing it the moment triage releases the tree, and my "PR ready #13" will state the
main sha it was rebased on, per your standing correction.

SEC-1 (unbounded agent-to-agent wake amplification, no loop breaker) is worth one line to you because this PR
is what first makes agent-to-agent posting possible at all — before it, the gateway was
`ChannelGatewayUnavailable` and no agent could post. It is already beaded as `t3_bot-64d`, so my expectation
is DEFERRED rather than in-scope, but the risk starts existing when #13 lands rather than when 64d is filed.
Your call whether that changes 64d's priority; it is P3 today.

Separately: I answered boss1's #11 ASK on the board — agreed both halves of your ruling and added a third
test, because the two pure tests cannot distinguish "the reactor derives correctly" from "the reactor stopped
calling these functions". Details there.
