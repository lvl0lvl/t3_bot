FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-5yq · #79 security lane, two measured findings that need your ruling before the fix commits: (1) the projector WATERMARK crosses a skipped row permanently — bootstrapProjector resumes from lastAppliedSequence, which the next applied event moves past the hole, so upgrading back to the newer build never replays the skipped event; only a full projection rebuild recovers it, and nothing says one is needed. Options: (a) record the lowest skipped sequence per read and refuse an incremental resume below it, (b) pin lastAppliedSequence to skipped-1 so a later build replays from the hole, (c) correct the disclosure only: the hole survives the upgrade and the repair is a rebuild. (2) an unknown aggregate KIND still fails the whole read at decodeRows: the read row keeps aggregateKind and aggregateId as closed unions, and the repo's own history (8660c7933c, channel as a third aggregate) added a kind and nine types in one change. Options: widen kind and id to plain strings on the read row with the same per-row Schema.is skip and the same test shape, or disclose that forward compatibility covers a new type on an existing kind only · need: (1) a/b/c and (2) widen or disclose; I build the rest of the review's fixes meanwhile
AT: 2026-09-15 11:54 EDT

Both were executed by the lane (fixture: known at 1, future type at 2, known at 3, then
bootstrap: all 11 projectors land at lastAppliedSequence 3, and a resume read returns nothing;
a row with aggregate_kind 'workspace' fails readFromSequence at decodeRows with no warning).
The lane's third item is a Suggestion I will just fix: the warning interpolates the raw type
unbounded (a 200,000-char type made 4 MB of warnings from one read; a newline plus an ANSI
escape forges a second plausible line) — bound and escape the message value, keep the raw
value in the structured annotation.

My recommendation, for the record: (1c) plus (1a)'s cheap half — the disclosure states the
durability and the rebuild, and the store's warning is enough of a record for now; (1b)
changes the projection pipeline's resume semantics and is a separate concern. (2) widen: it is
the same shape as the type skip, same helper, same fixture, and the counterexample is our own
history; the ruling on 355 said "a type literal outside this build's union" because that was
the case I posed, not because kind was considered.
