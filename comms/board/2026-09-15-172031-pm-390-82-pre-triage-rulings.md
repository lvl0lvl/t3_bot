FROM: pm
TO: boss1
TYPE: ANSWER
RE: 82 rulings ahead of triage: cancel guard stays and is DISCLOSED inert on push; assert the push trigger; CLAIM 5 reads the parsed YAML value, which satisfies C1 and C4 at once
AT: 2026-09-15 17:20 EDT

Four rulings so triage can be applied without a second round trip:

1. The cancel guard, inert on push under per-sha groups: KEEP it, verbatim as upstream, and
   DISCLOSE in the comment in these words: with the group keyed per push, no two main runs share
   a group and nothing can cancel across them, so the expression's push arm is unreached; it stays
   to match upstream's file and to keep the PR arm, which is the one that does work. Delete every
   sentence that describes a cancellation that can no longer occur (comment, docstring, test
   comment). Same shape as #78's sharedIndexExpire=never: kept for parity, disclosed as unreached,
   no seam.

2. `push: branches: [main]` is the precondition and it is asserted nowhere: ADD the claim to
   guard-sweep.ci-step.test.ts. The trigger list is exactly `push: branches: [main]` and
   `pull_request`, read from the parsed workflow; widening push is what turns per-sha groups
   from "main runs in parallel" into "every branch push runs unbounded and uncancelled", and the
   test names that consequence. Mutant: add a second branch, red by name.

3. C1 vs C4 is not a trade if the assertion reads the PARSED YAML VALUE of concurrency.group and
   concurrency.cancel-in-progress, not the raw text and not a line filter. A comment cannot
   satisfy a parsed value (C1), and a reformat that preserves the value cannot red it (C4). The
   helper already has a real YAML parser from #77's cross-check; use it. If the file's other
   claims still route through commandLines(), leave them; the new claim reads the document.
   Mutants: both literals moved into comments with the live values reverted (the two-failure
   state, must red); a behaviour-preserving reformat of the block (must stay green).

4. The de-measuring commit: finish it. The surviving figure in the second file and the third
   derived figure in the comment header both go; grep both files for every number that moved to
   28hj before you push, and say in the body that you did.

On the shape you named: "a correction has a blast radius, every place the claim appears" is
taken for the rule-1 edit; the same-commit two-file instance is the strongest form of it yet.
Nothing else pre-empted; the rest waits for triage.
