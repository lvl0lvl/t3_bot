FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-1ez + t3_bot-s4l · PR READY #34 at 2c33de0d9 · count gate exit 0 over 14 workspaces against origin/main@4ac476084, sweep exit 0 with all 12 rows killed baseline 702, CI green by SHA · DESKTOP MEASURED ON BOTH SIDES UNASSISTED, no SKIPPED — that is (c), and the generator refuses to write the sentence if the log carries a SKIPPED line or desktop is absent · verified the gate's named base is in my history with merge-base --is-ancestor rather than trusting it · 4 findings recorded not fixed: z7u uw9 7iw 2wm · next: 2wm PR, then qa7, then jaq
AT: 2026-09-12 22:38 EDT

**PR #34 is ready at `2c33de0d9`.** All three instruments green on that head, body sections generated from their logs.

- **`pnpm test:count-gate --base origin/main` — exit 0**, **14 workspaces** against `origin/main@4ac476084`. `decider.channels.test.ts` 30 → 34, nothing lost by count or by name.
- **`guard-sweep` — exit 0**, baseline 702 / 0 already failing, nothing NOT RUN, **all twelve rows killed** including the two this PR adds.
- **Fork CI green by SHA**, 4 checks, `not-success: 0`.

**Desktop measured on both sides, unassisted** — that is (c). No `SKIPPED` line, `@t3tools/desktop` in the measured list, from a cold base checkout with no warmed tree and no manual install ahead of the run. The generator **refuses to write that sentence** if the log carries a SKIPPED workspace or if desktop is absent from the scope line, so it is the run's claim rather than an inference from #35 having merged.

I also verified the base the gate named is genuinely in my history rather than taking it on trust: `git merge-base --is-ancestor 4ac476084 HEAD` passes — it is the beads commit my rebase picked up one past `352df641b`. A gate that names its base is only worth the check if someone runs the check.

**What #34 ships.** `(memberKind, memberId)` unique per channel, stated over the delta: **no second seated handle for one ref after this command**. `create` seats the whole roster; `member.add` seats one row against the rows that exist. The function is renamed so a caller left behind is a compile error rather than a guard wired at one of two sites, and `refKey` is one function so the seated rows and the added rows cannot be keyed differently.

**Both defects closed:** the stored `authorHandle` no longer depends on array position (`1ez`), and removing a handle now removes the only row carrying that ref (`s4l`).

**Four findings recorded rather than fixed**, each with its measurement: `z7u` the replayed population, `uw9` the one repair command for both the rename hole and the non-canonical handle, `7iw` the cross-kind collision this PR deliberately leaves and now protects with a test, `2wm` the sweep's anchor pre-flight.

**The two things I got wrong in this PR, both caught by lanes, both now in the body.** bug34: I renamed a function inside a docstring and left the sentence asserting the opposite of the guard, one line below its name, in the diff that added the guard. api34: my tuple-key comment claimed a hazard `memberKind`'s closed literal union makes unreachable, and my error message claimed the pair resolves mentions — it resolves authorship; mentions go by handle and the reactor dedupes memberIds, so I had overstated the harm on the half I had wrong.

**And two anchors I orphaned, one round apart**, both times by changing code the config points at without checking the config, both times on the row pinning this PR's own guard, both found four minutes into a sweep only because exit 3 outranks a survivor. The body says so. `check_anchors_resolve.py` is my pre-push check now and ran clean before this push — 12/12 resolve exactly once — and `t3_bot-2wm` moves it inside the sweep as a refusal before the baseline.

Next: `t3_bot-2wm` as its own PR, then `qa7`, then `jaq`.
