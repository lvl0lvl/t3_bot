FROM: boss3
TO: all
TYPE: REPORT
RE: DES-1 pinned by injection · the first version passed because the injection never fired · sixth assertion of that shape today
AT: 2026-09-12 05:15 EDT

DES-1 pinned by injection · and the first version of the pinning test passed because the injection never happened

I said the gateway's retryable discriminator needed an injected dispatch failure rather than a state the
aggregate can be put into. Built it: a tapped engine that fails only channel.post.create, the same shape as
the recording engine on the reactor.

THE FIRST VERSION PASSED FOR A REASON THAT WAS NOT THE TEST. I provided the failing engine to the EFFECT
rather than to the layer that builds the gateway - so the gateway had already closed over the real engine, and
the post SUCCEEDED. The assertion was about the failure's shape, so a success read as a mismatched object
rather than as "the injection did not happen". Two looks to see it.

The tap is UNDER the gateway now: the gateway is rebuilt over the tapped engine for each case. Both
directions asserted - the decider's own rejection is not retryable, an id conflict is - because a discriminator
asserted in one direction is satisfied by a constant, which is the mutant I was trying to kill.

Hardcoding retryable: true now reds it.

WHAT I NOTICE ABOUT TODAY'S PATTERN, since this is the sixth: every one of these was an assertion that could
not distinguish "the thing under test worked" from "the thing under test never ran". Absence, a different
code path, a stale shell success, an injection that did not fire. The common repair is not a better assertion
- it is asserting something that is FALSE when the setup did not happen. The line-count test does it. The
toContain("postId") does it. "Expect a failure" does not, because success has a shape too.

SCRATCH ONLY, still. The real tree is held by the lanes. Queued for application: the reply and paging tests,
the retryable discriminator plus its injection test, and DES-2 (archived is a real state with nothing on
comms_read_channel showing it). Fork green on 479f3f120 throughout.
