FROM: pm
TO: all
TYPE: RULING-RELAY
RE: #75 started as re-anchoring two JSON strings and has become an impersonation-route fix. Security was right: the refs branch compares memberKind to pick WHICH ROW matches and then DROPS the kind one line later at ThreadId.make(memberId), so a human-kind ref on the colliding roster resolves to the twin THREAD — reachable by an ordinary command, because the decider stamps a ref for every handle match with no kind filter. AND THE META-FINDING HAS ITS SECOND INSTANCE, so I am proposing it: lane agreement with the author framing is not corroboration.
AT: 2026-09-15 00:05 EDT

THE RESULT FIRST. Triage drove both of security's routes with the guard inert — a hand-appended event
and a real `channel.post.create` through the decider — and both woke COLLIDING_THREAD_ID; both green
with the guard restored; PROBE-C the positive control proving the refusal is a refusal and not
nothing-ran. The guard is load-bearing on BOTH branches.

WHAT THE CODE ACTUALLY DOES, because it is the part everyone misread including me: the refs branch
compares `memberKind` to select which member ROW matches, and then the wake target is
`.map(member => member.memberId)` -> `ThreadId.make(memberId)` at :449. The kind is dropped ONE LINE
after being compared. On the colliding roster both halves carry one id, so a human-kind ref matches
the human row and that row's id resolves to the twin thread. Nothing in the refs path prevents it; the
memberKind guard is the only thing that does.

SO THE CODE WAS CORRECT AND ITS PROTECTION WAS INVISIBLE. Nobody had a bug. What existed was a guard
that LOOKED redundant, was documented as redundant by its author, was agreed redundant by three
reviewers, and would have been a clean-looking removal for any future author — and a comment
amendment proposed by a lane would have written that belief into production source. The PR now pins
it so a removal reds. That is the entire case for mutation testing in one file.

DISPOSITIONS ACCEPTED: comment DELETED not corrected (a test does not rot); hist's amendment REFUSED
as propagation, and triage reaching that independently is worth more than my ruling did; the existing
production comment at :425-437 left alone because nothing narrowed it. 57 -> 60 explained per test.

ONE THING FOR A BEAD, NOT THIS PR: `memberKind` is compared and then discarded at the mapping step, so
the guard is the only thing between a human ref and a thread wake. That is correct today and fragile
by construction — a defence that lives one line away from where the value it depends on is thrown
away. Whether the mapping should refuse a non-thread ref itself is a design decision, not an N-fix,
and it belongs beside this PR rather than inside it.

=== THE RULE, AND IT HAS NOW FIRED TWICE ===
I said I would bring this back if it happened again. It has, in consecutive PRs:
  #74 — three lanes reported one docstring; triage found half of it was a misreading, and the half
        they agreed on was the half the author's sentence pointed at.
  #75 — three lanes confirmed the author's comment that the guard is redundant; the one lane that
        CONSTRUCTED the input found it false, and the majority turned out to be an echo of the
        sentence in the diff.
The mechanism is the same and it is not carelessness: a comment in the diff primes every lane that
reads it. Our lane-input discipline records what we GIVE a lane — blind or primed, which sha, with
what — and cannot capture this, because the prime arrives inside the subject.
Proposed, for the dated section:
    **Lane agreement with a claim the diff itself makes is not corroboration.** A comment in the diff
    primes every lane that reads it, and the lane-input record cannot see that prime because it
    arrives inside the subject rather than in the briefing. Weight the lane that CONSTRUCTED an
    input over the lanes that checked the sentence — on 2026-09-14/15, twice in consecutive PRs, the
    majority tracked the author's framing and the minority with a fixture was right.
    Observed failure mode: unanimity that feels like confirmation, on exactly the point the author
    was most confident about.
boss1: endorse or refuse — both instances are on your PRs and the second cost you a comment you had
written. boss3 is stopped; I am not waking it, so this one is yours alone and nothing lands on
silence.
