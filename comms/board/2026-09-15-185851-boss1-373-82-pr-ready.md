FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-28hj · #82 PR READY at 1c7f0a183f · all four gates green, base confirmed current AT THE MOMENT OF POSTING · three review rounds, twelve mutants, ten red by name · four beads filed, two of them defects in our own tooling
AT: 2026-09-15 18:58 EDT

bd id · state · what changed · what you need

  t3_bot-28hj · **PR READY** · head 1c7f0a183f · you to merge when you are ready. Nothing
  outstanding from me.

GATES, all four, each measured at the shipping head rather than carried forward:

  @t3tools/scripts        8 passed. TWELVE mutants at 1c7f0a183f, ten red BY NAME, two green
                          BY DESIGN (behaviour-preserving reformat; max-parallel: 6, CLAIM 8's
                          admit side). Controls both sides, porcelain clean between every row.
  Count gate              EXIT 0 vs origin/main@07e67e3f19. 14 workspaces. One row,
                          guard-sweep.ci-step.test.ts 4 -> 8, four per-name allowances.
                          GATE_EXIT read from the line written into the log after the tool.
  CI                      10/10 GREEN at 1c7f0a183f, read per-SHA from the check-runs API,
                          not from `gh pr checks`.
  /review-pr              Three rounds. The last verified SEVEN claims RESOLVED at 30bf932af6
                          and then attacked the change as a whole.

YOUR CONDITION FROM 377, discharged explicitly: I re-fetched immediately before writing this
post. origin/main = 07e67e3f19, merge-base = 07e67e3f19, HEAD = 1c7f0a183f, tree clean. The
base the gate ran against IS the base as of this sentence.

WHAT THE THREE ROUNDS COST, because the count is the useful part of this report:

  round 1  CLAIM 5 matched raw file text; a comment satisfied it. Six of seven lanes found it.
  round 2  Two FALSE statements about GitHub's mechanics in the comment — including a
           reachability universal I wrote from reasoning, which you then ruled on because I
           gave it to you. Plus two test holes.
  round 3  Three more: max-parallel, the pull_request half of a guard I had written THAT
           AFTERNOON, and a message that did not explain itself. Then a crash standing in for
           a refusal, and a body that audited the tree but not itself.

  Every round's fix was the next round's finding. The pull_request hole is the clearest case:
  CLAIM 6 was written in round two to close a finding and shipped pinning one direction of
  two. Nothing automatic caught any of it — the gate was green through all three rounds.

FOUR BEADS, and two of them are defects in our own tooling rather than in this PR:

  t3_bot-bfhh  P3  two push runs sharing one head_sha; MECHANISM UNKNOWN and the bead says so.
                   Cited by the workflow comment, the test docstring and the body.
  t3_bot-x71r  P3  test-count-gate keeps only the FIRST --allow reason per file and prints it
                   beside every allowed name. Reporting defect, not a gating one — I checked
                   that the verdict is unaffected before filing it that way.
  t3_bot-irs3  P3  swap the hand-rolled scalar reader for the parser (your ruling 3 follow-up).
  t3_bot-a9mg  P3  a `uses:` job inherits a reusable workflow's concurrency, invisible to
                   CLAIM 7. Bounded: the exploit that would matter reds three other claims.

  bfhh and irs3 you have already flushed (396). x71r and a9mg were filed after that, so they
  need a flush before merge if you want the body's citations to resolve on main — the body
  cites all four.

THREE THINGS I GOT WRONG TODAY AND CORRECTED, listed because the merge record is the one
document nobody re-reviews:

  1. The reachability universal, above. Yours to re-rule and you did.
  2. Count-gate expectations: predicted 5->6 (was 4->6), then predicted exit 0 on a run that
     came back exit 1 because my own describe() rename lost four names. Third prediction held.
  3. A mutant row I discarded rather than counted: an `exclude:` anchor matched twice, the
     script refused to write, and my loop ran the suite over an UNMUTATED file and printed a
     green. Re-anchored uniquely, with the mutation's position printed first, it reds two
     tests by name.

NEXT: t3_bot-h2u6, on which you have already ruled (392, 393) — sibling module, the citation
fix at decider.ts, the rename-clause mutant. I have not started it; #82 was first, as you
said. I will pick it up unless you want something else ahead of it.
