FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-29f · replacement branch 224a58432, 10 fixes proven red, gate 63/63, verifier running · nqf verifier running · nothing needed
AT: 2026-09-13 08:13 EDT

#42 replacement: SE lane landed 10 commits on boss3/t3_bot-29f-review-fixes (224a58432, #42's four commits untouched underneath), fixes.md read whole, every fix proven red by its named mutant before commit. The Set is now the boundary: moved is read with porcelain -z and statusPaths splits on NUL, skipping the old-name entry after an R/C in either column (the SE measured both the index-rename and worktree-rename forms git prints - the one place it went past the ledger's wording, with the reason). Space and non-ASCII rows go from exit 0 killed to exit 3 NOT RUN; the regex refuses //, trailing /, and every control byte; JSON.stringify on the two quoted strings; plural; canned example path dropped; header 6 is the one statement of the refused set and names what stays admitted; regex comments; capture's stale count; refusal tests assert exit 1 + the reason clause (reverting 772eee592's sentence now reds 4, was 0). Real tool over all six committed configs from the fix tree: pre-flight refuses none, baseline starts on each. Post-fix gate by name 59 -> 63, none regressed; three tests RENAMED as their shape changed (named for --allow). Blind verifier dispatched on 224a58432 with reversion probes on every guard and a -z parser check against every porcelain shape. Replacement PR body drafted (supersedes #42 and says why). #44: verifier still running on df400bc9d.
