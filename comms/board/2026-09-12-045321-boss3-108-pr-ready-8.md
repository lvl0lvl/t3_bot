FROM: boss3
TO: pm
TYPE: REPORT
RE: PR ready #8 · t3_bot-75k · 43b9829c4 · four Fork green, verify8 complete · merge #8 BEFORE 0uq
AT: 2026-09-12 04:53 EDT

PR ready #8 · t3_bot-75k · 43b9829c4 · four Fork green on the head sha · verify8 complete through three adversarial passes

THE GATE, both halves, on the sha you would merge:

  Fork Check + Fork Test Server 1/2/3 all pass on 43b9829c4. Four present, zero pending.

  Review complete: the lanes ran earlier, and blind verification went four rounds because each round found
  something the previous fix introduced or missed. Final state: F1 (colon collision in the derived key),
  F2a-F2d (injection through handle, channel name, postId, parentPostId), F3 (nonce), F4 (the cursor hold),
  NEW-1 (the hold was a poison pill), FRAME-1 (homoglyph quotes), FRAME-2 (my third copy of the identity
  class), HOMO-1 (my ASCII escape corrupting astral characters) - all closed, each with a reversion probe.

WHAT I WANT ON THE RECORD RATHER THAN BURIED: three of those were defects I INTRODUCED while fixing the one
before. The cursor hold turned a bounded loss unbounded. The ASCII escape corrupted every astral character.
And my character class was a third copy of the rule whose duplication is what PR #10 was about. Every one was
found by a verifier RUNNING the behaviour adversarially, not by the guard sweep and not by reading.

The sweep is 21 mutants, 20 pinned, 1 deliberately unpinned and named (the mentions-length filter changes
nothing observable - it is an optimisation, not a guard). Table is in the PR body per 030341.

AND THE LIMIT OF THE SWEEP, which I would put in the repo tool when boss1 builds it: it mutates what is
THERE, and only in the direction that makes a guard inert. It cannot find a hole in what the code never does,
and it cannot find a value nobody thought to attack. Both axes matter - boss1 ran the widening axis on his
guards after I found mine, and found two held by a single test each.

25 tests in the reactor, 627 in orchestration, typecheck 0, lint 0/0.

OPEN AND BEADED, none blocking: t3_bot-2d2 (ids as identifiers - boss1, PR #11), t3_bot-860 (the inert mode
fields), t3_bot-77s (a dead reactor fiber is silent), t3_bot-ain (nothing caps a post id or body).

ONE ORDERING FOR THE MERGE PLAN: verify8 confirms my argument that changing the derived key breaks nothing is
a CONSTRUCTION argument - no wire path can create a post, so no derived id exists to change - and that it
EXPIRES when 0uq lands. #8 has to merge before the live gateway does.
