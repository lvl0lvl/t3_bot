FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1r1 · in_progress · #69 lanes 7/7 → 11 findings (1 Critical: rollback-first regresses on a typed restore failure + retries compound; upstream tried and reverted this reorder twice, prescribes it WITH a pre-check), triage executing three candidate shapes · your call on the shape if you want it before triage, else nothing
AT: 2026-09-14 04:07 EDT

#69: all seven lanes in, twenty-two lane findings synthesized to eleven (1 Critical, 2 Important, 8 Suggestion), blind triage running with an instruction to execute each candidate shape. The Critical is the one I posted at 4/7, now confirmed by six lanes: rollback-first regresses on a typed restore failure (index.lock) and a retry double-rolls-back. The history lane found the record: this exact reorder was tried upstream twice — Julius flipped it back in Feb after a review flagged precisely this, and #9069 (Sept 1) was reviewed with the same two objections and superseded; the maintainer's prescription closing #10299 is "a native target check before restoreCheckpoint, AND reorder so provider rollback runs before file restore" — the reorder WITH a pre-check, which my PR ships half of (hasCheckpointRef is already used twice in this reactor). Upstream/main today (#11338, #11358) keeps restore-first behind a restoreFiles guard, and my hunk conflicts with it (the base auto-merges). Three shapes are on the table for triage to execute: (i) restore-first with a compensating re-restore of the current ref on rollback failure (bugs, security); (ii) rollback-first with the target pre-check (history, upstream's prescription — still leaves the index.lock retry); (iii) rollback-first where a successful rollback commits the conversation half — dispatch thread.revert.complete on restore failure so the retry computes K = 0 (architect; upstream ships that end state as restoreFiles:false). My read: (iii) plus the pre-check is the one that converges on every executed input and matches upstream's own end state, but the triage rules. The rest: OpenCode's session.revert reverts files itself (wording must be what T3 did, not what the files are); the restore-fails test never asserts the revert did not complete; the rollback-fails test times out unnamed; the guard is one-directional; turn(s); the comment's wrong branch; the mock's double cast; a defect in the rollback bypasses the activity (pre-existing). Then SE → verify → PR-ready. Nothing needed from you unless you want to rule the shape before triage does.
