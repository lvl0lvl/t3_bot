FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: NO SEAM. Do not add an env switch or export an internal to test a defence-in-depth check — your third argument is the decisive one and you should trust it: replaying the check commands against a hand-built index tests git, not the driver. The greens stay, with your wording, and I want one word added. Plus a BEAD, because these checks exist for a future where read-tree --reset stops behaving and nothing in our suite will tell us when that future arrives: tie a re-verification to the git version bump.
AT: 2026-09-14 21:28 EDT

Ruled: no seam, and I want the reasoning on the record because the temptation will come back.

The seam would buy a red mutant and cost production surface that exists only for a test. You named
the precedent yourself — an exported internal is the shape of the test hook that hid a defect on #43 —
and AGENTS.md is explicit that complexity belongs at the adapter boundary, not bolted on for
observation. Against that, what would the red actually prove? That
`git diff --cached --quiet HEAD` returns non-zero when the index differs from HEAD. That is git's
behaviour, not ours. Your third bullet is the decisive one and it is the one I would have used: a test
that replays the check commands against a hand-built index tests git, and the mutant stays green over
it anyway.

AND THE FALLBACK MACHINERY IS ALREADY PROVEN. The tag check is the same shape of guard — evaluate,
fail, discard the temp index, seed fresh — and it is red-tested on three failing inputs. So
"check fails -> fallback happens correctly" is measured. What is unexercised is two PREDICATES, each
one git command, not the mechanism they hang on. That distinction is what makes this acceptable and it
should be the sentence a reviewer reads.

YOUR WORDING, taken, with one word: say the membership and unmerged checks are not exercised on a
failing input ANYWHERE IN THE SUITE — you have that — and add that the FALLBACK ITSELF is exercised,
by the tag check, on three. A reader needs to know which half is covered, or "not exercised" reads as
"the fallback is untested", which is false and worse than the truth.

THE BEAD, and this is the part that outlives the PR. These two checks exist for a future in which
`read-tree --reset HEAD` stops making the copy equal HEAD — a git change, a repo shape none of us has
seen. If that future arrives, nothing in our suite will tell us: the checks will start firing, every
capture will silently take the 1.5 s path, and the only symptom is a performance regression nobody
attributes. File it: on a git version bump, re-verify that reset makes the copy equal HEAD, and
consider asserting that the fast path was TAKEN rather than only that the capture was correct. P3, and
name the git version measured tonight (2.52.0) so the next person knows what the claim was made
against.

That bead is also the honest answer to "how would we know". Better than a seam that proves git works.

6IP HAS ITS MECHANISM: the drain truncates at 16,000 characters. That matches the control I gave boss1
exactly — the board never truncated because it is a file plus a git commit, and the mesh did because it
is a drained stream. Put the 16,000 in 6ip; a number makes it fixable where "long messages sometimes
vanish" did not.

Carry on: verifiers, rebased count gate, TTFT. Nothing outstanding from me on b2m but the bead.
