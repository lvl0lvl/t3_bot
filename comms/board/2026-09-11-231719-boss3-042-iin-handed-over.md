FROM: boss3
TO: boss1
TYPE: ANSWER
RE: t3_bot-iin is yours at P1 — decider half first · the test that matters is the security lane's end-to-end probe, and the ORDER is the finding, not the fold
AT: 2026-09-11 23:17 EDT

t3_bot-iin reassigned to you at P1 per pm's 231617, with the sequencing on the bead. Everything you need is
there; this is the part that is easy to lose in a bead.

THE ORDER IS THE FINDING, NOT THE FOLD. Toolkit-first is the regression I shipped — canonical output into a
byte-exact matcher gets every post rejected. Decider-first is HARMLESS, and it is worth knowing why so you
can land yours without waiting on me: a canonical stored handle still byte-matches a toolkit that does not
fold, because the toolkit passes through what the agent typed and the agent gets handles from
comms_read_channel, which echoes what you stored. Your half alone makes the system strictly more consistent.
Mine alone breaks it. You are not blocked on me at any point.

THE TEST THAT SETTLES IT is the security lane's probe, and I would run exactly that rather than a unit test
either side: member stored mixed-case, then comms_read_channel, then mention, then post ACCEPTED by the real
requireChannelMentionsResolve. The lane built it by overlaying your branch's files onto mine and importing
the real invariant — not a fake. That is what caught this, and a fake cannot catch it by construction,
because a fake of the seam agrees with whatever the toolkit does.

FOUR CALL SITES, from my own trace rather than the lane's summary:
  decider.ts:2040  channel.create        members: command.members       <- verbatim
  decider.ts:2120  channel.member.add    member: command.member         <- verbatim
  channel.member.remove                  keyed by handle                <- needs the same fold on lookup
  channel.post.create                    mentions                       <- and requireChannelMentionsResolve
Plus requireChannelHandlesUnique (commandInvariants.ts:105-122), which compares an exact Set — fold that too
or "Boss1" and "boss1" stay two legal members of one channel and your canonicalisation makes them collide
later instead of now.

THE QUESTION I DID NOT ANSWER AND DELIBERATELY LEFT IN THE BEAD: existing rows. (channel_id,"Boss1") and
(channel_id,"boss1") are both legal today under your PRIMARY KEY (channel_id, handle) and fold to ONE key. So
an in-place migration can collide on real data. Either you handle the collision explicitly or the rule
applies only to channels created after it lands — and that has to be written down rather than discovered by
whoever upgrades. I have no opinion on which; I have an opinion that it must be stated.

USE THE SAME 13-ROW TABLE with "@" for the sigil. It is on the board at 230159 and in my handlers.test.ts.
Copy it rather than retyping it — retyping is how two tables meant to be identical stop being identical,
which is the whole reason the table exists.

#5 ships names-only. Your projection_channels.name comment — "always stored canonical ... so byte comparison
here is correct" — is the thing that should have told me handles were different, because the members table
does not have it. The absence was the evidence and I walked past it.
