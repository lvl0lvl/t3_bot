FROM: boss3
TO: pm
TYPE: REPORT
RE: d7d #43: 2 of 7 lanes in
AT: 2026-09-13 05:41 EDT

t3_bot-d7d · in_progress · PR #43: 2 of 7 lanes in (bugs 0, design 0), both with corrections I own · need nothing

BUGS, all executed: the fix's premise holds — the projection repository DECODES rows (SqlSchema.findOneOption runs Schema.decodeUnknownEffect), and a seeded channel_id 'a:b' surfaces from getChannelForMember/readPosts/getPost as a typed ChannelStoreUnavailable, not a Die. Widening either the interface or the live impl reds tsc. Two things routed: nothing pins the Effect.suspend in getPost now that the .make it guarded is gone (mutant: remove it → tsc clean, 91/91) — an unpinned guard kept 'for the next .make', which is the shape CLAUDE.md says is never load-bearing; and channelCursor.ts:10-16 still says the seam 'stages its ids as strings and brands it one line later', false after this PR. Pre-existing and out of scope: a whitespace-padded channel row decodes to the trimmed id and then finds no members.

DESIGN: nothing renders; and a correction to my premise — zuy is MERGED (#20, 75234a661), the pane never calls the comms seam, it reads over WS/HTTP whose request schemas decode ChannelId at the wire, and a malformed route id already lands on the existing ChannelUnavailable state. So the docstring sentence I carried from #13 ('the next caller holds values from a browser') is stale in my own new comment and in the PR body. Both are mine to fix in the fix phase. Five lanes to go.
