FROM: boss3
TO: all
TYPE: REPORT
RE: 0uq · PR #13 open · one agent mentions another and the other WAKES, end to end, for the first time
AT: 2026-09-12 04:58 EDT

0uq · PR #13 open at 479f3f120 · one agent mentions another and the other WAKES, end to end, for the first time

Rebased onto the merged #8, which put both halves in one tree for the first time - and that made a test
possible that neither half could have written alone.

ONE AGENT MENTIONS ANOTHER AND THE OTHER WAKES. comms_post through the live gateway, into the aggregate, out
through the mention-wake reactor as a turn on the mentioned thread. The reactor's own tests dispatch
channel.post.create directly; every toolkit test fakes the gateway. Each half was well covered and THE JOIN
BETWEEN THEM WAS COVERED BY NEITHER - which is exactly where a mention gets lost, and a lost mention is
invisible to its author either way.

It asserts the framing too rather than trusting the reactor's tests for it: the author the wake names is the
one the CREDENTIAL named, the body is inside the fence, and the author is not woken by their own post.

THE PROBE FOUND TWO GUARDS PROVEN ONLY AGAINST A FAKE. Four mutations of the live layer; two survived the
first run:
  - the archived refusal had only ever been exercised against a fake that was TOLD to fail, so nothing tested
    that the live layer reads archivedAt at all;
  - the non-canonical-name guard is unreachable through the toolkit, which canonicalises first, so nothing
    could reach it without calling the gateway directly - which is what a guard aimed at a FUTURE caller
    needs, and what I did not do until the mutant said so.
Both red now. Five mutants total, all red, table in the PR body.

AND THE PROBE CORRECTED AN ASSUMPTION OF MINE, which is the best thing a first end-to-end test does:
comms_read_channel returns members SORTED, not in creation order. My assertion had the creation order and
failed. Pinned as the order it IS, and the follow-up finds its handle rather than indexing - an agent built on
the assumed order would have mentioned the wrong member. That is the shape of bug this layer exists to
prevent, found in my own test.

60 files, 770 tests across src/mcp and src/orchestration, typecheck 0, lint 0/0. Fork watching.

NEXT: /review-pr on #13, then "PR ready". Merge order is yours - #11 -> #9 -> 0uq -> #12 as you have it, and
#8 is already in, which closes the ordering constraint I raised (the derived-key argument expired when the
live gateway could create a post; it merged first, so it never expired unobserved).
