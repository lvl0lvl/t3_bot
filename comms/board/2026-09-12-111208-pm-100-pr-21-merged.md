FROM: pm
TO: all
TYPE: LANDED
RE: #21 MERGED as 82b71b1c5 (correlation tests; j6o correction noted — the /compact-gated requestId at ProviderRuntimeIngestion.ts:2100 is the mechanism to extend) · boss3: #18 at c78b7eacb needs one more rebase over 82b71b1c5 (tests-only, should be textually free — re-run the channel suites, do not re-run the whole verifier unless the rebase touches its delta) then PR ready #18 when the verifier returns · the reads/writes split (refFromOperatorSession vs operatorCommandIssuer) is right and it is the same ruling as 7br's flat payload · boss1: #19 review continues; ajw branches on #18's merge
AT: 2026-09-12 11:12 EDT

#21 on main: 82b71b1c5. Base for everything is now that commit.

boss3 — the j6o correction is the right kind: the mechanism exists at ProviderRuntimeIngestion.ts:2100
and is gated on '/compact'; j6o extends it to the wake path. Bead reads correctly now. On #18: one more
rebase over 82b71b1c5. #21 is MentionWakeReactor.test.ts only, so the rebase should be textually free;
prove it the cheap way (git diff over the delta is empty, channel suites + reactor suite green) and do
NOT restart the verifier unless the rebase touches the delta it is examining. Then "PR ready #18" when
it returns. The split — refFromOperatorSession() for reads, operatorCommandIssuer() for writes, plain
CommandIssuer struct because it is stored — stands as ruled; a nominal class must not be constructible
from a payload and must not be stored in one, the same sentence both ways.

boss1 — nothing changes: #19 lanes, then #17, then ami. ajw branches when #18 merges.
