FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-a50 · in_progress · PR #47 open, empty-id stall reproduced under mutant and closed at one door, gate running, review next · nothing needed
AT: 2026-09-13 09:06 EDT

a50 built and open: PR #47 https://github.com/lvl0lvl/t3_bot/pull/47 at 2 commits on boss3/t3_bot-a50-codex-turn-id-decode off origin/main@ebaa251b6. Mechanism measured: the client's dispatchNotification catches typed failures only, so a .make throw in a handler propagated into the protocol's stdin reader loop and ended it, and the runtime's forked queue consumer died with it - hence sendTurn succeeds then nothing. Fix: every notification and request handler is registered through one door (onNotification/onRequest) that decodes turnId/itemId/turn.id/item.id with the brand's schema first; a refused id emits kind:error 'codex/malformed-id' naming codex, the method and the field (the adapter maps it to runtime.error, ingestion sets the session to error with activeTurnId null, so the turn closes), and a refused REQUEST is answered invalidParams so the app-server is not left waiting on an approval; a handler that fails or dies anyway is reported as codex/handler-failed and the pump keeps running (the second criterion from #43's security lane). Tests over the mock peer: empty turn id, empty item id on the queue path, empty turn id on an approval request. Mutants: refusal removed -> red; whole door removed -> the recorded stall reproduces (8 s timeout); request refusal removed -> the catchDefect backstop answered internalError and the turn completed, assertion red in 30 ms. The queue consumer's catchCause alone is unpinned (no input reaches it once the refusal exists); the body says so. Four Codex suites 106/106, tsc 0 after the last edit with only 'suggestion TS377' filtered, lint/fmt clean. Count gate running; /review-pr next. Also filed earlier for #44: t3_bot-dup, t3_bot-bwh.
