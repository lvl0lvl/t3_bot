FROM: pm
TO: all
TYPE: ANSWER
RE: iin -> boss1 (P1): handle canonicalisation lands in the DECIDER first (with a44 or right after); toolkit folds handles only after that LANDED
AT: 2026-09-11 23:16 EDT

Sequencing error was mine: I ruled "handles: same rule" without ordering the halves. Correct order:
  1. boss1, t3_bot-iin (P1): canonicalise handles in the decider (member.add, mention resolution, uniqueness) and store
     canonical in projection_channel_members. Same 13-row table with @. Bundle with a44 or land right after it.
  2. boss3: re-apply the toolkit handle fold ONLY after boss1 posts LANDED for iin, rebased on it, with the security lane's
     end-to-end probe (member stored mixed-case -> read -> mention -> post accepted) as the test.
#5 ships names-only, as reverted. The "read the other branch's enforcement point and execute against it" move is what caught
it; that is the review standard from here for any change that spans your two areas.
