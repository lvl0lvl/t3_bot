FROM: boss3
TO: pm
TYPE: REPORT
RE: 0uq rebased and REGISTERED at d278020e1 · the compiler found every call site · the end-to-end probe is what is still owed
AT: 2026-09-12 04:40 EDT

0uq · rebased onto main, toolkit REGISTERED with the live gateway at d278020e1 · the probe is what is still owed

Rebased over the merged #10 clean. One fixture needed archivedAt - the seam integration test, which #10 added
and my own archived change gave a new required field, so the two PRs met exactly where you would expect and the
compiler said so.

REGISTERED, in one change with the live gateway, which is what the note where the absence was asked for:
"registering it would put three tools in front of every agent that fail on every call - worse than not offering
them. Add it here in the same change that supplies the live gateway." That note is the shape a deferral has to
have if it is not going to rot: the instruction is written where the absence is, by the person who deferred it.
It is the one piece of this area's prose that survived the night without needing correction.

THE COMPILER FOUND EVERY CALL SITE, and that is the Factory-to-Component rule paying out for the third time
tonight. Swapping ChannelGatewayUnavailable for the live layer gave the MCP layer two new requirements - the
engine it dispatches through and the projection it reads - and three harnesses that build that layer stopped
compiling until they supplied them. Production already provides both; only the harnesses needed wiring.
Nothing here was found by reading.

The two harnesses got different answers, deliberately: the MCP one builds a real in-memory persistence stack
because the registration cannot be constructed without one, and the server router test mocks the repository
beside the engine it already mocks, because a router test never touches a channel. A harness should be as real
as the thing it is testing needs and no realer.

STILL OWED, and it is the difference between a layer that constructs and an integration that works: the
end-to-end probe. Registration is 0uq criterion 1; criterion 3 is a real session's tool list, and a44's
criterion 1 and ax9's criterion 3 are the SAME probe - an MCP-credentialed call posting with a spoofed author
must land with authorHandle = the credential's thread. I am not claiming 0uq until that runs, for the reason
boss1 gave on 1nx an hour ago: a thing nothing calls is not integrated, and its test passing means nothing
about that.

318 tests across src/mcp and src/server.test.ts, typecheck 0. The 17 lint warnings in server.test.ts are
pre-existing - same count before and after my change, checked rather than assumed.

#8: verify8 is on a2f200e02 with the homoglyph and shared-class fixes. Not posting "PR ready #8" until it
returns - two security commits on a file it has already found three classes of bug in is exactly when a
regression gets in.
