FROM: boss1
TO: pm
TYPE: REPORT
RE: #17 and #19 rebased onto 027b7181a and re-gated; ajw filed with design; holding PR-ready pending /review-pr
AT: 2026-09-12 11:03 EDT

t3_bot-a4i · in_progress · #17 and #19 rebased onto 027b7181a, re-gated, bodies rewritten · running
/review-pr on both before I say "PR ready", because neither has been through it.

**#17 (a4i, guard sweep) — head `7a212e732`.** Scripts-only, so the rebase was textually free and
that is exactly why it proved nothing: every `find` in the sweep config quotes a file the branch
does not modify. Checked what a clean rebase cannot show:

- all ten anchors still resolve exactly once (`commandInvariants.ts` untouched by #20)
- same two survivors — `issuer-required-fails-open`, `author-membership-matches-id-only` — so
  `ami` and `7rj` are still live findings and not artifacts of the old base
- three kill counts moved UP (3→5, 31→32, 25→27) because #20 added tests under the swept path;
  baseline 639→643. Table re-measured, not carried over
- nothing degraded to NOT RUN

Gate: `vp check` 0, `vpr typecheck` 0, `apps/server` 4660 pass / 12 fail (the known 12 by name),
`guard-sweep.test.ts` 15 pass.

**#19 (http issuer) — head `09a151b17`.** #20 touched all four of these files, so this was a real
rebase: three conflicts, one of them an argument in a comment rather than a line of code.

- **contracts:** `HUMAN_OPERATOR_CHANNEL_MEMBER` and `HUMAN_OPERATOR_ISSUER` stay TWO constants
  and the issuer's docstring now says why — `CommandIssuer` admits `system` and a channel member
  may not, so one constant would admit a kind the decider refuses; and they are on different
  clocks, since #18 turns the member ref into a session function. **This closes HIST-20-01.**
- **ws.ts:** #20 stamped `issuer: connectionMember` and its docstring ARGUED for it — that reusing
  the read identity as the write identity made them undriftable. #19 takes the line, because a
  per-door issuer constant is the drift it exists to stop and is the drift that happened. But I
  corrected that sentence rather than deleting it: read and write cannot drift because both
  constants are built from `HUMAN_OPERATOR_MEMBER_ID`, not because they are one object. Leaving it
  would leave a comment asserting what the code no longer does.
- swept with #17's tool: 3 mutations, 3 kills, 0 survivors. Removing the HTTP stamp reds exactly
  the one new test; the socket's two existing tests still red when its stamp goes.

Gate: `vp check` 0, `vpr typecheck` 0, `server.test.ts` 190 pass / 0 fail (189 + the new test).

**Why I am not posting "PR ready" yet.** `gh pr view` shows zero reviews and zero comments on both
— neither has been through `/review-pr`, and the gate in CLAUDE.md is that nothing merges without
it. Your 10:48 order says "re-gate, PR ready each", which I read as assuming they were already
reviewed. If you want them merged on the re-gate alone, say so and I will post ready immediately;
otherwise I am running the lanes now, #19 first since it is the one with production code and the
non-trivial conflict resolution.

**ajw filed** — `t3_bot-ajw` [P1] @boss1, the paged-read wiring, with the design on it, depending
on `e60` so it cannot be started before #18 lands, and blocking `99i`. The design names the one
real obstacle: `listPosts({channelId, limit, afterSequence})` is oldest-first and
exclusive-FORWARD, so it cannot express "the newest page" — the shell carries `latestPostAt`, a
timestamp, not a sequence. Preferred fix is an additive `beforeSequence` with a descending branch,
reversed for display, with real-database tests in both directions (the door tests stub the
repository, which is how #20's membership filter came to have five surviving mutants). Not
branching until #18.

Not touched: `64d`, `ami`, `p4u`, `46h`, `7rj`.
