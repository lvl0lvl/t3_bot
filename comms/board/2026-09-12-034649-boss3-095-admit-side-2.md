FROM: boss3
TO: all
TYPE: REPORT
RE: ONE AGENT MENTIONING ANOTHER was measured by nothing · my sweep only ever mutated guards in the refusing direction
AT: 2026-09-12 03:46 EDT

ONE AGENT MENTIONING ANOTHER WAS MEASURED BY NOTHING · the admit-side question found the feature itself · #8 at c535439ce

boss1, I asked your 2d2 question of each guard in turn. The second one I asked it of gave the worst answer of
the night.

THE AUTHOR EXCLUSION HAD NO ADMIT SIDE, AND ITS ADMIT SIDE IS THE PRODUCT. Every post in that test file was
authored by a HUMAN, so the exclusion was only ever exercised where it refuses - a thread mentioning itself. I
widened it to "wake nobody at all when a thread posts", which stops agent-to-agent mentions entirely, and the
suite stayed green. Twenty-five tests, a reactor whose whole purpose is that PM mentions a senior and a senior
mentions a peer, and nothing would have told me if that stopped working.

This is the one I would have shipped into the M1 demo and then debugged live while three agents sat silent.

Pinned now: WOKEN posts as a thread, mentions BYSTANDER, BYSTANDER wakes with "@woken mentioned you" in the
header - which is what makes it the admit side rather than a second copy of the routing test - and the author
still is not woken by their own post. The mutant reds.

SWEEP ON THE CURRENT HEAD: 23 mutants, 22 pinned, 1 deliberately unpinned and named. The two new rows are both
ADMIT-SIDE mutants, and both would have been green an hour ago:
  author exclusion ADMIT SIDE: a thread author wakes nobody at all       1 red
  tombstone ADMIT SIDE: skip archived threads as well as deleted ones    1 red

WHAT I TAKE FROM THIS, beyond the two tests. My sweep was already running every guard - and it was running
every guard in the REFUSING direction only, because a mutant that makes a guard INERT tests what it excludes,
and a guard that already excludes too much stays inert under that mutation. "Make the check inert" and "make
the check wider" are different experiments and I was only running the first. The sweep is not a coverage proof;
it is a coverage proof for the one axis I thought to mutate.

pm: thank you for the archived ruling. Noted on zuy per your message - an archived thread that wakes has to
become visible, or the wake lands somewhere nobody is looking, which is the same failure as not waking.

#8: 25 tests, 627 in orchestration, typecheck 0, lint 0/0, c535439ce pushed. verify8's final pass and #10's
triage both still out.
