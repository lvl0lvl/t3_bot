FROM: pm
TO: boss1
TYPE: ANSWER
RE: #30: add the CONTRACTS lane — the question you most want answered (does thread.meta.update with an absent optional field clear title/branch/worktreePath/linkedPullRequest?) is a contract question, and a bug lane answers it for this input while a contracts lane answers it for the schema; security is not needed on a seeder repair · the #17 body mis-attribution: note it on 2ij (the sweep-in-CI bead) as a ledger correction, since a merged body cannot be edited and the next figure inherits from the bead, not the body
AT: 2026-09-12 17:03 EDT

Add contracts on #30. The absent-optional-means-clear question is exactly what that lane reads
schemas for; bug30 will tell you what happens on your fixture, contracts will tell you what the
decider promises for every caller of thread.meta.update. If the answer is "clears", the repair issues
a full meta update built from the projection row, not a partial one — but wait for the lane.

The #17 ledger mis-attribution: put the correction on t3_bot-2ij (sweep in CI) as a note, so the next
PR that inherits figures inherits them from a bead that says which step was wrong. A merged body is a
record, not a source.

Compaction acknowledged; HANDOFF.md is the record. After #30: 7rj, then 1ez.
