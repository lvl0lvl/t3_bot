FROM: pm
TO: boss1
TYPE: ANSWER
RE: #19 ruling: option 3 — boss3 has already written operatorCommandIssuer() on #18 (packages/contracts/src/channelMemberRef.ts:129, called at ws.ts:536), so #19 does not add it: finish the lanes and the independent fixes now, hold Phase 5, and when #18 merges (imminent) rebase #19 to call #18's function from http.ts, delete HUMAN_OPERATOR_ISSUER and the 'different clocks' paragraph, one docstring at the function · API-19-06 (shared dispatchFromClient stamping the issuer, optional issuer? on dispatch options) is a bead, not #19 — file it, depends on e60, and it is the natural home for the N-sites problem
AT: 2026-09-12 11:19 EDT

Read #18 for you: operatorCommandIssuer() exists there.
  packages/contracts/src/channelMemberRef.ts:129  export const operatorCommandIssuer = (): CommandIssuer => ({
  apps/server/src/ws.ts:536                       const connectionIssuer = operatorCommandIssuer();
So (1) collides for certain and (2) ships a constant that #18 deletes within the hour. Option 3:

  - Now: let the four lanes finish, fix every finding that does not depend on the issuer shape, run
    triage. Do not touch the write-identity code in #19 yet.
  - When #18 lands: rebase #19 onto it. http.ts calls operatorCommandIssuer() from contracts; delete
    HUMAN_OPERATOR_ISSUER; delete the "different clocks" paragraph and the ws.ts:510-523 paragraph that
    argues the other side — the reason both halves cannot drift now lives in one place, the docstring
    of refFromOperatorSession()/operatorCommandIssuer() in contracts, and a comment at each door says
    only which one it calls and why. Then re-gate, verifier on the rebase delta, "PR ready #19".
  - Your API-19-01/02 finding stands as stated: the `: CommandIssuer` annotation manufactured the
    distinction. Put that sentence in the PR body, not in the code.

API-19-06: bead, not #19. File it depending on e60 (#18) with the contracts lane's text: `issuer?:`
optional on the engine's dispatch options, ~50 call sites, 3 stamp; fix is one shared dispatchFromClient
that stamps the issuer and two thin transports, so "wired at N sites" is not representable. That is also
the shape I set for ajw's door; whoever builds ajw first builds the shared dispatch, and the other
rebases onto it. Say so on both beads.
