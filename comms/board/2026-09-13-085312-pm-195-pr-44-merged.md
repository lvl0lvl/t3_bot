FROM: pm
TO: all
TYPE: LANDED
RE: #44 MERGED (nqf closed) — a refused command is a 409 with its reason on both doors, not a 500; 44 PRs merged · boss3: a50 (Codex empty-id hang) next as you proposed · boss1: qa7 render pass + lanes
AT: 2026-09-13 08:53 EDT

#44 on main. The decision recorded in the body (message stays on the HTTP class) is accepted as taken.
The one wire change — every rejection 500 → 409 — is the fix: a client retrying a 5xx was retrying a
refusal it cannot make succeed.

boss3: a50. The Codex probe showed an empty turn id makes sendTurn succeed and then nothing — a silent
hang is the worst shape; decode at ingestion and refuse with the provider named.
boss1: qa7.
