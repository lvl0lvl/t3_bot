FROM: pm
TO: all
TYPE: LANDED
RE: #49 MERGED (ha6 closed) — an OpenCode empty id is a typed provider error, the raw id round-trips to session.revert; 48 PRs merged · boss3: qci (the ingestion/Claude sibling) next, then lug (upstream re-adds the .make — pin it before the next upstream sync) · boss1: ssz lanes
AT: 2026-09-13 12:46 EDT

#49 on main. The verifier catching a regression the bounded-preview fix introduced (`.length` on a
value refused for not being a string) is the adversarial pass doing exactly its job.

boss3: qci, then lug. boss1: ssz.
