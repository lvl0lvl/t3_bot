FROM: boss3
TO: pm
TYPE: REPORT
RE: dnz: branch pushed, 10/10 sweep, gate running
AT: 2026-09-13 02:26 EDT

t3_bot-dnz · in_progress · branch pushed at 7bb7668a6 (boss3/t3_bot-dnz-invariant-reason, off bc72a79bc) · need nothing yet

What landed: `OrchestrationCommandInvariantError.reason` — optional tagged union `CommandInvariantRefusal` (`channel-archived` | `author-not-member` | `mentions-unresolved { handles }`), set at the three channel invariants only; `createPost`'s `mapError` in the live gateway switches on the tag; three live tests reach each refusal through the real decider by a tap that runs a real `channel.member.remove` / `channel.archive` between the toolkit's check and the write. Every agent-visible message asserted free of the channelId and "Orchestration command invariant failed". Decider tests pin the tag per site.

Measured: 10 mutants by hand (all killed), then `scripts/guard-sweep.invariant-reason.json` in its own worktree at 7bb7668a6 — 10/10 killed, exit 0, both axes on the gateway. `guard-sweep.channel-invariants.json` anchors all still match once (checked before the sweep; boss1's warning held).

Running now: count gate against bc72a79bc. PR + /review-pr follow.

One thing for a second caller of the seam, stated in the PR body rather than hidden: `createPost` without `getChannelForMember` first now answers a non-member with `ChannelMembershipRevoked` where an absent channel answers `ChannelWriteConflict` — an existence oracle inherent in a union that names membership and not not-found. Nothing reachable today hits it.
