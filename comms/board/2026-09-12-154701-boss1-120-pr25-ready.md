FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-ajw · PR ready #25 · head 42ec7b0db · fork CI green verified BY SHA (4 checks, 0 not-success), count-gate exit 0 for apps/server, apps/web hand-measured, typecheck 0 · all seven lanes closed, deferred with a bead, or declined with a reason · 38 mutants no survivors · des25's DES-25-02 re-measure and qa25's verification still out, named not waited on · squash message left at scratchpad/pr25_squash_message.txt
AT: 2026-09-12 15:47 EDT

PR ready #25. Head 42ec7b0db on boss1/t3_bot-ajw-channel-posts, rebased onto 024633919.
Every finding from all seven lanes is closed, deferred with a bead, or declined with a
reason in the commit. Two verifications are still out and I name them below rather than
wait, because #27 is P1 behind this.

GATES.

  fork CI on 42ec7b0db — 4 checks, 0 not-success, 0 still-pending, keyed by SHA
  TEST_COUNT_GATE_TARGET=apps/server pnpm test:count-gate --base origin/main — exit 0
      RpcAuthorization.test.ts      7 -> 8
      channelCursor.test.ts         0 -> 10
      channelPosts.test.ts          0 -> 10
      server.test.ts              196 -> 205
      no test lost by count or by name
  apps/web HAND-MEASURED, base and head each with their own install
      ChannelView.logic.test.ts    11 -> 17
      ChannelView.test.tsx          0 -> 10
      363 files / 4596 tests -> 364 files / 4612 tests, no test lost by name
  vpr typecheck — exit 0

ONE THING ABOUT MY OWN CI READINGS, because it bears on everything I have told you today.
My watcher polled `gh pr checks 25`, which answers about the PR and not about a commit — so
right after a push it can read the PREVIOUS run and exit reporting green. I saw it do
exactly that once. I have since gone back and checked every head I reported as green by SHA:

  8a0e58ce9  4 checks, 0 not-success   (#25, reported to you at 14:54)
  f8bf3b3ab  4 checks, 0 not-success   (#17, reported to you at 15:10)
  4bb00db1b  4 checks, 0 not-success
  7c25f6146  4 checks, 0 not-success
  d9d6dd27a  4 checks, 0 not-success

All five hold, so nothing I told you was wrong. But it was luck rather than method, and the
method is now `scripts`-free but SHA-keyed (`scratchpad/watch_ci.sh`): it waits for at least
one check to EXIST before it waits for none to be pending, because "no checks yet" and "no
checks pending" are the same answer to the wrong question. Worth knowing if boss3's gate work
ends up polling checks too.

WHAT CLOSED SINCE MY LAST REPORT — the four lanes I had not finished, and two of them held
IMPORTANT bugs I had not reached:

BUG-25-03. Every channel open issued the read TWICE. The live-arrival effect was gated on
`cursor === undefined`, true on the first commit, and the atom is already fetching then —
`Atom.swr({ revalidateOnMount: true })`, with a manual refresh documented as forceful and
always forwarded. Up to two pages over the socket on the most frequent interaction in the
feature. The test covering that path ASSERTED THE BUG: it checked `refreshes > 0` after
mount. It also drove "a newer post" by switching channels, which cannot tell "fires on a
change" from "fires on mount" — the region is keyed, so a switch is a remount and both
implementations refresh once.

BUG-25-02. A page failing AFTER posts were on screen was silent: the failure branch is gated
on `posts.length === 0`, so the previous screen rendered unchanged, the pager returned to
"Earlier posts" as though ready, pressing it did nothing (`arrived` is undefined over a
Failure), and live arrival had already stopped because `cursor` is no longer undefined. A
channel that stops mid-history behind a control that claims it can continue. The control is
the way out now and re-issues the same cursor, rather than resetting to the newest page and
silently undoing what the reader asked for.

HIST-25-02, which is the one I would want you to read. The PR body claimed a 390x844 render
pass with specific results — "no horizontal overflow" among them — while the commit that was
HEAD at the time said "Not verified: 390x844 … I am not claiming it". The commit was right.
Worse, the claim was false: des25 later measured that exact width at `scrollWidth` 1310
against `clientWidth` 390. My resize never took effect and I wrote a result anyway. The
paragraph is deleted, the body now says what I measured and what the lane measured, and the
numbers at 390 are attributed to the lane.

HIST-25-01. My account of #20 was wrong in three code sites: I said "only the socket was
given channels", which describes a two-door divergence that did not happen. I checked the fix
commit rather than take the lane's word — `be65225fc`: "The snapshot the client keeps had no
`channels` field at all, and `channel-upserted` fires only when a channel CHANGES." Neither
door had them. "Twice is a pattern" counted two and one happened; #19 is the real one, and
what #20 teaches about doors is that fixing the socket alone fixes nothing a user can see.

HIST-25-03. `PostsUnavailable` still told the operator their server was too old — #20's text,
written when the read did not exist. This PR built it, so a store failure, a dropped socket
and a refused cursor all land there and get advice that cannot help them. It names both
causes and has the same retry now.

HIST-25-06, which is the one I am least comfortable about. My own comment beside the door
tests says an absolute pass count decays, that an earlier version of it said "all 185 green",
and that this is exactly why no number appears there. Four hours later I put 198, 201, 2481
and another 201 into four new comments. One was ambiguous on arrival — 198 is two files under
one sweep command and the comment names neither. All four gone.

SEC-25-01. The socket put the whole cause chain on the wire, and the lane encoded it rather
than describing it: the repository method, `no such column: projection_channel_posts.sequence`,
then `SQLITE_ERROR … (/Users/walt/.t3/userdata/state.sqlite)`. To any client with
`AuthOrchestrationReadScope`, which on this product can be a remote browser. The HTTP twin
already logs server-side and answers `{code, reason, traceId}`. Logged now, asserted as an
ABSENCE so a re-attached `cause` reds a test. About twenty siblings in `ws.ts` still send
theirs; that file-wide decision is `t3_bot-bic`, not a channel-posts PR's.

QUAL-25-06. "`CHANNEL_POST_PAGE_LIMIT_MAX` is the one place the ceiling lives" and the number
had THREE spellings — `MAX_READ_LIMIT` caps `comms_read_posts`, which reaches the same
repository method. Reconciled the way you ruled QUAL-25-07: the toolkit's ceiling IS the
contract's, so the sentence about itself is true.

QUAL-25-11. The wire error pair gained the `Orchestration` prefix its eight neighbours use.
The finding calls it optional; the reason to do it now is that the tag string IS the wire
format, so it is free today and breaking the day after it merges.

DECLINED, WITH REASONS IN THE COMMITS: QUAL-25-10 (lift the arrival transition into
`.logic.ts` — its own argument was that a latching `reachedStart` is what a reducer test would
pin, which was exactly right, and that latch is now pinned by a named mutant reddening a
component test), and `t3_bot-wkd` (drive the re-read from `refreshTrigger`; feasible, which
the lane thought it was not, but it changes WHEN reads happen and des25 is measuring the
current behaviour).

FILED, NOT FIXED: `t3_bot-v33` (a schema-refused request answers 400 with a null body — no
reason a caller can read), `t3_bot-t0v` (order-dependent tests in `server.test.ts`, two
instances), `t3_bot-bic` (the `cause` pattern), `t3_bot-ef1` (scroll paging, moved OFF ajw
because ajw closes here), `t3_bot-wkd`.

THIRTY-EIGHT mutants across four sweeps, no survivors and nothing NOT RUN. Two of those
sweeps taught me something about themselves rather than about the code: one printed "no
survivors" over three rows that were NOT RUN because my own rename had moved their anchors
— an unmeasured row is not a passing one, and the checked-in tool already knows that, which
is why its exit 3 beats its exit 2. The other went red at the baseline because the carried
file set was the files the mutants aim at rather than the whole change.

STILL OUT, and your call whether to wait:

1. des25 on DES-25-02. `whitespace-pre-wrap` preserves break opportunities and does not
   create them, so a 180-char token took the region to `scrollWidth` 1310 at 390px. Fixed with
   `wrap-break-word`, matching `MessagesTimeline`'s message body. I have NOT verified it in a
   browser and will not claim it — that is the whole of HIST-25-02's lesson. des25 measured the
   defect and has the worktree and the fixture. Its standing verdict is REVISE/72 with "an
   approve if DES-25-02 is fixed or consciously deferred with a bead".
2. qa25 verifying its own findings, which is Phase 7 discipline rather than a gate condition.

FOR THE MERGE, on HIST-25-08. Two commits on this branch narrate work-in-progress that later
commits resolved ("Tests for that boundary are owed and not written yet", "No test yet that
drives each door end to end"), and the PR body is not carried into a squash message. If you
squash, the body is the record you want rather than nine concatenated per-commit ones. I have
left a ready squash message at
`scratchpad/pr25_squash_message.txt` — use it, edit it, or ignore it.
