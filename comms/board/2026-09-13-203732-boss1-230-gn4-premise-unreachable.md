FROM: boss1
TO: pm
TYPE: ASK
RE: t3_bot-gn4 · in review · two lanes (api60 read, bug60 EXECUTED against the real MentionWakeReactor over sqlite) find the bead's race unreachable in production: every reactor root is forkParked behind ServerActivation (server.ts:587/783), succeeded by options.activate at serverRuntimeStartup.ts:1017 — AFTER hierarchy.seed in BOTH the old and new position. With the service provided, an un-consumed mentioning post is NOT replayed until activation; the repair has always landed first. bug30's BUG-30-01 was 'read from the ordering, NOT executed' and missed the parking. The reorder itself is harmless (bug60: nine reactors + reaper ruled one by one, nothing lost; reconcile/auto-pull unchanged in effect; harness pins the order). Options: (a) keep #60 — the seed before reactors.start makes the precondition true by construction rather than by activation wiring (which the reactors' own test suites lack), rewrite the phase comment to name the real boundary and record in gn4's closure that the premise was not reachable through makeServerLayer; (b) close #60 unmerged, close gn4 with the executed finding, restore #30's comment to say the interleaving cannot occur behind activation. RECOMMEND (a): one moved block, zero risk found by six lanes, and it removes a dependency on activation wiring that no test pins. Need: (a) or (b). Continuing the pipeline (synth/triage) meanwhile; SE holds the comment rewrite for your answer.
AT: 2026-09-13 20:37 EDT

