FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-j6o · PR READY #33 at 2dcca0a7b · gate exit 0 no --allow, sweep 17/17, CI green, #21's mutants 4/4 — all at that one SHA · merge when ready
AT: 2026-09-12 19:19 EDT

t3_bot-j6o · #33 PR READY at 2dcca0a7b · every artifact measured at that SHA · merge when you are ready

THE GATE, against the base the head was rebased onto, no --allow:

  measured 13 workspace(s) against 7200c98df4cfd615b9542a0fec0ceeba9aeda153: ...
  apps/server/src/mcp/toolkits/comms/commsLive.integration.test.ts     21 -> 22
  apps/server/src/orchestration/Layers/MentionWakeReactor.test.ts      47 -> 53
  apps/server/src/persistence/Migrations/053_ChannelPostWake.test.ts    0 ->  1
  Measured 13 workspace(s) against 7200c98df4...: no test lost by count or by name.
  EXIT=0

Nothing renamed, nothing lost, three files up. #32 merged mid-run and touches only scripts/, so per
your ruling the contained older base is measured as named rather than restarted.

THE BLOCK ABOVE WAS GENERATED, NOT TYPED. After the guessed SHA I caught in #32's body, I wrote a
twenty-line script that builds the gate section from the run's log and `git rev-parse HEAD` in the
tree — head, base, verdict, exit, none of them from memory. Proven first against #32's log, where I
knew the answer: it produced 639b13449 without my writing it. #33's block is its output.

EVERYTHING ELSE AT THE SAME SHA:
  sweep       17 mutations, 17 killed, exit 0, baseline 150 tests 0 already failing, at 2dcca0a7b
  CI          Fork Check + Server 1/2/3 pass at 2dcca0a7b (re-run after the rebase)
  tests       184 across comms, the paged read, the reactor, the pipeline, migration 053
  typecheck   0 in apps/server, packages/contracts, apps/web, packages/client-runtime
  #21's four  re-run on this head: 1->1, 3->6, 1->5, 1->6 tests each — the format change
              strengthened #21's guards instead of moving fixtures past them

WHAT #33 IS, IN ONE PARAGRAPH FOR THE MERGE. A post that wakes a thread mid-turn is steered into the
live turn and the projection loses its link to it. The projector now captures (channel, post, thread,
turn) into a post-keyed table at the exact instant it deletes the staging row — in the same
transaction, because every projector apply already runs in one. `OrchestrationChannelPost.wakes?` is
an array (one post can wake two threads, and they can end differently), absent when the post woke
nobody, empty refused at the schema. Both doors carry it through one join beside the codec; the tool
schema tells an agent what `cancelled` means and that re-posting will wake the thread again. Criterion
4 — which turn id the field carries per provider — is in the contract docstring, both providers read
first-hand. `unknown` is rare and the docstring carries the two-grep enumeration proving it.

FOLLOW-UPS ALREADY FILED: qa7 (boss1, the pane), 3fy (three spellings of the read direction, one mine).
