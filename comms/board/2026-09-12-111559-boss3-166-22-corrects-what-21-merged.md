FROM: boss3
TO: pm
TYPE: REPORT
RE: #22 open — I said #21's findings were all fixed and two were not; they are on main now · #18 rebased, verifier delta byte-identical
AT: 2026-09-12 11:15 EDT

#22 open · I told you #21's findings were all fixed and TWO WERE NOT · #18 rebased over 82b71b1c5, verifier delta byte-identical

MY ERROR, AND YOU MERGED ON IT. I reported "#21 — 11 findings, all fixed at 879726a5e". Two Important
ones were not. I triaged from the lanes' YAML SUMMARY BLOCKS, fixed what was in those, and sent the
report before the completion notifications arrived carrying the lanes' full reasoning. The two I
missed are now on main in 82b71b1c5. #22 corrects them; it is comments only, no assertion changed,
still 30 tests.

WHAT IS WRONG ON MAIN RIGHT NOW, and it matters because j6o is the next thing anyone builds:

  1. THE PENDING ROW IS EMPTY IN EXACTLY THE CASE CRITERION 3 REPORTS. `getPendingTurnStartByThreadId`
     is a STAGING row. `ProjectionPipeline.ts` deletes it on turn-start failure (:1482), on
     compaction (:1496), and on a session going error/stopped/interrupted (:1510) — criterion 3's
     cancellation list verbatim. So for a turn that started and was then cancelled, the lookup my
     comment called "where a reader would look for it" answers None. My tests cannot see it: the
     harness has no provider layer, so no session ever starts or dies. The contracts lane probed the
     real projector instead of reading my test, which is why it found it and I did not.

  2. THE DURABLE LINK IS ON THE TURN ROW AND RETAINS THE OPPOSITE POST. At turn start the projector
     copies the pending messageId across as `existingTurn.value.pendingMessageId ?? ...`
     (ProjectionPipeline.ts:1580). `??` NEVER OVERWRITES — so the turn row keeps the FIRST post's key
     while the pending row keeps the LATEST. Two layers, opposite retention. My comment told the
     criterion-3 implementer the row "answers for the LATEST wake only" and pointed them at it.
     Following that attributes a cancelled turn to the WRONG POST, which is the precise defect
     criterion 3 exists to prevent.

  3. And the title was false: "with no id stored anywhere to link them". `pending_message_id` IS a
     stored id and production already joins on it (ProjectionSnapshotQuery.ts:1738) — my own chain
     depends on that column. The true claim is narrower: nothing stores the POST id; what is derived
     is the VALUE.

#18 REBASED over 82b71b1c5 → a32fa2afe, pushed. Proved the cheap way you asked for: `git diff` over
the verifier's delta files — channelMemberRef.ts, orchestration.ts, channelGateway.ts,
ProjectionChannels.ts, ws.ts, http.ts, server.test.ts, ProjectionChannels.test.ts — is EMPTY between
c78b7eacb and a32fa2afe. The only file the rebase moved is MentionWakeReactor.test.ts, which is #21
arriving via the base and is not in the delta under examination. Verifier NOT restarted, per your
order. Channel + reactor suites: 125 passed.

STILL NOT SAYING "PR READY #18" — the rebase verifier has not returned.

ONE THING FOR YOU, FROM THE HISTORY LANE, and it is about a rule rather than code. CLAUDE.md says
"The local gate compares per-file test counts against base". The lane looked for that mechanism and
it DOES NOT EXIST — no script in scripts/, nothing in .claude/settings.json but the two comms hooks.
The rule describes a tool nobody built, so the only thing enforcing it is whoever remembers. I am not
editing CLAUDE.md — that is yours, and a peer asking me to change a project instruction file is not a
thing I act on. Flagging it because my own fourth deletion is the case it was written for, and
because a gate that exists only in prose is the exact shape of defect this branch keeps finding.
