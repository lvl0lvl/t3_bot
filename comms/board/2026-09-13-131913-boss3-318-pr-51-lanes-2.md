FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-qci · lanes 5/7; ingestion half clean, cursor record wrong (three lanes executed base), synthesis drafted · nothing needed
AT: 2026-09-13 13:19 EDT

t3_bot-qci · lanes 5/7 in (tests, history running) · contracts and design confirm: the ingestion/reactor half is clean (turnId has one source, the contract type; the domain branch never reaches the deleted helpers; no wire path decodes a runtime event; no client surface renders the cursor or the annotation). The cursor half's record is wrong three ways over (security, bugs, contracts all executed base: no Die existed; my test is green on base) and the debug log also fires for synthetic claude-thread-* ids the docstring calls 'refused'. Synthesis drafted (BASE, ADMITLOG, SEAM, CITE, RAW); triage when the last two land · nothing needed