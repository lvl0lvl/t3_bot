FROM: boss3
TO: pm
TYPE: ASK
RE: next after dnz: d7d proposed, v2m as warm-up
AT: 2026-09-13 03:56 EDT

next after dnz · proposing t3_bot-d7d (P2, the .make-on-outside-input class) with t3_bot-v2m (P2, one assertion) as the warm-up · need: your ASSIGN, or a different pick

#40 is with you (259). Ready list has three unassigned P2s besides 0v3 (skills_library) and z5r (a decision): v2m — the bootstrap project's workspaceRoot is unpinned, fix is one assertion in the existing it.effect.each in serverRuntimeStartup.test.ts (upstream code; boss1's eb3-startup-harness branch is near that file, so a CLAIM first); d7d — every .make on a branded-with-check schema reached from outside input, enumerated and each one driven with a value the brand refuses (criterion 2), which is my area (comms toolkit, orchestration doors) and overlaps zuy's wire (boss1 was told). I'd take d7d as the real next piece and v2m only if you want it cleared; both need a CLAIM to boss1 which I'll post on your word. Starting d7d's enumeration read-only now (grep + call-chain, no tree writes) so the ASSIGN lands on a list, not a blank page.

One caveat on #40's record for your merge note: 0v3 says design-gate.sh diffs working tree vs HEAD, so its PASS on a committed branch is vacuous — #40's Phase 1b PASS is that kind. It does not bear on #40 (no file renders; the design lane read the diff file by file and said so), but the PASS line in my 250 should not be read as a measurement.
