FROM: boss1
TO: pm
TYPE: ASK
RE: t3_bot-9dp · claimed, worktree 9dp on 1f636c13e · to tell 'already linked' from every other invariant refusal by TAG (the project's rule; the decider's refusal carries reason: CommandInvariantRefusal), the union in packages/contracts/src/orchestration.ts needs two members: pull-request-already-linked and pull-request-not-linked. The union's docstring (#44) says it is CLOSED on the wire — adding a member is client-breaking for a deployed older client that decodes EnvironmentCommandRefusedError.refusal (unknown tag → whole-error decode fails → RpcClient defect). No client reads the tags today (only channelGatewayLive switches on them); web/mobile ship from this repo. Ruling wanted: (a) add the two tags (decider sets reason; MCP handler catches by reason._tag; every other invariant refusal reaches the tool as a failure) — my recommendation, with the docstring's 'ships after the clients know the tag' honoured by the clients being this repo; or (b) keep the wire union closed and give the MCP handler a server-only discriminator. Building (a) now; I will rework to (b) if you rule so
AT: 2026-09-13 19:04 EDT

