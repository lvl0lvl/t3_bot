FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1r1 · in_progress · #69 lanes 4/7: rollback-first regresses on a typed restore failure (index.lock: conversation shortened unnamed; retries double-roll-back) — two lanes point at the bead's other shape (restore first, re-restore the current ref on rollback failure); triage next · nothing
AT: 2026-09-14 03:45 EDT

#69: gate exit 0 (32→34), body published, wave 1 in (four lanes, eleven findings), wave 2 running. The reorder has a hole two lanes found independently and executed with real git: a TYPED restore failure after a successful rollback (`.git/index.lock` — any concurrent git write in the worktree) skips my `restored === false` branch and lands in the generic catch with git's message only, so the conversation is one turn shorter than the files and nothing says so; and the natural retry recomputes `rolledBackTurns` from a read model that did not move and rolls the conversation back AGAIN (three revert clicks → three Codex `thread/rollback`s, the last one reporting success over a conversation at turn 0). At base the same index.lock input failed BEFORE the rollback and left everything consistent — so on that input the reorder is a regression. Bugs also reports OpenCode's `session.revert` reverts the agent's file patches itself, so "leaves the tree untouched" is false for that provider (read from OpenCode's source; not run — opencode is not installed here). Both lanes point at the bead's other shape: restore first and, when the rollback then fails typed, restore the CURRENT turn's ref back — a filesystem restore is idempotent where a conversation rollback is not, retries are safe, and the reverse branch does not exist. That is where I expect the triage to land and what I will build if it does. Also: the restore-fails test never asserts the revert did not complete (a fall-through past the failure activity survives), the rollback-fails test times out unnamed at 15 s when the subject is absent, the detail's guard is one-directional, `turn(s)` breaks the repo's plural convention, my comment sends the reader to the wrong failure branch, and the harness's rollback mock needs a double cast the sibling mock already avoids. A defect (not a typed failure) in the rollback bypasses the failure activity entirely — pre-existing, routed. Contracts, history, design running; then synthesis → triage → SE. Nothing needed from you.
