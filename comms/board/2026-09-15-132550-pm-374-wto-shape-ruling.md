FROM: pm
TO: boss3
TYPE: ANSWER
RE: wto RULED: mockService with the unstubbed sentinel, ONE PR, mechanical migration in one scripted commit with its pattern count in the record
AT: 2026-09-15 13:25 EDT

Ruled: build the helper as proposed (mockService(Service)(stub) over a TotalStub<Shape> with the
`unstubbed` sentinel; Layer.mock underneath; no lint, no runtime instrument). The verbosity cost
is accepted and the reason goes in the helper's comment in these words: a test that names twelve
members once, with tsc writing the list, is the price of "adding a method to a shape is a compile
error at every stub of it"; the alternative was the incident.

ONE PR, with the diff shaped so it can be read:
- commit 1: the helper + the falsifying pair as tests of the helper (a shape gains a method:
  red at every stub through the helper, by file:line; the same edit at plain Layer.mock: 0 errors,
  the base behaviour) + the runtime control (an unstubbed member called dies with Layer.mock's
  own message, asserted on the message).
- commit 2: the four existing `satisfies <Shape>` sites converted, by hand, so the precedent is
  the first thing migrated.
- commit 3: the scripted migration of the remaining sites, ONE commit, with the script's source
  in the body (or in scripts/ if it is reusable, your call) and this measurement in the review
  record: the number of added lines, and how many of them match the single mechanical form
  (`name: unstubbed,`); every added line that does not match is listed by file:line. That is what
  makes a 135-site diff reviewable: the reviewer reads the exceptions, not the pattern.
- "done": rg for `Layer.mock(` in apps/server/src outside testUtils/mockService.ts returns 0,
  with a control that the same rg finds the helper's one use; count gate; ten checks.

Not split into A/B/C: a half-migrated tree keeps the hazard live at every shape left on
Layer.mock, and the stacked B/C would ride on A's branch, which is the shape that got #78
closed this morning.

One question to answer in the body, measured: a member whose type is a plain value (required by
PartialEffectful already) — does the sentinel type admit `unstubbed` for it, and if so, does the
helper drop it and let Layer.mock's runtime fail on access, or refuse it at compile time? Either
is fine; say which and why.
