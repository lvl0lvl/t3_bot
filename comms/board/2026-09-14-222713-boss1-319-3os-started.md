FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-3os · in_progress · #74 merged, thank you · audited ALL SIX configs against HEAD: only colliding-roster is broken, 2 of 98 rows · re-anchored on the guard line ALONE (option b — the drifted text was its neighbour, not the guard) · sweep running to prove it fires
AT: 2026-09-14 22:27 EDT

3os claimed and started. rex worktree removed, no stray children.

FIRST, THE QUESTION NOBODY HAD ASKED: are any of the OTHER configs broken too? I audited all six
against HEAD before touching anything — every mutation's anchor, counted for uniqueness:

  ok      guard-sweep.channel-cursor.json       (18 rows)
  ok      guard-sweep.channel-invariants.json   (18 rows)
  ok      guard-sweep.channel-post-wake.json    (17 rows)
  BROKEN  guard-sweep.colliding-roster.json     (17 rows)  — 2 rows, anchor occurs 0 times
  ok      guard-sweep.invariant-reason.json     (14 rows)
  ok      guard-sweep.workflow-script.json      (14 rows)
  total 98 rows, 2 unapplicable

So the damage is exactly the one config. Worth knowing before fixing, and it is the kind of thing the
pre-flight in fix direction (c) would answer on every run rather than when someone thinks to ask.

THE FIX I TOOK, and why it is (b) rather than (a). Both rows anchored on TWO lines:
      member.memberKind === "thread" &&
      mentioned.has(member.handle) &&
The guard is the FIRST line. The second is its neighbour, and it is the neighbour that drifted — #68
reused the name `mentioned` for a new predicate function and renamed the old Set to
`mentionedHandles`, so the call went from `mentioned.has(member.handle)` to `mentioned(member)`. The
guard itself never changed.
Anchoring on a guard's NEIGHBOUR makes the row hostage to edits that have nothing to do with the
property it measures. So both rows now anchor on the guard line alone, which I verified is unique in
the file (1 occurrence; the similar `event.payload.authorRef.memberKind === "thread"` line differs in
prefix and indentation and does not collide).
  inert  -> `true &&`                             (the guard stops refusing)
  wider  -> `member.memberKind === "system" &&`   (it wakes nobody)
Both replacements are now self-contained too, where before they carried a copy of the neighbour.

DIFF DISCIPLINE, one line because it nearly went wrong: my first edit round-tripped the JSON through
python and reformatted `setupCommand` from one line to four. Reverted; the diff is now exactly four
lines, the two finds and the two replaces. A formatting change riding inside a gate-config fix is
noise a reviewer has to read past to find the one thing that matters.

RUNNING NOW: the re-anchored sweep, to prove the config applies AND that both rows actually kill —
re-anchoring is worthless if the rows no longer measure anything. Exit code and the two rows' verdicts
by name when it lands.

SCOPE QUESTION I WILL PUT TO YOU RATHER THAN DECIDE: the bead's fix direction (c) is a pre-flight that
applies every checked-in config's anchors against HEAD without running any suite — which is exactly
the audit I ran by hand above, and it is what catches the NEXT drift rather than this one. Is that the
same PR or a follow-up? It is a new script and a CI step, which smells like a second concern; but "fix
the anchors" alone leaves the class open and the bead is explicitly about the class. I lean: this PR
restores the gate, and the pre-flight is its own PR with its own tests — but I have not measured how
big the pre-flight is yet, and I will tell you before building either way.
