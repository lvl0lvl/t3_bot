FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · in_progress · #71 blind lanes landed TWO Important findings the dead round did not have · posting before fixing, per your condition · your call on full round vs ride-on
AT: 2026-09-14 20:07 EDT

Per board 264: "If (2) lands anything Important, post before fixing and I will decide whether #71
goes back through a full round." Two of three lanes are in and BOTH landed one. Nothing fixed yet.

Both were EXECUTED, not reasoned — each lane built its own scratch worktree and ran the real tool.
Both reports arrived truncated; I have asked each for the remainder and the recommendation text.
Full detail follows in my next post. The findings themselves are unambiguous.

--- SECURITY (HIGH, verdict ISSUES not BLOCK) -------------------------------------------------
Refusal 7 now has two limbs and they ask DIFFERENT ORACLES. The symlink limb asks the INDEX
(`git ls-files -s`, mode 120000). The new hard-link limb asks the FILESYSTEM (`fs.stat`). Any
shape where index and filesystem disagree walks through the symlink limb.

Measured, two routes, both ending exit 0 with the mutation left in a file outside the tree:
  A. worktree mode — the test command relinks src/thing.ts to an outside file DURING the baseline.
     `moved` was snapshotted before the baseline, ls-files still says 100644, stat follows the
     link, nlink is 1.  -> "killed by 1", EXIT 0, `if (false) {` permanent in outside.ts.
  B. --in-place, no arbitrary code — `git update-index --assume-unchanged src/thing.ts`, then
     make that path a symlink out of the tree. Porcelain empty so requireCleanTree passes.
     Same result. (--skip-worktree identical.)

THE ASYMMETRY IS THE EVIDENCE, and it is the part that matters for this PR: the SAME post-snapshot
swap done with a HARD link is CAUGHT — "NOT RUN — src/thing.ts has 2 links", exit 3 — because the
limb this PR added reads the filesystem per row. The limb this PR did not touch is the weak one.

Lane's own honesty, which I am repeating rather than softening: the defect is PRE-EXISTING ON MAIN
and this diff does not introduce it. Reachability is NARROW — it needs a plumbing flag on the
operator's own tree or a suite that relinks its own sources mid-run. A setupCommand that symlinks
is NOT a route (measured: git reports the typechange as " T src/thing.ts" and `moved` catches it).
Verdict ISSUES rather than BLOCK because nothing regresses.

WHAT THIS PR DID INTRODUCE is the thing I think you will care about: the rewritten refusal 7 now
claims the general property — "a file whose write would land where the restore does not reach" —
and the code delivers that COMPLETELY for hard links and INDEX-ONLY for symlinks. The header is
now broader than the code. That is the same false-universal shape you ruled on two hours ago for
the nlink comment, in the same header, written by the same PR.

--- TESTS (MUST FIX) --------------------------------------------------------------------------
The new guard is wired at TWO call sites and only ONE has a hard-link test. `linkRefusal` is
called from the pre-flight loop (the `continue` near line 1002) and from the row loop (line 1102).
Removing the pre-flight `continue` reds exactly one test — the SYMLINK one. No hard-link test
names that site. This is CLAUDE.md's "a guard wired at N call sites needs N tests", on the guard
this PR adds.

Executed both ways on a tracked file hard-linked outside the tree WHOSE ANCHOR IS ABSENT:
  fixed tool:              EXIT 3, "on-hardlinked-plain: NOT RUN — src/plain.ts has 2 links…"
  pre-flight continue gone: EXIT 1, "GuardSweepConfigError: anchor not found in src/plain.ts"
So the pre-flight skip is load-bearing for the hard-link class exactly as for the symlink class —
it is the difference between exit 3 naming the link count and exit 1 blaming a stale anchor — and
nothing pins it. The header calls the exit code a verdict; this silently turns "something NOT RUN"
into "the tool or config failed".

The same lane's other result is worth saying plainly because it is the good news: 10 mutants, every
branch this diff introduces dies in BOTH directions, no new test survives the guard it names, and
the admit side is real (`links > 0` reds 4). The coverage is sound except for the missing call site.

--- WHAT I RECOMMEND, AND WHAT I AM NOT DOING ------------------------------------------------
I am not fixing anything until you rule. My read:

1. TESTS/MUST FIX — fix on this PR. It is one test, it is the guard this PR adds, it is our own
   stated rule, and the lane has already executed the distinguishing input.
2. SECURITY/HIGH — do NOT close the symlink limb on this PR. It is pre-existing, the fix is not
   one line (Effect has no lstat, which is WHY the index-mode test must come first — the tool's
   own docstring says so), and closing it is a different PR with its own mutants. Instead NARROW
   REFUSAL 7's HEADER to what the code delivers, which is the same one-line discipline you already
   ruled for the nlink comment, and file the real fix as a bead.
3. That makes it ONE commit: the missing call-site test + two narrowed claims (nlink platform,
   refusal 7 symlink limb). Then re-gate and re-sweep at the new head.

I do not think this needs a full seven-lane round. The three highest-yield lanes ran blind on the
head, found what the dead record could not have contained, and the coverage lane independently
re-derived the same both-directions result my mutation pass got — which is the cross-check that
tells me the lost record was not hiding a third thing. But that call is yours.

Bugs lane still out. Sweep re-run still going (exit code). Heavy slot still mine.
Bead filed for the harness error per board 266: t3_bot-icr (P3).
