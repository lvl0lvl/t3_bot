FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · #72 6/7 lanes in: pre/N keyed on count is the hole (Codex mid-turn regression + stale after a dead wake, 3 lanes); turn-1 card/panel seam; triage next
AT: 2026-09-14 06:20 EDT

#72: six of seven lanes in (history running), 19 lane findings merging to 14. Three lanes (bugs, security, architect) executed the same hole independently: keying pre/N on the checkpoint COUNT and taking it once means (a) a Codex follow-up sent mid-turn — Codex accepts them and is the one adapter that emits the mid-turn placeholder — takes pre/N+1 halfway through the running turn, and the queued turn's card lists the running turn's later writes (a regression on that input: head `[late, t2]`, base `[t2]`); (b) a wake that dies or is refused before its checkpoint leaves pre/N stale, the bead's symptom by another door (not a regression). All three propose keying on the actual turn start (the runtime turn.started for a new turnId, overwrite; or skip the snapshot while a turn is active / a placeholder is at max). Bugs and design both found that turn 1's panel goes through the all-turns query (turn/0) while its card uses pre/0 — a seam after a revert to 0 introduced here. Security: the fallback's catch is wider than a missing ref (a 30 s timeout is swallowed and the stale diff served), and the "may write" window is reactor-queue lag on one worker, not provider latency as I wrote. Tests: four surviving mutants (the once-per-turn guard, the domain start site, always-pre on completion, the canonical turn-0 pin my rename retired). Quality: "pre-turn" now names two refs in one file, a redundant rev-parse per completion, an overclaiming receipt comment. Architect, by its own account a bead: the card now says one file while a revert on the shared checkout still restores the whole tree. Blind triage executes the candidate shapes next; SE builds what it rules. Heavy slot still boss1's (sweep re-run). Nothing needed from you unless you want to rule the pre-snapshot keying before triage does.
