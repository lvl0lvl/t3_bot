FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-cvg5 · PR ready · PR #83 at 13f2878ff4, not stacked, ten of ten CI green, review CLEAN (5 of 5 RESOLVED), count gate exit 0 at the final sha · need: squash merge with the fenced SQUASH BODY via --body, then I close cvg5; the n33f shape ASK follows now
AT: 2026-09-15 17:52 EDT

PR #83 · https://github.com/lvl0lvl/t3_bot/pull/83 · head 13f2878ff4bc0c53f9712365246e0b328cc37577 on origin/main 49e0ba1425 · NOT stacked · one file.
CI: all ten check runs success on 13f2878ff4, read from check-runs at 17:52 EDT.
Gates at 13f2878ff4 (tree still): file order 14/14; seeds 1..10 green (the fix pass ran 1..20, the verifier 11..20 again); lint 0 warnings read from output; server tsc 0; count gate exit 0, no per-file row, no test lost by count or by name (the first push's gate was exit 1 with three names lost — the block rename; fixed by keeping the suite name, fullNames byte-identical to base both ways).
Review: six blind lanes at 1272cb80e8 (8 finding ids, 5 defects); executed triage: 3 LEGITIMATE, 1 DECISION resolved by the same-name fix, 1 already done; SE 3 commits; blind verifier at 13f2878ff4: 5 of 5 RESOLVED, reversion probes red, adversarial pass clean (one removed line in the whole diff, a comment; three assertions added, none removed; the base defect re-measured red on seeds 1,2,3,5,9 and gone at head); verdict CLEAN keyed by run-attest at 13f2878ff4 (.rigor/review/13f2878ff4.../{findings.yaml,triage.md,verify.md}, untracked in the cvg5 worktree). Body final 17:44 with the verifier line.
Your criteria (386): whole file green under shuffle on seeds 1-5 measured before (red, named: 1 json+branded, 2 branded, 3 branded+invalid, 4 green, 5 json+branded) and after (all green through 20); no test renamed (fullNames identical); the fix is isolation plus one added emptiness read per moved test, not a widened assertion (0 assertions removed, 3 added, verifier-counted).
Bead cvg5 to close at merge (title and description already corrected to three tests). Next: the n33f shape ASK, posted after this.
The repo squashes title + commit details; please pass the body below with --body.

SQUASH BODY (title: Store test file is green under shuffle: the global-read tests get a database each (#83)):
```
The first layer block of OrchestrationEventStore.test.ts shares one
in-memory database across its tests. Three of them read the whole log
or poison it: one counts the replayed events, one reads from 0 to find
its row, one plants an invalid-JSON row that fails every later global
read. Under --sequence.shuffle the file was red on seeds 1, 2, 3 and 5
at 49e0ba1425 and green only in file order (bead t3_bot-cvg5).

Each of the three now runs in a layer block of its own. The isolation is
per layer(name, fn) call, so the blocks keep the bare suite name and
every test's fullName is byte-identical to base; the count gate, which
compares fullNames, sees nothing renamed (the first push named the
blocks descriptively and the gate went red with three names lost). Each
of the three also reads the log empty before its first append, so a
sibling that appends first fails there by name instead of at a count.
The shared-database mechanism is stated once in the file.

After: seeds 1 through 20 and file order green, 14 of 14; assertions
unchanged plus the three emptiness reads; mutants red by name (the base
layout restored; the poison test back in the shared block; a sibling
that appends). Reviewed by six blind lanes, an executed triage, one fix
pass and a blind verifier (PR #83 carries the record).

Model: Claude Fable 5.1 in Claude Code, session boss3.
Claude-Session: https://claude.ai/code/session_01KDtYTy86Xj4WoQoQn6Dszh
```
