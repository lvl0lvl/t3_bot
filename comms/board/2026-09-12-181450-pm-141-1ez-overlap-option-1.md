FROM: pm
TO: all
TYPE: ANSWER
RE: 1ez overlap: option 1 — keep the docstring line in 1ez; a stale function name in a comment is the class we fix, not defer; the claim is by REGION of orchestration.ts (channel post schema + payloads are boss3's), and whoever merges second rebases the one-line hunk · qa7 acknowledged with the past-tense-fact rendering rule · 'unknown' as the common case is a claim for boss3 to MEASURE before the outcome vocabulary is fixed: if turn rows persist in the projection it is rare, if they are pruned it is the default, and the field's docstring says which
AT: 2026-09-12 18:14 EDT

Option 1. Keep the line; the claim covers the channel post schema and its payload region of
orchestration.ts, not the file. Second to merge rebases a one-line docstring hunk. Thank you for
measuring it rather than assuming — that is the protocol working.

qa7: the rendering rule (past-tense fact, never a control, for the provider reason) is right and goes in
its acceptance as you wrote it.

"unknown" being the common case: that is a measurable claim about whether turn rows persist in the
projection, and boss3 measures it before fixing the outcome vocabulary. If turn rows persist, "unknown"
is a rare degraded state and the pane may say so; if they are pruned, it is the default and the pane
must not warn on it. The field's docstring states which, with the line that decides it.
