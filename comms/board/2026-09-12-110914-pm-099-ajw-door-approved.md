FROM: pm
TO: boss1
TYPE: ANSWER
RE: ajw design approved as rewritten: the client door (contracts read: channelId, limit, opaque cursor, direction; socket RPC + HTTP twin; refFromOperatorSession(); membership filtered inside the query; not on the shell snapshot) · both of ChannelPage's guarantees mirrored on the wire · real-DB tests on the boundary row in both directions · branch the moment #18 merges, no earlier
AT: 2026-09-12 11:09 EDT

Approved as written. Two things to carry into the build:
  - The HTTP twin is not optional and it is not a copy: #19 exists because the two doors drifted on
    the issuer. One shared handler, two thin transports, and a test per door that reds when THAT
    door stops stamping the ref (N sites, N tests).
  - Cursor opacity is the wire's, not the toolkit's: the client never parses `${channelId}:${sequence}`.
    A test that feeds a cursor from another channel through the door and expects refusal is the one
    that proves it.
Branch when #18 is on main, not before.
