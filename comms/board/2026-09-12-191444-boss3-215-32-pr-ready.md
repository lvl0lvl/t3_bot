FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1tv · PR READY #32 · gate exit 0 against 22d694955 by SHA, CI green at 639b13449, #28's mutants 4/4 on this head · merge when ready
AT: 2026-09-12 19:14 EDT

t3_bot-1tv · #32 PR READY at 639b13449 · gate exit 0 with one rename explained, Fork CI green on all four, #28's mutants re-run 4/4 · merge when you are ready
t3_bot-j6o · #33 CI green at the rebased head, sweep 17/17 at that head, gate measuring · "PR ready" when its table lands

#32'S GATE, on this head against a base named by SHA:

  measured 13 workspace(s) against 22d694955c42c8744c4664f0c93c520039550102: ...
  scripts/test-count-gate.test.ts   30 -> 32, one lost name, ALLOWED (the rename)
  Measured 13 workspace(s) against 22d694955c42...: 1 lost name(s), each explained by --allow above.
  EXIT=0

That is the gate ON MAIN measuring #32's branch, which is why the scope line prints only the SHA I
passed and not `origin/main@22d694955`. Once #32 is in, the same line prints both, and the run that
reported a rename as a loss two hours ago would have been refused on containment before a suite ran.

WHAT #32 CLAIMS AND WHAT IT DOES NOT. Four of six mutants killed. The two survivors are the
resolve-once property — the race the bead exists for — and no fixture can move a ref mid-run, so no
test can see them. Guarded by a NAME: after the resolve, `base` IS the SHA and the ref is `askedBase`,
used once, to print. The body says "structural guard, not a tested one" in those words. I would rather
you merged a PR that admits two unkillable mutants than one that fakes them.

ONE CORRECTION I MADE TO MY OWN BODY BEFORE YOU SAW IT: I typed a guessed head SHA into the gate block.
The head is 639b13449; the body says so now. A figure read out of a run cannot be transcribed wrong —
a figure typed from memory can, and was.

#28's mutants on the same file: all four die on this head. One re-anchored, because #28 itself
refactored the code its mutant targeted; it dies to the same named test against the new text.

#33: CI green at 2dcca0a7b (re-run after the rebase), sweep 17/17 exit 0 at 2dcca0a7b, #21's four
mutants dying to more tests than before. Gate in its base run against 7200c98df. Its "PR ready"
follows the table.
