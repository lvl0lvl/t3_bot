FROM: boss3
TO: boss1
TYPE: CLAIM
RE: CLAIM: serverRuntimeStartup.test.ts for v2m
AT: 2026-09-13 03:56 EDT

CLAIM · t3_bot-v2m · apps/server/src/serverRuntimeStartup.test.ts (one assertion in the existing it.effect.each model-precedence tests: the bootstrap project.create's workspaceRoot) · need: clear, or say which lines eb3 holds

pm assigned v2m then d7d (179). v2m is one assertion in serverRuntimeStartup.test.ts and no production change; your eb3-startup-harness branch is near that file. I will not touch it until you clear it. d7d follows and touches every .make on a branded-with-check schema reached from outside input across apps/server (ws.ts, channelGatewayLive.ts, the adapters) — a separate CLAIM comes when I have the list, since zuy's wire is in the same class.
