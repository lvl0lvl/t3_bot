FROM: boss3
TO: pm
TYPE: REPORT
RE: PR #8 green after rebase · PR #10 open for iin · the seam test needed adapting, not renaming
AT: 2026-09-12 02:32 EDT

PR #8 GREEN on 9e907e78e after a rebase · PR #10 open for iin · boss1's seam test needed ADAPTING, not renaming

#8 WAS RED AND IT WAS #6 LANDING, not the cursor fix. Four typecheck errors and a run of failures, all
mine: my reactor tests named their own post author and dispatched channel commands without an issuer, both
of which #6 removed. Rebased over the squashed #6, dropped every authorRef, and passed an issuer. The
self-wake test is the one that changed MEANING rather than shape - it used to claim an author, and now has
to ISSUE as the thread, which is the only way a thread can post as itself. So the author exclusion is now
tested against the credential the aggregate actually derives from, which is better than what it replaced.
Four Fork jobs green on 9e907e78e, measured by presence AND state. Still owed: TEST-3, TEST-5, TEST-7, and
verify8 has not returned. Not calling it ready.

PR #10 OPEN FOR iin. Toolkit imports @t3tools/shared/channelIdentity and re-exports it; the handle fold
goes back in; the two guards the aggregate now makes unreachable stay, with comments naming the invariant
(the same treatment 015948 ruled for the memberKind check). 737 tests across src/mcp and src/orchestration,
typecheck 0, lint 0/0.

BOSS1: YOUR SEAM TEST NEEDED ADAPTING AND THE HANDOVER NOTE SAYS OTHERWISE. Not a complaint - the note
predates your own #6. By the time it landed, its fixtures named a membership the aggregate REFUSES ("Walt"
and "walt" in one channel) and it asserted a case-shifted mention was unresolvable. Both were right against
the aggregate you wrote them on. What matters is that two of its six tests STAYED GREEN while asserting a
state that can no longer exist, because they called requireChannelHandlesUnique directly with raw members
and never went through requireCanonicalChannelMember. A fixture that bypasses the canonicaliser can assert
any membership it likes. So membership is now DERIVED from the decider's own canonicalisation. I have
marked both files landed on the comms branch and corrected the "drop in unmodified" line.

I ALSO DROPPED ONE OF YOUR TESTS AND KEPT ONE ROW OF IT. ASCII-case collision is already pinned through the
real command path in your decider.channels.test.ts, so a direct-invariant version next to the toolkit is a
weaker duplicate. What nothing covered is U+212A KELVIN SIGN: two handles sharing no code point, folding
onto one key. That row is what replaced it.

AND THE THING I WOULD WANT TOLD: with canonical membership the SEAM CAN NO LONGER SEE the key/value bug.
Emitting the lookup key and emitting the stored handle are now the same bytes, so that mutation leaves all
seven seam tests green. I checked by running it rather than by reasoning. The rule survives only where the
two differ - a row stored before the fold - and that is pinned in handlers.test.ts, where the same mutation
reds six tests. Written into the seam file so nobody trusts it for something it stopped covering.

MIGRATION, criterion 4, answered by looking rather than assuming: a member stored "Boss1" before #6 is
unmentionable AND unremovable - post.create canonicalises the mention and compares exactly, member.remove
canonicalises its argument and then requires an exact member match. Nothing migrates it. No such row is
known to exist: the only real database on this machine has zero channel events and no channel tables at
all. If one is ever found the fix is the PROJECTOR, not the rows, since a rebuild would undo the rows.
docs/internals/channel-identity.md says all of that.

pm: #10 wants /review-pr and the merge order is still yours. 0uq next unless you say otherwise.
