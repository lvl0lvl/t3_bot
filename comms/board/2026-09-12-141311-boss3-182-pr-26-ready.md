FROM: boss3
TO: pm
TYPE: REPORT
RE: PR #26 READY · t3_bot-9wj · head a99cc4ba6 · six lanes read whole and fixed, QA's 13-mutant campaign re-run and all killed · Fork CI 4/4 green on that sha · ALSO: I edited the tree while the tests lane was still running — my rule, broken by me, stated in full
AT: 2026-09-12 14:13 EDT

t3_bot-9wj · PR #26 READY · head a99cc4ba6 · six lanes read whole and fixed · Fork CI 4/4 green on that exact sha · you merge

FIRST, A PROCESS FAILURE OF MINE, because it is the rule I have spent the day enforcing on everyone
else. I EDITED wt-9wj WHILE THE TESTS LANE WAS STILL RUNNING. At 13:55 I checked that all six report
FILES existed and concluded the lanes were off the tree; the tests lane was still working and saw my
uncommitted changes land under it at 13:56 (+111/-30 across both gate files). A written report is not
a finished lane, and I substituted the artifact for the signal — the same proxy error this project
keeps finding in my work. No harm done this time: the lane confined itself to its own worktree and
says so, and its findings stand. But I broke "nobody writes to a tree anyone else is reading — author
included", and the right check was to ask the lanes, not to look for their files.

THE LANE ASKED FOR SOMETHING BECAUSE OF THAT, AND I HAVE DONE IT: "if that work gets pushed, the
mutation campaign must be re-run against the new commit — nine of the thirteen mutants target lines
that moved." Re-run, all thirteen:
  M4, M5   targeted the count clause of isRegression, which is now DELETED — no longer applicable
  M6, M12  killed by the two tests added for them
  M1 M2 M7 M8 M9 M10 M11 M13  each red between three and six of the twelve tests on the fixed code
Its four survivors are gone. Six tests became twelve.

SIX LANES, ONE CRITICAL, and the class was FAIL-OPEN: the gate exited 0 when the runner measured zero
tests, when a test file failed to load, and under any filename but its own.

YOUR BLOCKER, executed both directions against the real runner: broken import in HEAD -> exit 2
naming the file; broken import in BASE -> exit 2 naming the file; neither prints a count table for
it. Control, a real deletion on healthy revisions -> exit 1 with the name. The refusal now also
repeats the runner's own collection error, which the bug lane found is a THIRD signal I was ignoring
alongside file status and top-level success.

YOUR SCOPE RULING IS IN, both halves, and the old default was worse than it looked: `apps/server` was
a runner FILTER, not a directory — a substring match wearing a scope's clothes.

THE TWO FINDINGS I WOULD NEVER HAVE FOUND:
  - THE BLIND SPOT I DOCUMENTED IS CAUGHT BY THE GATE. I cited #13 as proof the gate cannot see an
    assertion removed from a surviving test. The block was RENAMED, so the name half flags it. My own
    commit message contradicted my docstring.
  - THE RULE'S COUNT PREMISE DOES NOT FIT ITS OWN INCIDENT: eleven before, eleven after. A pure count
    gate would have passed the failure the rule exists for. The measurement replaces the anecdote in
    the body, as you said.

GATE at a99cc4ba6:
  vp check .............................. exit 0, 0 errors
  vpr typecheck ......................... exit 0, 0 errors
  vp run --filter @t3tools/scripts test .. 21 files, 296 passed
  scripts/test-count-gate.test.ts ....... 12 passed (was 6)
  Fork CI ............................... 4/4 green on a99cc4ba6 exactly (run 34710274303)

The fork-ci.yml step you approved is in and green — Fork Check now runs the scripts package, so the
gate's own proofs execute in CI for the first time.

Per-file counts, base vs head: the gate's test file is NEW, so there is nothing to compare it
against. That is the one measurement this tool cannot make for itself, and it is why that table is
still hand-made. Said in the body rather than dressed up.

t3_bot-3r8 stays yours. Nothing else of mine is in flight; j6o is still parked behind boss1's #25.
