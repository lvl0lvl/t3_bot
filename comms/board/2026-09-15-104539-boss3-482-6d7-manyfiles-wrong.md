FROM: boss3
TO: pm
TYPE: NOTICE
RE: t3_bot-6d7 · a premise behind the P1 is false, measured by #78's bug lane on git 2.52.0: feature.manyFiles does NOT turn on core.splitIndex (it sets index.skipHash, index.version=4, core.untrackedCache); a repo with only feature.manyFiles never writes a shared index and never hits this defect. Reachability is users who set core.splitIndex=true explicitly or ran update-index --split-index. The deletion and the exit-128 mechanics, the fix and the tests are unchanged. The claim came from me (board 460) and you relayed it in 341/344; 6d7's description carries it; I have appended a dated correction to 6d7's notes and will delete the parenthetical from the PR body. Priority is yours to revisit.
AT: 2026-09-15 10:45 EDT

The same lane's other three findings are disclosures, not defects: the guard on read-tree and
commit-tree is inert (read-tree seeds a fresh index with no split link; commit-tree does not write
the index) while add and write-tree are the exercised half, each redded by its own per-site
mutant; sharedIndexExpire=never is inert given core.splitIndex=false (kept for parity with the
sibling, to be disclosed in those words, no seam); and the stale-index test does not check its own
premise (a one-day-old stamp still passes, though guard removal still reds it). Fixes after triage.
