FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1tv · PR #32 open, 4 of 6 mutants killed and the 2 survivors are UNKILLABLE by any test — guarded by a name instead, said as such · gate + j6o sweep running
AT: 2026-09-12 18:44 EDT

t3_bot-1tv · PR #32 open at 4ec851939+1, tests 32/32, gate running against 22d694955 by SHA · nothing needed
t3_bot-j6o · sweep config committed (13 mutations), sweep running · nothing needed

1tv IS BUILT, on the value the gate already had. `git worktree add` was printing `HEAD is now at
5aa0804c1` into every log under a screen of install noise; the work was putting it where a reader is
and refusing on it:

  1. `resolveBase` turns the ref into a SHA ONCE. Every later site — diff, worktree, both printed
     lines — takes the SHA.
  2. Exit 2, "rebase first", when HEAD does not contain it. CONTAINMENT, not staleness: a head that
     contains an older base is measured against the base it names, and the printed SHA is what shows
     a reader it was older.
  3. Both scope lines print `origin/main@5aa0804c1` — as asked and as resolved.

Does NOT fetch; a gate that mutated the repo it measures is the worse bug.

THE FIXTURE THE REPO CANNOT SUPPLY: HEAD always contains origin/main in CI and usually locally, so a
test over the real repo passes against a resolver that never checks. Throwaway repo, two commits on
main, a branch forked before the second. Both refusals are exit 2, so the MESSAGE is asserted.

MUTANTS: 4 of 6 killed, and the other two are worth your attention because I am NOT claiming them.

  killed   containment removed / containment inverted / resolver returns the ref / SHA dropped from the line
  SURVIVED the diff reads the ref instead of the SHA
  SURVIVED the worktree reads the ref instead of the SHA

Those two ARE the resolve-once property, i.e. the race this bead exists for, and no test can kill them:
no fixture can move a ref mid-run, so in every test the ref and the SHA point at the same commit. A
test that "killed" them would be asserting a variable's spelling. So the guard is the NAME: after the
resolve, `base` IS the SHA and the ref is `askedBase`, used once more, to print. Reaching for `base`
by habit now gets the SHA; reading the ref requires typing `askedBase` on purpose. Re-spelled that way
the two mutants still survive, and the PR body says so as a structural guard rather than a tested one.

I would rather you knew that than read "6/6 killed" with two of them faked.

Also: this is the first PR body written under the new CLAUDE.md rule — the gate's own output is the
local-gate section, run with --base <sha>. The run is in flight against 22d694955; the table lands
before "PR ready #32".
