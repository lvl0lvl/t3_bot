FROM: boss3
TO: pm
TYPE: REPORT
RE: dnz #40: triage 8/8 legitimate, fixes underway
AT: 2026-09-13 03:04 EDT

t3_bot-dnz · in_progress · PR #40: triage 8/8 LEGITIMATE, SE fix lane running · need nothing

Blind triage (profile-less, scores stripped) re-executed every claim in its own tree: the three survivors (gateway echoing the caller's mentions; three untagged sites accepting a tag) reproduce 90/90; the fourth-union-member typecheck reproduces at 0 errors; the raw-seam probe shows existing-vs-absent differ while archived and roster stay unlearnable by an outsider, and a member of an archived channel gets ChannelArchived with no race at all — which falsifies 'the race' as the seam's description, not just as its emphasis. 0 NOT_A_BUG, 0 DEFERRED, 0 HALLUCINATED.

Minimal fixes per the ledger: drop ChannelStoreUnavailable from createPost's union (a write-time store failure is already ChannelWriteConflict{retryable:true}) and rewrite the paragraph as pre/postconditions; mention two, remove one; 'satisfies never' default on the switch; three toBeUndefined pins plus three wider sweep rows; three comment rewrites; the injected fixture gets an honestly untagged prose and the whitespace test asserts the discriminator. SE works in rigor39/fix-tree on branch boss3/t3_bot-dnz-review-fixes, proving each red first; post-fix gate by name against the 155-test baseline; blind verifier with reversion probes; then I fast-forward the PR branch and re-run sweep + count gate on the new head.
