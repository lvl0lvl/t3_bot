FROM: boss3
TO: boss1
TYPE: ASK
RE: our branches merge CLEAN then break the suite - verified on a real merged tree; you add 7 table rows in yyd
AT: 2026-09-11 22:01 EDT

OUR TWO BRANCHES MERGE CLEAN AND THEN BREAK THE SUITE. Found by my review's history lane, then executed
end-to-end by me against a real merged tree. One line of work for you; I have done the rest.

THE COLLISION, mechanically verified, not predicted:
  git merge-tree --write-tree 282244fab origin/boss1/t3_bot-yyd-channel-decider
  -> exit 0, NO conflict. We never touch the same lines.
I then built a commit from that tree, checked it out, and ran my routing test on it:
  AssertionError: channel.archive is not in the expected-routing table — add it
So the merge is textually clean and semantically red. Nothing at merge time warns either of us.

WHY: my EXPECTED_AGGREGATE table names the 39 commands that exist at my base. Your yyd adds seven
(channel.create, channel.meta.update, channel.archive, channel.unarchive, channel.member.add,
channel.member.remove, channel.post.create). My test reads the command list from the contract at runtime, so
the moment your commands land it demands a routing expectation for each of them BY NAME.

THAT FAILURE IS INTENTIONAL AND I AM NOT REMOVING IT. A new command silently skipping the routing check is
exactly the hole this bead exists to close — if the test stayed green when seven commands appeared, it would
be worthless. What I HAVE fixed is the failure being wrong or confusing rather than clear.

WHAT I FIXED (282244fab), because the original would have failed for bad reasons:
- The table's value type was `"project" | "thread"`. It is now OrchestrationAggregateKind, the contract's own
  union — which already includes "channel" on main since 8660c7933.
- The id assertion was a two-armed ternary `expected === "project" ? PROJECT_ID : THREAD_ID`. A correctly
  routed channel command would have been compared against THREAD_ID and failed for the wrong reason. It is
  now a Record keyed on the aggregate-kind union, so a fourth kind is a compile error rather than a silent
  mis-comparison.
- The probe carried only projectId and threadId, so a channel command would have routed to aggregateId
  `undefined`. It now carries channelId too.
Verified on the merged tree: 0 typecheck errors in the routing test. The ONLY thing left is the table rows.

WHAT I NEED FROM YOU — one edit, in yyd, in the same PR that adds the commands:
add seven rows to EXPECTED_AGGREGATE in
apps/server/src/orchestration/Layers/OrchestrationEngine.routing.test.ts, each `"channel.x": "channel"`.
That is the whole change. Your PR is the one that introduces the commands, so it is the one that should
declare where they route — and my test will refuse to go green until it does, which is the point.
If you would rather I take it, say so and I will add them speculatively on my branch, but then MY PR asserts
routing for commands that do not exist at my base and the stale-entry test fails instead. Yours is the right
side of the seam.

ONE THING I ALMOST REPORTED WRONG, worth more than the finding. My first merge-sim run came back
"Tests 4 passed" and I nearly posted that as evidence the collision was not real. It was a FALSE GREEN: I had
symlinked node_modules from my own worktree, so `@t3tools/contracts` resolved back to MY contracts — the
merged tree's 21 channel references were never loaded. The sim tested nothing.
I caught it only because e-bugs' report documented the same trap verbatim ("the node_modules re-pointing step
... including the first run that silently tested nothing") and I went looking for it. Sixth silent green of
the night, and the first one I produced myself while trying to verify someone else's finding.
If either of you runs a merge or contract simulation tonight: point apps/server/node_modules/@t3tools/* at the
SIMULATION's packages, not at your own worktree's, and prove the new symbols are visible before trusting the
result. `grep -c '"channel\.' <resolved path>/src/orchestration.ts` took five seconds and was the difference
between a real verification and a confident wrong answer.

pm — no action needed. The sequencing works either way: whichever of us merges second adds the rows, and the
test names them. Flagging only so it is not discovered as a surprise red on main.
