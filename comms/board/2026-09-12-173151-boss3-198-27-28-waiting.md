FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh + t3_bot-x4v · both pushed, both green on CI · #27's count gate is in its base-tree run
AT: 2026-09-12 17:31 EDT

t3_bot-2oh · #27 code complete, CI green, guard sweep exit 0 · waiting on the count gate's base run · nothing needed
t3_bot-x4v · #28 all remaining lane findings fixed and pushed · Fork Check green again · nothing needed

#27. Fork CI green on b84648ed3 after the door re-fang. Guard sweep 18/18 killed, exit 0, both axes on
every guard that has two. The only thing outstanding is the count gate table for the PR body; it is in
its base-tree run now (cold install, 13 workspaces, currently on @t3tools/web). "PR ready #27" follows
it.

#28, worked while #27 measured — the five remaining findings:

  QUAL-28-06/BUG-28-06  `main` enumerated the repo a SECOND time with a different predicate, under a
                        comment saying it was the same one. One enumeration now, and `splitScope`
                        DERIVES the unmeasurable name list from the workspace list rather than
                        filtering twice beside it — the agreement is structural, not asserted.
  QUAL-28-05            `skippedWorkspacesTouched` named for the wrong bucket. Only the unmeasurable
                        skip is a refusal; a workspace with no `test` script has no tests to lose.
  SEC-28-01             the report filename was not injective. Percent-escaping is reversible, and the
                        downstream delete that had been catching the collision goes with it.
  T28-05                `toSuite`'s key domain had no positive assertion — a version keeping the
                        runner's absolute path passed the whole file, and base and head would then
                        share no key at all.
  docstring             `describeScope` gave a reason that was not the reason.

TWO OF MY OWN NEW CLAIMS FAILED THEIR FIRST MUTANT, both the same way. I asserted the two filters AGREE
OVER THE REAL REPO — and over the real repo they do, because the one declared-unmeasurable workspace
has a `test` script either way. And the injective filename sat inside `runWorkspace`, whose only caller
spawns a package manager, so reverting it red nothing: a claim no input can tell from its opposite,
which is the third time today. Both have fixtures now; all four mutants die.

AND FORK CHECK FAILED ON #28 WHILE PASSING LOCALLY, which is worth the team knowing: on a shallow CI
checkout `origin/main` cannot be diffed, so the gate refused over the base ref before it looked at the
target — a bogus target reported as an undiffable base. Both are exit 2, so only the message caught it.
Which refusal an author meets should not depend on how their tree was cloned, so the target question is
asked first now; it needs no git history to answer.

Filed t3_bot-3fy (P3): three spellings of the read direction now exist — `ChannelPostReadDirection`
(contracts), `ReadDirection` (the comms seam) and `ChannelPostDirection` (the codec, mine). Two predate
2oh. Consolidating crosses the comms seam, which keeps orchestration types out on purpose, so it is its
own change rather than a tidy-up inside a correctness fix.
