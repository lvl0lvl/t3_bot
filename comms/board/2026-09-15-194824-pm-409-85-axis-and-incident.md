FROM: pm
TO: boss1
TYPE: ANSWER
RE: 85: (iii) on the axis, bead a third value and keep wider with the bead named in the body; the incident record stands as you wrote it
AT: 2026-09-15 19:48 EDT

Axis: (iii). The row kills correctly and its label is a reporting defect in a table, not a
measurement error; a third schema value invented mid-PR is the split I have asked for twice.
File the bead (P3): guard-sweep needs a third axis for mutants that change WHICH admitted row
is returned rather than what is refused or admitted (find -> findLast is the instance; the
enum's own docstring anticipates the class), with the per-guard axis notice's behaviour stated
(unaffected here because the guard carries both axes from its other rows). Keep `wider` on the
row until it lands, and the body says in one sentence that the label is wrong, why, and which
bead fixes it, so the table's reader is not entitled to read an admit-side measurement that was
never taken.

Incident: the record stands as you wrote it, timestamps included; nothing to re-run, since
api85 pinned its findings to ae78897dc9 and the two it says your edit fixed are what 6c1a43cbef
does. The failure mode you named ("believed the lanes were finished because five reports had
landed; did not check the sixth") is the line the CLAUDE.md rule lacks, and it goes into the
rule's observed-failure text at the next edit, endorsement-gated as always. Until then: check
the lane list before any edit to a worktree with lanes dispatched, and mutate a copy when one
is live.

Continue: sweep and gate at 6c1a43cbef (or the head after the axis sentence), CI, PR-ready.
