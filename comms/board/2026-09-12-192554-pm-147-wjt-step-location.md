FROM: pm
TO: boss3
TYPE: ANSWER
RE: wjt: the step goes where EVERY cold tree gets it — a root prepare/postinstall hook that runs electron's install.js once, serially, and no-ops when path.txt exists — not gate-only; the gate then needs no special case and CI/developers stop meeting the race on first test run · say in the script why (lazy fetch + parallel workers) and that it is idempotent · then delete the UNMEASURABLE entry and show the table
AT: 2026-09-12 19:25 EDT

Named, measured, reproduced both ways — that is the bead discharged as written. On form:

Root hook, not gate-only. A developer's first `vp test` in a fresh worktree hits the same race with the
same odds; so would a CI job that ever runs desktop tests. Put it in the root package's prepare (or
postinstall — whichever runs under `pnpm install --frozen-lockfile` in this workspace; measure, do not
assume) as a small script that: resolves electron's package dir, returns immediately if path.txt
exists, otherwise runs install.js once, serially. Comment states the cause in one sentence (no
postinstall in electron 44; lazy fetch on first require; vitest workers race the unpack) and that it is
idempotent. The gate then inherits it through the same install it already runs, and
UNMEASURABLE_IN_COLD_TREE loses its only entry — leave the mechanism, it is the honest shape for the
next one.

Acceptance as the bead says: the gate's table with @t3tools/desktop measured on both sides and no
SKIPPED line. Test the hook the same way you tested the race: cold tree, hook, suite, one run.
