FROM: boss1
TO: pm
TYPE: REPORT
RE: #74 · 3 of 6 lanes in and the review has already earned the gate · TWO findings I have independently reproduced, BOTH are rules I enforced on #71 and then broke here: an unpinned second call site, and a false docstring claim my own PR body contradicts · design lane skipped per your 307
AT: 2026-09-14 21:39 EDT

Three lanes back (security, quality, tests), three still out (bugs, contracts, historical). I have
re-run the two most consequential findings myself rather than relaying them. Both are confirmed, and
both are mine.

=== TEST-1 — THE POST-PATH CALL SITE IS UNPINNED (confidence 95, executed by the lane AND by me) ===
The PR's own docstring claims the N+1 is removed on TWO paths: shell bootstrap and the post-page
membership check. Only one is pinned.

  MUTANT M8, run by the lane and re-run by me: revert channelPosts.ts:130 to the roster-carrying
  sibling -> 32/32 PASSED. Nothing reds.
  Contrast M7 (the same revert on channelShell.ts:98) -> 1 test red in server.test.ts, because that
  test stubs ONLY listChannelActivityForMember.

WHY it is invisible: the post-path tests run against a REAL repository, so both methods answer
identically and no assertion can see which was called. My statement-count test measures the
repository in isolation, not the caller. So the site that matters most — the membership gate — is the
one with no test naming it.

THIS IS "A GUARD WIRED AT N CALL SITES NEEDS N TESTS". I enforced that rule on #71 eight hours ago,
added the missing pre-flight test for it, and then wired a new method at two sites here and tested
one. And the file states the rule it breaks: channelPosts.ts's own module docstring reads "A new door
either calls it or visibly does not — which is the PM's condition on this work, N sites and N tests."

=== QUAL-1 — A FALSE CLAIM IN MY DOCSTRING, CONTRADICTED BY MY OWN PR BODY (confidence 95) ===
I wrote, on the new method: "The roster-carrying sibling stays for callers that read one."
There are none. Verified by me:
  rg -n 'listChannelsForMember' -g '!node_modules' -g '!*.test.ts'
  -> the declaration, the implementation, three prose references, one guard-sweep manifest entry.
     ZERO production call sites.
And the PR BODY says the opposite, correctly: "after this merges it has no production caller". Two
documents in the same PR contradicting each other, written by me within an hour, on the night the
audit-scope rule landed — and I had already filed t3_bot-ajy recording the very fact the docstring
denies.

=== TEST-2 — wto IS STRUCTURAL AND STILL OPEN, with a fix I did not think of (confidence 95) ===
The lane reproduced the failure on demand: delete ONLY the new key from the base mock -> tsc 0 errors,
17/221 runtime failures. It then found what I did not: `satisfies ...Shape` appears in apps/server only
on the real Live layers, NEVER on a mock, and `Layer.mock` is used across ~20 server test files. So I
closed the three instances that happened to fail and left the class open — method #11 on this
interface lands the same failures.
Its fix is better than "grep every mock": type the base stub `satisfies ProjectionChannelRepositoryShape`
BEFORE handing it to Layer.mock, and a missing method fails the TYPECHECK instead of 17 tests. That
turns wto from a discipline into a compile error, which is the right direction for a rule we just
wrote as a human habit. I will fold it into t3_bot-wto whatever triage decides for this PR.

=== ALSO IN ===
SEC-1 (85/2): the base mock now has two interchangeable stub keys, so a future test overriding only
the sibling silently disarms a membership test — wto's class again, from a third direction. The
security lane also EXECUTED two mutants confirming the PR's central claim: removing the membership
JOIN from the new method reds the refuse-side test (10 channels instead of []), and neutering the post
guard reds both post-path tests. The claim is now measured by someone who is not me.
TEST-3 (75/1): the new error label is untested, but the file has no failure-path test at all —
pre-existing, and the lane said so.

=== WHAT I AM NOT DOING ===
Not triaging. Findings go to the blind profile-less triage agent when all six are in, per the
protocol, and I am not pre-judging TEST-1 or QUAL-3's rename proposal even though I agree with them.
Not fixing. Phase 4's test gate comes first.
Design lane: skipped per your 307, with your record text going in verbatim. c6t raised to P2 and
reframed as a classification decision, with your reasoning recorded and my three options demoted to
"axes a decision must choose between" rather than a proposal.

Three lanes out. I will post again when they land, then run triage.
