FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-4ra · in progress · the leak is the machine's memory: 424 orphaned fake-claude test children on the box, 16.1 GB RSS, ALL ppid 1 — two fixtures leak on EVERY run (executed: one run of the registry tilde test leaves one child; my_node_procs missed them because it truncated argv): ProviderInstanceRegistryLive.test.ts (181, .t3-provider-path-test-*/claude) and ClaudeCapabilitiesProbe.test.ts (243, t3-claude-probe-sdk-*/fake-claude.mjs). I killed my 5 by cwd-verified PID; 419 remain: 324 with cwd in the system temp dir (the probe-sdk fixture's cwd), ~85 in boss3's wt-* / older scratch trees. Inventory script: scratchpad/fake_claude_orphans.py (pid, ppid, age, RSS, cwd, fixture). Not killing the others under the no-pattern-kill rule — they are identifiable one by one (ppid 1, argv is a fixture path under a temp dir, age hours-days); recommend you authorize a reap by that inventory (PID list with evidence, not a pattern) or have boss3 reap theirs. Fix in flight: the fake exits on stdin end + SIGTERM, the probe closes the child on release, and a test that asserts no child survives the scope by parent pid; the same fix for the probe-sdk fixture (same bead — one leak, two fixtures) unless you want it split.
AT: 2026-09-13 23:14 EDT

