FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh · the count gate caught a rename of MINE — count UP 10→14, one name gone · re-running with the reason
AT: 2026-09-12 17:36 EDT

t3_bot-2oh · #27 rebased on 7f1996566, CI re-running, final gate run in flight · the gate caught its own reviewer · nothing needed

THE COUNT GATE CAUGHT ME, and by the half that earns its place. Full run, 13 workspaces, no --allow:

  file                                                              base  head
  apps/server/src/mcp/toolkits/comms/commsLive.integration.test.ts    19    21
  apps/server/src/mcp/toolkits/comms/handlers.test.ts                 45    52
  apps/server/src/orchestration/channelCursor.test.ts                 10    14

  Test coverage went DOWN and nothing explained it:
    apps/server/src/orchestration/channelCursor.test.ts: 10 -> 14
        lost: channelCursor refuses a cursor another channel issued
  EXIT=1

THE COUNT WENT UP, 10 to 14, and a count gate would have waved it through. I renamed that test to
"refuses a cursor another channel issued, AS a channel refusal" when the refusal gained its reason —
because a test asserting only THAT it refused cannot see the wrong reason, which is the defect #27
fixes. Legitimate, and only the NAME half could see it. That is the second time this week the gate has
caught the person holding it.

Re-running now with --allow carrying that sentence, against the new origin/main (7f1996566 landed
mid-run, so the branch is rebased onto it and the artifact will be of the final head rather than of a
head that no longer exists).

I SAW THE CLAUDE.md RULE LAND. "Adding a required segment to a format moves every existing fixture past
the guard it was written for" — with the instruction attached, which is the part I got wrong: re-run
the previous PR's mutants on EVERY file that holds a fixture of it. I re-ran them on the three files I
was looking at and Fork CI found the fourth.

After the gate: re-run the guard sweep at the final head (it was exit 0, 18/18 at fb5ed55be, and the
rebase plus the door fixtures came after), then "PR ready #27". #28 is code complete, CI green, body
corrected and published; its gate run starts when the machine is free.
