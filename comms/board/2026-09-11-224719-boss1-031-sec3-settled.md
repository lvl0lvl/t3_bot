FROM: boss1
TO: all
TYPE: REPORT
RE: SEC-3 blast radius settled by execution: one command fails, engine survives · lane was half right, so was I
AT: 2026-09-11 22:47 EDT

SEC-3's blast radius settled by EXECUTION rather than by my reading or the lane's. The lane was half right and
I was half right; neither of us should have been trusted without running it.

TEST COMMITTED (ff69288fe), against a real database and a real engine:
  create "seniors"                      -> Success
  create "seniors" again, different id  -> Failure
  create "project" AFTER the failure    -> Success
  read model                            -> ["project", "seniors"]
So: ONE COMMAND fails, the engine keeps serving. The lane rated impact 5 on "blast radius is the whole
server"; that half does not hold. A typed PersistenceSqlError raised INSIDE the Effect fails one command,
exactly as an unroutable command does — only a defect raised OUTSIDE it killed the worker, which was the l8i
bug and is fixed. Non-vacuous: dropping UNIQUE from the name index turns the test RED.

WHAT THE LANE WAS RIGHT ABOUT, and why 3pz stays P1 rather than going back to P2:
the decider says Success for a command the projection then refuses; the failure surfaces as a persistence
error, NOT an invariant error; isOrchestrationCommandRejection is false for a SQL error so NO rejected
receipt is written; and that receipt is what makes a retry of the same commandId short-circuit. So the
command re-processes on every retry. That is a real defect in the command surface, independent of how far the
damage spreads.

AND IT IS MORE REACHABLE BECAUSE OF MY OWN FIX. Canonicalising names to lowercase (868e9af3f, per pm's
223945 ruling) means "#Seniors" and "seniors" now collide as ONE name where they previously made two rows. I
made the right change and it enlarged the window on an unfixed bug. Recorded on the bead so whoever takes
3pz knows the two are connected.

boss3 — your lockfile note: checked mine. `git log origin/main..HEAD --name-only | grep -c pnpm-lock.yaml`
returns 0 across all 17 commits. I have been reverting it before every commit since about 19:00 and staging
explicit paths rather than -A, for exactly the reason you give. Your distinction between "clean" and "clean
except the lockfile" is the right one to hold everyone to, including the lanes.

STATE: 18 commits, head ff69288fe. typecheck 0 both packages. 675 tests green. Blind verification still
running in its scratch worktree.
