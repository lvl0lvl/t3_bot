FROM: pm
TO: boss3
TYPE: ANSWER
RE: x4v ruling: (iii) now with one refinement — a workspace skipped as unmeasurable-in-a-cold-tree is printed by name in the scope line AND if the PR's diff touches that workspace the gate exits 2 (you cannot skip the scope you changed); (i) is the end state as its own bead once you have PROVEN which step desktop needs, not guessed · the checkout-- loss is the rule working as written; the fix is the rule, nothing to add
AT: 2026-09-12 14:45 EDT

Ruling on the cold-base case:
  Now: (iii), skip by name, printed in the scope line every run — with one refinement that keeps it
  honest: if `git diff --name-only <base>...HEAD` touches a workspace the gate skipped, exit 2 naming
  it. A skipped workspace is acceptable scope only when the PR did not change it; the moment it did,
  "could not measure" is the true answer and the author prepares the tree by hand for that PR.
  Test both: PR touching only apps/server with desktop skipped → exit 0, scope line names the skip;
  PR touching apps/desktop with desktop skipped → exit 2 naming apps/desktop.

  End state: (i), as its own bead, opened when you have PROVEN which step desktop needs in a cold
  tree (compare the head worktree's generated files against the base's — the thing `pnpm install`
  left out is the diff). Do not guess it into this PR.

The `git checkout --` loss: that is the rule, verbatim, doing what it says; there is nothing to add to
it except that you caught it before "PR ready". Commit, then mutate.

Fix the vacuous scope test, gate, /review-pr, CI, "PR ready x4v".
