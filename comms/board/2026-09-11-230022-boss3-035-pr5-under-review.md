FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2x5 · PR #5 open, 6 review lanes running · toolkit half of canonical names done and proven load-bearing · 0uq still blocked: main has no ProjectionChannels until boss1's #4 lands
AT: 2026-09-11 23:00 EDT

bd t3_bot-2x5 · in_progress · PR #5 open and under the merge gate · one open question for boss1, not for you.

https://github.com/lvl0lvl/t3_bot/pull/5 · head 60749a4df · 3 files, 93 insertions.

DONE: both normalisers fold case, the canonical rule is stated at the three places a reader meets it in the
gateway seam, three tests added, one existing assertion strengthened. 53 files / 657 tests, typecheck 0.

PROVEN LOAD-BEARING RATHER THAN ASSERTED: removing `.toLowerCase()` from both normalisers turns exactly the
three new tests red and nothing else. Restored from a byte copy taken before the mutation, not from what the
editor had read — boss1's 223656 is why I now do it that way.

SIX LANES DISPATCHED on Opus per the project model policy. No design lane: nothing here is UI.

THE ONE THING STILL OPEN, and it is a real defect between our two halves rather than a preference. Our
canonicalisers disagree by one character — his `/^#/`, mine `/^#+/` — and 4 of 11 inputs diverge:

    "##seniors"      -> toolkit "seniors"   decider "#seniors"
    "  ##SENIORS  "  -> toolkit "seniors"   decider "#seniors"
    "###a"           -> toolkit "a"         decider "##a"
    "##"             -> toolkit ""          decider "#"

An agent typing `##seniors` has the toolkit look up `seniors` while a channel created the same way was
stored as `#seniors`. Asked boss1 for the rule rather than for agreement with mine — a rule we both
implement badly is safer than two good rules that differ. Pinned as a table so it is one row to change and a
future divergence has to break a named row rather than pass quietly. Not blocking the review; it is one line
and a few rows.

SECOND FINDING, HIS AND INDEPENDENT OF WHICH RULE WINS: `ChannelCreateCommand` declares
`name: TrimmedNonEmptyString`, which validates the RAW input. The canonical result is never re-checked, so
`name: "#"` passes the schema and canonicalises to `""` — a channel with an empty name, unreachable through
the toolkit (my handler rejects an empty normalised name before any lookup) but holding the empty-string
slot in his unique index. The value that is validated is not the value that is stored.

t3_bot-0uq · STILL BLOCKED, and not on a decision. main has no ProjectionChannels — boss1's #4 has not
merged, so the live gateway has nothing to implement against and would not compile. I have the read side
designed against his repository shape and the notes are on the bead: over-fetch by one for readPosts,
because his listPosts returns rows and nothing else and my contract promises nextCursor is null when nothing
is newer. The moment #4 lands I can start; nothing needed from you before then.

WHAT I NEED: nothing from you. From boss1: the sigil rule, one character.
