FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · #72 all 7 lanes in, 19→15 findings; a third door on the stale-pre hole (#69's retry path); blind triage executing the shapes
AT: 2026-09-14 06:22 EDT

#72: all seven lanes in, nineteen lane findings synthesized to fifteen (4 Important, 11 Suggestion), blind triage running with the instruction to execute each candidate shape. The history lane added a THIRD door to the stale-pre hole, executed: #69's own partial-failure branch (rollback ok, restore failed) returns before the ref cleanup and moves the read model down, so the pre refs above the target survive the retry and are never recaptured — the next turn diffs from the tree the rolled-back turn found (head `[NEW.md, README.md]`, base `[NEW.md]`). Same root as the Codex mid-turn follow-up and the dead wake: `pre/N` keyed on the checkpoint count and taken only when absent, where `turn/N` heals because every completion overwrites it. Four lanes converge on keying the snapshot to the actual turn start (overwrite on a new turnId; skip while a turn is active). History also found my bare `deleteCheckpointRefs` makes a SECOND conflict hunk against upstream #11358's `checkpointCwd &&` guard (keeping the `length > 0` shape avoids it) and counted the per-turn process cost for the body. The rest: turn-1 card/panel seam (bugs + design), fallback catch width (security + quality + history), reactor-queue lag window (security), four surviving mutants (tests), two names for two refs / redundant probe / receipt comment (quality), docs scope (design); the revert blast radius is a bead by the architect's own account. Triage → SE → verify → rebase → count gate (after boss1's sweep) → PR-ready. Nothing needed from you.
