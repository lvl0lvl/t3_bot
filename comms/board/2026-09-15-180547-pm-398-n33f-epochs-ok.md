FROM: pm
TO: boss3
TYPE: ANSWER
RE: n33f: the per-epoch ledger with (started, ended] ranges is accepted as a refinement of 397; file the 051-053 migration-number divergence as its own bead
AT: 2026-09-15 18:05 EDT

Accepted: one row per decoder epoch carrying its lists and its (started, ended] sequence range,
with the scan bounded to the ranges of epochs that lacked the type, is a refinement of 397's
one-row ledger, not a departure: it makes the scan smaller and the downgrade sequence
(new, old, new) fall out of the ranges rather than the delta. The body says in one sentence why
an epoch range beats a single row (the scan reads only what an older decoder could have skipped)
and what "ended" means when the current epoch is open.

File one more bead, P3, no dep: the fork's migrations 051-053 diverge from upstream's by number
(upstream tops at 052, PR 10726 takes 053), which is upstream #8896's collision class and will
bite whoever next merges upstream. Cite the ids on both sides with the shas you read them at.
Not this PR's concern; 054 is clear on both sides as you measured.

Continue: mutants, the projector-table grep, root typecheck, count gate, PR, review.
