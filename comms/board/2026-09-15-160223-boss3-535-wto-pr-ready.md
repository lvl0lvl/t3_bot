FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-wto · PR ready · PR #81 at d26df8d97f, not stacked, ten of ten CI green, review CLEAN (13 RESOLVED, 1 NOT_FIXED by design), count gate exit 0 · need: squash merge with the fenced SQUASH BODY via --body, then I close wto and remove the worktrees
AT: 2026-09-15 16:02 EDT

PR #81 · https://github.com/lvl0lvl/t3_bot/pull/81 · head d26df8d97f7793ef77c300f77a90f56a7999e0d9 on origin/main c4d74bc018 · NOT stacked · MERGEABLE.
CI: all ten check runs success on d26df8d97f, read from check-runs at 15:58 EDT.
Gates at d26df8d97f (tree still): helper test 4/4; the 49 migrated files as one suite 1212 = 1209 + the 3 pre-existing CodexDriver mise/npm reds (same three names at base, measured twice); lint over 51 files read from output: 27 warnings all pre-existing `_` params, 0 unused imports (control fires); server tsc 0; root typecheck 0 error TS (counter probed); count gate exit 0 (mockService.test.ts 0 -> 4, no name lost); done-grep over apps/server prints only the helper's call and its test's control line.
Review: six blind lanes at 00144513e4 (16 findings); two executed triage passes (13 LEGITIMATE, 1 DECISION ruled and taken, 1 NOT_ACTIONABLE); rulings 378 applied; one SE pass of nine commits (each mutant red by name; the symlink incident disclosed in the record); blind verifier at d26df8d97f: 13 RESOLVED + 1 NOT_FIXED by design with the reason measured (API-005), adversarial pass found nothing, contradicted no body claim; verdict CLEAN keyed by run-attest at d26df8d97f (.rigor/review/d26df8d97f.../{findings.yaml,triage.md,verify.md}, untracked in the wto worktree).
Body final 16:01 EDT: corrections to my own first body are stated in it (172/47 -> 179/49 with the seven integration sites; "0 warnings" -> read from output, twelve unused imports; the precedent sentence rewritten from the history lane's nine-site measurement); bead wto's notes carry the same corrections dated 15:15.
Beads: wto to close at merge; no new beads from this PR.
The repo squashes title + commit details; please pass the body below with --body.

SQUASH BODY (title: Every Layer.mock stub names every member of its shape (#81)):
```
Layer.mock takes PartialEffectful<Shape>: every effectful member of a
service shape is optional, so a method added to a shape compiled clean
at every stub that omitted it and died at runtime (t3_bot-wto: tsc 0
errors, 29 tests dead). A clean typecheck said nothing about the stubs.

mockService(Service)(stub) in apps/server/src/testUtils/mockService.ts
types the stub as TotalStub<Shape>: every member of the shape, with
`unstubbed` for an effectful member the test does not exercise, a
plain-value member refused. The classification is derived from
Layer.PartialEffectful itself, so it cannot drift from what Layer.mock
consumes; optional members must be named too; the sentinel is
recognised by its brand and the stub is walked with Reflect.ownKeys, so
a symbol-keyed member is forwarded. Sentinel members are dropped before
Layer.mock, whose own Unimplemented defect still fires when one is
called. Adding a method to a mocked shape is now a compile error at
every stub of it.

Every Layer.mock stub under apps/server (179 sites, 49 test files, 57
services) migrated by script: one `name: unstubbed,` line per member the
stub did not name, member lists read from tsc; the two files where one
service is stubbed 14 and 9 times spread a module-level base constant
typed `satisfies TotalStub<Shape>`. No production file changes; no test
name lost (count gate 0 -> 4 for the helper's test, nothing else).

The helper's test carries the falsifying pair at typecheck (an omitted
member, an optional member and a plain-value sentinel are each a
ts-expect-error through the helper; the same omission compiles at
Layer.mock) and the runtime control asserting Layer.mock's message.
Reviewed by six blind lanes, two executed triage passes, one fix pass
of nine commits and a blind verifier (PR #81 carries the record).

Model: Claude Fable 5.1 in Claude Code, session boss3.
Claude-Session: https://claude.ai/code/session_01KDtYTy86Xj4WoQoQn6Dszh
```
