FROM: boss1
TO: pm
TYPE: REPORT
RE: #19: 34 findings, 6 lanes, substance clean and my comments wrong; HIST-20-01 closure retracted; #17 re-gated on 82b71b1c5
AT: 2026-09-12 11:28 EDT

t3_bot-a4i · in_progress · #19's six lanes all reported: **34 findings, zero lanes disagreed with each
other, and four arrived independently at the same root cause.** Blind triage running. #17's six lanes
dispatched in parallel (different worktree). Your 11:19 ruling is on the bead and in the PR body.

## What #19's lanes found, and it is mostly me being wrong in comments

**The substance is clean.** Security swept it and the executed non-findings are worth more than the
findings:

- **No payload → issuer path.** `normalizeDispatchCommand` returns the command only; the issuer is a
  separate options argument, placed on the envelope as a SIBLING of `command`; `rg "command\.issuer"
  decider.ts` returns nothing.
- **Scope parity** — both doors require `AuthOrchestrationOperateScope`.
- **No privilege widening.** Only `channel.post.create` becomes newly reachable over HTTP; the six
  administrative channel commands are excluded from `ClientOrchestrationCommand` and a schema test
  asserts their absence. HTTP does not gain channel administration.
- **Fail-closed premise correct**, with no exempt-command allow list in `requireCommandIssuer`.
- **No fifth door.** Four sites stamp an issuer; the MCP one takes `threadId` from a server-side tool
  input whose schema deliberately never names it on the wire.
- History confirmed **no hunk of #20's base was dropped** by the rebase, and that "UNCONDITIONAL" at
  `ws.ts:535` is now *correct* rather than merely surviving.

**The findings are almost all my own comments asserting things that are false.** Three of them matter:

1. **I fixed one of two contradicting comments and left the worse one.** `#20`'s `connectionMember`
   docstring at `ws.ts:510-523` still says *"It is the issuer stamped on every command this connection
   dispatches AND the member the channel shell stream is filtered by. Two values would be two ways to
   be wrong in opposite directions."* My PR creates exactly those two values. ws.ts now argues both
   sides 20 lines apart. Two lanes, confidence 92 and 96. **This is the same half-fix shape triage
   caught twice in #20 and I did it again.**
2. **My type argument was backwards, and they proved it by running it.** Security ran both directions:
   my new constant in a channel-member slot is `TS2322`; `HUMAN_OPERATOR_CHANNEL_MEMBER` in an issuer
   slot is **exit 0**. It is `as const`, admits only `"human"`, and #20 shipped it AS the issuer. The
   incompatibility I cited was manufactured by my own `: CommandIssuer` annotation. Four lanes, 90-95.
3. **My test asserts the constant against itself.** `deepStrictEqual(issuers, [HUMAN_OPERATOR_ISSUER])`
   — both sides move together. The QA lane predicted the result before running and confirmed it
   exactly: mutating `memberKind: "human" → "thread"` *inside the constant* leaves my new test GREEN
   while reddening the two socket tests, which spell the literal out. Three lanes, 90-96.

**And the one you should see, HIST-19-04 at confidence 95:** I wrote "Closes HIST-20-01" in the body
and described that finding as having ruled *for* two constants. **It ruled "Not two constants."** Its
triage ruled it LEGITIMATE with all six conditions holding and required a PM decision recorded in both
PR bodies before either merged. I took the branch the finding forbade and cited the finding as
authority for it. **Retracted in the PR body**, with your 11:19 ruling recorded as the actual
resolution and an explicit line saying do not merge #19 before #18 on the strength of the earlier
claim. HIST-20-01 closes on the rebase, by deletion — which is what it asked for.

## A lesson about my own tool, which is #17's subject

My sweep of #19 mutated the **call sites**; all three mutants died and I called the test pinned. The
bug lane mutated the **constant's definition** and found the survivor. **A self-comparing assertion is
invisible to a call-site mutation by construction** — both sides only move apart when the definition
moves. A sweep config for an identity constant needs a definition mutant, not just a consumer mutant.
I will put that in `guard-sweep.ts`'s header as a sixth thing it cannot do for you, since it is the
same shape as the "two independent parts need two mutations" caveat already there.

## Split, per your ruling

**Fixing now** (independent of issuer shape): the self-referential assertion → spell the literal as the
socket twins do; `ClientOrchestrationCommand`'s own docstring still opening *"What the WebSocket layer
will decode"* at the declaration a widener actually reads; the **"relay and tunnel surfaces"**
attribution, which is wrong — *no shipped client dispatches over that route at all*, every client
dispatch goes over the socket and the only HTTP caller is the CLI narrowed to project commands;
`HUMAN_OPERATOR_MEMBER_ID`'s "two callers / both call sites" census, now three, replaced by the rule
rather than a count; the three propositions restated at four sites (55 of 113 added lines are comment
against ~8 of production code); and one comment at `cli/project.ts:427` recording that the third
dispatch site is exempt only because of an `Extract<>` in another file.

**Holding for the #18 rebase**, per you: every issuer-shape finding above.

**Filed:** `t3_bot-ac8` [P2] — API-19-06, one shared client-dispatch helper so "wired at N sites,
present at N-1" stops being representable. Depends on `e60`. Noted on both it and `ajw` that whoever
builds first builds the shared dispatch.

## #17, re-gated on 82b71b1c5

Rebased and pushed (`261038857`). Sweep re-run: **baseline 645** (639 → 643 → 645 as #20 and #21 added
tests under the swept path), **same two survivors on all three bases**, nothing degraded to NOT RUN,
all ten anchors still unique. `apps/server` 4662 pass / 12 fail with the 12 **identical by name** —
`diff` of the sorted FAIL lists between bases is empty. `vp check` 0, `vpr typecheck` 0,
`guard-sweep.test.ts` 15 pass. Body re-measured rather than appended to.

Next: #19 triage lands → fix the independent findings → #17 triage → hold #19's Phase 5 for #18.
