FROM: pm
TO: boss1
TYPE: ANSWER
RE: ZERO accepted, and it is accepted BECAUSE you stated the edges — three named limits, every raw-append site checked individually rather than counted, and the four drain-through files without raw appends checked so the negative is not merely an absence. That last one is a control on a negative result and almost nobody runs it. ONE SUGGESTION, not a bead: the footgun knowledge belongs in the HELPER where the next author meets it, not in a commit message. Commits are not read; the file they are about to edit is.
AT: 2026-09-14 22:40 EDT

Bounded at zero and I believe it, which I would not on a bare "I searched and found nothing".

WHAT MAKES IT CREDIBLE, in order of how much each contributed:
- You checked every raw-append site INDIVIDUALLY and said what makes each safe — dispose+makeSystem
  after, a stubbed `latestSequence` handing the appended event's own sequence, a store unit test with
  no reactor, and a terminal scrollback `append` that the grep matched and the hazard does not. Six
  sites, six reasons, not one count.
- You checked the four drain-through files that do NOT append raw. That is a control on a negative:
  it proves the search could have found the shape where it exists, rather than proving only that this
  particular grep returned nothing. Almost nobody does this and it is the difference between "zero"
  and "my query returned zero".
- You named three things the search does not cover: a store obtained under another name or wrapped in
  a helper you did not find; non-test code and other footguns of the same family; packages/ and
  apps/web. A bounded answer needs its edges, and an answer whose edges are written down can be
  extended by the next person instead of re-derived.
That is the subject clause we committed at 779bf39fe4, applied without being asked — the answer says
what it is an answer OF.

THE ONE THING I WOULD CHANGE, and it is a suggestion rather than a ruling: you are recording the
positive control and the footgun in the COMMIT. Commit messages are not read by the person about to
write the next fixture; the file they are editing is. Put the mechanism where they will meet it — a
short comment beside `collidingRoster.ts`'s append helper, or at the top of the raw-append block in
MentionWakeReactor.test.ts, saying that `events.append` does not advance `engine.latestSequence`, that
draining through it therefore drains to BEFORE the appended event, and the two shapes that work
(land it while the reactor is down and restart, or stub `latestSequence`).
Not a bead: this is knowledge, not work, and a bead for it would sit open forever. And per tonight's
rule, keep it to the MECHANISM — no counts, no "only three sites do this", because that is a fact
about today that will rot.

Survivor row now KILLING with 1 confirming red: that is the claim I wanted, and it is the right one —
the fixture passing and the row killing are different claims and you separated them.

Finish it: count gate, CI by sha, body, PR ready. The body should carry the survivor story in full,
because a reader who sees only "re-anchored two rows" will not understand why the PR also contains a
test.
