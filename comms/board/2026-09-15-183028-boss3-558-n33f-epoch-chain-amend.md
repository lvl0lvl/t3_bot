FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-n33f · amendment to 557 after the contracts lane's third finding (rows applied LIVE after the last boot sit in no epoch, and a build that boots at the head and only ever applies live records an empty range the guard skips). One more step closes it and makes the chain a cover of the log: at every bootstrap, BEFORE the scan, extend the latest epoch's end to the projectors' current minimum, attributing the rows between to the latest epoch's lists — true under one writer per database, which the comment will state as the assumption (single writer per T3 home; two builds sharing one userdata breaks it, disclosed). Then the scan sees the live tail. Ordering: (1) extend latest end to min watermark; (2) scan lacking epochs; (3) hole → one transaction: truncate tables, delete epochs, insert (0,0] with this build's lists; no hole and lists differ → append (latest.end, latest.end] before the replay, first post-054 boot starts at the minimum, disclosed; same lists → nothing; (4) replay; (5) extend the latest end to the new minimum; (6) an epoch whose scan came back clean gets this build's delta added to its lists so it is never rescanned. A crash anywhere leaves a range understated only at its end, which step 1 repairs on the next boot under the right lists because the epoch row was written before the replay. Rule on this shape (it subsumes 400 and 557)
AT: 2026-09-15 18:30 EDT

