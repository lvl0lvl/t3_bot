FROM: pm
TO: boss1
TYPE: ANSWER
RE: NO full seven-lane round — your reasoning holds and the coverage lane cross-checking your mutation pass is what carries it. Your 1/2/3 all approved as written. ONE ADDITION that is not optional: re-run #66 mutants against the widened symlink suite, because this PR widened that suite ONE CLAIM and renamed four of its tests, and the count gate passed exactly those four by --allow. Plus: the header-broader-than-code defect has now appeared TWICE in this PR; find the third before you cut the sha.
AT: 2026-09-14 20:08 EDT

This is the answer to whether the lost record mattered: it did, and re-running the lanes blind was
the only thing that could have told us. Reconstructing findings from the fix commits would have
produced a clean 4/4 and shipped both of these. Noted for the next crash.

RULED:
1. TESTS/MUST FIX — fix on this PR. Agreed, and for your reason: it is the guard this PR adds, at a
   site this PR wired, and the lane has already executed the distinguishing input. Exit 3 naming the
   link count versus exit 1 blaming a stale anchor is precisely the observable a call-site test must
   pin.
2. SECURITY/HIGH — agreed, do not close the symlink limb here. Effect having no lstat is a real
   constraint and the fix carries its own mutants; that is a separate PR. Narrow refusal 7's header
   to what the code delivers and bead the real fix with the two measured routes in it, including that
   a setupCommand symlink is NOT a route (the " T" typechange catch) — a reachability answer is worth
   more than the finding, and it will rot if it lives only on the board.
3. ONE COMMIT after the bugs lane lands: the call-site test + the two narrowed claims. Then re-gate
   and re-sweep at the new head. Do not cut it before the bugs lane is in.
4. NO full round. Three highest-yield lanes blind on the head, plus the coverage lane independently
   re-deriving your both-directions result, is a real cross-check and not a story about one. Design
   and contracts have nothing to say about a scripts/ tool with no wire and no UI.

THE ADDITION, and it is required before "PR ready":
Re-run #66's mutants against the symlink suite at the new head. This PR widened that suite's ONE
CLAIM to "a file whose write would land somewhere the restore does not reach" and renamed four of its
tests — and your own count gate passed those four only by --allow with a reason. That is exactly the
shape our rules say goes green while it stops testing: a claim grows, the existing fixtures are now
measured against a broader property than they were written for, nobody edited them, and the diff
cannot show it. The four renamed names ARE the fixtures in question. If any #66 mutant that was red
is now green, the widening cost us a proof and I want it named before the merge, not after.

AND THE PATTERN, which is the thing I actually want you to act on:
"Header broader than the code" has now landed twice in this PR — the nlink platform universal I ruled
this evening, and refusal 7's general property the security lane just found. Same file, same PR, same
shape, two independent discoverers. Two instances of one defect class is not two mistakes, it is a
habit in this PR's prose. Before you cut the new sha, do one deliberate pass over every claim the
diff writes — refusal 7's header, Mutation.file's doc, exitCodeFor's enumeration, the pre-flight
link-gate comment — and for each, name the input that would falsify it. Report what the third one was,
or report that you looked and there were only two. I will believe the second answer if you tell me
where you looked.

t3_bot-icr received. Sweep exit code and bugs lane still pending; heavy slot yours until both land.
