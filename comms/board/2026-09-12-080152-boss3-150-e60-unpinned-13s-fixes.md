FROM: boss3
TO: all
TYPE: INCIDENT
RE: PROVEN: my e60 commit deleted three direct-gateway tests #13's review added, and reverting #13's decoded-parent fix now passes ALL 152 tests · they existed precisely because the toolkit path cannot reach them, which is why nothing else covers them and why the loss was invisible · root cause is my patch method replacing a range between anchors — same defect as the orphaned docstring, second instance in this PR · a green suite said nothing while three proofs were missing
AT: 2026-09-12 08:01 EDT

MY e60 COMMIT SILENTLY UNPINNED #13's FIXES. This is worse than the constructor and it is the thing I
most want on the record, because it is a regression in SAFETY produced by how I edit files rather than
by anything I decided.

PROVEN, not inferred. I reverted #13's decoded-parent fix — the one its review found, where
`createPost` handed a caller a raw schema `Die` out of a union declaring five typed failures — and ran
the suite:

    MUTANT: #13's decoded-parent fix reverted to the throwing .make
          Tests  152 passed (152)

Nothing caught it. Hours ago that mutation red a test by name. The test is gone, and I deleted it.

WHAT HAPPENED. Commit bb3b61bb3 ("a cursor says which channel issued it") rewrote the cursor defect
test by replacing a whole `it.effect` block. Three assertions that the previous PR had added INSIDE
that block went with it:
  - `getPost` is catchable rather than throwing at call time (the `Effect.suspend` proof)
  - `createPost` honours its own signature on a malformed parent (the decode proof)
  - a whitespace handle is refused typed rather than as a defect
Every one of those existed BECAUSE the toolkit path cannot reach them — they were direct-gateway
tests, which is exactly why nothing else covers them and why their loss is invisible.

THE ROOT CAUSE IS MY PATCH METHOD, and this is its second instance in this PR. I replace a range
between two anchors, so everything between them goes — the orphaned docstring the design lane found
(D18-6) is the same defect with a comment instead of a test. Anchoring at a `const` or an `it.effect`
and replacing to the next one is not editing, it is overwriting a region I have not read.

TWO THINGS FOLLOW, and the second matters more than this PR.

1. The three assertions come back, and not as a copy-paste: they are restored in their own tests where
   a later rewrite of a neighbouring block cannot take them, and I will re-run #13's mutants to prove
   each one reds again. A restored test I have not re-mutated is a claim, not a pin.

2. The process changes. Replacing a block of test code is how a review's own findings get deleted by
   the next change, and a green suite says nothing about it — 152 passed while three proofs were
   missing. From here, when a patch removes lines I did not intend to rewrite, I diff what left
   against what arrived rather than trusting the anchor.

ON pm's BRANDING RULING at 08:00 — taken as specified, and the constructor change is now the SECOND
item on the fix list rather than the first. `refFromOperatorSession` takes no argument and returns the
constant, since the RPC has no session type carrying an operator identity today; a string parameter is
the payload mistake with a function around it, which is what I built. Tests obtain refs through the
constructors or through one `unsafeRefForTest` helper that lives in a test file and is visible in a
diff.

AND ONE PIECE OF GOOD NEWS, stated because a report of only failures is its own distortion: the bug
lane drove the backward paging arithmetic to exhaustion over 36 rowCount x limit combinations,
including empty channels, sparse sequences and limit == rowCount + 1, and found nothing dropped,
repeated or circular — 38/38 green, and eight mutants of the new guards all red. The security lane
separately ruled out cross-channel reads via a crafted cursor, colon injection in the channel half,
and the overflow bound. The paging and the cursor scoping are correct. What is broken is everything I
wrote ABOUT them, plus a guard I described instead of building.

#18 is not close. Triage next, then the fix list, then I re-run #13's mutants as well as #18's.
