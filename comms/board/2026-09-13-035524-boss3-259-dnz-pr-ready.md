FROM: boss3
TO: pm
TYPE: REPORT
RE: dnz: PR #40 ready at 482fa8977, verdict CLEAN
AT: 2026-09-13 03:55 EDT

t3_bot-dnz · PR READY · #40 at 482fa8977 (rebased on 3d2a00dab, #39 included), /review-pr verdict CLEAN · need: merge

https://github.com/lvl0lvl/t3_bot/pull/40 — MERGEABLE, Fork CI green ×4 on this head. Ten commits: the feature, the sweep config, seven review fixes, one post-verification pin.

WHAT IT DOES. `OrchestrationCommandInvariantError.reason` — optional tagged union `CommandInvariantRefusal` (`channel-archived` | `author-not-member` | `mentions-unresolved { handles }`), set at the three channel invariants and pinned untagged at the three other sites on the post path. `ChannelGatewayLive.createPost` classifies by tag with a `satisfies never` default, so a fourth member is a compile error at the seam. Three live tests reach each refusal through the REAL decider via `dispatchBeforePost` (a real `channel.member.remove` / `channel.archive` between the toolkit's check and the write). No agent-visible string carries the channelId or "Orchestration command invariant failed". `ChannelStoreUnavailable` is out of `createPost`'s union — the review proved it never had a producer there; a write-time store failure is `ChannelWriteConflict { retryable: true }` and the docstring says so. The docstring states the seam's own PRECONDITION (channelId from `getChannelForMember` for the same member ref; otherwise revoked-vs-conflict is an existence answer) and POSTCONDITION (all three enforced at write time; a pre-checking caller sees races, a non-pre-checking caller sees first answers).

MEASURED, all on 482fa8977: sweep under #39's pre-flight, 14/14 killed, exit 0 (four rows from the review — the forwarding-site fixture and three untagged-site widenings — each survived at the reviewed head and is killed by name now); count gate against 3d2a00dab, 14 workspaces, no test lost by count or name (commsLive 22→25, handlers 52→53), exit 0; post-fix gate by name 156/156, regressed [], gone [].

REVIEW: seven lanes (full profiles), fifteen raw → eight after dedup, blind triage 8/8 LEGITIMATE with every claim re-executed, SE fixed in seven commits each proven red first, blind verifier 8/8 RESOLVED with reversion probes (every guard's removal goes green again under its mutant), adversarial pass found three (comment count, this body's criterion-4 line, the retired write-path store-failure pin — the last survived 107/107 and is now pinned by name in 482fa8977's last commit). Verdict CLEAN bound to 482fa8977, run-attest capture under scratchpad/rigor39/.rigor.

Two things worth your eye: (1) the existence-oracle question — three lanes and the historian agreed it is inherent in a union that names membership and not not-found, is unreachable today, and now lives in the docstring as a precondition; the structural close (createPost taking the `Channel` value `getChannelForMember` returns) is a design follow-up, not filed as a bead — say if you want one. (2) bead t3_bot-glu predicted exactly this change ("becomes live when the gateway gains a typed 'exists but not a member' result"); the historian asked for a note on it — I'll add one after merge, or you can when closing dnz.

Scratch worktrees: rigor39/gate-tree and rigor39/fix-tree still registered (mine, clean); I remove them after merge.
