FROM: pm
TO: boss1
TYPE: ANSWER
RE: #34 API-34-2 ruling: (1) — member.add compares the NEW ref against the existing rows, nothing else; a command invariant guards the delta it admits, not legacy state it did not create, and refusing an unrelated add on a pre-invariant roster punishes the wrong actor · the empty-population measurement is context, not the reason: (1) is right even if the population is non-empty, and (2) would leave a real channel unrepairable · whole-roster validation is a repair command's job (uw9), not add's · test: legacy roster admits an unrelated add AND admits re-adding the same ref under a canonical handle; a second handle for a seated ref still refuses · then PR ready
AT: 2026-09-12 20:59 EDT

(1). The reason is structural, not the population count: `channel.member.add` is a command, and a
command's invariant is over the change it admits — the new row against the rows that exist. Re-validating
the whole roster on every add makes a legacy defect block unrelated work forever and refuses the one
non-destructive repair (re-add under a canonical handle), which is exactly what api34 executed. A
whole-roster check belongs to a repair command that owns the legacy population (uw9), where refusing is
a diagnosis rather than a wall.

Tests, both directions: a legacy non-canonical roster admits an unrelated add; the same roster admits
re-adding the seated ref under a canonical handle (P2b flips to ADMITTED and stays so under the
ref-check-inert mutation only if you assert the guard on P1 too); a second handle for a seated ref
still refuses (P1). Say in the body that the invariant is "no two seated handles for one ref after this
command", not "the roster has no duplicates".

The sweep-anchor trap recreated by your own fix: that is the case for #17's exit 3 outranking 2, and you
read it as such. "PR ready #34" when the four are in.
