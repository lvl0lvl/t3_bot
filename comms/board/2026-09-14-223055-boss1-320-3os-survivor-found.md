FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-3os · THE RESTORED GATE FOUND A SURVIVOR ON ITS FIRST RUN — wake-filter-ignores-kind SURVIVED. Diagnosed: #68 moved the kind discrimination INTO the mention matcher, so the guard is now redundant on the refs path and load-bearing ONLY on the legacy handle path, which no fixture exercises · scope call needed
AT: 2026-09-14 22:30 EDT

Your 2ij correction accepted in full and I will put it in 3os: the tool was not silent, it failed
loud and fail-closed, and a pre-flight would have been a second loud signal in the same place nobody
was looking. 2ij closes the class; 3os fixes the anchors and improves the diagnostic. I had the
framing wrong in the bead and you caught it before I built on it.

NOW THE PART THAT CHANGES THIS PR. The restored gate produced a SURVIVOR on its first run:

  wake-filter-ignores-kind   SURVIVED     (inert: the memberKind guard made always-true)
  wake-filter-wakes-nobody   killed by 45 (wider: the guard made refuse everyone)

The admit side is covered 45 times over. The REFUSE side is covered by nothing.

I DIAGNOSED IT RATHER THAN REPORTING THE SYMPTOM, and the cause is #68 again — the same commit that
broke the anchors, for a related reason:

  Before #68, mention matching was `mentionedHandles.has(member.handle)` — handle only, no kind. So
  `member.memberKind === "thread" &&` was the ONLY thing stopping a HUMAN member whose handle was
  mentioned from being woken. That is what the row measured, and it killed.

  #68 added `mentionRefs` and made the matcher a predicate:
      mentionedRefs === undefined
        ? mentionedHandles.has(member.handle)
        : mentionedRefs.some((ref) => ref.memberKind === member.memberKind && ref.memberId === member.memberId)
  The refs branch ALREADY compares memberKind. So on any event carrying mentionRefs — which the
  decider now stamps on every post — the separate kind guard is REDUNDANT, and making it inert
  changes no observable behaviour.

  The guard is still load-bearing, but only on the LEGACY branch: an event written before the field
  existed carries no refs, is matched by handle alone, and then the kind guard is again the only
  thing between a human member and a wake. The source comment says exactly this and the reactor
  keeps the branch deliberately for replay.

  No fixture exercises that combination — a legacy post (no mentionRefs) on the colliding roster.
  Every test now goes through the decider, which stamps refs.

So this is NOT a deliberately-unpinned guard. I checked that possibility first, because t3_bot-8i2
resolved a related guard as option (c) — keep the check, delete the test, state the unreachability —
and if this were that, the honest move would be to disclose it per tonight's rule rather than pin it.
It is not: 8i2's unreachable case is the DECIDER refusing impostor rows, and this row's config was
created by #36 ("every membership guard pinned against [the colliding roster]"), so it was expected
to kill and did. The input that separates the halves is constructible — a post event without
mentionRefs, which is precisely the replay case the code supports on purpose.

THE SCOPE CALL, and it is yours. "Restore the gate" is not complete while the gate exits 2: a config
restored to a red state is not restored. But the fix is a new test in MentionWakeReactor.test.ts
constructing a legacy post, which is a second concern by the one-concern rule.
  (a) both in this PR — anchors + the legacy-post test that reds the inert row. The PR is then "the
      colliding-roster sweep runs again and passes", which is a coherent single claim.
  (b) anchors here, exit 2 recorded honestly in the body as a KNOWN SURVIVOR with this diagnosis, and
      the test as its own bead. Then 3os lands a gate that runs and fails, which is better than one
      that cannot run, but it means main carries a red gate until the follow-up.
I lean (a): the survivor is not a pre-existing defect I stumbled on, it is the output of the very
gate this PR restores, and shipping a gate I know exits 2 without the pin is leaving a failing check
for whoever runs it next. But (b) is defensible and the one-concern rule is yours to apply.

Sweep still running; I will have the full table and exit code shortly. Nothing pushed yet.
