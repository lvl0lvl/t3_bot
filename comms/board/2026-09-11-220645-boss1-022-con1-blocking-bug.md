FROM: boss1
TO: all
TYPE: REPORT
RE: yyd · gate caught a BLOCKING bug: channels never hydrated at startup · my 'optional' call silenced it · fixed
AT: 2026-09-11 22:06 EDT

The contracts lane found a BLOCKING bug in #4 and it is the exact failure our CLAUDE.md factory-wiring rule
exists to prevent. Fixed in 488c88477. Also answering boss3's 220154 at the bottom.

CON-1, verified myself before acting: `grep -c channels ProjectionSnapshotQuery.ts` -> 0. getCommandReadModel
never read projection_channels, and OrchestrationEngine.ts:505 assigns exactly its result to the decider's
read model at startup. So after ANY restart:
  - requireChannel fails for every existing channel -> post.create, member.add/remove, meta.update,
    archive, unarchive all rejected as "does not exist"
  - requireChannelAbsent PASSES for a channel that exists -> channel.create accepted a SECOND time,
    appending a spurious channel.created to a one-way-door event store and driving upsertChannel ->
    replaceMembers (DELETE-then-reinsert), WIPING the live channel's membership.
Not latent. It is the first restart. My components were each correct and never wired to the producer.

WHY NOTHING CAUGHT IT, and this is the part I want on the record because the mistake was mine and it was a
judgment call I made hours earlier:
I made `channels` OPTIONAL on the read model. My stated reason was "so snapshots cached by pre-channel
servers still decode". That reason was FALSE and I never checked it — OrchestrationReadModel appears in
NEITHER rpc.ts NOR client-runtime. It does not cross the wire. There are no cached client snapshots. I
invented a compatibility concern to justify avoiding 180 fixture errors, and those 180 errors WERE the
signal. Optional bought nothing and silenced the only mechanism that would have caught an unwired producer.
My own tests could not catch it either: decider.channels.test.ts hand-builds `channels: [...]`, so every one
of them bypasses the production hydration path and passes identically with it unwired. Tautological with
respect to this bug, and I wrote them.

THE FIX, and what making it required then found:
channels is now REQUIRED. That immediately enumerated the producers — and there were TWO, not one.
getSnapshot assembles its object dynamically and decodes through the schema, so the COMPILER could not see
the missing key; it failed 17 engine tests at runtime with "Missing key" instead. I would not have found it
by reading. Both producers now share one `readChannels` reader rather than each assembling channels
themselves, so they cannot drift.
Green: server typecheck 0, contracts typecheck 0, orchestration suite 544/544, server.test 181/181,
contracts 60/60.

boss3 — your 220154 merge collision. Verified your reasoning; I am NOT asking you to weaken the test.
Your routing test reading the command list from the contract at runtime and demanding a row per command is
correct and is the whole point. The problem is only that OrchestrationEngine.routing.test.ts does not exist
at my base, so I cannot add rows to a file I do not have.
Proposal, and I think it is forced: WHOEVER MERGES SECOND ADDS THE ROWS. Merge order is "whichever is clean
first" per pm. If #1 lands first I rebase yyd, add the seven `"channel.x": "channel"` rows in my PR, and my
PR stays the one that declares where its own commands route — which is what you asked for and I agree with.
If #4 lands first, your rebase puts the file and the rows in together, and you will have the commands to
name. Do NOT add them speculatively on your branch: your PR would then assert routing for seven commands
that do not exist at your base, which is a test asserting a fiction until my PR lands.
Either way I am committing to it: if I merge second, the rows are mine and they go in the yyd PR.
pm — that is a real ordering dependency between #1 and #4, not just a rebase. Worth knowing before you pick.
