FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-rm2 · PR ready · #67 at 815a2c0a1: review 11/11 fixed+verified, count gate exit 0 (+4), Fork CI green on the sha, verdict CLEAN attested · merge; then bd ready
AT: 2026-09-14 02:56 EDT

#67 is ready to merge — https://github.com/lvl0lvl/t3_bot/pull/67, head 815a2c0a1 (rebased onto main past #66, base 4eeaf4630), Fork CI green on that exact sha (Check + Test Server 1/2/3), mergeable CLEAN.

What it does: one shared door, runtimeItemId.ts, for a provider's item id — decode, drop a refused id with a bounded debug log, carry the decoded value — spread at the Claude adapter's nine emission sites, in Codex's runtimeEventBase (a sync fold over the decoder, the two-brand coupling named), and now also in OpenCode's buildEventBase (its own copy of the fold retired; #58's tests prove the helper). Claude's nativeProviderRefs, which .makes the same provider id on the same event, takes the same gate — a non-string tool_use id no longer kills the turn. providerRefs and the adapters' own state keep the raw provider string. ACP unchanged by executed evidence (trimmed or minted before .make). The reason, per your board 237: the store decodes every persisted identity while ingestion's in-memory caches key by what the adapter emitted; decoded carry keeps them equal; the base defect the table proves is a "" Claude tool_use id dying in the pump.

Review record: /review-pr at 96deb6185, seven lanes, 20 → 14 findings, blind triage 11 LEGITIMATE / 3 DEFERRED (beads x6n, e5l, mcu), SE seven commits with every guard red by name (each of the four previously unproven Claude sites reds the new lifecycle test alone; the providerRefs gate reds by the pump Die; the helper's log reds two suites), post-fix by-name gate 324/324 (one new test), blind verifier 11/11 RESOLVED with every reversion probe red and one new comment find (fixed after the verify, comment-only), adversarial pass otherwise empty. Count gate at 815a2c0a1 exit 0 (+4). Verdict CLEAN written and attested against the live body. Beads from this PR: x6n (P2, subagent index eviction), e5l (P3, id length bound), mcu (P4, one previewer), 02p (P3, #58's test comment carries the withdrawn sentence).

After merge I tear down wt-rm2 and the fix tree, close rm2, and take the next from bd ready (1r1 P2 is the highest of mine).
