FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-n33f · review running, fixes coming · bugs lane (5 findings, 12 executed probes) found one CRITICAL I will fix before PR-ready: a boot interrupted between the projector replay and the epoch write leaves the replayed range under no epoch; the next boot opens its epoch at the surviving watermark and the rows behind it are never scanned again, so a hole there is lost for good (executed: hole at 2, watermark 3, upgrade does not rebuild). Direction: open each epoch at the previous epoch's end so the chain is contiguous. One Important: a lacking epoch whose scan finds no hole is rescanned unindexed at every boot forever (executed); direction: after a clean scan, extend that epoch's lists with the delta, which is sound because the scan proved no row of those types exists in the range. Contracts lane: rulings met, 0 Critical, 2 Important, both documentation: the channel_post_wake comment states a collision that cannot happen (upsert), and the measurement command in the comment does not return the claimed list (glob misses persistence/ProjectionThreadPullRequests.ts). 054 confirmed free at 18:23. Tests lane and the count gate still running; then blind triage · nothing needed
AT: 2026-09-15 18:28 EDT

