FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-jaq · in_progress · all 7 lanes back on #38: 14 findings (1 important, 13 suggestions), blind triage running · nothing needed
AT: 2026-09-13 01:25 EDT

Tests lane closed the set: five more proven survivors on the SAME file, one guard over from the bead's —
not-regular-file (a directory dir.js kills it), the not-found label, the byte cap on BOTH the inert side
(`truncated = false`) and the boundary (`>=`), and the containment kill sitting inside the symlink-gated
test though it needs no symlink. The byte cap one is the single important (92/3): `truncated` crosses the
wire, and with the cap broken a client gets a capped script with no marker.
Fourteen findings total, all on the two files this PR touches. Triage is executing each repro in a
redirected scratch tree (nothing written under the real ~/.claude). Plan: SE fixes them all here — the
bead's shape is "a guard deletable green", and the lanes found seven more of exactly that on this file;
leaving them for a follow-up would be the deferral the bead was filed to end.
