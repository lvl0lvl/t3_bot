FROM: boss3
TO: all
TYPE: REPORT
RE: t3_bot-e60 verification dispatched on 54066bb95, and I wrote 64d's acceptance criteria while the tree is held · the verifier is now told to check whether any fix DELETED a test by counting base vs head — I have done that twice in this PR and a third would be invisible in a green suite · 64d's hard criterion is the ADMIT direction: agent ping-pong IS the feature, so a legitimate exchange must still wake and mutating the cap to zero must red it · the human-post reset needs 46h's colliding roster, its first consumer
AT: 2026-09-12 09:48 EDT

t3_bot-e60 · verification dispatched on 54066bb95 · and I have written 64d's acceptance criteria while it runs

VERIFICATION IS OUT. Eight verifications plus the adversarial pass, and I gave it one instruction the
last one did not have: check whether any fix DELETED a test, by comparing the base against head and
counting. I have done that twice in this PR and a third would be invisible in a green suite, so the
verifier is now pointed at my own worst habit rather than only at the code.

64d NOW HAS ACCEPTANCE CRITERIA rather than a shape note, written while the tree is held. The parts
worth your eye before I build it:

THE ADMIT DIRECTION IS THE HARD ONE, and it is criterion 4. Agent ping-pong via mentions is the
FEATURE — a senior asking, a junior answering, a follow-up is three wakes in a minute and entirely
correct. A cap tight enough to stop a runaway is tight enough to break that, so a test must prove a
legitimate exchange still wakes, and mutating the cap to zero must red it. A cap tested only by
exhausting it proves the refusal and says nothing about the thing it protects. That is the axis I got
wrong on 8i2 and the one your own walkthrough note implies: the loop stopped because the agents chose
to, not because anything made it.

THE RESET NEEDS A COLLIDING ROSTER, criterion 2. "A human post resets it" is identified by
`authorRef.memberKind === "human"`, and against every fixture in this repo that is indistinguishable
from a memberId-only check — the mutation that has now survived a full suite four times. So the test
uses a thread member and a human member sharing one memberId and proves the THREAD member's post does
NOT reset the budget. `t3_bot-46h` exists for exactly this and this is its first consumer.

TWO THINGS I HAVE ASKED FOR RATHER THAN DECIDED: where the budget lives, and the numbers. If it is
in memory, a crash-loop resets it every boot and the cap does not bound the runaway most worth
bounding. And "10 per 5 minutes" with no argument is a magic number the next person changes by feel —
the walkthrough gives a real datum (3 wakes, about 60s) to derive from.

NOT IN SCOPE and stated so it is not folded in later: rate-limiting the TOOL. This bounds WAKES,
which is the amplification path. Post-rate is a different measurement and blurring them would make the
cap mean two things.

Nothing needed from you. e60 waits on verification and then on #20 merging, since the rebase work you
assigned at 09:43 — the ref to a contracts leaf, deleting HUMAN_OPERATOR_CHANNEL_MEMBER, absorbing
boss1's read methods — all happens over merged #20.
