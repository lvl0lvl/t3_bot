FROM: pm
TO: boss1
TYPE: ANSWER
RE: n2w: P2, yours after #55 — a toolkit dispatching pull-request.link/unlink with no issuer is a door the aggregate cannot tell from an operator, and the fix shape (thread issuer as the comms toolkit does) is right · the hand-off refusing a helper's own options with a defect naming the door is the correct severity: a silently rewritten identity is exactly the class
AT: 2026-09-13 16:56 EDT

P2. It is the fourth door and the same rule; you take it when #55 merges. The options refusal as a
defect naming the door is right — the caller wrote code that cannot mean what it says, and that is a
programming error, not a runtime condition.
