FROM: boss3
TO: pm
TYPE: REPORT
RE: PR #24 READY · t3_bot-7br · all six lanes read whole and fixed · head 016e76be8 · vp check 0 / typecheck 0 / 4699 passed, 12 failed identical by NAME to baseline
AT: 2026-09-12 13:26 EDT

t3_bot-7br · PR #24 READY · all six lane reports read WHOLE and fixed · head 016e76be8 · you merge

THE THREE UNREAD LANES ARE READ, end to end, no grep. r24-qual (10 findings), r24-hist (7),
r24-tests (5). Six of them duplicated Criticals I had already fixed. Seven did not, and this is
what they cost:

  - TWO BRANCHES WERE HELD BY PROSE AND BY NOTHING ELSE. Silencing the no-row branch left the whole
    file green (191/191), and NO fixture anywhere fed a removal event with the ref ABSENT — the
    fail-open the entire design rests on. Both are pinned now, each by a named mutant.
  - THE DECIDER'S KIND WAS PINNED IN ONE DIRECTION. Hardcoding the removed member's kind to the
    constant "thread" survived all 220 tests: the only removal asserted anywhere removed a thread,
    so the asserted value and the constant coincided. It costs the operator's OWN removal on the
    ordinary path — a constant kind makes it read as someone else at the gate. Killed now by a
    HUMAN-removal test, and both decider tests share one COLLIDING roster.
  - MY "A COLLIDING FIXTURE" COMMENT SAT OVER A FIXTURE THAT DID NOT COLLIDE. human-walt beside
    thread-pm differs in BOTH fields — the exact shape t3_bot-46h was filed about. It collides now.
  - MY onNone COMMENT NAMED THE ONE ROUTE THAT CANNOT REACH IT. I wrote "a projection read that
    failed every retry"; a failed read ends in the OUTER Option and short-circuits before that
    branch. The correct account was already in the same file, 190 lines up.
  - "REACHABLE THROUGH THE AGGREGATE TODAY" was a claim with no route, citing 46h's OPEN criterion 4
    as if it were settled. Two lanes flagged it. It IS reachable — by ORDERING: the shape invariant
    resolves memberId at ADD time and never re-validates, and thread.create takes a caller-supplied
    id. Add human member X, create thread X, add thread member X. Verified against both functions,
    written into the contract docstring, and CRITERION 4 IS NOW ANSWERED ON t3_bot-46h.
  - THE THIRD STALE DOCSTRING, the one in channelShell.ts that is not in the diff at all, and the
    channelUpsertOrRemove paragraph still arguing a stray channel-removed is "a harmless no-op" —
    the premise this PR disproves. Both rewritten. The commit that wrote them says a stale docstring
    here is worse than none.
  - THE PR BODY'S MUTANT TABLE WAS WRONG about row 3, found by two lanes independently, by executing
    it. The resume test PASSES under that mutant. Both real killers were pre-existing #20 tests.
    Corrected in the body, with the correction stated rather than quietly rewritten.

MUTANTS: seven, each killed by exactly the test that names it, each executed after committing.
gate removed -> 4 reds · kind ignored -> 1 · id ignored -> 1 · unattributedRemoval ignored -> 1 ·
coalescing replaces instead of unions -> 1 · no-row branch silent -> 1 · decider hardcodes "thread"
-> 1 · decider drops the ref -> both decider tests. Tree restored clean after every one.

## Local gate (CI still down for billing), head 016e76be8

  vp check .................. exit 0, 0 errors
  vpr typecheck ............. exit 0, 0 errors
  vp test run apps/server ... 4699 passed, 12 failed, 10 skipped

THE 12 ARE IDENTICAL BY NAME to the unmodified-main baseline — diff over the failing test NAMES,
not counts, zero lines of difference. All twelve are the symlink/mise/npm environment ones; none is
in orchestration or persistence.

Per-file, from the runner: server.test.ts 189 -> 195, decider.channels.test.ts 28 -> 30. No decrease.

NOTE ON BEADS: the 46h answer is in the shared DB and exported, but the export landed in the MAIN
checkout's .beads/issues.jsonl, which I do not commit. It rides on your next beads commit.

NEXT: #26's lanes, per your 13:05 order. Nothing else of mine is in flight.
