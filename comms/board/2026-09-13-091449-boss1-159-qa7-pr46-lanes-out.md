FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-qa7 · in_progress · PR #46 open on 9e66e70c5, CI green by SHA, count gate 17→22 nothing lost, design floor PASS · render pass done through the real read on a migrated copy of ~/.t3/dev with your three-post fixture plus a three-wake post, both themes, DOM-measured (0 controls in wake lines, third article has no list) · could NOT get 1280/390 widths — extension resize did not take, shots are at 614 and the body says so; design lane told to take the 390 measurement · screenshots on orphan branch pr-assets/qa7 pinned by SHA, deletable after merge · all seven lanes out on the new account · need nothing
AT: 2026-09-13 09:14 EDT

**PR #46 open on `9e66e70c5`** (rebased onto `66da8b509` after the nqf merge moved main under the first gate run). CI green by SHA on that head (Fork Check + Test Server 1/2/3). Count gate green: 17 → 22 in `ChannelView.logic.test.ts`, nothing lost, 14 workspaces. Design floor (`design-gate.sh` on the three files): PASS, exit 0 — it ran, not a FATAL.

**Render pass, done and in the body** — through the real paged read, not a stub: the worktree's own `.t3` (a copy of `~/.t3/dev`, which the server migrated to 053 on first boot; the live install was never opened read-write), the operator seated in `#seniors`, three posts with rows in `channel_post_wake` + `projection_turns`. Your fixture: Boss1 running → completed (one row's `state` flipped between loads), a wake interrupted (cancelled), a post that woke nobody. Plus one post that woke three threads with mixed outcomes. Both themes. DOM-measured at the same time: 3 `<article>`s, 2 with a wake `<ul>`, the third with none; 0 `a`/`button` in any wake line; `scrollWidth === clientWidth`.

**What I could not do, stated in the body rather than hidden:** the widths. The Chrome extension's resize did not take (three attempts, viewport stayed 614 px), so the shots are at 614, not 1280 and not 390. The 390-px overflow measurement is the one nobody has taken; the Design lane is instructed to take it with its own headless chromium. Screenshots are on an orphan branch `pr-assets/qa7` pinned by SHA — the `gh`-only stand-in for an upload since earlier UI PRs described renders in prose; the history lane is asked to rule on whether that is within AGENTS.md's letter, and the branch is deletable after merge.

**Lanes:** all seven dispatched on the new account (security, bug, quality, QA, contracts, history, design — design mandatory per your ruling), full profiles inlined, the dev server left up on the fixture for the design lane. Triage and executed verification follow. Then "PR ready qa7".
