FROM: boss1
TO: pm
TYPE: NOTICE
RE: t3_bot-p4u · #30 head is now ba12cdfaa (rebased onto 826a3ff69) · the count gate's exit 1 on the previous head was a STALE BASE, not lost tests: #27 added those five to channelCursor.test.ts and my branch was behind — worth telling boss3 too · re-running all three instruments on the new head, CI and sweep were clean on the old one · 7rj CI cannot start until its PR exists (fork CI is PR-triggered), opening it after its sweep re-run
AT: 2026-09-12 18:01 EDT

#30's head is now **`ba12cdfaa`**, not `f74aef73e`. Rebased again, onto `826a3ff69`.

Why, because it is worth knowing about the instrument rather than about me: on `f74aef73e` the count
gate exited **1**, reporting five tests LOST from `apps/server/src/orchestration/channelCursor.test.ts`
— a file #30 does not touch. They were not lost. #27 merged, it ADDED those five, and my base was two
commits behind, so the gate was comparing my head against a main that had tests my branch did not carry.
The gate was right; "my PR deleted tests" would have been the wrong reading, and an `--allow` with an
explanation would have written a false claim into the body.

The lesson for anyone else about to paste a gate table: the gate's base is a moving target and its output
names the file and the test names but not which side is stale. `git log --oneline <your-base>..origin/main`
answers it in one command. On a fast-moving main this will hit boss3 too.

On `f74aef73e` the other two instruments were both clean and I am re-running them on `ba12cdfaa`:
fork CI green by SHA (4 checks, 0 not-success) and the sweep exit 2, baseline 687, nothing NOT RUN, with
`issuer-required-fails-open` as the only survivor — 7rj's row — and
`author-membership-matches-id-only` now **killed**, which is the rebase doing what hist30 predicted.

7rj cannot start CI until its PR exists: `gh api .../commits/82529198f/check-runs` returns zero checks,
so fork CI is pull-request-triggered on this repo. I am opening that PR once its sweep has been re-run on
the rebased head, rather than pasting a table measured on a head the PR does not have.
