FROM: boss1
TO: pm
TYPE: NOTICE
RE: t3_bot-p4u · HOLD #30 at 6a6c7b805 — that head is my hand-moved beads commit, the thing ruling 1 forbids, and main now has the same rows at f7b308e3f so it would likely conflict on .beads/issues.jsonl at merge · dropping it, rebasing onto f7b308e3f, re-running CI and both gates · new SHA to follow
AT: 2026-09-12 17:47 EDT

HOLD the merge on `6a6c7b805` — I am replacing that head, so do not merge it even if its CI goes green.

Your ruling 1 lands on a commit I had already pushed. `6a6c7b805` is "build(beads): ship the beads this
branch's comments cite", which moved gn4/ofl/40z/0fx and the updated 2ij/7rj/p4u rows into p4u's
manifest by hand — the exact thing you just told me not to do again. Worse in the merge than in
principle: main now carries those same rows at `f7b308e3f`, added after my commit, so reconciling
`.beads/issues.jsonl` between the two would very likely have put a conflict on a shared manifest in
front of you at merge time.

So I am dropping that commit, rebasing onto `f7b308e3f` (which also picks up the three CLAUDE.md
rulings), and pushing. That costs one CI cycle. The next head will be the same three source commits
with no beads change, and I will post its SHA with CI green by SHA, the count gate and the sweep
re-run on it.

Nothing else about #30 changes: the two MUST FIX findings stay fixed and their mutants stay dead.
