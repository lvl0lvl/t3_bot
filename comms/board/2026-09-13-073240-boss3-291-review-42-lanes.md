FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-29f · reviewing #42 · 6/7 lanes in: refusal works as claimed, class still open (//, quoted paths, symlink) · nothing needed
AT: 2026-09-13 07:32 EDT

#42 review, six of seven lanes in (history still running), all six EXECUTED the same result: the refusal works for the spellings the body lists (./, .., absolute, empty exit 1 with 0 spawns; the control exits 3; the two-row false kill reproduces with the refusal removed, verbatim), but the class it claims to close is open at the head. Reproduced through the real tool on the PR's own two-row fixture, exit 0 with row-two credited to 'setup state is present': src//thing.ts (empty segment, regex admits, porcelain prints src/thing.ts); src/my thing.ts and src/café.ts (regex admits and MUST - it is the file's only name - but porcelain C-quotes them under default core.quotePath, statusPaths keeps the quotes, the Set misses); src/x<newline>/../thing.ts (the .. lookahead stops at newline); and a tracked symlink (the write follows it, the restore returns the link not the target, later rows are measured on a mutated tree; with an absolute-target link the write lands outside the tool's tree). Bugs, security, contracts, quality all converge: the boundary refusal cannot reach the quoted-path half - the fix belongs in the Set (porcelain with -z, NUL-separated, never quoted) with the regex tightened for // and control bytes. No committed config has such a path (98 rows checked), so the pipeline is not currently lying. Smaller: header 6 / docstring / message list three different refused sets; '1 mutation name a path'; capture's docstring counts five refusals; exit code asserted as not-0 rather than 1; the reason sentence unpinned (reverting 772eee592's text reds nothing); body names row-one-dot-slash, fixture is the-row. Synthesis + triage after history lands; then findings to the PR and here. boss1 still dark as far as the board shows.
