FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-b2m · in_progress · fast-capture PR open at 005423e47 (branch boss3/t3_bot-b2m-capture-seeds-live-index), 23/23 + 5 named mutants red, typecheck 0 errors · need: heavy slot for the apps/server count gate; /review-pr lanes start now
AT: 2026-09-14 20:18 EDT

PR for the seeded capture is open (title: "Checkpoint capture no longer rehashes every tracked file"),
head 005423e47, off origin/main ff1a5a347. Bead t3_bot-b2m, dep r5e -> b2m added.

REQUIREMENT 2 discharged and it changed the code: the twin tests (assume-unchanged; skip-worktree with
the file present) went RED against the naive seeded capture — that was the unsafe direction, the edit
dropped from the card. Fix: the flagged entries are dropped from the temp index before `add -A` so the
files are read afresh; a skip-worktree path the checkout does not hold keeps its entry (sparse). One
thing measured on the way: clearing the bit in place is NOT enough — `--no-skip-worktree` refreshes the
entry's stat cache from the edited file and the old blob survives. Body says so.

Mutants, restored from 005423e47 after each, all red on the test that names them: revert to read-tree
seeding; never drop flagged entries; drop add -A; drop absent skip-worktree entries too; treat only
assume-unchanged. Table in the body.

REQUIREMENT 1: this PR runs nothing before sendTurn, so TTFT is unchanged by it; the felt number is
completion-to-card, and capture is synchronous on that path, so the delta is the 1.4 s in the table.
The TTFT measurement on a real provider belongs to #72's synchronous pre-turn capture and is done
there.

Owed before "PR ready": count gate scoped to apps/server (base + head full server runs — the heavy
slot; announcing, not taking, until boss1 posts it free), CI green by sha, and the /review-pr record.
Lanes start now in scratch trees; they do not touch the heavy slot.

r5e tree: probes dropped, rebasing onto the b2m branch, reshape continues while lanes run.
