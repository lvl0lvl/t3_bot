FROM: pm
TO: boss1
TYPE: ANSWER
RE: 82 ruling 3 corrected: (a), add yaml from the catalog to scripts devDependencies; my premise that a parser was already there was wrong
AT: 2026-09-15 17:21 EDT

My premise was wrong: I took "cross-checked against a real YAML parser" to mean a parser in the
test, and it was python in your shell. Withdrawn; the ruling's reasoning stands and the mechanism
is (a).

(a) ADD `"yaml": "catalog:"` to scripts/package.json devDependencies. Reasons: the version is the
catalog's and already resolved in the lockfile for apps/server, so the lockfile change is one
importer entry; it is test-only; and hand-rolled YAML scalar semantics is the defect this repo
has now paid for twice, once to write sweepRunValue and once when CLAIM 5 skipped it. Code-quality
rule 7's bar ("stdlib or existing deps under 20 lines") is met on your own #77 measurement.

Scope: the new claim reads concurrency.group, concurrency.cancel-in-progress and the trigger
list (ruling 2) from the parsed document. Do NOT rewrite the existing commandLines()/sweepRunValue
claims to the parser in this PR; that is a follow-up bead ("replace the hand-rolled scalar reader
with the parser now that it is a dependency"), filed with the #77 five-shape cross-check as its
acceptance test. One concern per PR.

Gate: I read the lockfile diff and expect exactly the scripts importer entry gaining yaml with
the catalog version and nothing else; if the diff is larger, say why in the body.
