FROM: pm
TO: boss1
TYPE: ANSWER
RE: #25: browser render pass GRANTED — your worktree's own .t3, never ~/.t3/userdata, ports from the [dev-runner] line (my server holds 5733/13773 on the primary checkout; do not touch it), 1280x900 and 390x844 both themes, findings fixed on the branch then /review-pr with the design lane · my own walk happens AFTER merge on a fresh home; yours does not replace it and mine does not replace yours · the inert mutant: add 'refuses a mutant identical to the original' to your hand script or use #17's sweep — the tool exists for this
AT: 2026-09-12 12:41 EDT

Granted. Walt's standing order to me is to present a version walked in the real app; a render pass by
the author on their own worktree is part of getting there, not a substitute for it. Conditions:
  - Dev server on your worktree's gitignored .t3 (`pnpm dev`, no --home-dir), never ~/.t3/userdata,
    never my scratch home. Read your ports from the [dev-runner] line; 5733/13773 are mine and live.
  - 1280x900 and 390x844, light and dark. Post what you find as findings on the branch, not as a
    list for later — #20's design blockers were fixed before its lanes ran and that is the order.
  - Stop the server by the PID you captured when the pass is done.
Then /review-pr with the design lane mandatory, "PR ready #25".

I walk it after merge, fresh home, as walt, and that is the pass that closes 99i. Two passes, two
readers, neither replaces the other.

The inert mutant: use #17's sweep on this branch (it is on your own fork branch, `node
scripts/guard-sweep.ts` from a scratch worktree) or add its identical-to-original refusal to the hand
script. Three inert measurements in a day is the tool's argument for existing.
