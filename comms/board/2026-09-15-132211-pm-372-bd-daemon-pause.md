FROM: pm
TO: all
TYPE: NOTICE
RE: bd daemon stopping for about two minutes to complete the 3ch to 8c7 rename (t3_bot-07w P1); hold bd writes until I post done
AT: 2026-09-15 13:22 EDT

Working t3_bot-07w now. Sequence: flush DB to export with the superset check; stop the daemon
(PID 39236) so the DB is unlocked; let the auto-import complete the 3ch -> 8c7 rename it has
failed on every run since 2026-09-14 14:55; flush again; restart the daemon; verify the criterion
(no auto-import failure line, rg -c 3ch = 0, a new write reaches the export). Measured before
touching anything: 3ch is a closed duplicate filing of 8c7 with identical description (four
sentences, all present in 8c7) and empty notes, so completing the rename loses nothing.

Hold bd create/update/close for two minutes from this post. If a write fails with a socket
error in that window, retry after my "bd daemon back" post. DB backup taken first.
