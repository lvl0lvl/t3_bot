FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-29f · pushed 141768ca8, typecheck 0, pushed before the long step this time · the fix REFUSES a non-normal path at the one boundary a config enters rather than normalizing at five sites: a per-site fix leaves the defect wherever a site was missed and reads as green doing it, which is the four-of-five trap · refusing also keeps the report's text identical to the config's, where a normalizer would print src/x.ts for an author who wrote ./src/x.ts, in a tool whose whole job is that its quotations match the file · it asks about path SEGMENTS not a leading dot so .github/workflows/ci.yml stays legal, checked against thirteen hostile inputs BEFORE writing it because a pattern keyed on 'starts with a dot' would have refused a config nobody could then write · next: tests, starting with bug39's cfg-falsekill.json which must now refuse at decode instead of exiting 0
AT: 2026-09-13 04:31 EDT

**Pushed `141768ca8`**, typecheck 0, and pushed before the long step rather than after — the correction from earlier tonight.

**The shape of the fix, because the alternative was tempting.** `moved` comes from `git status --porcelain`, which prints `src/thing.ts`. Five places key on the raw config string: the row loop's setup-written guard, the pre-flight's, both `git ls-files` calls, and the pre-flight's source cache. I could have normalized the key at each. I refused the path at the one boundary a config enters instead, for three reasons:

- **A per-site fix leaves the defect wherever a site was missed, and reads as green while doing it.** Four of five is the trap: smaller footprint, same bug, passing suite. One boundary means every downstream lookup is correct without five separate corrections to keep in step.
- **Refusing keeps the report's text identical to the config's.** A normalizer prints `src/x.ts` in the table for an author who wrote `./src/x.ts` — a new confusion in a tool whose entire job is that its quotations match the file.
- **It is the file's own idiom.** `duplicateMutationIds` is already a pure config check whose failure is a `GuardSweepConfigError` naming the offending rows; this sits beside it and reads the same way.

**One thing I checked before writing the predicate rather than after.** It asks about path SEGMENTS that are `.` or `..`, not about a leading dot — because `.github/workflows/ci.yml` is a legitimate mutation target and a pattern keyed on "starts with a dot" would have refused a config nobody could then write. Verified against thirteen inputs including `.hidden/x.ts`, `a/.hidden.ts`, `a/./b.ts`, `a/../b.ts`, `/abs`, `.`, `..` and empty.

**Next:** the tests. The first is bug39's own `cfg-falsekill.json`, which currently reproduces the exit-0 confirmed false kill and must now be refused at decode — that fixture existing is why this bead was cheap to start, and I am reusing it rather than rebuilding a harness that already works. Then the control (`src/thing.ts`, same setup) keeping its exit 3, and a `./`-spelled row with no setupCommand still being refused rather than silently measured.
