FROM: boss3
TO: pm
TYPE: ANSWER
RE: t3_bot-b2m · CLOSED with the record in its notes (five defects by class, numbers, upstream PRs, what a future attempt must show); #73 closed unmerged with the pointer; hbm closed as moot; #72 marked DRAFT with the status in its body; the moot gate re-run stopped; fix/gate worktrees removed · t3_bot-r5e · in_progress, draft · standalone from #73: ONE candidate (a contract test block), the rest evaporate · #72 options below, none built · need: your pick on the standalone PR and on #72's shape
AT: 2026-09-14 21:48 EDT

1. WHAT STANDS ALONE FROM #73. Your candidates: split-index handling — evaporates, we never copy
the index (there is no split index in a temp index built by read-tree HEAD; the sibling copy in
GitVcsDriverCore.ts already carries its own guard). fsmonitor/untracked-cache config — evaporates
for the same reason, and NOT beaded: a bead for a config that protects a copy we no longer make is
a chore. utimes/racy — moot, correct. The one thing that stands: SEVEN of the twelve capture tests
pass at base and pin the contract the next optimizer must keep: force-staged ignored file not
recorded; stop-tracked file recorded with its edit; capture from a subdirectory with a conflict
outside; staged edit outside the capture directory recorded at HEAD; HEAD-and-no-index-file seeds
from HEAD (red under "drop the read-tree HEAD fallback", which today has NO test); tracked file
.gitignore matches recorded with its edit; untracked file recorded with the user index untouched.
Plus the racy same-second fixture and the flagged-file fixtures, which pass at base trivially
(a fresh index hashes everything) and exist to red a future seeded attempt — disclosed as such,
per the new rule. That is a test-only PR, "pin the capture contract", one commit, count gate 8 -> ~17
in the file. I recommend it; it is the executable form of the record in b2m. Say yes and it goes up
tomorrow as its own bead, not tonight.

2. #72 OPTIONS, none built, with what each costs:
(A) ATTRIBUTE BY TOOL EVENTS, no snapshot. The adapters already surface the provider's file writes
(Claude's Write/Edit tools, Codex's apply_patch items). The card lists the diff turn/N-1..turn/N
restricted to the paths the turn's tool events touched; other writers' changes are excluded by
PATH, not by time. Cost: zero capture before the send; a turn's write through a shell command
(sed -i via Bash) is invisible unless the adapter reports it, so the card can UNDER-report — the
unsafe direction again, but bounded to shell edits, and disclosable. No git machinery.
(B) A T3-OWNED PER-THREAD INDEX. Never read the user's index: keep our own index file per thread,
built by read-tree HEAD + add -A once (1.5 s, at thread start, off the turn path), then each capture
reuses OUR file: read-tree --reset HEAD to follow HEAD, add -A with our stat cache. No user flags,
no user membership, no split index, no listing to truncate — of the five classes only the stat
cache's timing (racy, #3) remains, and it is ours to stamp. Cost: a persistent file per thread in
the git common dir to manage and to invalidate; and it IS a sixth run at "trust a stat cache", so
by your bar it carries the same burden of constructed inputs before it ships.
(C) FILESYSTEM SNAPSHOT AS THE START. Clone the worktree with APFS clonefile (cp -c) or a reflink
before the send — O(files) syscalls, no hashing — and hash the clone at completion off the turn
path. Cost: platform-bound (APFS, btrfs/xfs; plain ext4 has no reflink and falls back to the 1.5 s
or a full copy), and cross-platform is a stated invariant of this product.
(D) MILESTONE 2: keep the snapshot shape, design the async capture with race detection (start the
capture at sendTurn in a forked fiber, and compare its stat data against the provider's first
write timestamps to detect a lost race and mark the card "base unknown") — real design work, not a
day-shaped PR.
Recommendation: (A) for M1 if under-reporting shell edits is acceptable and stated on the card; (D)
otherwise. Not (B) tonight or without your bar restated for it; not (C) for a product that is
"remote ready" on Linux hosts.

3. RECORD: b2m's notes carry the five defects by class with the input that found each, the secret
one named, the numbers, the upstream PRs and what a future attempt must show constructed. chm, gus,
hwo, bo4, 6ip stay open; hbm closed as moot; new bead for the fsmonitor-daemon hang in VcsProcess
(pre-existing, reproducible at shell level, timed out the verifier's probe). verify-a is still
running its adversarial pass; if it names a sixth class it goes into b2m's notes as well.
