FROM: boss3
TO: pm
TYPE: REPORT
RE: PR ready #18 · head 736cf2e69 base 82b71b1c5 · verifier disproved two of my claims, both corrected
AT: 2026-09-12 11:31 EDT

PR ready #18 · head 736cf2e69 · base 82b71b1c5 · verifier returned and disproved TWO of my claims; both corrected rather than dropped

VERIFIER VERDICTS, 9 items: 7 RESOLVED, 2 FALSE. Both FALSE ones were mine and both were stated with
confidence in a comment and in the PR body.

  V4b FALSE — "a nominal class cannot survive being stored, so the issuer must be a plain struct."
    NOT BORNE OUT, and it answered the question I had left open rather than reasoning about it: it
    dispatched a real post with a class-instance issuer through a real engine and sqlite, read the
    raw row, and scanned every table in the database for the string `nominal`. No hit. The stored
    event is BYTE-IDENTICAL, because `requireIssuerCanAuthor` rebuilds the authorRef as a fresh
    literal and the issuer is never persisted at all.
    THE SPLIT STILL STANDS — reverting it still reds two named tests, and `CommandIssuer` admits
    `system`, which is not a channel member kind. So the conclusion survives on a DIFFERENT reason
    and the false one is corrected in place at both sites rather than quietly deleted.

  V5c FALSE — "both fields are still asserted, so this is not weaker."
    Weaker on two axes, each demonstrated with the file's own assert: `==` admits `["human"]` for
    `"human"`, `1` for `"1"`, and a boxed `String`. Now a two-field projection compared with
    `deepStrictEqual` — strictness back, still works against a class. What is genuinely lost is the
    old form's implicit "and no other own properties", which a class instance cannot satisfy, so the
    comment now says "weaker on shape, and that is unavoidable" instead of "not weaker". Re-measured:
    both operator-ref mutants (wrong kind, wrong id) still red it.

PLUS A1, A FALSE COMMENT IN THE THING I HAD JUST WRITTEN. I said a private field is "the version a
spread cannot carry, because spreading an instance yields a plain object without it". Backwards:
`useDefineForClassFields: true` emits `nominal` as a REAL own property, so `Object.keys` is
`["nominal","memberKind","memberId"]` and a spread DOES carry it. The guard is TypeScript's
private-member nominality, not a runtime absence — which is also why four server-seam tests went red
and why V5c's assertion had to change at all. One wrong sentence, three downstream consequences.

PLUS A2, THE COVERAGE HOLE, and it is the sharpest thing in the report: NOTHING DROVE A REAL REF
THROUGH THE PERSISTENCE LAYER. Every fixture forges a plain object via `unsafeRefForTest` and
`server.test.ts` stubs the repository — so no test could distinguish "the repository accepts the
nominal type" from "it accepts anything", which is the precise distinction this rebase exists to
create. Production passes a class instance with a non-Object prototype and a third own property
straight into a `SqlSchema` request struct. Test added; it reds on BOTH operator-ref mutants, which
is more than the verifier expected of it.

WHAT I DID NOT RE-VERIFY, said plainly: the three comment corrections are verified against the
verifier's own executed evidence rather than by a second run, because they are corrections TO that
evidence. The assertion change and the new test I re-measured myself with both mutants.

GATE on 736cf2e69 over 82b71b1c5 (CI unavailable: billing): vp check 0 errors, vpr typecheck 0,
apps/server 4674 passed / 12 failed — identical by NAME to baseline.

PER-FILE COUNTS FROM THE RUNNER, now in the body as the rule requires:
    commsLive.integration.test.ts   11 -> 19
    handlers.test.ts                43 -> 45
    ProjectionChannels.test.ts      17 -> 19
    commsSeam.integration.test.ts    7 -> 7
    server.test.ts                 189 -> 189
No decrease. And the body states WHERE THE TABLE IS BLIND: server.test.ts sat at 189 across exactly
the weakening V5c caught. A count could not see it; the diff is the check.

#22's three lanes are still out. Opening 64d and dispatching its lanes now, per your 11:20 order.
