FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-46h · PR ready · #36 at b5f437287: /review-pr 12/12 resolved and blind-verified, sweep 17/17, gate green, Fork CI green · merge please
AT: 2026-09-12 22:17 EDT

PR #36 — https://github.com/lvl0lvl/t3_bot/pull/36 — head b5f437287, Fork CI success on that SHA
(Check + Server 1/2/3). Body regenerated from the v2 logs; it carries a Review section.

/review-pr, complete:
  Phase 1   seven lanes (design forced by the .ts extension; found no UI). 12 findings synthesized:
            0 critical, 2 important, 10 suggestions. Gate PASS.
  Phase 3   profile-less triage: 12/12 LEGITIMATE, deciding repros executed (it spelled #23's mutant
            itself: killed by the one test #23 named).
  Phase 4/6 331/331 before and after, compared by NAME: no regression, nothing renamed or lost.
  Phase 5   SE lane, full profile: 12 fixes, 12 commits, pushed.
  Phase 7   profile-less verifier, never saw the diffs: 12/12 RESOLVED — 8 executed (with reversion
            probes on the guard-adding fixes: old assertion back -> seed 2 reds; spread swapped back ->
            probe flips), 4 inspection-only (comments). Adversarial pass over the whole PR: all 17
            sweep rows by hand, base-vs-head test names, the two mutants the weakened assertions could
            admit — found nothing, stated explicitly.
  Verdict   CLEAN, bound to b5f437287, keyed capture via run-attest under scratchpad/rigor36/.rigor
            (artifacts kept out of the PR tree so the sweep and gate ran clean).

Sweep v2 at b5f437287: 17/17 killed, exit 0, every row confirmed at its first count; the new row
`budget-reset-by-roster-lookup` (#23's mutant) killed by exactly `is not reset by a thread member
sharing the human member's id`. Count gate v2 at b5f437287 against ceb9e5645: 14 workspaces, desktop
both sides unassisted (second run in a row), reactor 53->54, no test lost, EXIT=0.

The two important findings were mine: ProjectionChannels test 1 order-depended on my collision test
(fixed: names its channel), and #23's named mutant was claimed by omission (fixed: sweep row, proven).
Also mine: a comment claiming order matters for a boolean `some` — disproved by execution and rewritten.

FOR 7iw (security lane's informational, routed here rather than a new bead): channel.member.add is NOT
on the wire, so step 3 has no external caller — but step 1 is already done by the seeder (human-walt
in #project/#seniors), so ONE thread.create with threadId "human-walt" yields a colliding THREAD
credential; the seven memberKind conjuncts this PR pins are the only defence. Structural fix
suggested there: refuse a threadId equal to any human member id, or give ThreadId the opaque charset.

On merge: bd close t3_bot-46h. #34 (1ez) still open; if it lands with a colliding fixture of its own,
that is one more collapse, not this PR's.
