FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2ij · PR #77 · sweep runs in CI, all six green · AND THE LOCAL TABLE DID NOT PREDICT THE CI ONE — your 20m-fits ruling was based on a premise the real run falsifies
AT: 2026-09-15 10:08 EDT

bd t3_bot-2ij · in_progress · PR #77 open, sweep running in CI, 10/10 checks green · need: nothing
yet; /review-pr next.

THE REAL CI NUMBERS, beside the local ones, both subjects labelled as you asked. Measured at
92b97fe2dc from the check-runs API, per job, not from a wrapper:

  config                LOCAL (this Mac)     CI (ubuntu-latest)    ratio
  channel-cursor              101s                  272s            2.7x
  channel-invariants          476s                 1443s            3.0x   <- SLOWEST IN CI
  channel-post-wake           172s                  478s            2.8x
  colliding-roster            699s                 1112s            1.6x   <- slowest LOCALLY
  invariant-reason            143s                  202s            1.4x
  workflow-script              91s                  134s            1.5x
  -------------------------------------------------------------------
  serial total               1682s (28m)          3641s (61m)
  matrix wall                 699s (12m)          1443s (24m)

THE ORDERING CHANGED AND THAT IS THE FINDING. Locally colliding-roster was slowest; in CI it is
channel-invariants, by 331s. The six ratios spread 1.4x to 3.0x, so there is NO single factor that
turns my local table into the CI one — an average scaling (2.2x) would have put colliding-roster at
25m and channel-invariants at 17m, i.e. exactly backwards.

WHICH MEANS YOUR RULING RESTED ON A PREMISE THE MEASUREMENT FALSIFIES, and I want that on the board
rather than quietly absorbed into a timeout value. You wrote "(A) fits the 20m house timeout with
room". That was a correct reading of the numbers I gave you, and the numbers I gave you were local.
In CI channel-invariants is 24m: THE HOUSE 20m WOULD HAVE TIMED IT OUT ON EVERY RUN. Not once —
every run, deterministically, on the config that sweeps the most.

The only reason it did not ship that way is that I set 30 rather than 20, on the stated ground that a
local figure does not license a CI timeout. I am not claiming foresight: I did not predict the
ordering flip, I declined to predict anything. The refusal did the work, not an insight.

Timeout is now 40, set from the CI measurement (1443s observed, 40m gives real headroom rather than
the 20% that 30 gave). Commit 6bcf350257 carries the reasoning and the ratio spread in the file.

THE MATRIX RULING IS MORE RIGHT IN CI THAN IT WAS LOCALLY: serial would be 61 MINUTES, over any
timeout this file could carry without being absurd. Matrix wall is 24m. You ruled on 28-vs-12; the
real gap is 61-vs-24.

WHAT THE FIRST RUN FOUND, which is the part I would have missed entirely without pushing: all six
jobs FAILED, in under 90s, with
    NotFound: ChildProcess.spawn (pnpm install --frozen-lockfile)
Every config's setupCommand spawns `pnpm` inside the tool's own scratch worktree, and setup-vp puts
`vp` on PATH but not `pnpm`. Fixed with the step mobile-showcase-screenshots.yml already uses —
derives the pinned version from packageManager, appends vp's vendored pnpm bin to GITHUB_PATH, and
closes with `pnpm --version` as its own control. House idiom copied, no new action, one style in the
file.

AND THAT FAILURE IS THE ARGUMENT FOR THE WHOLE PR. Six red jobs, each NAMED for its config, in 90
seconds. Had I piped the sweep to tee for log capture, all six would have gone GREEN over a sweep
that never ran a single mutant — a workflow that looks like a gate and is not one. CI supplied the
evidence for the PR's own test for free, on its first run.

NOT ADDING A TEST for the Expose pnpm step, deliberately and disclosed: removing it reds loudly and
by name, which I just watched happen. The two tests in this PR are for the two failures that are
SILENT — a piped verdict, and a config the matrix does not cover.

FOLLOW-UP I AM NOT DOING IN THIS PR: channel-invariants is slow because its testCommand is the whole
apps/server/src/orchestration directory, 51 test files today, re-run per mutant, 36 times. Narrowing
it is a correctness question (which tests actually kill its rows) and belongs with the mirror-row
half of this bead, not in a CI wiring PR.

/review-pr next, unless you want the second half folded in first.
