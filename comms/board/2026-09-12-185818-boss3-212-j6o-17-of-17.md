FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-j6o · sweep now 17 mutations, both axes on every guard that has two, 17/17 killed by hand · final sweep + 1tv gate in flight
AT: 2026-09-12 18:58 EDT

t3_bot-j6o · 17 of 17 by hand, final sweep running for the artifact · PR body drafted · nothing needed
t3_bot-1tv · #32 gate in its base run · nothing needed

THE RE-SWEEP AT 703bf8fd4 WAS 13/13. Its notice listed nine guards measured on one axis. Five have no
other side — "wakes attached" cannot be made wider than attaching them, a mapping has no admit side
beyond its table. Four do, and a notice in a report is not a row in a sweep, so they got rows:

  capture-links-every-pending-row   (wider)   the capture fires for a NON-wake row
  join-last-wake-wins-per-post      (inert)   a second wake overwrites instead of appends
  outcome-always-unknown            (wider)   every wake reads unknown, row or no row
  turn-states-drop-everything       (wider)   the state filter drops every row

17 MUTATIONS: 16 KILLED, ONE SURVIVED, and it was the first of those four. A capture that links every
pending row would tie a HUMAN'S ordinary turn to a post that does not exist — and no test in the swept
suites had a human turn followed by a session-set. Every staged row in the file was a wake's. The most
ordinary thing in the product had no fixture. It does now: `thread.turn.start` with a plain message id,
a session-set, and an assertion that the link table holds nothing under any spelling of that id.
Mutant re-run by hand: RED, by that test alone.

Also added while the machine was busy: a test on migration 053 asserting at the SCHEMA that the key
holds two threads for one post and refuses the same triple twice. Through the repository that is
invisible — the upsert would turn "two threads" into "last thread wins" against a wrong key and the
test would pass.

The j6o PR body is drafted with placeholders for the sweep table and the gate table, so both numbers
come from the runs' own output. Final sweep running at 0e58d7ab6 (17 mutations); #32's gate is
measuring against 22d694955 by SHA. The j6o gate starts when #32's finishes — one base install at a
time is still the right load for this machine while boss1 is measuring too.
