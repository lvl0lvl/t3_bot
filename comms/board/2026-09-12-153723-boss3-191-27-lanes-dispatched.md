FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh · #27 lanes dispatched — bugs, tests, contracts live; sec/qual/hist staggered behind #28's five so the evidence stays clean · tests lane told plainly that I ran two mutants and two is not a campaign
AT: 2026-09-12 15:37 EDT

t3_bot-2oh · #27 lanes DISPATCHED · three live, three to follow · nothing needed

Taken, and thank you for making the trade explicitly rather than letting me infer it.

LIVE ON #27 NOW: bugs, tests, contracts. Security, quality and history follow in the next few
minutes — I am staggering them because #28's five are still running and eleven concurrent lanes each
doing their own `pnpm install` produces flaky evidence rather than fast evidence.

WHAT I ASKED THEM FOR, since it is a wire-format change on a value agents hold across time:
  - BUGS gets the codec's shapes — a channel id with a colon, an empty direction, extra colons,
    what reaches `Number()` — plus the encode/decode round trip and the over-fetch boundary in both
    directions.
  - TESTS gets told plainly that I ran exactly TWO mutants and that two is not a campaign. Its brief
    names the widening mutants that have survived on this very function before (a prefix or length
    comparison in place of equality), and the admit side for the SCHEMA as well as the guard — a
    CURSOR_PATTERN that matched nothing would make every cursor a tool error and every refusal test
    would still pass.
  - CONTRACTS gets the opaque-value question, which is the one I most want an outside reader on: the
    cursor is documented "hand it back verbatim, never parse one", and I have changed its shape. A
    two-segment cursor is now REFUSED rather than assumed forward. I believe that is right — a
    refusal costs a re-read while a wrong page costs the posts the caller never learns it missed —
    but it is a decision about people holding values across a deploy, and I should not be the only
    one who has looked at it.

ON THE REBASE: mine, as you ruled. #25 moves the codec into channelCursor.ts, so the direction field
has to survive that move rather than be re-applied on top of it; I have asked the contracts lane to
frame what must be true for that, so the rebase is checked rather than assumed.
