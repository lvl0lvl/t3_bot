FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-a44 · P0 built and pushed at 847371e77 on boss1/t3_bot-a44-command-issuer (stacked on #4) · SEC-1 and SEC-2 both closed, 3 mutants killed · one deviation from your ruling's wording that I need you to confirm or overrule · criterion 1's end-to-end MCP probe is blocked on 0uq, not done

STACKED ON #4, NOT ON MAIN. a44 needs yyd's channel commands to exist, so the branch is based on f0ca3fea3.
It rebases onto main after #4 merges. Built in a scratch worktree with its own pnpm install — I am not
editing the #4 tree while three lanes read it.

ONE DEVIATION FROM THE RULING, AND IT IS THE ONE THING I WANT YOUR RULING ON. You wrote 'channel.post.create
carries NO authorRef — the decider reads command.issuer'. I did the first half exactly. I did NOT put issuer
on the command: there are 89 command structs in the union, so a field on the command is an 89-struct change,
and the wire schema would then have to carry a field we spend three sentences explaining is never read from
the wire.

Instead the issuer rides the DECIDER'S INPUT, next to the existing optional userInputActivity — precedent
already in the signature. The engine takes it off the envelope and passes it, exactly as it already does with
origin ('the decider stays pure; attribution is an engine concern', its own comment). Every observable
property you specified holds. If you meant the literal command field, say so and I will change it, but I
think the wording was shorthand and this is what it was shorthand for.

THE COST OF THAT CHOICE, STATED PLAINLY, because it is a real weakness and not a free win: the compiler
CANNOT force a caller to supply an issuer. 144 call sites, and making it required would have meant ~140 files
of test churn burying the security change. So I made absence FAIL CLOSED instead — every channel branch
refuses a command with no issuer — and then pinned it by enumerating channel commands off the schema union
and asserting all seven refuse. A channel command added next month is covered without anyone remembering to
cover it. That is the guard that replaces the compiler, and it is weaker than the compiler; I would rather
you know that than discover it.

WHAT IT DOES:
  channel.post.create      no author field at all; author derived from the issuer, authorHandle from membership
  administration (6 cmds)  human or system only; a thread issuer is refused with a typed error
  system issuer            can administer, cannot author — a reactor has no handle to appear as
  engine                   stamps from the credential and OVERWRITES rather than rejecting, per your ruling

THREE MUTANTS, EACH KILLED, because a guard that is present but inert reads as resolved:
  requireCommandIssuer made to fail open        -> 1 failed
  administration guard made to allow everyone   -> 1 failed
  engine stops passing the issuer to the decider -> 2 engine tests failed
The third is the one I cared about: it proves the wiring is on the PRODUCTION path, not just that the
invariant works when called directly.

CRITERION 3 IS DONE AND WAS THE MOST INTERESTING PART. Channel commands stay off ClientOrchestrationCommand,
now with a comment saying why and a test asserting none of the seven decode. I mutated it — added
ChannelCreateCommand to the wire union — and the test went red naming channel.create. So the exclusion is
load-bearing by test now instead of by accident, which was the actual finding in that criterion.

CRITERION 1 IS HALF DONE AND I AM NOT CLAIMING IT. 'An MCP-credentialed call posting with a spoofed author
lands with authorHandle = the credential's thread' — I have proven the decider half: a command carrying
spoofed authorRef and authorHandle keys lands with the ISSUER's identity. The end-to-end MCP probe cannot be
written yet because the gateway's createPost is still notWired (Effect.die) and there is no MCP entry point
to stamp. That is 0uq's half. Verified there is no OTHER production dispatcher of channel commands today, so
nothing is currently unstamped and reachable.

A NOTE FOR WHOEVER TAKES 0uq: dispatch now accepts { issuer }. An entry point that forgets it gets a
rejection, not a bypass — but it does get a rejection, so the MCP wiring must stamp
{ memberKind: 'thread', memberId: <threadId> } or every agent post fails closed.

ALSO FIXED A STALE COMMENT I would have missed: the duplicate-name engine test said 'the decider accepts it,
the projection refuses'. My name-availability check in #4 made the decider refuse it first, so the test still
passed for a DIFFERENT reason than its comment claimed. Corrected. And the recreate-after-restart test now
passes a valid issuer on purpose — without one it would fail closed on authorization and never reach
requireChannelAbsent, which is the only thing it exists to check.

State: contracts 387/23 green, server orchestration+persistence+mcp 757/80 green, 0 type errors both
packages. Needs /review-pr before it goes near main, and #4 lands first.
AT: 2026-09-11 23:48 EDT

