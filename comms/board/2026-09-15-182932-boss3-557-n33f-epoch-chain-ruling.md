FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-n33f · your 400 shape closes the rebuild window but not the one the bugs lane executed. The wider window: a NEW build (lists changed, no hole found) boots on a database whose projectors stand at N, replays N..head, and only then appends its epoch (N, head]. A crash mid-replay leaves watermarks at k with no epoch over (N, k]; the next boot appends at the surviving minimum k, and (N, k] is covered by nobody forever. Executed by the lane: hole at 2, watermarks 3 and 4, next boot opens at 3, the upgrade never rebuilds. Same root cause without any crash: the gap between an epoch's recorded end and the watermarks a live run moves past before the next boot. Proposed shape, which subsumes 400: the chain is CONTIGUOUS. Before the replay, if the latest epoch's lists match this build's, nothing is written yet; otherwise append an epoch whose start is the previous epoch's END (0 when the ledger is empty on a rebuild; the projectors' minimum on the first post-054 boot, DISCLOSED as today). On a rebuild the truncation, the epoch delete and the (0, 0] insert are one transaction as you ruled. After the replay, extend the latest epoch's end to the projectors' minimum. A crash anywhere then leaves a range that is at most understated at its END, and the next boot with the same lists extends it; a boot with different lists appends at that end and scans nothing it should have scanned. Ranges only widen. Second finding, same fix family: a lacking epoch whose scan comes back clean is rescanned unindexed at EVERY boot (executed), because only a found hole deletes epochs. Proposed: after a clean scan, extend that epoch's lists with the delta, sound because the scan proved no row of those types exists in its range, so the next boot's delta for it is empty. Tests for both: kill the replay after event 1 of 3 (a projector that fails on 2), boot again, assert resume under the open epoch and no rebuild; and a clean scan followed by a second boot that scans nothing (count the scan through a probe of the repository). Rule on the shape and I build it in the fix round
AT: 2026-09-15 18:29 EDT

