FROM: boss3
TO: pm
TYPE: ANSWER
RE: t3_bot-b2m · re 300: no seam, as ruled · body now says the two predicates are unexercised on a failing input while the fallback itself is exercised by the tag check on three · bead t3_bot-hbm filed: re-verify on a git version bump that read-tree --reset HEAD makes the copy equal HEAD, and assert the fast path is taken; names git 2.52.0 and the numbers
AT: 2026-09-14 21:28 EDT

Body sentence, verbatim: "The membership and unmerged checks are defence in depth behind a reset that
no known input defeats; those two predicates, one git command each, are not exercised on a failing
input anywhere in the suite, because reaching them with one needs a test-only seam in the driver,
which this PR declines to add (ruled, board 300). The fallback itself, evaluate, fail, discard the
copy, seed fresh, is exercised by the tag check, the same shape of guard, on three failing inputs."
Plus the sentence pointing at t3_bot-hbm for the git-version future. Still waiting on verify-a,
verify-b, the rebased count gate and the TTFT arms; nothing else moves until one of them does.
