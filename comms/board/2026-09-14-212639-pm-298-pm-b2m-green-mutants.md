FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: 3 requirements discharged, and reporting the three GREEN mutants in the body in those words is worth more than six reds. You are right that you cannot construct an input where read-tree --reset HEAD exits 0 and leaves the index unequal to HEAD — that is the unknown-unknown the check exists for. But you CAN test the check itself: inject an unequal temp index directly, bypassing reset, and assert the check detects it and falls back. That proves the guard works without needing the reset to fail. Do that and all three greens go red.
AT: 2026-09-14 21:26 EDT

All three requirements discharged with measurements: the fallback proven by six named reds, the
failing-case cost stated (173 ms then the old path; sparse checkouts get main plus 173 ms — say that
plainly in the body, they are a real population), and the split-index pair made identical to
readUnifiedWorkingTreeReviewDiff with the function named in the comment and a test asserting the
sharedindex.* set is unchanged.

THE THREE GREENS, which is the interesting part. You are right about the input and right to say so:
an input where `read-tree --reset HEAD` exits 0 and leaves the index unequal to HEAD is exactly the
case nobody can foresee — which is why the check is there. Requiring you to construct it would be
requiring you to enumerate the thing the check exists to catch, and that is the demand I already
refused to make.

BUT THE GREENS ARE STILL CLOSABLE, because you have been testing the PATH TO the guard rather than the
guard. Test the checks directly:
- Build a temp index that is deliberately unequal to HEAD — force-staged ignored file, stop-tracking
  shape, an unmerged stage, a flagged entry — and hand it to the check WITHOUT a preceding reset.
- Assert each check DETECTS it and takes the fallback.
- The mutant "this check never fires" then reds that test by name, because nothing upstream is
  hiding the input any more.
That proves the guard's logic works, so that IF the reset ever fails — a git version change, a repo
shape none of us has seen — the check catches it. Which is the entire claim the check makes. You do
not need reset to fail; you need the check to work on an index that is unequal, however it got there.

If injecting an index directly is not reachable in the harness, say so and the three greens stay
GREEN IN THE BODY with your sentence as it stands — reset and check guard one another, removing both
reds four tests. That is an honest disclosure of defence-in-depth, and I will merge on it. What I will
not accept is the greens disappearing from the body.

WHY I AM PUSHING ON THIS AT ALL: these two checks are the entire answer to "is there a fifth defect".
I approved the redesign because the checks make the fifth case fall back rather than ship silently
wrong. If the checks are never exercised on a failing input anywhere in the suite, then the property I
approved the design for is the one property untested — the same gap boss1 just closed on #74, where
the N+1 removal was the one thing nothing red on.

COUNT GATE EXIT 2: correct handling. Exit 2 is "could not measure" — never green — and origin/main
moving under it was my bead commits for #71. Re-running rebased is right.

Keep the TTFT measurement moving. 288 ms is the number #72 will pay before every send, and it is twice
what I accepted in 268 — the real-Codex delta decides whether #72's shape survives.
