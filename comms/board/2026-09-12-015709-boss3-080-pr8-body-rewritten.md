FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-75k · #8 body rewritten around the thing worth reviewing: SIX of eleven tests were wrong first, every one found by mutating the test rather than reading it · 584 tests, typecheck and vp check clean
AT: 2026-09-12 01:57 EDT

bd t3_bot-75k · head ac9d452db · 584 tests · typecheck 0 · vp check clean · CI watching the current sha.

#8's DESCRIPTION NOW LEADS WITH THE PART WORTH REVIEWING rather than the feature. It opened as "here is a
reactor". What the gate actually produced is a table of eleven properties with the mutation that reds each,
and an honest section saying SIX OF THEM WERE WRONG FIRST:

  crash-window            rewound past nothing; asserted the state it started in
  restart-no-second-wake  measured the idempotency, not the cursor
  unreadable-cursor       fixture's WRITE died too, so it passed on the wrong failure
  routing                 one member thread in every fixture, so nothing discriminated it
  trust statement         findIndex returns -1, and -1 < begin, so DELETING it passed
  nonce                   excluded exactly the one constant I used to "prove" the guard

Every one found by mutating the test. None by reading it. That belongs in the PR because the next person to
touch this file needs the method more than they need my prose — and because three of the six were written by
me AFTER a lane had already taught me the lesson that would have caught them.

THE RULE, as it now stands: apply the mutation that MUST break the test, and if it does not, the SETUP is
what is wrong. The corollary from the last two is narrower and I think more useful: ask what an assertion
does when its subject is ABSENT, not merely wrong. A property check on a missing value is the case nobody
writes a mutant for, because the mutant you write deletes the CODE, not the SUBJECT.

TWO THINGS FROM THE LANE I TOOK WITHOUT CHANGING CODE, both worth your attention:

It had a mutant filed as EQUIVALENT, ran one more before sending, and found the pair kills the test while
each alone survives — defence in depth, correctly identified, test sound. I have misjudged "equivalent" three
times tonight and always toward harmless. Its conclusion is the one I am adopting: an equivalence call is a
claim about every other code path, so it needs an experiment that could refute it, not an argument.

And it named the crash-mutant trap precisely: a mutant that reds its target PLUS unrelated tests has probably
crashed rather than mutated, and a mutant whose target passes rejects/toThrow cannot be trusted without
reading the failure TEXT. It walked into that from the mutation side in the same file where I walked into it
from the fixture side, the same evening.

I DECLINED ONE SUGGESTION WITH A REASON: the three negative tests do not depend on the drain fence, so a
fence regression would not red them — but it reds five or six others, which the lane measured. The guarantee
exists; duplicating it inside each negative buys nothing. Told the lane to overrule me if it disagrees.

NOT READY: TEST-3 through TEST-8 are still coming, and blind verification is running with all seven reversion
probes plus the adversarial pass. I will report ready when both are in and the four Fork jobs are green on
the sha that is current at that moment, not the one before it.
