FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-eas · PR #3 READY · blind verification 7/7 RESOLVED, 13/13 guards proved load-bearing, 0 inert, adversarial pass found no behavioural regression · head 003f6ef06 · one deliberate non-fix you can overrule
AT: 2026-09-11 22:50 EDT

bd t3_bot-eas · PR READY · https://github.com/lvl0lvl/t3_bot/pull/3 · head 003f6ef06

BLIND VERIFICATION RETURNED CLEAN, and it is worth saying what "clean" covers because it is more than a
re-read.

  7 findings re-verified   -> 7 RESOLVED, 0 STILL_PRESENT, 0 inspection-only
  13 reversion probes      -> 13 proved LOAD-BEARING, 0 inert
  adversarial pass         -> no behavioural regression; 3 findings, all comment text
  isolation                -> proven empirically, not assumed

Every verdict is backed by an executed result rather than a reading. The two that matter:

- The router/decider agreement assertion and the hazard-set coverage assertion are the ONLY things in a
  54-file, 654-test suite that catch the thread.pull-request.sync misroute. Verified by flipping it and
  watching everything else stay green.
- The unroutable-command test bites on BOTH halves. Reverting the call site to a throw turns it red as a
  120-second TIMEOUT, and a second variant where the Deferred fails but the worker dies anyway also times
  out. That is the fiber-death failure mode reproduced, not argued.

It also checked its own isolation rather than claiming it: duplicated the new test in-file and got 31/31
(a shared :memory: database would have failed), and measured temp dirs at 32 before and 32 after.

THREE ADVERSARIAL FINDINGS. Two fixed, one deliberately not, and the third is the one I want you to be able
to overrule.

FIXED — the table docstring's arithmetic was wrong: "35 of the 40" where the union declares 39 types across
41 members and the compiler settles 37 of them. I measured it off the schema rather than patching the number
to match the reviewer's, and my count differs from theirs: they said 34 of 39, counting only what the project
group rejects; the compiler blocks BOTH directions, so it is 34 thread-only plus 3 project-only, leaving the
2 dual-id hazards. A wrong number in a docstring, in a PR whose entire subject is claims nobody checked.

FIXED — the Schema.Literals guard fired as a bare "expected false to be true". Every other assertion in the
file names what it choked on. It now reports position and field names, which are the only handles an
unreadable member leaves. Proven red: `expected [ '#7 {commandId,threadId,type}' ] to deeply equal []`.

NOT FIXED, DELIBERATELY — the discredited claim still stands verbatim in OrchestrationEngine.ts:73-76
("attaching its events, receipt, and hasEventAfter scope"), and now contradicts the corrected docstring in my
test file. BOSS1 HAS ALREADY FIXED IT on his yyd branch and I read his version rather than taking his word:
he drops the events/hasEventAfter claim and states the real invariant — two independent mappings whose
agreement protects receipt scope. It is better than what I would have written.

My PR does not touch that hunk (my only edit to that file is the __testing export at the end), so his rebase
applies clean. Fixing it here manufactures a conflict in that exact hunk between two corrections that say the
same thing. Cost of leaving it: a contradictory comment on main for one merge. Cost of fixing it: a conflict
in the hunk carrying the better text. I took the first — but it depends on #3-then-yyd holding, so if you
change merge order, tell me and I will land the correction here instead.

STATE: head 003f6ef06, tree clean, PR is exactly three files, 53 files / 654 tests, 0 typecheck errors in
apps/server and packages/contracts. PR body rewritten — the old one repeated the premise the review
disproved, and now carries the corrected trace, the mutation table, and a section naming what this still does
not cover (t3_bot-aig).

FOLLOW-UPS FILED, none blocking: t3_bot-aig P3 (agreement unchecked for the 15 commands the decider refuses
on a bare probe), t3_bot-d0y P3 (conflict path never driven through production dispatch), t3_bot-glu P3
(non-member vs missing channel indistinguishable only by convention), t3_bot-2x5 P2 (canonical names, now
carrying your ruling and boss1's 868e9af3f).

ONE THING THE VERIFIER FOUND THAT IS YOURS, NOT MINE: it restored pnpm-lock.yaml in my worktree to HEAD, and
said plainly that had anyone parked an intentional uncommitted lockfile change there, it would have destroyed
it. It also pointed out the lockfile is at repo ROOT, so the `git diff HEAD -- apps/ packages/` check I have
been running — and recommending — would never have shown it. I have widened mine. Worth putting in the
scratch-worktree rule alongside the node_modules gap.

Ready to merge on your word. I am not merging it myself.
