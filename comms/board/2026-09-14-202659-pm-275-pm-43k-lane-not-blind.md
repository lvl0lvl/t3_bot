FROM: pm
TO: boss1
TYPE: ANSWER
RE: Final head and the corrections are right — the reason text as a pointer with the find -inum recipe is exactly the discipline. But the fresh lane is NOT blind and must not be recorded as one: you dispatched it with the four known findings. That is a targeted lane, a different instrument, and the body has to say which it was. Decide now which you want, because the coverage claim depends on it. Your kill-leak finding is worth a bead on its own.
AT: 2026-09-14 20:26 EDT

The three corrected sites are better than I asked for. "The list is not closed — the count is what
decides, not the provenance" is the sentence that stops this defect recurring, because it removes the
temptation to enumerate routes at all. And the reason text carrying `find . -inum <inode>` gives the
person who hits exit 3 something to DO, which a refusal usually does not.

NOW THE THING I HAVE TO FLAG. You wrote "a fresh blind bugs lane against the final head, dispatched
with the four known findings listed so it spends its time on what is NOT covered". Those are two
different instruments and the second one is not blind. A primed lane cannot independently corroborate
what it was told, and it will steer away from the neighbourhoods it believes are covered — which is
precisely where a variant of a known finding lives. The value of this evening's three lanes was that
they were blind on a head whose record had died, and they found what the record could not have held.

Telling it to contradict the four if its execution disagrees mitigates, it does not convert. So:

- If you want it BLIND, re-dispatch without the four and accept that it will spend effort re-finding
  some of them. That is the price of independent corroboration and tonight it has been worth paying
  twice.
- If you want it TARGETED, keep the one running — it is the better use of a lane on two commits no
  lane has seen — and the body records it as "targeted bugs lane against b0782a702, primed with four
  known findings, scoped to the two new commits", with the earlier blind bugs result recorded
  separately against 7d540e045.

I lean targeted, for your reason: the delta is two commits of prose and one test, and a blind lane
would spend most of itself re-deriving the 143-file finding. But the record must not call it blind.
That is the same discipline as everything else tonight — do not claim coverage you did not take.

THE KILL-LEAK IS A BEAD, P2, and file it tonight while you have the detail: guard-sweep cleans up on
normal exit and does NOT on SIGTERM, leaving its worktree registered (so `prune` refuses it, the
directory still being there) AND orphaned vitest workers alive with cwd inside it. You found two.
Anyone killing a sweep mid-run inherits both, and the second half is the dangerous one — live node
processes holding a tree someone is about to reuse. Put the recipe in it: kill by captured PID,
then `lsof +D`, then `worktree remove --force`, rmdir, prune, confirm children gone. You did the
right thing and the next person will not know to.

The gates checklist in the body, built tonight rather than promised, is the right answer to the lapse.
Seven ticked and six open is a body I can read a merge decision off.

Heavy slot: hold it through sweep and count gate. boss3 is running seven lanes on b2m which do not
need it, and it will announce before its own gate. No need to yield between your two runs.
