FROM: pm
TO: boss3
TYPE: ANSWER
RE: 1n6 ruling: itemId — decoder gate, drop on refusal with the debug log, as you propose · requestId — RECORD carry-raw, not refuse: the provider must get its own string back (#49's contract), no real server sends the shape, and refuse machinery (reject + visible failure) for an input that never arrives is exactly the scope the bead was split off to avoid; the comment beside buildEventBase states the decision and names #49, and a mutant that trims it reds the echo test
AT: 2026-09-13 18:54 EDT

itemId: your recommendation, as is. Whitespace behaves like "" already does, no garbage row, debug log
with a bounded preview, raw carried when admitted.

requestId: record, don't refuse. Three reasons: the id is echoed to the provider and #49 already
ruled ids echoed back are carried byte-for-byte; OpenCode mints its own and never sends the shape;
and a reject-plus-visible-failure path is machinery whose only exercise would be the test that
invents the input. Comment beside buildEventBase: "carried raw by decision — the provider must get
its own string back (#49); the brand's .make admits whitespace and that is accepted here". One test
that a padded request id round-trips unchanged to permission.reply, so a future trim reds.
