FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-5yq · SHAPE for ruling: the store skips a row whose TYPE is unknown to this build, records it positively (a warning log line with sequence, type, aggregate kind and id, plus a per-read skipped count on the receipt path where one exists), keeps failing on a KNOWN type with a malformed payload (that is corruption, not a newer build), and advances the page cursor by the raw row's sequence so a skipped row at the end of a page cannot stall the read · decision site: OrchestrationEventStore.ts:31 decodeEvent AND the row schema's type field (OrchestrationEventPersistedRowSchema types it as the closed union, so the refusal can fire at decodeRows before decodeEvent ever runs) plus the cursor at readFromSequence ~:312 · falsifying test: three rows inserted through the real sqlite layer — known, unknown type from a newer build, known — read from 0 must return both known events in order, record one skip, and a page size of 1 must still reach the third row; mutants: silent skip (log dropped) red; stop-at-unknown red; cursor from last decoded event (not last raw row) red at page size 1 · need: your ruling on (a) skip-and-record vs a typed unknown carrier, (b) the record's form, (c) whether readAggregateRange and readAll get the same treatment (I say yes, one helper)
AT: 2026-09-15 11:31 EDT

WHAT IT DOES TODAY, measured by reading, not yet by execution: every reader of the log — the
projection pipeline replay (ProjectionPipeline.ts:2174, runForEach to the projector),
its cleanup scan (:2249), the engine's reconcile-after-dispatch-failure (OrchestrationEngine.ts:223,
runCollect then publish to the pubsub), the engine's client-facing readEvents (:526), the
mention-wake reactor catch-up (MentionWakeReactor.ts:840) and readAll (:418) — consumes
readFromSequence, whose Stream fails on the first row decodeEvent refuses (PersistenceDecodeError,
:306). A log with one row from a newer build therefore stops projection replay, stops the
mention reactor's catch-up, and fails the engine's reconcile: not a dropped row, a dead server on
that log until the row is removed by hand.

TWO FAILURES THAT LOOK ALIKE AND MUST NOT BE TREATED ALIKE: (1) a type literal outside this
build's union — a newer build wrote it, this build cannot act on it, and the projector's
uncased-default (z5r ruling) is exactly the behaviour it would get if it could reach it; (2) a
KNOWN type whose payload fails its schema — corruption or a bug, and reading past it hides data
loss. The shape skips only (1) and keeps failing (2). The row schema decides (1) BEFORE
decodeEvent: type is currently `OrchestrationEventType` in the row schema, so the split is: type
as a plain string in the row schema, then `Schema.is(OrchestrationEventType)` per row → unknown:
skip-and-record; known: decodeEvent as today.

THE RECORD (your "positive record of what it skipped"): a per-row `Effect.logWarning` with
{sequence, type, aggregateKind, aggregateId, occurredAt} — cheap, greppable, and the test can
assert it through the logger; and, for the projection pipeline and the engine's reconcile, a
skipped count they can put on their own receipts/metrics if you want it visible. A typed
`OrchestrationUnknownEvent` carrier in the stream is the alternative: it costs a contract change
and a branch in every consumer, for a value nothing downstream can use. I recommend against it.

CURSOR TRAP, the part a naive skip gets wrong: readFromSequence pages by `last.sequence` of the
DECODED events; if the last row of a page is skipped, the cursor does not move past it and the
read either repeats the page or ends early. The cursor must come from the last RAW row. The page
size of 1 in the test is what reds that.

WHAT "DONE" LOOKS LIKE, matching your definition: a read over a log with an unknown type returns
every decodable event, in order, with the skip recorded; the test drives real rows through the
real sqlite read path (no cast objects — z5r's trap); named mutants for silent skip, stop-at-
unknown, and cursor stall; readAggregateRange and readAll share the helper. Building only after
your ruling.
