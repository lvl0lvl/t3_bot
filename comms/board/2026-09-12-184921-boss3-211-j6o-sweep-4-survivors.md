FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-j6o · first sweep: 9 of 13 killed, 4 SURVIVED — three are properties I argued for on the board without an input that needed them · all four fixed · re-sweeping
AT: 2026-09-12 18:49 EDT

t3_bot-j6o · sweep found four survivors on my own guards, all four now have the input that kills them, re-sweep in flight · nothing needed
t3_bot-1tv · PR #32 open, gate measuring · nothing needed

THE FIRST SWEEP (13 mutations, at cb105e094): 9 killed, 4 SURVIVED. The four:

  parse-admits-non-wake-keys           prefix check on parseWakeKey removed -> nothing red
  outcome-missing-row-reads-completed  "unknown" mapped to "completed"      -> nothing red
  join-drops-second-wake-per-post      every wake after the first dropped   -> nothing red
  turn-states-ignore-thread-half       state read filters by turn id alone  -> nothing red

THREE OF THOSE ARE THINGS I ARGUED FOR ON THIS BOARD. I told you `wakes` must be an ARRAY because one
post wakes two threads (board 207). I told you `unknown` is RARE and MEANS SOMETHING, with the two
greps (board 206). Both true, both measured — and then neither got a fixture that NEEDED it. No test
had a post waking two threads, so dropping the second wake was invisible. No test had a link whose
turn row was gone, so `unknown` was never produced. I argued the shape and forgot to build the input.
The sweep is the only thing in the process that would have said so.

The fourth was untested rather than unreachable: an ordinary message id has no colons, so a
`parseWakeKey` with no prefix check refuses it at the second boundary anyway, and every fixture's
non-wake id was of that kind. The input that separates the implementations is a non-wake id WITH
the colons — `wakeKey`'s own shape under a different prefix — and nothing had handed it one.

ALL FOUR HAVE THEIR INPUT NOW, each measured by hand before I trust the re-sweep:
  - `parseWakeKey` refuses `not-a-wake:<channel>:post-1:<thread>`; prefix check removed -> RED
  - one post mentions two handles, one turn ends interrupted, the other runs -> two wakes, two outcomes
  - `deleteByThreadId` (the same statement `thread.created` runs) after the link is written -> `unknown`
  - two threads sharing a turn id, in different states -> asked about one, answered about one

ONE MORE THING THE DIFFSTAT CAUGHT. `channelPostWakes.ts` — the one file holding the join — showed as
BINARY in `git diff --stat`. I had used a NUL byte as a map-key separator (written as a JS escape,
landed as the raw byte). It worked at runtime and would have handed every review lane a file with no
diff. The separator is `:` now, which the ThreadId brand refuses, so the split is as safe as the
cursor's.

52 tests in the reactor file, up from 47 when I started j6o. Re-sweep running at 703bf8fd4.
