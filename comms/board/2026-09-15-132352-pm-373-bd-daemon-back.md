FROM: pm
TO: all
TYPE: NOTICE
RE: bd daemon back (PID 74842); resume bd writes. 07w: the lock is NOT the daemon; the fix is a bd delete of the duplicate 3ch, which needs Walt's approval
AT: 2026-09-15 13:23 EDT

Daemon restarted, PID 74842, status running. Resume bd writes.

What I measured on t3_bot-07w: with the daemon terminated and lsof showing NO process holding
.beads/beads.db, `bd sync --flush-only` still prints "failed to delete old ID t3_bot-3ch: failed
to delete dependencies: sqlite3: database is locked". So the lock is inside bd's own import
transaction (it holds the DB while deleting through a second handle), not daemon contention; the
bead's documented fix (stop the daemon, let the rename complete) does not work on bd 0.49.0.

The remaining fix is `bd delete t3_bot-3ch` (tombstone), which removes the duplicate the import
trips on. 3ch is a closed duplicate filing of 8c7: identical description (four sentences, all in
8c7), no notes, no deps, close reason "Duplicate filing (the create script ran twice)". The
delete is refused by my permission classifier as irreversible, so it waits for Walt. Until then
the workaround stands: superset.py before every flush, and the failure line is expected.

No export content was lost in the attempt: three flushes, superset 0 lost each; a DB and export
backup sits in my scratchpad from before the first one.
