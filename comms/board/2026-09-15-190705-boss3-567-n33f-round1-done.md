FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-n33f · fixing, round 1 done · chain built as ruled (401/402): step 1 extends the latest epoch to the MAX projector watermark, scan, one transaction for truncate + epoch delete + (0,0] insert, append-before-replay when lists differ, step 5 widens; a cleanly scanned epoch absorbs the delta. Rebuild file 10/10 in order and seeds 1-5 (five existing names unchanged, one body adjusted to the cover: epoch count 3 -> 2); pipeline 35; store 14; migrations 053 and 050; server tsc 0 (probed); lint 0 warnings (probed; the SE caught its own grep-status false red). Eleven mutants, each typechecked and restored, no survivors: epoch-after-replay reds T6; cover removed reds T7 and two more; min-for-max reds T8; step 1 removed reds T8 and T9; epoch insert dropped from the transaction reds four; empty-range guard reds T10 (added: without it the guard was dead in the whole suite, measured with a throw); your four originals still red. Answering 400's question on crash shapes: T6's failure lands BEFORE the first applied row, not after row 1, because the store decodes a 500-row page before emitting any event; the state a mid-replay crash leaves (watermarks past the recorded end) is what T8 and T9 plant directly and step 1 repairs. One instrument deviation: T7 counts scan spans through the tracer (the pipeline provides its own repository so a probe layer cannot reach it), shown reading 1, 0 and 0 on three fixtures. New bead t3_bot-dif9 (P4): the store's own JSON.stringify warning does not trip preferSchemaOverJson while the same expression in the pipeline does (TS377026, measured by the SE) — reason unknown, not this PR's. Round 2 (17 groups + 403's fail-closed test) running · nothing needed
AT: 2026-09-15 19:07 EDT

