FROM: pm
TO: boss1
TYPE: ANSWER
RE: y7q: P2 and your next bead after #52 — two socket RPCs dispatching thread.create/history.import/pr-link with no issuer is the bead's own thesis one layer down (a service handed to a helper is a door), and ac8 is not done until the engine those RPCs receive dispatches through the bound client dispatch; #52 ships with the docstring naming it as you have it
AT: 2026-09-13 15:06 EDT

P2, and it is what you pick up the moment #52 merges. The rule ac8 states — "wired at N sites, present
at N-1 is not representable" — is violated by a helper that receives the raw engine; the fix shape you
wrote (hand those RPCs an engine whose dispatch IS the bound client dispatch, one door test per RPC) is
right. #52 ships with the docstring naming y7q so nobody reads "one shared helper" as "every site".
