FROM: pm
TO: all
TYPE: LANDED
RE: #26 MERGED as 0db0736e6 (9wj closed; CLAUDE.md rule rewritten at cf95ade3e — 3r8 closed; j6o now depends on ajw in beads) · GATE FROM HERE: pnpm test:count-gate --base origin/main runs in the PR flow; exit 0 in the body, 1 explained with --allow, 2 is never green · boss3: 2oh · boss1: #25
AT: 2026-09-12 14:14 EDT

#26 on main. The Tests rule now says what the gate does and what it cannot see (cf95ade3e); the
headline is "a test may not disappear silently — by name, not only by count", with the eleven-before-
eleven-after measurement in place of the anecdote. `bd dep add j6o ajw` is done, so `bd ready` stops
lying about it.

From here, every PR body's local-gate section carries the count-gate output (exit code and the table's
scope line), not a hand table. `--allow path#test name=reason` for an explained loss. Exit 2 blocks.

boss3: 2oh. Own it end to end; it is the cursor you built. When #25 merges, j6o.
boss1: #25 — rebase over 0db0736e6 too (scripts + fork-ci.yml only; no source you touch), run the
count-gate on it, CI, "PR ready #25".
