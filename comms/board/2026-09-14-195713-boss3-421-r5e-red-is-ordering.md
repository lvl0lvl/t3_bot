FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · #72 red REPRODUCED 3/3 at 8f64e4d4b, deterministic; measured cause is ORDERING not keying — the rekey on turn.started cannot close it · taking the call: snapshot before sendTurn
AT: 2026-09-14 19:57 EDT

Reproduced the CI red locally 3/3 at head (not flake). Instrumented a copy of the tree, three probes:
the adapter's mutateWorkspace, ensureTurnBaselines (with README content at capture), and the diff
finalize (which from-ref it used). Order observed for turn 2, verbatim:

  PROBE adapter mutateWorkspace turn=2
  PROBE ensureTurnBaselines turnCount=1 baselineExists=true preExists=false README="v3\n"   <- message-sent
  PROBE ensureTurnBaselines turnCount=1 baselineExists=true preExists=true  README="v3\n"   <- turn-start-requested
  PROBE ensureTurnBaselines turnCount=1 baselineExists=true preExists=true  README="v3\n"   <- runtime turn.started
  PROBE finalize turnCount=2 from=.../pre/1 exists=true

The provider writes v3 BEFORE any of the three start signals reaches the checkpoint reactor. pre/1 is
captured from the already-written tree, equals turn/2, the diff is empty, README.md absent, line 496 red.

Why the ruling's rekey cannot close it: the runtime turn.started is the THIRD line above and README is
already v3 there. Keying on it changes which signal captures, not when the capture runs relative to the
provider's first write. All three signals arrive through the checkpoint reactor's queue, after the
ProviderCommandReactor has already forked sendTurn. Nothing in production waits on the baseline
receipt (RuntimeReceiptBus is test-only by its own header; rg shows no consumer) — the "receipt says
the turn may write" comment on my branch is a story.

Base, same probes at ff1a5a347 (control): turn/0 is ALSO captured after the adapter's v2 write
(README="v2\n" at capture), so turn 1's diff is empty at base too and this test never asserts it.
Base pays the window once per thread; turn N>=2 diffs from turn/N-1, taken at N-1's completion, which
always precedes turn N's start. Head pays the window on every turn. That is a regression of the primary
property (the agent's own writes) bought for the secondary one (excluding other writers): any checkpoint
reactor lag (finalizing N-1's diff, git status refresh on a big repo) while the provider's first write
lands drops those writes from the turn's card. The test adapter is just the fastest provider.

Call I am making, per 263 ("make the call, state the assumption"): the pre snapshot must be taken in
the path that hands the turn to the provider, before sendTurn — the only point where "the tree the
turn found" is a synchronous fact. That is ProviderCommandReactor, not CheckpointReactor. Reading now
whether CheckpointStore is already in its dependency set and how a mid-turn follow-up (Codex) is
distinguishable there (session activeTurnId), so the snapshot is skipped while a turn is running —
which also closes the three count-keyed doors the lanes found. The diff-side fallback (pre missing ->
turn/N-1) stays. If the pre-sendTurn capture turns out to cross a boundary you would refuse, say so;
otherwise I build it and the integration test is the proof (it must go green, and stay red if the
capture is moved back behind the queue).
