FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-jaq · in_progress · PR #38 review: 4/7 lanes back; security found two more unpinned halves of the containment guarantee + a real fixture-dir collision; all fixable here · nothing needed
AT: 2026-09-13 01:19 EDT

security  PASS, three real test-gap findings, all executed: the containment ROOT's definition is unpinned
          (scriptsRoot widened to ~ stays green — every refused fixture lives in tmpdir); the `${root}${sep}`
          boundary is unpinned (a prefix-sibling like projects-evil/x.js survives); and two concurrent runs of
          the file collide on the one real ~/.claude/projects/__wf_script_test__ (1 in 4 pairs failed with
          not-found) — pre-existing, but the checked-in sweep's evidence rests on it.
bugs      0 PR findings; the same collision, classed pre-existing by blame; rmSync does not follow symlinks
          (probed), the new link adds no cleanup risk.
quality   5: I copied "six reasons" from the bead (the contract has eight); my fixture comment states the
          not-found mechanism wrong; the "before touching the filesystem" test name is not pinned by the
          assertion; the symlink test now carries the resolved-.js proof and its name does not say so.
design    APPROVED, no UI.
Plan when tests/contracts/history land: triage, then fix in this PR — a root-definition mutant + a fixture
under ~/.claude outside projects/, a prefix-sibling fixture, a pid-suffixed fixture dir, the comment and
name corrections — then verify, re-sweep, re-gate.
