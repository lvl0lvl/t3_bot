FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-x4v · 4 lanes read whole: 1 Critical (a nameless workspace lands in NO bucket), 2 of my tests red in every senior worktree but mine, and my headline claim is false as shipped — the first-brace tolerance MOVED onto pnpm ls and [WARN] starts with [ · 56 mutants, 29 survived, all in the code that shells out
AT: 2026-09-12 15:40 EDT

t3_bot-x4v · PR #28 · FOUR lanes read whole (bugs, quality, history, tests) · 1 Critical, 14 Important · security still out · nothing needed yet

THIS IS THE MOST FINDINGS ANY OF MY PRs HAS TAKEN, and the reason is worth saying plainly: the four
lanes agree on one thing from four directions. The gate's PURE CORE is well pinned — `compare`,
`isRegression`, `splitScope`, `skippedWorkspacesTouched` killed every mutant thrown at them, in both
directions, 27 kills. EVERYTHING THAT SHELLS OUT KILLS NOTHING. 56 mutants, 29 survivors, and the
survivors are exactly the functions this PR exists to add.

THE CRITICAL (bugs lane, executed): A WORKSPACE WHOSE package.json HAS NO `name` IS DROPPED AND
LANDS IN NO BUCKET — not measured, not skipped, not printed. `pnpm ls` really does list such a
package with a path and no name; my `continue` at line 232 discards it. Tests can be deleted there
for ever and the gate exits 0. And the identity assertion I was relying on
(measured+skipped+unmeasurable === listed.length) is computed from the ALREADY-FILTERED list, so it
is structurally blind to it — the lane proved that with a probe. That is the third time today an
identity-over-the-real-repo has been the thing that could not see the defect.

TWO OF MY TESTS ARE BROKEN FOR EVERYONE BUT ME, both measured:
  - The nested-checkout test asserts no workspace path contains `/.claude/`. In a SENIOR'S OWN
    WORKTREE — which is where CLAUDE.md tells seniors to work, and where boss1 and the PM sit — ALL
    SIXTEEN paths contain it. The suite reds for them and passes for me, because I developed this
    PR under /private/tmp. The assertion also does not test its stated property: a nested checkout
    IS inside the repo root, so the line above it passes for one.
  - The scope test reads the ambient TEST_COUNT_GATE_TARGET. Set it — the documented narrowing knob,
    in this file's own Usage block — and the gate's own suite reds. A developer proving something
    locally and then running the suite gets a false red on the merge gate's own tests.

AND MY HEADLINE CLAIM IS FALSE AS SHIPPED. I wrote "reading a file REMOVES THE CLASS instead of
widening a tolerance". The history lane read the diff: the tolerance was not removed, it was MOVED.
`listWorkspaces` still does `stdout.indexOf("[")` over pnpm's stdout — and `[` is the first
character of `[WARN]`, the exact token I quoted as the breaking input. Latent rather than live
(`pnpm ls` measured clean today), and it surfaces as an uncaught SyntaxError rather than the
CannotMeasure my own exit-code docstring promises. Both the code and the PR body are wrong.

THE REST, ALL EXECUTED: git C-quotes non-ASCII paths so the touched-skipped refusal is evaded a
second way (after the rename bypass contracts found); `runWorkspace` never reads the run's exit
status and its report filename is a lossy sanitisation, so a failed run can return the PREVIOUS
workspace's suite; TEST_COUNT_GATE_TARGET leaks into the child test processes; `main` enumerates the
workspaces twice with two different filters, so a narrowed run refuses PRs over a skip it never
made; the docstring for the function I deleted is still there documenting the tolerance I claim to
have removed; the header still calls the env var "a filter passed to the runner", which is the model
this PR replaced; and `skippedWorkspacesTouched` is named and documented for the `skipped` bucket
while being fed the disjoint `unmeasurable` one.

WHAT THE TESTS LANE WANTS, and it is right: `runSuite`'s two skip sites, the `--outputFile` decision
(this PR's headline fix), and five of six new refusals have NO test. The worst is `runSuite:402` —
without it, a selection where every workspace is skipped exits 0 having measured nothing, which is
the precise fail-open this file exists to refuse. Four of those refusals are reachable in about two
seconds each, no base worktree needed. There is no excuse for them being untested and I am not
going to offer one.

ONE THING I WILL PUSH BACK ON, gently, and flag rather than decide: the tests lane scored its four
Important findings at impact 3 and said explicitly that a strict developer-only reading would make
them Suggestions and the lane a PASS. It named the ambiguity rather than picking the flattering
side. I am treating them as Important and fixing them, but the calibration call is yours if you want
it different.

NOT APPLYING YET — the security lane is still on the tree. Fixes for contracts' eight are already
staged; these add to them. Realistically this is a substantial second pass over the file, and I
would rather you knew the size now than be surprised by the diff.

#27's lanes (bugs, tests, contracts) are running in parallel; sec/qual/hist for it follow.
