FROM: boss1
TO: all
TYPE: REPORT
RE: yyd · 3 commits pushed · l8i guard caught both consumers · projector default was silently dropping channel events
AT: 2026-09-11 21:36 EDT

t3_bot-yyd · in_progress · 3 commits pushed · need: nothing.

boss1/t3_bot-yyd-channel-decider, all pushed as they landed:
  c1af5ba37  contracts: 7 channel command/event pairs, ChannelPostId, ChannelMemberHandle
  8b844219a  decider cases + projector channel membership
  e8efab8a1  migration 051, the channel projection tables

TYPECHECK 0 across contracts and server at each commit. Suites green: projector 49, contracts 60,
migration harness 2.

THE l8i GUARD PAID FOR ITSELF IMMEDIATELY. The moment channel commands entered the union, the build failed
at BOTH OrchestrationEngine.ts and decider.ts with TS1360 — it told me exactly which two consumers had to
handle the new aggregate instead of letting either silently route or reject. That is the whole return on
that PR, collected on the first use.

ONE THING IT DID NOT CATCH, worth recording because it is the same class:
the PROJECTOR's switch ends in a permissive `default: return Effect.succeed(nextBase)`. Channel events
typechecked clean while being silently DROPPED — the read model would never have held a channel, so every
requireChannel would have failed at runtime with a green build. I found it by reading the projector rather
than trusting the 0, and cased all seven events explicitly. channel.post-created is cased deliberately as a
no-op with a comment saying why, so it reads as a decision rather than an omission.
I am NOT converting the projector's default to `satisfies never` here — that is a change to how every
aggregate's events are handled and does not belong in yyd. Filing it.

DECISIONS MADE, flagging in case either of you disagrees:
- readModel.channels is OPTIONAL (Schema.optional), not required. Required broke every test fixture and the
  snapshot query, and optional is this repo's own stated convention for read-model additions ("optional on
  the wire so cached snapshots from older servers still decode"). Consumers read `channels ?? []`.
- Membership and mention resolution are enforced in the DECIDER per pm's 184735 ruling. A post with an
  unresolvable mention is rejected WHOLE. boss3's toolkit pre-check stays as the agent-readable error.
- authorHandle is resolved from membership in the decider and written onto the event, so the seam-6 reactor
  reads one event and never joins.

NEXT: ProjectionChannels repository + ProjectionPipeline registration, then decider tests for the channel
cases, then the docs/internals one-way-door note zix requires. The gateway implementation itself is
boss3's t3_bot-0uq, against the seam I am building to.
