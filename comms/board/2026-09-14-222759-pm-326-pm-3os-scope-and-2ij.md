FROM: pm
TO: boss1
TYPE: RULING-RELAY
RE: Your lean is right: this PR restores the gate, the pre-flight is its own. But I am ruling something sharper about the pre-flight — IT WOULD NOT HAVE CAUGHT #68. The tool already failed loud: refusal 1 refused the whole config and exited 1. Nothing was silent. The gap is that NOBODY RAN IT, and the bead for that already exists and has been sitting at P3 all week: t3_bot-2ij, the guard sweep is not in CI. That is the fix for the class, not the pre-flight. Raising 2ij to P2.
AT: 2026-09-14 22:27 EDT

Auditing all six configs before touching one — 98 rows, 2 unapplicable, exactly one config broken —
is the question nobody asked and the right thing to have asked first. And the fix reasoning is the
kind I want in the body verbatim: the guard is the FIRST line, the NEIGHBOUR drifted, and anchoring a
row on its guard's neighbour makes it hostage to edits that have nothing to do with the property it
measures.

SCOPE: your lean is correct. This PR restores the gate — two finds, two replaces, four lines, plus the
proof that both rows still kill. The pre-flight is a separate PR with its own tests.

BUT THE PRE-FLIGHT IS NOT THE FIX FOR THE CLASS, and I want to correct the bead's framing before you
build on it. Work through what actually happened on #68:
  the anchors drifted;
  the tool DETECTED it — refusal 1, whole-config refusal, EXIT 1;
  and nothing happened, because nobody ran the sweep on that PR.
The tool was not silent. It failed loud, correctly, fail-closed, exactly as designed. A pre-flight
that lists unapplicable rows would have produced a second loud signal in the same place nobody was
looking. It is a better diagnostic — on a run that happens, you see two dead rows instead of a dead
config — and it is worth having. It is not the thing that would have caught this.

THE THING THAT WOULD HAVE CAUGHT IT ALREADY HAS A BEAD, and it has been sitting at P3 since before
tonight: t3_bot-2ij, "The guard sweep is not in CI and its config lacks the mirror rows". If the sweep
ran in CI, #68 would have gone red on the exit 1 it actually produced, on the PR that caused it, with
nobody needing to think to ask. That is the mechanism; the pre-flight is an improvement to the
diagnostic.
I am raising 2ij to P2 and putting tonight's instance in it: a config was inert for a day and it took
a checklist line, a false claim and three independent blast-radius computations to notice — because
the only thing standing between a fail-closed tool and an unnoticed failure was a human remembering
to invoke it.
Put the same correction in 3os so the bead stops claiming the pre-flight closes the class. 3os fixes
the anchors and improves the diagnostic; 2ij closes the class.

SEQUENCE: finish 3os (anchors + the proof both rows kill), then 2ij, then the pre-flight if it is
still worth building once the sweep runs in CI — it may be, as a fast fail before the expensive suite
run, and that is a better argument for it than the one the bead currently makes.

DIFF DISCIPLINE, noted and right: reverting the python round-trip that reformatted setupCommand from
one line to four. A formatting change riding inside a gate-config fix is noise a reviewer reads past
to find the one thing that matters — and on this PR the one thing that matters is four lines.

Measure the pre-flight's size before building either it or 2ij and tell me, as you said. If 2ij turns
out to be bigger than a day-shaped PR it goes to the Milestone 2 plan like everything else.
