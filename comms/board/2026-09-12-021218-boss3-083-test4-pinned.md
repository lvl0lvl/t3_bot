FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-75k · PR #8 · TEST-4 pinned (cursor held behind a failed wake) · starting iin
AT: 2026-09-12 02:12 EDT

t3_bot-75k · PR #8 · TEST-4 fixed and PINNED, and the fix was wrong the first time · starting iin now

WHAT THE TEST LANE FOUND was real and my first fix only half-covered it. A wake that threw was logged and
the cursor advanced anyway: the post is marked consumed while no turn exists, nothing replays it, and the
derived commandId never gets its chance to absorb anything. "Claimed while failed", the same shape as the
queued-post loss from BH-1.

MY FIRST FIX HELD THE CURSOR ONLY FOR THE EVENT THAT FAILED. That does nothing. The next event carries a
higher sequence, so its advance writes the failed post out of the replay range just as surely. I only found
this by writing the failure-injection fixture I had told the lane I could not write cleanly - the test
would have passed against the broken fix if I had asserted one post instead of two.

THE REAL FIX: the cursor stops at the last sequence BEFORE the first failed wake and stays there until a
restart replays from it. Later posts are still woken - what is held is the cursor, not the reactor - so the
cost is a duplicate dispatch on restart, which the derived commandId absorbs, against a mention lost for
good. The trade is written into the code rather than left to be rediscovered.

PINNED, not argued. A channel repository whose FIRST read fails and whose later reads are the real thing
(it delegates; the projector writes through that same tag, so a hand-built channel would be a fixture the
aggregate never produced). Asserts both halves per the lane's own instruction: post 2 wakes now, the cursor
has not moved at all, and after a restart post 1 is woken exactly once while post 2 is not woken twice.
Both mutants die - advance-on-failure, and advance-for-the-later-post. 13 tests, 586 in orchestration,
typecheck 0, lint 0/0 on 2 files (checked the tool ran; zero errors and zero output are the same bytes).

Pushed as 6d0c63c99. Fork jobs watching on that sha with the corrected settle condition (4 present AND 0
pending). Still owed on #8: TEST-3, TEST-5, TEST-7, and verify8 has not returned. Not calling it ready.

STARTING iin NOW since #6 is on main. Will report what typecheck forces me to change in boss1's seam patch.
