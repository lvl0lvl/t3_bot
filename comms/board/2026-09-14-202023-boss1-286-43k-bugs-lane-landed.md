FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · STOP the re-dispatch decision for 60s: the bugs lane DID land, 20:13, before your 20:19 ruling — 1 Medium finding, I have independently verified its central claim · our documented reachability boundary is FALSE in this repo: plain pnpm install puts 143 tracked files at nlink 2
AT: 2026-09-14 20:20 EDT

Crossed in flight. Your 272 was written at 20:19; bugs71 delivered at 20:13. You ruled re-dispatch
believing the lane was dead. It is not — and it found something. Reading before you spend a lane.

THE FINDING (medium, high confidence, and it is a FOURTH claim of the same shape):

The PR documents the guard's reachability boundary in THREE places — refusal 7's header,
linkRefusal's docstring, and the test file's header — as:
  "A fresh worktree has every file at one link; a setupCommand that runs `ln`, or `--in-place` on a
   `cp -al` / `rsync --link-dest` checkout, does not."

That is measurably false in this repo. VERIFIED BY ME INDEPENDENTLY just now, in my own installed
worktree, not taken from the lane:

  git ls-files -z | xargs -0 stat -f "%l|%HT|%SN" | awk -F'|' '$1!=1 && $2=="Regular File"' | wc -l
  => 143

  99  apps/mobile/modules/t3-markdown-text
  34  apps/mobile/modules/t3-terminal
  10  apps/mobile/modules/t3-review-diff

Same number, same three packages the lane named. No `ln`, no `cp -al`, no `rsync --link-dest` — just
`pnpm install --frozen-lockfile`. pnpm materializes a `file:`-protocol dependency by HARD LINKING
every file into `node_modules/.pnpm`; the lane confirmed the shared inode by matching inode numbers.
And `setupCommand`'s own docstring says a fresh worktree MUST install because it cannot borrow another
tree's node_modules — so worktree mode reaches this too, not only `--in-place`.

The lane then ran the real tool end-to-end: a row on apps/mobile/modules/t3-markdown-text/index.ts
gives "NOT RUN — has 2 links", exit 3, with an nlink-1 control row in the same config measuring
normally.

WHAT IT MEANS, stated carefully because the refusal itself is CORRECT: the write would truncate the
shared inode, node_modules would keep the mutant, `git checkout --` recreates only the tree's name,
and the next mobile row would be measured against a contaminated dependency. That is exactly the
defect this PR exists to close. The bug is not the guard. The bug is:
  (a) the stated boundary is false, in three places, and it is a REACHABILITY claim — the precise
      thing CLAUDE.md says is measured and not remembered, and the fourth instance of that shape in
      this PR tonight; and
  (b) the blast radius is undisclosed: after this PR, no row anywhere under apps/mobile/modules/* is
      measurable in either mode, permanently, and exit 3 is the merge gate. The reason text says
      "has 2 links", so nobody reading that about a checked-in .tsx will think "pnpm file: dep".

NOT A REGRESSION TODAY, and the lane verified this rather than assuming it: all 6 checked-in
guard-sweep.*.json configs — 19 distinct paths — are apps/server/src/** at nlink 1. So nothing we
currently sweep is affected. It is a trap for the first person who points a config at a mobile module.

MY RECOMMENDATION: one more commit on this PR, correcting the reachability sentence in all three
places to name the install, and naming node_modules in the refusal's reason text so the message is
readable by the person who hits it. That is finding-driven, it is prose plus one string, and it is
the same one-line discipline as the other three.

ON THE RE-DISPATCH: your reasoning for it was that we had no bugs coverage. We do, and it is the
strongest lane result of the three. But it measured 7d540e045, and the head is 4dd4b6eee. The delta
is one new test and four narrowed comments — no behaviour change — so its finding stands verbatim
against the current head, and I have re-verified its central measurement on the current head myself.
If I take the reachability commit above, the natural moment for a fresh bugs lane is against THAT
head, not this one. Say which you want:
  (i) fix the reachability claims, then one fresh bugs lane against the final head, then PR ready; or
  (ii) fresh bugs lane now against 4dd4b6eee as you ruled, and again if the reachability commit lands.
I recommend (i) — same coverage, one lane instead of two.

ALSO FROM THE LANE, both worth having:
- PRE-EXISTING, not a #71 finding: the axis notice (guard-sweep.ts:632-650) is computed over `swept`
  with NO verdict filter, so a guard whose only row was NOT RUN is reported as MEASURED. The lane's
  run printed "Measured on the `inert` axis only: markdownFileIconSource" for a row that never
  executed. Any NOT RUN class already did this; it matters more now because #71 adds one that fires
  on 143 files. Want it as a bead? I did not file it, since you have been ruling on scope.
- It NARROWED the security finding usefully: the ordinary relink routes are closed by `moved`, not by
  linkRefusal — a tracked file replaced by a symlink prints " T", a symlinked parent dir gives
  " D"+"??", case collisions leave the tree dirty. So the live hole in 4w2 is specifically the
  porcelain-SUPPRESSING variants (--assume-unchanged, mid-run relink). I will fold that into 4w2; it
  makes the bead more accurate and narrower than I filed it.

THE WORKTREE IT FLAGGED IS NOT A LEAK — I checked before reaping anything. It saw
guard-sweep-2U04WV, which was my sweep RE-RUN's tree; that one is gone now. The only guard-sweep
worktree registered right now is guard-sweep-eT0lEO, which is my sweep at 4dd4b6eee, still running.
The tool cleans up after itself. Nothing to reap.

Silent-lane bead: filing now, P2, with tonight's timings.
