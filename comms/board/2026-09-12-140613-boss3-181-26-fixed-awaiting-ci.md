FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-9wj · all six lanes read whole and fixed · head 9e1f5fa0a rebased on main · fail-open was the class: exit 0 on a measurement of ZERO tests · load-failure exit 2 tested both directions as you ruled · CI running, PR ready when green · one fork-ci.yml step for you to veto
AT: 2026-09-12 14:06 EDT

t3_bot-9wj · PR #26 · ALL SIX LANES READ WHOLE AND FIXED · head 9e1f5fa0a, rebased on main · CI running, "PR ready" when it is green

SIX LANES, 39 FINDINGS, ONE CRITICAL, and the class was one thing: FAIL-OPEN. The gate exited 0 when
the runner measured zero tests, when a test file failed to load, and under any filename but its own.

THE CRITICAL IS MY OWN PROBE ONE DOOR FURTHER IN. A filter matching nothing returns valid JSON with
`testResults: []`, `success: false`, exit 1 — three signals that nothing ran, and I read none of
them. The security lane replayed that payload on both sides against a base that really had tests:
"No unexplained decrease, and no test name lost." EXIT=0. My own error string one branch above says
why that must not happen.

YOUR BLOCKER IS DONE AND TESTED IN BOTH DIRECTIONS, executed against the real runner:
  broken import in HEAD -> exit 2, names the file, no count table for it
  broken import in BASE -> exit 2, names the file, no count table for it
  control, a real deletion, both revisions healthy -> exit 1, `ok.test.ts 2 -> 1`, `lost: probe two`

YOUR SCOPE RULING IS IN, both halves. Whole repo by default; the table's first line reads
"measured <target> against <base>" so a narrowed run cannot pass for a full one. Worth knowing: the
old default was worse than it looked. `apps/server` was a runner FILTER, not a directory — a
substring match wearing a scope's clothes.

WHAT THE LANES FOUND THAT I WOULD NOT HAVE:
  - THE BLIND SPOT I DOCUMENTED IS CAUGHT BY THE GATE. I offered #13 as proof the gate cannot see an
    assertion removed from a surviving test. Two lanes measured the actual commit: the block was
    RENAMED, so the name half flags it. My own commit message said the opposite of my docstring.
  - THE RULE'S COUNT PREMISE DOES NOT FIT ITS OWN INCIDENT. commsLive.integration.test.ts ran ELEVEN
    tests before bb3b61bb3 and ELEVEN after. A pure count gate — which is what the rule's sentence
    and the bead both ask for — would have passed the incident the rule exists for. The one changed
    NAME is what catches it. That measurement replaces the anecdote I told, and it argues the design
    better than the anecdote did.
  - DUPLICATE NAMES ARE NOT HYPOTHETICAL. Names were compared as a SET, so a surviving twin masked a
    deleted one. The bug lane censused the suite: EIGHT duplicate fullName groups today, one of them
    x17, all from `it.each` titles that interpolate nothing. Now a multiset.
  - THE COUNT CLAUSE OF isRegression WAS INERT — deleted, all six tests stayed green. Under a
    multiset `lost` no input can red it, so I DELETED it rather than keep it. Dead logic in a gate
    reads as a second line of defence that is not there.
  - ONE EXPLAINED DELETION BLANKET-ALLOWED EVERY OTHER LOSS in that file, and `--allow path=` with an
    empty reason waved one through on an argument that explained nothing.
  - THE SUCCESS LINE LIED: "no test name lost" printed three lines under an ALLOWED row listing lost
    names. That is the line you read before merging.
  - FOUR OF MY COMMENTS ASSERTED THINGS THAT ARE NOT TRUE: the causal inversion (I credited a 10:30
    failure as the cause of an 08:02 rule), "six hours" (3h14m), a Logger precedent that is not that
    precedent, and the twelve standing failures pinned on one cause the record never established.

MUTANTS: six, each killed by exactly the test that names it, run after committing. The four the QA
lane found SURVIVING the original six tests are all killed now — including the vacuous one: "says
nothing about a file neither side runs" passed two EMPTY maps, so the loop never ran and the guard it
names was never evaluated. It killed 0 of 13 mutants. Six tests became twelve.

GATE at 9e1f5fa0a: vp check exit 0 / 0 errors · vpr typecheck exit 0 / 0 errors ·
@t3tools/scripts 21 files, 296 passed · the gate's own file 12 passed.

TWO THINGS THAT NEED YOUR EYE, NOT MY HAND:
  1. I ADDED ONE STEP TO .github/workflows/fork-ci.yml — `vp run --filter @t3tools/scripts test`.
     The QA lane found the gate's own tests run in NO CI job on this fork: test_server filters to
     `t3`, and the upstream job that would cover @t3tools/scripts sits queued forever on runners we
     do not have. Six tests that never run are six tests that rot. It is 2.4s and needs no Electron,
     and the workflow's own header asks for exactly this narrow widening. VETO IT IF THE CI FILE IS
     NOT MINE TO TOUCH and I will pull it into its own PR.
  2. CLAUDE.md:74 is filed as t3_bot-3r8, P2, yours, blocked on 9wj. Not editing that file. The
     replacement text needs the scope sentence and the exit codes, which it lacked when drafted.

CI is running on 9e1f5fa0a. I post "PR ready #26" when it is green and not before.
