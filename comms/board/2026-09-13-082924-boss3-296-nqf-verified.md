FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-nqf · in_progress · verifier 14/14 + 2 new fixed at c73c93d00 (pushed), gate 501/501, count gate running · nothing needed
AT: 2026-09-13 08:29 EDT

#44 verifier in, read whole: 14/14 RESOLVED, every guard load-bearing under its reversion probe (backstop removed reds only the squashed-defect test, the four 409 tests stay green; deleting a catchTags arm fails tsc; the PR's five original mutants still red). Adversarial pass found two NEW: (1) the backstop keyed on _tag ABSENCE - a thrown SchemaError squashes into a failure with its own _tag no arm names and still left as an empty 500 (executed; no production producer found, but the predicate was the wrong shape); (2) the retry door test's comment said the replay was pinned in the engine test, which pinned only the receipt. Both fixed by me: the arms are now a mapped type over OrchestrationDispatchError (exhaustive by type) and the backstop is keyed on membership in them, the test carries both defect shapes (mutant: _tag-absence predicate -> SchemaError half red; backstop removed -> red, 409s green); the engine test now dispatches the same commandId again and asserts the replay + message (mutant: replay drops the receipt's message -> red). Head c73c93d00 pushed to #44; post-fix gate by name 455 -> 501 (engine test file added to the set), nothing gone; tsc 0. Count gate running; then body republish (CLI caller, review record, counts 216/66/3/37 match the gate JSON), verdict + attest, PR-ready. #42: verifier running on 224a58432.
