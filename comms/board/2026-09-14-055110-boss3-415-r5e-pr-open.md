FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · #72 open: a turn's diff starts from the tree it found (pre/N), mutants red, suites green · review starting; count gate after the heavy slot frees
AT: 2026-09-14 05:51 EDT

https://github.com/lvl0lvl/t3_bot/pull/72 — head 8f64e4d4b on ff1a5a347. What the bead's title claimed, run at base: a turn's file summary is `turn/N-1 → turn/N`, so on a shared checkout anything written between turns (another thread, a merge) is listed under the next turn — turn 2 writes nothing, OTHER.md written by nobody's turn between them, turn 2's card says OTHER.md. The seeded threads all share the main checkout and main merges between their wakes: that is the "10 changed files". Fix: every turn start takes `pre/N`, the tree the turn found (once per turn, before the baseline receipt that says the turn may write); the completion diff and the on-demand single-turn diff start from it. Legacy threads without pre refs: the query tries the pre ref and lets git's refusal pick the `turn/N-1` fallback — no preflight probe, since upstream #2586 removed those and pinned it (still green). A revert drops the pre refs at and above its target. Five files, +2 reactor tests, +2 query tests (one re-pointed); six named mutants red by name (snapshot never taken, base diff order, revert keeps pre refs, fallback removed, guard widened, guard inert); the three checkpoint suites 54/54; fmt/lint/tsc clean; docs/internals/overview.md gets one sentence. Not changed and said so in the body: the all-turns view (0 → N) still spans between-turn writes; revert still restores `turn/K` and on a shared checkout undoes others' writes since — pre-existing, separate decision if you want it. Count gate waits for the heavy slot (boss1's #71 gate + sweep); /review-pr lanes start now (cold installs in waves ≤4). Nothing needed from you.
