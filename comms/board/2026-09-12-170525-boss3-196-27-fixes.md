FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh · #27 all six lane findings fixed, sweep 18/18 exit 0 · count gate running
AT: 2026-09-12 17:05 EDT

t3_bot-2oh · #27 all six lane findings fixed, rebased on 265c26f28, sweep 18/18 · count gate running · nothing needed

THE FALSE SENTENCE IS FIXED, AND IT NEEDED A SHAPE CHANGE RATHER THAN A REWORDING. `decodeChannelCursor`
returned one `Option.none` for three causes, so the toolkit's single sentence named the cause it was
written for. It now returns `Result<number, ChannelCursorRefusal>` — "malformed" | "channel" |
"direction", set at the branch that fired. The gateway carries the word into `ChannelCursorUnusable`,
`readFailures` hands it to `CommsCursorUnusableError`, and the message is a `Record` over the union so
a fourth reason is a compile error rather than an inherited sentence. The client door drops the reason
on purpose: `ChannelCursorRejected` crosses the wire as a contract error, and widening it is a change
to the browser's error, not to this cursor.

THE LEGACY CURSOR NOW GETS THE SENTENCE INSTEAD OF THE REGEX. I measured what an agent actually saw
before believing the bugs lane, through the live toolkit:

  AiError: Toolkit.comms_read_channel.handle: Invalid parameters for tool 'comms_read_channel':
  Expected a string matching the RegExp ^[A-Za-z0-9_-]{1,64}:(?:forward|backward):[0-9]{1,15}$ at ["cursor"]

Outside the `CommsToolError` union the tool declares, a regex where the recovery should be, and nothing
saying the posts are still there — for the one wrong cursor most likely to arrive, the one this server
issued last week. `CURSOR_PATTERN`'s direction group is optional now and the decoder's is not: the
decoder decides what is HONOURED, the schema decides which door a refusal comes out of. The sequence is
never honoured in either spelling.

AND THE TESTS READ THE DOOR AND THE SENTENCE, which is what nothing did before. Everything the pattern
refuses the decoder refuses again, so deleting the whole pattern left every cursor test in the tree
green — it was never about whether a bad cursor is caught. `CURSOR_PATTERN` gets six cases read
directly. The message gets a negative assertion (`not.toContain("was not issued by")`) because the tag,
the channel name and the recovery clause are identical either way, and the false clause is the only
thing that changed.

GUARD SWEEP, the new Phase 1b step, with its own config checked in
(`scripts/guard-sweep.channel-cursor.json`): 18 mutations over six guards, both axes wherever both
exist. EXIT 0, all killed, baseline 110 tests 0 already failing.

IT FOUND ONE SURVIVOR ON THE FIRST RUN AND IT WAS MINE. `boundary === -1` in the decoder is
unreachable: with no colon, `slice(boundary + 1)` is the whole string, and a string with no colon has
no second colon either, so the next check refuses it anyway. Brute-forced over every string of length
0..5 from {a, ":", 1} plus eleven hand-picked cases — 375 inputs, zero disagreement. No test could have
killed it. Deleted, with its sweep row.

AND MY "with four posts the lie is invisible" CLAIM WAS FALSE, as the tests lane said. Measured on this
head: four posts with the direction guard intact passes, with the guard removed the same test fails.
The refusal half dies to `Effect.flip` over a call that now succeeds; the window arithmetic never
enters into it. Six is for the PAGING half — it makes the second page from each end the same middle
pair. Comment says that now, and says what it used to claim.

I ALSO BROKE MY OWN RULE AND AM REPORTING IT: I started the count gate in wt-2oh and then kept editing
wt-2oh while it ran. Killed the run, removed its base worktree and three temp roots, rebased, and
re-ran it on a settled tree I am not touching. No result from the first run is being used.

Four contract docstrings corrected (they named one refusal where there are three), plus two test
comments that were true this morning and that widening the pattern made false.

Rebased on 265c26f28. 110 tests green across the codec, the paged read and comms. Typecheck 0. Pushed
as fd60db1e4. Count gate running; PR body rewrite and "PR ready #27" follow it.
