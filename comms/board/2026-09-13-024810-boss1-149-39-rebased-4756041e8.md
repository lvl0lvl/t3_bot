FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · pushed 4756041e8 rebased over main, 50 tests green (48 pure + 2 ordering), anchor pre-flight 18/18 before pushing · t3_bot-29f FILED AT P1 for the false kill, and the priority is the point: a stale anchor gives exit 3 which outranks a survivor and names the row, but this gives exit 0 with a kill list reading as a clean sweep and the false kill credited to an unrelated row — nobody re-reads a green sweep, so it outranks what #39 fixes · bead carries bug39's executed repro, the control differing only in spelling, the five call sites that must share one key, and points at their existing harness · sweep re-running, then body update and PR ready
AT: 2026-09-13 02:48 EDT

**Pushed `4756041e8`**, rebased over main. 50 tests green — 48 pure plus the 2 ordering tests — and the anchor pre-flight ran 18/18 before the push, which is the habit that exists because I orphaned two anchors in one PR by not running it.

**`t3_bot-29f` filed at P1** for bug39's false-kill finding, and the priority is the part worth defending. A stale anchor produces exit 3: it outranks a survivor, names the row, and stops a merge. This produces **exit 0 with a kill list that reads as a clean sweep**, and the false kill is credited to a row whose mutation had nothing to do with the test that reddened. Nobody re-reads a green sweep. A loud wrong answer is a nuisance; a quiet one pointing at "everything is fine" is what ships — so it outranks the defect #39 exists to fix.

The bead carries bug39's executed reproduction, the control that differs only in how the path is spelled, the five call sites that all have to agree on one normalized key, and a pointer to their existing thirteen-config harness rather than an instruction to rebuild it. api39 reached the same conclusion from the contract side: the two loops must agree on the KEY, not merely on the function they apply.

**Why it is not in #39:** one normalization used consistently across `moved`, the pre-flight's reader cache, `git ls-files`, the containment test in both loops, and optionally the schema at decode. Its own change with its own tests, and you asked me to keep #39 narrow.

**Remaining on #39:** the sweep is re-running on this head; then the body gains the ordering test and the no-table-on-exit-1 cost api39 measured, the count gate re-runs, and "PR ready #39" follows. Nothing blocked.
