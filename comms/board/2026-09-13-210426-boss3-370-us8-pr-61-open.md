FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-us8 · in_progress · #61 open at 4ec6a14a4: absent revert boundary no longer matches an id-less message; base probe red, 3 mutants red, 128/128; count gate running, review lanes next · nothing
AT: 2026-09-13 21:04 EDT

#58 torn down (wt-1n6, fix-tree, gate-tree, both branches) plus eight finished worktrees from earlier PRs of mine (2oh, 64d, 75k, 7br, 9wj, j6o, base-wjt, cold-wjt — all closed beads) — fewer trees for fseventsd to watch.

us8 claimed and built: https://github.com/lvl0lvl/t3_bot/pull/61 at 4ec6a14a4 on main 1ee38fbbe. The new test row run against the UNCHANGED source reproduces the bead: an id-less assistant with no revert boundary → readThread succeeds with the turns before it (`turns: [assistant-1]`), rollbackThread(1) reverts nothing and reports success. Fix: the boundary is bound once and compared only when the session has one (`revertBoundary !== undefined && entry.info.id === revertBoundary`); an id-less assistant now reaches #49's gate and fails typed, an id-less user message is skipped. No `it(` added: the refused-id test gets the undefined row with no boundary, an id-less user row on the admit side, and a read that should have failed dies naming its row. Mutants: base restored → red `undefined with no boundary: the read succeeded with 1 turn(s)`; the same with the assistant row removed → the user row reds `actual: []` (that row first ran with a boundary left set by the previous row and proved nothing — fixed); inverted and removed → three existing boundary tests red. 128/128, fmt/lint clean, tsc alone exit 0. Count gate running detached (board 369); rigor52 lane files (seven, profiles pasted), triage brief, gate and verdict scripts are prepared and dispatch when the gate lands. Nothing needed from you.
