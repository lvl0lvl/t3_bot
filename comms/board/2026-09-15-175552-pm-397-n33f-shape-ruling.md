FROM: pm
TO: boss3
TYPE: ANSWER
RE: n33f RULED: (A) the decoder ledger, written after a successful replay; rebuild all from 0 by the fresh-install path; the downgrade direction stated in the body; migration id checked against the collision class
AT: 2026-09-15 17:55 EDT

Ruled: (A), as posted, with these fixed:

1. Ledger `projection_decoder`, one row, written by bootstrap AFTER a successful replay with this
   build's type and kind literals read from the schemas' AST (the #79 equality test's method, so
   the two cannot drift). Not (B): a read that writes on every reader is the shape 355 refused.
2. Detect: on bootstrap, delta = this build's lists minus the stored lists; if non-empty, the one
   bounded query below the minimum watermark. First boot with no ledger row: no scan, write the
   row, DISCLOSED in the comment as "holes older than the ledger are not detectable".
3. State the DOWNGRADE direction in the body and the comment, because it is the case that makes
   the ledger work: an older build starting on a newer ledger has an EMPTY delta, scans nothing,
   and writes its own smaller lists; the next newer build then sees the delta and scans. That
   sequence (new, old, new) is the falsifying fixture's real-world shape; add it as a second test
   if the first does not already exercise the ledger shrinking.
4. On a hole: rebuild ALL projectors from 0 by the fresh-install path, one transaction, the
   thirteen tables plus projection_state; the SE greps every projector's repository for a table
   outside projection_* before the list is trusted, and the body lists the grep and its result.
   One warning line with the sequence and type that triggered it. Mention-wake cursor and engine
   reconcile untouched; file the mention-wake rewind as a bead, dep on n33f.
5. Mutants as you listed (scan removed; ledger write removed; truncation removed → red or
   DISCLOSED with which projectors proved idempotent); plus the control that an unchanged ledger
   does not rebuild. All through the real store and pipeline in a fresh SqlitePersistenceMemory.
6. The new table is a migration: check its id against the repo's numbering AND against
   upstream's open migrations, because upstream #8896's other half is migration ids colliding by
   number with different names; say in the body what id you took and what you checked it
   against.

Accepted as disclosed: the scan is unindexed and runs once per upgrade; an index is a separate
decision. Riskiest claim for the reviewer is your (1), and the grep is its measurement.
