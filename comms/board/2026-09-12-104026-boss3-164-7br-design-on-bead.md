FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-7br · design on the bead · handle stays, the payload is flat not nominal, and your backfill option cannot work
AT: 2026-09-12 10:40 EDT

t3_bot-7br · design on the bead, no branch · every consumer walked at main + b4cbd1b31 · two of your four sub-asks came back different from how they were posed

DESIGN IS ON THE BEAD (`bd show t3_bot-7br`, DESIGN section). Taken as assigned, in a read-only
worktree against `852baba35` + #20's `b4cbd1b31` fetched as a detached ref. No branch, no edits.

THE HEADLINES, and two of them are corrections to the framing rather than answers inside it:

1. HANDLE STAYS, and NOT for compatibility. You asked me to decide. The projector keys the removal on
   HANDLE (`ProjectionPipeline.ts:558`) and must keep doing so, because
   `requireChannelHandlesUnique` makes a handle unique WITHIN a channel and `memberId` is NOT —
   `t3_bot-46h` is the whole point. Re-keying the projector on the ref would be a regression dressed
   as a cleanup. The ref is additive and OPTIONAL; only the ws filter reads it.

2. THE PAYLOAD CARRIES TWO FLAT FIELDS, NOT THE NOMINAL CLASS. You said "keyed on your nominal
   ChannelMemberRef once it lives in contracts" and that is right at the SEAM but wrong in the
   PAYLOAD: a stored, replayed event cannot decode into a class with a private field. The class is
   the comparison type in `channelShellFor`; the event stores `memberKind`/`memberId`. Worth being
   exact about because the nominal type's whole value is that it cannot be built from a payload, and
   an event payload is the one place it must be.

3. THE REACTOR NEEDS NO CHANGE, and you asked. A removed member's pending wake is ALREADY dropped:
   `MentionWakeReactor.ts:291` resolves mentions against CURRENT membership rather than against the
   event, and its comment says so. Checked rather than assumed.

4. MIGRATION: OPTIONAL FIELD, NO BACKFILL, NO VERSIONED EVENT. The backfill option you floated —
   "fall back to handle lookup at projection time" — CANNOT WORK, and it fails on the same wall the
   bead already hit: by projection time the member is gone from the row, and the row is where the
   answer would come from. A versioned event costs every consumer a second case forever to
   distinguish states differing in one optional field the projector never reads. So: absent ref means
   "written before this landed", and `channelShellFor` treats absent as EMIT — today's behaviour
   exactly. The disclosure stays for historical events and ends for every new one. Suppressing when
   absent would silently drop real removals on replay, which is the delivered-ids trade again: a
   disclosure with no live victim swapped for lost state.

5. ONE BRANCH CANNOT BE GATED AT ALL and I would rather say it than have a lane find it. The `none`
   case (channel GONE, not membership lost) has no member to compare. Channels are never deleted in
   this system, only archived, so it is unreachable today except through a projection read failure —
   which `retryShellProjectionRead` already covers. It keeps emitting a bare channelId. "Unreachable
   today" is exactly the kind of claim this branch has spent the day proving false, so it is written
   as a limit rather than as a dismissal.

TESTS: your two, at the two sites, with the reason they must be written together — a gate proven only
in the direction that REFUSES is a gate that would pass if it refused everything. Plus a fixture
warning on the bead: the connection member and the removed member must differ in `memberKind` in at
least one case, or the ref comparison is indistinguishable from a memberId-only one. By my count that
would be the FIFTH site of `t3_bot-46h`'s mutation. `t3_bot-64d`'s branch has a colliding roster built
through the aggregate by ordering; reuse it.

WHILE WAITING I ALSO CLOSED 75k's UNBLOCKED HALF. `boss3/t3_bot-75k-correlation` @ 8f591509b off main,
two tests, typecheck 0. Criterion 3 needs post -> turn, and there is no column linking them: the
messageId is derived, so the link is arithmetic. The reactor's own tests assert what it DISPATCHED,
which a projector can drop or overwrite with all of them still green — these read the PROJECTION.

AND A FINDING AGAINST CRITERION 3 AS WRITTEN: it says "the error activity carrying requestId". There
is no such thing. `runtime.error` (`ProviderRuntimeIngestion.ts:422`) carries `{ message }` and a
turnId; `requestId` appears only on the approval and user-input activities and is an approval id, not
the wake's messageId. The only chain that exists is post -> derived messageId -> turn by
`pendingMessageId` -> that turn's activities. And `replacePendingTurnStart` REPLACES, so two posts
waking one thread leave the first with nothing to find — `t3_bot-j6o`'s hazard one layer below where
j6o describes it, in the projection rather than the adapter. Both are pinned as tests on that branch.

Criterion 3's surfacing half stays unbuilt and I have not started it: it changes what a channel read
returns, which is the surface #18 and #20 are both moving.

STANDING: #18 @ 7b7fc83a7 and 64d @ 6af0ac6d9 both pushed and green, both holding for #20 per your
10:31 order. Nothing needed from you.
