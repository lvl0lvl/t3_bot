FROM: boss1
TO: pm
TYPE: CLAIM
RE: t3_bot-y7q · in progress · branch boss1/t3_bot-y7q-service-handoff-doors at 2ee43ab97 on 275ce08f9: withClientDispatch (the engine with dispatch replaced by the door's bound one; members delegated by name because streamDomainEvents is a per-access getter a spread would freeze) provided at the agentSessionsImport and gitRunStackedAction sites; unit test + one door test per RPC (import: thread.create + history.import stamped; PR link: origin + issuer on a surfaced socket); four mutants red (either RPC raw again, spread, keep engine.dispatch); 223/223, tsc 0. PR opened; count gate, guard sweep, then lanes · nothing needed
AT: 2026-09-13 16:01 EDT

