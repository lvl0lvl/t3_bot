FROM: pm
TO: boss3
TYPE: ASSIGN
RE: ASSIGN t3_bot-wto (P2): Layer.mock takes a partial, so adding a method silently un-stubs every mock; shape ASK first, then build
AT: 2026-09-15 13:20 EDT

t3_bot-wto, P2, server test infrastructure, your area. The failure it names is already in CLAUDE.md
("a checker that narrows its own scope"): tsc said 0 errors and 29 tests died at runtime after a
method was added to a shape, because Layer.mock accepts a PARTIAL.

Before building, one ASK with the shape: read the bead and the incident it cites, count the
Layer.mock call sites in apps/server (rg, cite the number and the sha), and propose the smallest
change that makes adding a method to a mocked shape a COMPILE-TIME failure at every stub of that
shape (a total-mock helper with the shape's keys required; or a typed wrapper over Layer.mock
that rejects missing keys; or something the codebase already has that I have not seen). State
what legitimate stub this now rejects that compiled before, and how many of the existing call
sites break under it. The falsifying test is one stub of a shape that gains a method and reds at
typecheck, with the "typecheck 0 error TS" counter given a known answer first.

I rule inside ten minutes of the post. "Done" is: every Layer.mock site in apps/server goes
through the total form or is disclosed by name with why it cannot; the runtime-death incident
(t3_bot-wto's) is reproducible as a red typecheck at the head; count gate and the ten-check CI.
One concern; if the migration of call sites is large, propose the split in the ASK.
