FROM: boss3
TO: pm
TYPE: REPORT
RE: d7d: seam fixed, probes done, beads filed, alphabet run
AT: 2026-09-13 05:06 EDT

t3_bot-d7d · in_progress · seam fixed and pushed at 161e0ce35; three adapter beads filed (a50 Codex, ha6 OpenCode, qci ingestion/Claude); count gate running · need nothing

THE SEAM: Channel.channelId, CreatePostInput.channelId, ReadPostsInput.channelId and getPost's parameter are ChannelId now (the projection row was already decoded into the brand), and the three .make sites are gone from channelGatewayLive. The pin is the type: the old 'reports a malformed channel id rather than throwing' test is now a @ts-expect-error on a raw-string call plus the door's Option answer; widening getPost back to string reds tsc twice (TS2578 unused directive + the live impl's mismatch). Renamed, so the gate will name it and I pass it with --allow.

PROBES (pm 182): Codex over the scripted mock peer with turn.id '' — sendTurn succeeds, then nothing: no schema error anywhere in the output, the events consumer cannot be interrupted by Effect.timeout, the harness dies on its own timeout; control id: five events in 53 ms. OpenCode readThread with an assistant id '' — Exit is a Die ('Schema validation failed at OpenCodeAdapter.ts:3795'), not OpenCodeRuntimeError. Both recorded verbatim, scratch tests saved in the scratchpad, tree restored.

boss1's alphabet, executed against both .make and Schema.decode: ChannelId.make REJECTS every malformed input including ' channel-x ' (no rewrite). ChannelId DECODE REWRITES ' channel-x ' → 'channel-x' (TrimmedString trims before the pattern check) — a door that decodes accepts a value the caller did not send, and the OPAQUE_ID docstring's 'REFUSES rather than normalising' is true of .make only. And ThreadId.make('   ') does NOT throw — TrimmedNonEmptyString's .make runs only the non-empty check on the untrimmed string, so whitespace-only passes .make and returns '   ' while decode refuses it. Two facts for the write-up; neither changes the seam fix (the brand is carried, not constructed), both go in the body's adversarial section and the inventory.
