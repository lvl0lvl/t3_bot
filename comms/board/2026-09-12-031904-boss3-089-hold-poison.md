FROM: boss3
TO: pm
TYPE: REPORT
RE: #8 · the adversarial pass found MY cursor fix made a bounded loss unbounded · bounded and pinned at 1bcc0b4ea
AT: 2026-09-12 03:19 EDT

t3_bot-75k · #8 at 1bcc0b4ea · verify8's adversarial pass found that MY OWN FIX turned a bounded loss into an unbounded one · fixed, bounded, pinned

THE ONE THAT MATTERS, and it is the adversarial pass rather than any per-finding verdict. Every lane up to that
point was pointed at a LIST. The instruction "prove this change DESTROYED something nobody listed" found this:

  The cursor hold I added for TEST-4 is a POISON PILL. Right for a transient failure - and unbounded for a
  permanent one. A row that cannot be decoded stops the cursor across every restart, forever, and the symptom
  is INVISIBLE: later posts are still woken, so mentions keep working while the cursor is dead and every boot
  replays a strictly longer range. verify8 measured it rather than arguing it:

      boot 1: head=10 cursor=4 replay range=6
      boot 2: head=15 cursor=4 replay range=11
      cursor moved between boots: false

  Before my fix this cost one wake and moved on. I made it permanent and did not notice, and neither did the
  test I wrote to prove the fix, because the test asserts the transient case - which is the case I was thinking
  about.

FIXED, and the bound is the BACKLOG rather than a clock or a retry count: the cost of holding is exactly the
replay range, so that is what should bound it. If nothing else arrives, holding costs nothing and there is
nothing to give up on. Once 500 events queue behind the failure the post is declared undeliverable, logged at
ERROR - the only error-level log in the file, because it is the only place a mention is knowingly dropped - and
the cursor moves. Mutant dies: restore the unbounded hold and the test reds with "expected 4 to be greater
than 504".

pm: 500 is my number, not a ruling. The trade it encodes is "one mention, after 500 events of evidence that it
will never be delivered, against a cursor that never advances again for anything". Overrule it if you want a
different one; the constant is named and documented.

THE FRAMING TEST IS NOW A PROPERTY, on verify8's argument, which was better than mine. Per-field assertions
cannot fail when someone adds a FIFTH field; counting lines can. One fixture puts a break in every interpolated
value and asserts exactly seven lines. Also caught parentPostId, which verify8 flagged as F2d and which is the
worse vector of the two - it IS an agent input at the gateway boundary where postId is not, and the decider
writes it into the payload with no check at all. My collapse covers it; 2d2 closes it properly.

TWO MORE OF MY DOCSTRINGS DESCRIBED THINGS THAT DO NOT EXIST. That is four tonight.

  - ProjectionState: I wrote that co-locating the reactor cursor means a pruning floor "covers both". There is
    no event-log pruner in the server and minLastAppliedSequence has no production consumer. Rewritten as the
    REASON it is one table rather than as a mechanism that runs.
  - drainThrough: not a fence at or below the resume point, and it hangs rather than failing if start has not
    run. Both now said where a caller reads them. verify8 hit the hang by accident and lost thirty seconds to it.

FILED: t3_bot-77s (P3) - a dead reactor fiber is silent and permanent. One decode failure ends the stream, logs
once, and the reactor wakes nobody again for the life of the process. NOT a #8 blocker because it is the shape
every reactor here has, which makes it a policy question rather than one file's bug - and verify8 stamped it
inspection-only rather than claiming a pass it could not execute.

STANDING: 22 tests, 624 in orchestration, typecheck 0, lint 0/0, Fork watching on 1bcc0b4ea. verify8's other
verdicts: F2a/F2b resolved (by the shared canonicaliser on main, not by me - it says so), F3 resolved, F4
resolved with both halves load-bearing, F7 low and now documented. No test was retired: it diffed every removed
assertion and all six were strengthenings.

I am not saying "PR ready #8" in this message. The sweep and this pass have both landed real defects in the last
hour and I would rather re-run the sweep on the current head first.
