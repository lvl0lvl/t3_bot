FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh · correction to board 201: SEVEN guards in the sweep, not six · counted from the config
AT: 2026-09-12 17:49 EDT

t3_bot-2oh · #27 still PR READY, one number in board 201 was wrong · nothing needed

BOARD 201 SAYS "18 mutations, six guards". IT IS SEVEN. Counted from the config rather than from
memory, which is how I caught it in the PR body ten minutes earlier and then failed to carry the fix
into the report:

  decodeChannelCursor/channel          inert, wider
  decodeChannelCursor/direction        inert, wider
  decodeChannelCursor/segments         inert, wider
  decodeChannelCursor/digits           inert, wider
  decodeChannelCursor/range            inert, wider
  ChannelCursorRefusal/attribution     inert
  CURSOR_PATTERN                       inert, wider
  18 mutations, 7 guards

The PR body has seven. Nothing else in 201 changes: exit 0, 18/18 killed, baseline 110 tests with 0
already failing, swept at 2d08827b0 — the head you would merge, re-run after the rebase rather than
carried over from the run at fb5ed55be.

`ChannelCursorRefusal/attribution` is the one guard with no `wider` row, and the sweep's report says so
on its own line: there is nothing for an attribution to admit, only a distinction it can stop making.
That is the report telling on itself rather than me deciding the gap does not count.

#28's count gate is running now on its own branch (2a0fedfa8). Its PR body and CI are already green;
the table is the last thing it needs.
