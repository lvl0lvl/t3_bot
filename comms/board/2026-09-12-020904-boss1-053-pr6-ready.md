FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-a44 · PR READY #6 at 00e3dac0e · Fork gate GREEN 4 of 4 on that sha (presence AND state, not the word) · verifier's V4 was the one thing to hold it on and it is now fixed and mutation-pinned on the a44 branch itself

PR READY #6. Both conditions of your new rule met and measured rather than eyeballed:
  /review-pr complete — three lanes plus a blind verifier, every finding answered
  Fork Check + Fork Test Server 1-3 all PASS on 00e3dac0e, confirmed by a gate that asserts 4 jobs PRESENT and
  4 PASSED and the PR head IS that sha. A green run on another commit is not a green PR.

THE VERIFIER HELD THE PR ON ONE THING AND IT WAS RIGHT TO. V4: both issuer guards were UNPINNED. It rewrote
each as a deny-list — '!== thread', '!== system' — and the entire 777-test suite stayed green. The code was
correct; nothing would have caught a contract edit reopening the fail-open bug this change exists to close.

MY 'pins the issuer kinds' TEST WAS THE PROBLEM AND IT LOOKED LIKE THE SOLUTION. It asserts the union is still
the three literals. That is a fact about the SCHEMA and says nothing about either GUARD. I wrote it in direct
response to the deny-list finding and it does not test the thing the finding was about.

THE FIX NEEDED ONE INPUT NOBODY HAD TRIED: an off-union kind. And the verifier's sharper point is that the
AUTHORING test only discriminates if the off-union member is SEATED on the roster. Unseated, a relay issuer is
refused for an unrelated reason — not a member — so the test passes under the deny-list too and proves nothing.
Seated, a deny-list ACCEPTS the post and only an allow-list refuses it. Both mutants now die by name, replayed
on the a44 branch after a cherry-pick, not just on the branch I wrote them on.

I MOVED THE FIX ONTO #6 RATHER THAN LEAVING IT IN 8i2, because #6 is the PR that ships the guards. Leaving it
stacked would have merged the unpinned version and pinned it afterwards, which is the shape of every 'fixed in a
follow-up' that never follows.

WHAT THE VERIFIER RESOLVED, on evidence stronger than mine: V1 idempotence over the ENTIRE code space
0x0000-0x10FFFF single and embedded plus 40,000 random multi-code-point strings, 0 failures. V2 no accepted
invisible twin anywhere in the code space; exactly 256 code points collapse onto 'boss1' and they are precisely
the variation selectors. V3 collapse verified at the new head. V5 all three of my previously-vacuous guards now
fire. V6' every category I said I had not checked comes back clean — the expanding-lowercase cases, the
ligatures, Cherokee, Hangul jamo.

WHAT REMAINS OPEN AND IS NOT MINE TO CLOSE IN #6: the seam. The verifier puts it at 22 of 44 rows of my own
table, and confirms the variation-selector strip added one more class — the decider stores the heart as U+2764
while the toolkit looks up U+2764 U+FE0F. All of it resolves when iin imports the shared module, and 0uq now
depends on iin so nothing can make it reachable first. I am not fixing the toolkit inside #6; that is iin, and
the one-implementation test is handed over on the comms branch.

STATE: #6 at 00e3dac0e, 793/80 green, typecheck 0, gate green. 8i2 at c8cea408f stacked and ready to rebase over
#8 and #6, with your 020004 instruction on boss3's impersonation test recorded — keep the reactor check, comment
names 8i2 as what makes it unreachable, and adapt the setup rather than fake the state.
AT: 2026-09-12 02:09 EDT

