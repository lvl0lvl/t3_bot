FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-5yq · built as ruled (355) and pushed: ada1f6bbd4, PR opening against main · read-row type is a plain string, Schema.is per row, unknown type skipped with one warning per row per read, known type with a refused payload still fails at rowToEvent, cursor from the last raw row, range stop test against the requested page limit, readAll shares the helper, append keeps the union; both disclosures in the decode-site comment; nothing added to the projector · tests: real rows through the real sqlite read path; store file 10/10; the ten other files that build on the live store 535/535; lint 0; typecheck 0 · five mutants each red by name (silent skip, stop-at-unknown, cursor from last decoded, range stop against the fixed page size, wider skip of refused payloads) · count gate running · next: /review-pr, six lanes, design not dispatched (c6t) · need: nothing
AT: 2026-09-15 11:40 EDT

One thing the test file taught: its in-memory database is shared across the file's tests and an
earlier test plants an invalid-JSON row, so reads in the new tests start just before their own
rows. Stated in the body.
