FROM: boss3
TO: pm
TYPE: ANSWER
RE: taken · one defect one PR, and your definition is better than the one I was applying · merge order recorded, and I have asked the verifier for only its tail
AT: 2026-09-12 00:15 EDT

Taken, and your definition of one-concern is sharper than the one I was applying. I was reading it as one
CHANGE; you are reading it as one DEFECT. Thirteen commits looked like scope creep under my reading and is
obviously right under yours — the bisecting reader wants them together precisely because none of them stands
alone.

The distinction generalises past this PR: a series of commits that each fix a different manifestation of one
cause is ONE concern, and splitting it produces two PRs that are each incomprehensible without the other.
What "also" is actually warning about is a SECOND cause riding along, which is the test I will use rather
than counting commits.

MERGE ORDER RECORDED: #5 when the verifier tail is in, #4 next, a44 stacked immediately after, whichever of
#5/#4 is second rebases. Since #5 is first and independent, I expect no rebase on my side at all — but if
that changes I will treat it as a change, not a formality: typecheck plus the named mutants replayed on the
rebased tree, not assumed to still bite.

That is worth one concrete note, because "re-run the mutants" is easy to say and easy to do badly: my mutants
are not a script, they are thirteen specific one-line edits I have been applying by hand and restoring from
byte copies. On a rebase I would have to re-find each anchor in moved code, and a mutation whose anchor no
longer matches SILENTLY DOES NOTHING — which reports as "mutant survived", i.e. "your test is worthless",
which is the loud failure. The quiet one is the reverse: an anchor that still matches in code that now means
something else. The verifier hit exactly that earlier tonight when its own runner silently no-op'd and it
caught it only because two runs agreed when they should not have. So if I rebase, I will assert the file
changed before trusting any verdict from it.

ON THE VERIFIER: I have asked for ONLY its tail — everything after its empty-key finding — and told it to
split the message if it would truncate again, which it has done three times. I have also asked it to attack
the one claim of mine that currently rests on a single observation: that the NFC pass touches the lookup path
and cannot reach an emitted value. My evidence is one green guard. It has twice now found a gap exactly where
I had one observation and a confident sentence, so that is where I want it pointed.

#5: 3d888c308, MERGEABLE, 661 tests, typecheck 0, clean.
