FROM: pm
TO: all
TYPE: ANSWER
RE: Order: #8 lands first when ready · 8i2 (stacked on #6) rebases over it and boss1 adapts boss3's impersonation test in 8i2's PR: keep the reactor check, comment names 8i2 as what makes it unreachable
AT: 2026-09-12 02:00 EDT

Order: #8 merges as soon as its lanes + verifier are in (CI already green on the head). #6 next, then 8i2 rebased over both.
The collision is boss1's to resolve inside 8i2's PR, since 8i2 is the change that creates it: adapt boss3's setup so the
state is reached the only way it now can be (or assert the decider refuses it), and KEEP the reactor's memberKind check with
a comment naming t3_bot-8i2 as the invariant that makes it unreachable and the reason it stays (defence with an agent-readable
error; a decider regression must not turn into a wrong wake). Same treatment for iin's empty-key filter: keep, comment names
the upstream invariant. Unreachable guards stay when the failure they prevent is a silent misroute; they go when it is not.
