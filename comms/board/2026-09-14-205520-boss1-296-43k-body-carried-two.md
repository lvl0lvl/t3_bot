FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-43k · writing the body found TWO MORE instances of the same defect — in the PR BODY itself, carried since before the audit · nobody had re-read the body against what the lanes taught us · body is the document you merge off
AT: 2026-09-14 20:55 EDT

Reporting this because it is the same class and because you read the body to decide the merge.

Drafting the final body, I found the body ITSELF carrying two false claims — both of them claims the
code had already stopped making:

1. The opening paragraph still read "A fresh worktree has every file at one link; a `setupCommand`
   that runs `ln`, or `--in-place` on a `cp -al` / `rsync --link-dest` checkout, does not."
   That is verbatim the sentence the bugs lane falsified with 143 files, and I corrected it in THREE
   source sites in b0782a702 — while the body kept saying it.

2. "What changed" still read "A directory's link count is its entry count, so only a `File` is
   judged." That is the FIRST version of the directory claim — the one 9e05357e3 had already
   replaced for being wrong on APFS and ext4, before tonight's audit replaced THAT for being a false
   universal. The body was two corrections behind the source.

Both are fixed in the draft. Neither was caught by anything we ran, and the reason is structural
rather than careless: the claim audit, all four lanes, and the deletion commit were every one of them
SCOPED TO THE SOURCE FILES. The body is not a source file, so nothing pointed at it — and it is the
document the merge decision is actually read off. A PR body is the one artifact that describes the
code without being checked against the code by any gate we have.

That is the audit-scope lesson twice over. First version: an audit before the fix does not cover the
fix. Second version, and it is the sharper one: an audit scoped to the tree does not cover the
document ABOUT the tree, and the document is what a reader trusts when they do not read the tree.

I am not proposing a new gate — we have added enough tonight and I would rather say the problem than
bolt on machinery at 21:00. But if you want one line in AGENTS.md from this, it is: when a claim in
source is corrected, grep the PR body for the same claim before pushing. It is one command and it
would have caught both.

BODY IS DRAFTED, 115 lines, holding only the three unfilled gate lines. It carries the gates
checklist, the review record with every lane's INPUT named beside its findings, a table of all EIGHT
falsified claims with the input that falsifies each, the dated facts the deletions moved out of
source (145/143/2 with both exceptions named, 98 rows over 15 repo-unique paths all at one link, the
APFS series), and three method notes each bought by an error in this PR.

STATE: gate and re-sweep both still running. I had not armed a watcher on the re-sweep — caught it
when the old monitor expired and armed one. Worth noting only because an unwatched long job is the
same shape as tonight's silent lane: no positive signal either way.
