FROM: boss1
TO: pm
TYPE: REPORT
RE: PR ready #2 · pipeline complete · gate caught a worker-fiber kill I introduced · 3 beads filed
AT: 2026-09-11 19:15 EDT

PR READY #2 — https://github.com/lvl0lvl/t3_bot/pull/2 · head d46a978a8 · base main (rebased, 1 commit behind + 2 ahead)
t3_bot-l8i · pipeline COMPLETE · 3 beads filed, none blocking this PR.

PIPELINE, every phase actually run and not inferred:
- 7 review lanes on Opus 5, all COMPLETED (the earlier fable run is dead and reported as NOT RUN, not as clean).
- Phase 1b mechanical design floor: PASS on all 6 files via the explicit-file-args workaround, and
  CONTROL-PROBED — a scratch CSS with a planted antipattern drove the same binary to
  "FAIL — 1 blocking design finding(s): overused-font", exit 1. The green is earned, not inert.
- Phase 3 blind triage, profile-less, executed every deciding check: 4 LEGITIMATE, 1 NOT_A_BUG, 2 DEFERRED.
- Phase 4/6 test gate: baseline contracts 60/60 + server 36/36, post-fix IDENTICAL. No regression.
- Phase 7 blind verification, executed against the REAL engine, plus the adversarial pass.

WHAT THE GATE CAUGHT THAT I DID NOT — the whole justification for the merge gate:
My change threw from inside processEnvelope's synchronous prologue, outside the Effect.exit boundary. The
engine runs ONE unsupervised worker fiber and dispatch awaits an untimed Deferred, so a throw there killed
the fiber and hung every later command on the environment forever, silently. Three lanes found it
independently; bug2 reproduced it end to end. All my own checks were green.
FIXED: the default returns null, the call site rejects that one command with OrchestrationCommandInvariantError
(already a member of OrchestrationDispatchError, and the resolution decider.ts already uses — 0 of 3 existing
`satisfies never` sites in this repo throw, which is what HIST-1 caught and my PR body had claimed I matched).
VERIFIED against the real OrchestrationEngine over in-memory sqlite:
  item2_unroutable -> Exit Failure, Fail(OrchestrationCommandInvariantError) — a typed Fail, not a Die
  item3_good_after -> Exit Success, sequence 2 — the worker survives
  pre-fix control on the same harness -> items 2 AND 3 both hang, reproducing the original defect exactly.

ALSO FIXED: the id-union assertion could not fail (all entity ids are the same branded TrimmedNonEmptyString,
so decoding one proves nothing). Replaced with brand assignments. Mutation B — reverting the widening — now
fails the build with TS2322 where it previously left 60/60 green. I checked the tripwire is live rather than
assuming: ci.yml:56 runs `vpr typecheck` -> `vp run -r typecheck`, recursive across all packages including
contracts. A guard that only fires in a gate nobody runs is the inert-green class we spent the evening on.
ALSO FIXED: the doc comment, which overstated what the compiler catches.

GUARD ATTACKED AND HELD: adding an unrouted command to the union still fails the build — TS1360 at
OrchestrationEngine.ts:135 AND at decider.ts:2021. Returning null instead of throwing did NOT trade the
compile-time guarantee for the runtime fix; both are intact. Valid-command path confirmed unchanged by RUNNING
one: metrics, receipt, read model, span all present.

THE ADVERSARIAL PASS FOUND TWO REAL GAPS — stating them because an empty adversarial result must never be
inferred from silence, and these are not nothing:
The unrouted path emits NO metrics and writes NO rejected receipt (both observed, not reasoned). Filed as
t3_bot-l2m (P3). It is a narrow REGRESSION against main — main's catch-all misroutes the command to thread,
which means main DID meter it and DID write a rejected receipt via the decider. It is nevertheless a bead and
not a blocker, because on main that path is REACHABLE (a threadId-bearing channel command compiles straight
into the catch-all — the exact defect this PR closes) and on this branch it cannot compile at all. The gap is
real but sits on a path strictly harder to reach than the one it replaced, and the caller still gets a typed
failure.

A CORRECTION I OWE THE RECORD: the verifier's first framing of that comparison was wrong — it reported that
main has no `default:` case and would TypeError. Main does have one; I checked with `git show` rather than
taking it. Its bead-not-blocker conclusion survives, but for a different reason than it gave. To its credit it
had labelled that step as an inference it had not executed, which is why it was catchable in one command. That
is the third confident claim about main tonight that turned out wrong — two of them mine.

BEADS FILED, NONE BLOCKING #2:
  t3_bot-zix  P1  BLOCKS yyd — channel events are a replay one-way door: a pre-channel binary rejects
                  aggregateKind "channel", event-store replay decodes fail-fast, one row fails a whole
                  environment bootstrap. Must be decided (capability negotiation vs documented one-way door)
                  BEFORE the first channel event is ever persisted. Cannot fire from this PR.
  t3_bot-eas  P2  routing CORRECTNESS has no test — `satisfies never` is exhaustiveness only. Moving
                  thread.pull-request.sync into the project branch is typecheck exit 0 AND 54/54 green.
  t3_bot-l2m  P3  the metrics/receipt gap above.

pm — merge when you are ready. On your 182915 queue I go to yyd next, which inherits your 184735 contract
rulings (typed gateway errors, getPost, ordering spec, authorRef, decider-side write invariant) and is blocked
by t3_bot-zix, so my first move there is that decision, not code.
