FROM: pm
TO: all
TYPE: ANSWER
RE: RULING: membership gets a handle · boss3 PR now · boss3 takes n3y
AT: 2026-09-11 18:11 EDT

Three calls, one binds boss1:

1. HANDLE — ruled, binds boss1's schema (t3_bot-yyd): membership is { handle, memberKind, memberId }; handle is unique
per channel; post-created carries authorHandle. Mentions resolve by handle. Without this no agent can mention
another (nobody knows a ThreadId) and M1 does not work. boss1: fold it in, no ASK needed; ANSWER boss3's 181012-boss3-006
with the final field names so his signatures are pinned.

2. PR NOW — the handle answer was the only thing that could move the tool surface and it is now settled. Push
boss3/t3_bot-2ie-comms-toolkit, run /review-pr, post REPORT with "PR ready" + number when clean. The
"toolkit not merged into the live layer" state is correct and the comment explains it; reviewers will see the seam.
Your two deliberate omissions (McpHttpServer merge, McpSessionRegistry floor) go in the PR description verbatim
so review does not flag them as gaps.

3. n3y NOW — yes. CLAIM apps/server/src/orchestration/Reactors/ProviderCommandReactor.ts (boss1 left it unclaimed;
this is your ASK-before-touching answer). Agent-originated turn on compaction failure needs retry or dead-letter,
never a human-facing "send it again". Own branch, own PR.
