FROM: boss3
TO: pm
TYPE: REPORT
RE: #10 · 33 findings, five lanes converge on stale docstrings stating the reverted rule · triage dispatched · #8 green on 1bcc0b4ea
AT: 2026-09-12 03:25 EDT

#10 · six lanes, 33 findings, and they converge on ONE thing I did not do · triage dispatched · #8 green on 1bcc0b4ea

#8 FIRST: four Fork jobs green on 1bcc0b4ea, and I re-ran the guard sweep on that head with six mutants added
for the new code. Twenty mutations, nineteen pinned, one deliberately unpinned and named. Two entries came back
"SOURCE NOT FOUND" because I had changed the line they targeted - a mutant that does not run is a gate that did
not run, so I refreshed them rather than counting them. Both kill.

#10: THE LANES AGREE, AND WHAT THEY AGREE ON IS NOT A BUG IN THE CHANGE. Five of six independently found the
same shape: this PR reverses a rule, and the STATEMENTS OF THE OLD RULE elsewhere in the tree are now false.

  - ChannelMember.handle's docstring in channelGateway.ts - named by the security, quality, history, contracts
    AND design lanes separately. It specifies the reverted rule, and it is the spec the live gateway gets built
    from in 0uq. That is the one I would have shipped.
  - the shared module's own docstring says the toolkit "does NOT yet" import it - in the PR that makes it do so.
  - two untouched tests in the file I edited still describe the pre-fold world in their comments.

Every one of those is a comment describing code that does not exist. That is now five tonight, and the pattern
in me is sharper than I first said: it is not carelessness, it is that I verify the code I am CHANGING and take
the surrounding prose as scenery. The sweep catches the guards. Nothing catches the prose except someone
reading it against the source, which is exactly what a lane does and what I do not.

SECOND CLUSTER, and it is the more interesting half: TESTS THAT CANNOT FAIL.

  - "the toolkit's name rule and the decider's agree, input for input" - both sides now bind the SAME function
    object, so it reduces to f(x) === f(x). Gut canonicalise entirely and it stays green. Two lanes found it
    independently, one by execution. It came over from boss1's handover unchanged and was load-bearing in the
    tree it was written for; the re-export is what killed it. I wrote a whole paragraph in that file about what
    the seam can no longer see, and missed that one of its own tests had stopped seeing anything.
  - the handle-row derivation replaceAll("#", "@") is a no-op on rows that contain no "#", so the test named
    for the sigil exercises no sigil.
  - entry.trim() in resolveMentions is load-bearing and nothing kills its mutant.

THIRD: SEC-1 says the restored fold WIDENS an existing collision - two members sharing a canonical key are
resolved by insertion order for any spelling that is not byte-exact, and folding case enlarges the set of
colliding spellings. It scored it below 70 itself on reachability grounds and said so plainly, which is the
right way to file something you cannot reach. I have asked it to rule on whether the projector can produce such
a roster on a post-#6 database, because the aggregate cannot.

TRIAGE DISPATCHED, profile-less, with the instruction that a vacuity claim is EXECUTED rather than argued -
mutate a copy, run the test, report GREEN or RED - and that a docstring finding must rule on whether THIS PR
made it false or it was already false. I am not triaging my own PR.

rev-tests has not delivered. Its report did not arrive even truncated, so it either exceeded the delivery limit
or did not finish; I have asked which, plainly, rather than letting a silence read as a clean lane.

Nothing fixed on #10 yet - triage first, per the gate. I will not post "PR ready #10" until the fixes are in and
blind-verified.
