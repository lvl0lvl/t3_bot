FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-2oh · PR READY #27 · six lanes' findings fixed, sweep 18/18 exit 0, gate exit 0, CI green
AT: 2026-09-12 17:49 EDT

t3_bot-2oh · #27 PR READY · all six lanes' findings fixed, sweep 18/18 exit 0, CI green, gate measured · merge when you are ready

WHAT LANDED SINCE THE LANES. The refusal message was false and rewording it was not enough:
`decodeChannelCursor` returned one `Option.none` for three causes, so the toolkit served the sentence
written for the first of them. It returns `Result<number, ChannelCursorRefusal>` now — "malformed",
"channel", "direction", set at the branch that fired — the gateway carries the word, and the message is
a `Record` over the union so a fourth reason is a compile error rather than an inherited sentence. The
client door drops the reason on purpose: `ChannelCursorRejected` crosses the wire as a contract error,
and widening it is a change to the browser's error rather than to this cursor.

THE LEGACY CURSOR GETS THE SENTENCE INSTEAD OF THE REGEX. Measured before believing the lane, through
the live toolkit: an `AiError` quoting `CURSOR_PATTERN`, outside the `CommsToolError` union the tool
declares, no recovery, nothing saying the posts are still there — for the one wrong cursor most likely
to arrive. The direction segment is optional in the schema and required in the decoder: the decoder
decides what is HONOURED, the schema decides which door a refusal comes out of.

GUARD SWEEP, the Phase 1b step #17 added, with its own config checked in
(`scripts/guard-sweep.channel-cursor.json`): 18 mutations, six guards, both axes wherever both exist,
EXIT 0, baseline 110 tests 0 already failing. Its first run found a survivor that was MINE — a
`boundary === -1` check no input can reach, brute-forced over 375 inputs with zero disagreement between
the two versions. Deleted, with its row: a mutation that cannot be reached is not a gap in the suite.

COUNT GATE, full 13 workspaces against origin/main, with one allowed rename:

  file                                                              base  head
  apps/server/src/mcp/toolkits/comms/commsLive.integration.test.ts    19    21
  apps/server/src/mcp/toolkits/comms/handlers.test.ts                 45    52
  apps/server/src/orchestration/channelCursor.test.ts                 10    14
                                                                       - channelCursor refuses a cursor another channel issued
  ALLOWED  channelCursor.test.ts: renamed to "... AS a channel refusal": the refusal now carries
           which of three causes fired, and a test that only asserts THAT it refused cannot see
           the wrong one (t3_bot-2oh)

  Measured 13 workspace(s) against origin/main: 1 lost name(s), each explained by --allow above.
  EXIT=0

Measured with #28's per-workspace gate at 0081f1360, because the gate on main CANNOT MEASURE this PR:
it exits 2 on apps/web/src/terminal/ghostty/core.test.ts, which passes 13/13 inside apps/web and fails
to load under the root project. That is x4v, the PR behind this one. Both outcomes are in the body —
the merged gate refused to measure, and the replacement measured.

THE RENAME IS THE GATE DOING ITS JOB ON ME. `channelCursor.test.ts` went 10 -> 14 — the count went UP —
while `refuses a cursor another channel issued` disappeared, because I renamed it to
`..., AS a channel refusal`. Only the NAME half could see that, which is the whole argument for the name
half, and it is the second time this week the gate has caught its own author.

THREE CLAIMS OF MINE THAT WERE FALSE, all corrected in place rather than quietly dropped: "with four
posts the lie is invisible" (re-measured — four posts kills the mutant; six is for the paging half),
"the tool refuses a legacy cursor at the schema and that is the right door" (measured, it is not), and
my "110 tests green" scope, which is what let Fork CI find a regression I had shipped — the same
defanging, a third file out, at both client doors.

CI GREEN on the rebased head. 110 tests across the codec, the paged read and comms; 4745/4757 in
apps/server (the twelve are the symlink/provider set this repo fails on a developer machine, none of
them touching channels or cursors); 4612 in apps/web. Typecheck 0. PR body rewritten to match, including
the two corrections above.

#28 is next: code complete, CI green, body corrected and published. Its gate run starts when this one's
machine is free.
