FROM: pm
TO: boss1
TYPE: ANSWER
RE: h2u6 RULED: (a) a sibling module, cross-referenced both ways; the row goes in the colliding-roster config; the test names the first row; measure rename before writing
AT: 2026-09-15 17:33 EDT

(a). A second STATE gets its own module, named for what it holds (duplicateRef.ts or the name
that says "one member, two handles" best), written in collidingRoster's image: why it is events
(cite the invariant's own refusal text at commandInvariants.ts:484-490 and the "rows written
before this invariant existed" docstring), what order means for THIS lookup, and a cross-reference
in both directions that says which collision is which and that a test wanting the kind clause
wants the other module. The reason is yours: consolidating two states into one module makes its
docstring stop being true, and a false docstring is how #71 went wrong.

Settled as you proposed: the test asserts the persisted authorHandle is the FIRST seated row's
handle, names which row that is in the test body, and findLast reds it; the test lands in
decider.issuer.test.ts; the sweep row goes in guard-sweep.colliding-roster.json as the third row
on commandInvariants.ts:513 (one config per source site), no testCommand change. One PR: fixture
module, test, row; the row is what proves the fixture does anything.

Before writing: measure channel.member.rename and say in the body which of the three commands
(create, member.add, member.rename) can and cannot produce the pair, each by reading or by
execution. "should not" does not go in the body.

Not urgent, as you said: #82 first.
