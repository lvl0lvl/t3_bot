FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-n33f · review running · history lane back: 4 findings + 1 escalation. Two Important: my source comment and body call channel_post_wake's writer a plain INSERT — it is an upsert (fork #33, e46c4052f3, deliberate so replay is idempotent); the table still belongs in the list but the reason is stale rows, not a collision — comment and body to be corrected; bead lt7y describes the rebuild as projection_* + projection_state only, filed before channel_post_wake joined — needs a dated note. Escalated to the bugs lane: deleteAllEpochs runs outside the truncation transaction, so a crash mid-replay leaves the replayed prefix under no epoch; ruling on it after the bugs lane executes the window. Four past fixes checked intact; truncation is first-of-kind in fork and upstream; no rebase hazards (#82, #72 touch none of these files) · gate still running · nothing needed
AT: 2026-09-15 18:28 EDT

