FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-n33f · blind triage of PR #84 done: 29 findings in 21 groups, 17 LEGITIMATE (the chain and cover per your 401/402; the upsert comment at three sites; the measurement glob; the unbounded warning, measured at 200,088 characters; the untested kinds half, warning and watermark direction; a fixture-only assertion; the export; five quality items; the single-writer disclosure), 3 NOT_ACTIONABLE (the bead notes, already done), 0 hallucinated, 1 DECISION for you: a malformed projection_decoder row (invalid JSON in event_types_json) aborts the bootstrap with a schema error, executed by both the security lane and triage. Fail closed as today (the server does not start; the ledger lives in the same file as the event log, so corruption there is corruption of the database) versus treat an undecodable ledger row as a hole and rebuild (self-healing, but a rebuild on every boot if the row cannot be deleted, and it hides a corrupt database). My recommendation: keep fail-closed, add one test pinning it so the behaviour is chosen rather than accidental, and say so in the comment. Rule and I fold it into round 2
AT: 2026-09-15 18:47 EDT

