FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-n33f · ASK the shape · detect = at bootstrap, compare this build's decodable type/kind lists with the lists the LAST bootstrap stored (a new one-row table), and scan the log below the watermark for rows of any type this build newly knows; on a hit, rebuild ALL projectors from 0 by the fresh-install path (truncate the projection_* tables, delete projection_state, bootstrap) · the store's read path stays a pure read · falsifying test from 356's fixture · need your ruling (an alternative that writes a skip record from the read path is stated with its cost)
AT: 2026-09-15 17:55 EDT

Read at 49e0ba1425 (main): ProjectionPipeline.ts bootstrap (:2226-2290) resumes each projector from projection_state.last_applied_sequence (:2174-2176) and the watermark is written per APPLIED event (:2159, :2201); the store's reader skips an unknown type or kind with a warning only (OrchestrationEventStore.ts:102-113) — nothing durable records a skip. Thirteen projection_* tables exist (migrations, listed by rg); mention-wake keeps its own cursor tables (052). No index on orchestration_events.event_type.

WHAT "DETECT" READS — two candidates:
(A) RECOMMENDED · a decoder ledger, no write in the read path. New table `projection_decoder` (one row: event_types_json, aggregate_kinds_json, recorded_at) written by bootstrap AFTER a successful replay with this build's `OrchestrationEventType` literals and `OrchestrationAggregateKind` literals (both derivable from the schemas' AST, as #79's equality test does). At the next bootstrap, `newTypes = this build's types − stored types` (same for kinds); if newTypes is non-empty, one query: `SELECT sequence FROM orchestration_events WHERE sequence <= :minWatermark AND (event_type IN newTypes OR aggregate_kind IN newKinds) LIMIT 1`. A hit is a hole: a row below the watermark that the build which set the watermark could not have applied. Cost: one row write per bootstrap; one scan per bootstrap only when the union grew (rare: a deploy); the scan is a full-table read without an index — bounded by the log size, once per upgrade; an index on event_type is a separate decision I do not propose. It also catches holes the old build never logged (a crashed process, a log rotated away) because it reads the log, not a trace.
(B) a skip record written by the reader. `INSERT OR IGNORE INTO orchestration_event_skips(sequence, event_type, aggregate_kind)` inside decodeRowsSkippingUnknownTypes; bootstrap reads it and filters to rows this build decodes. Cost: readFromSequence becomes a read that writes, on every reader (bootstrap, cleanup, engine reconcile, client readEvents, mention-wake), one insert per skipped row per read; and it misses nothing only if every skip was recorded. Not recommended.

WHERE IT RUNS: bootstrap, before the per-projector replay loop (ProjectionPipeline.ts:2242): read the ledger; if the union grew and the scan hits, rebuild; then replay as today; then write the ledger with this build's lists. First boot on a database with no ledger row: no scan (nothing to compare), write the row — the ledger starts from the first upgraded build, so a database written by TODAY's main and upgraded later is covered; holes made by builds older than the ledger are not detectable and I say so (DISCLOSED in the comment).

WHAT IT DOES ON A HOLE: rebuild ALL projectors from 0 by the fresh-install path: in one transaction, DELETE FROM each projection_* table and projection_state (the thirteen, minus none), then the normal bootstrap replays from 0 — the same code a new database runs, which is the tested path. Not "the affected aggregate only": projectors apply whole events across aggregates and only the thread projectors are self-cleaning on `thread.created` (:1212); a partial rebuild would need a per-aggregate apply that does not exist, and the hole's blast radius is the state-removing classes (project.deleted, thread.archived, channel.member-removed) where a partial rebuild could leave a stale row standing. One full replay on the first start after an upgrade is the cost; it logs one line naming the sequence and type that triggered it. The mention-wake cursor and the engine's reconcile are NOT rebuilt (their cursors are not projections; a follow-up bead if you want the mention-wake cursor rewound).

THE FALSIFYING TEST (356's fixture, driven through the real store and pipeline in a fresh SqlitePersistenceMemory): append a known event (1); insert a raw row with event_type `project.future-event` (2); append a known `thread.created` (3); bootstrap → every projector at watermark 3, the ledger holds this build's lists, the thread from 3 projected, nothing from 2. Then stand in for "the newer build": UPDATE row 2's event_type to a known type with a valid payload (say `thread.created` for a second thread) AND UPDATE the ledger row to remove that type from its stored list — the row is now one this build decodes and the ledger says the build that set the watermark could not. Bootstrap again → the scan hits sequence 2 → rebuild → the second thread is projected (assert the projection row exists and every projector's watermark is 3 again). Mutants: the scan removed (bootstrap resumes from 3) → red, the thread from 2 absent; the ledger write removed → the second bootstrap sees no stored list and does not scan → red; the rebuild truncation removed (watermarks reset to 0 but tables kept) → a duplicate-row or stale-row assertion red where a projector is not idempotent, or DISCLOSED if every projector proves idempotent under replay. Control: with row 2 left unknown, the second bootstrap does not rebuild (no ledger delta) — asserts the rebuild is not a per-boot cost.

RISKIEST CLAIMS, named for the reviewer: (1) truncating the thirteen tables and replaying from 0 equals a fresh install (the list is measured from the migrations, and a projector that writes outside projection_* would break it — the SE greps every projector's repository for its table); (2) the scan's cost without an index is acceptable once per upgrade; (3) the ledger row's absence on first boot is safe to treat as "no comparison".
