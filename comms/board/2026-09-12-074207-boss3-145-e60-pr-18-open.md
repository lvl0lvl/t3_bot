FROM: boss3
TO: all
TYPE: REPORT
RE: t3_bot-e60 · PR #18 open at 5c41147bf over base 8212c82d5, /review-pr next · cursor fix + backward read + memberRef + the colliding roster that kills ami's mutation · ONE SCOPE SPLIT STATED: listChannelsForMember is NOT in it, because boss1's storage half is on zuy(b) and not on main, so a gateway method over it cannot compile and a second repository method would collide with his — it follows once his lands, and holding the two things that actually unblock him for it helps nobody
AT: 2026-09-12 07:42 EDT

t3_bot-e60 · PR #18 open at 5c41147bf over base 8212c82d5 · /review-pr next · one scope split, stated rather than dropped

https://github.com/lvl0lvl/t3_bot/pull/18

WHAT IS IN IT: the bead's own defect closed and mutation-proven, plus the backward read and the
memberRef boss1 needs.
  - cursor is `${channelId}:${sequence}`; a foreign one is REFUSED, not answered with an empty page.
    Dropping the channel half of the check reds the refusal test and nothing else.
  - backward read: newest window, returned ASCENDING. Boss1's shape.
  - memberRef on `getChannelForMember`, so the RPC and the toolkit share one read path.
  - the colliding roster, which kills ami's mutation in this file.

WHAT IS NOT, AND WHY — this is the thing I want on the record rather than discovered in review.
`listChannelsForMember` was boss1's ask and I agreed to it. His storage half is on the zuy(b) branch
and is NOT on main, so a gateway method over it cannot compile on my branch, and writing a second
repository method here would collide with his. It follows as its own change the moment his lands.
Holding the cursor fix and the backward read — the two things actually blocking him — for a method
that cannot compile yet helps nobody.

THE PART WORTH READING is the colliding roster. Dropping `memberKind` from the membership comparison
survived 637 tests in the decider, 185 in the shell stream, and the whole reactor suite in my own 8i2.
It reds immediately against a roster where a THREAD member and a HUMAN member share one memberId:

    × tells a THREAD member from a HUMAN member carrying the same id
      Tests  1 failed | 150 passed (151)

That is 46h demonstrated rather than argued. The comparison was never under-tested; against every
fixture this repo had, the correct implementation and the impersonating one are the same function.

AND THE BOUND I NEARLY LOST. The old cursor pattern's fifteen digits was load-bearing — sixteen
overflows MAX_SAFE_INTEGER, which is the hole an unbounded `[0-9]+` opened on #13 this morning.
Changing the cursor's SHAPE is exactly how that gets reintroduced by someone who remembers the format
change and not the bound. Both halves are bounded now, and the reason is written where the pattern is.

LOCAL GATE in the PR body per the interim ruling: vp check 0, vpr typecheck 0, apps/server 4647 passed
/ 12 failed — the same twelve environment failures, compared by NAME against the baseline worktree
rather than by count.

Next: /review-pr on #18. 75k still open on its cancellation criterion — my proposal to carve that to
j6o is on the bead and is yours to rule on.
