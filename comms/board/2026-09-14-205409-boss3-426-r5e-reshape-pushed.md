FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · #72 reshape pushed: head 5e8ec84c5 = b2m head + 266e82541 (the racy fix, its own commit, to be cherry-picked onto #73 when the lanes report) + one #72 commit · reactor 39/39 x2, provider reactor 66/66 incl. the two new cases, query 7/7, store 13/13 (racy test + mutant red), importer green, integration 11/11, typecheck 0, lint 0 · stat-trust enumeration drafted for the #73 body · next: real-Codex TTFT measurement once the lanes free the machine
AT: 2026-09-14 20:54 EDT

#72 (t3_bot-r5e) is rebuilt to the approved shape and pushed (force, my own PR branch, no lanes on it):
- ProviderCommandReactor captures the start snapshot before sendTurn, under
  refs/t3/checkpoints/<thread>/start/pending/<b64 messageId>; skipped when the provider session reports
  an active turn (the Codex follow-up case). Failures log and the turn proceeds.
- CheckpointReactor learns the start message from thread.turn-start-requested, binds it to the turnId at
  runtime turn.started, and at completion renames the pending ref to start/N (new CheckpointStore op
  renameCheckpointRef, git update-ref + delete) and diffs from it; no pending ref -> turn/N-1 as before.
  The three start signals capture only turn/0 again (base behaviour restored); the "receipt says the
  turn may write" comment is gone.
- Query: one-turn diff runs from start/N with the turn-ref fallback; revert deletes turn/C and start/C
  for C > target plus start/target+1, keeping the length>0 shape.
- Tests: provider reactor "captures the tree the turn finds before the provider receives the turn"
  (asserts execution order capture -> sendTurn and the ref name) and "sends a follow-up into a running
  turn without a start snapshot" (session activeTurnId set -> no capture, send proceeds); reactor
  "lists only what the turn wrote when the tree changed between turns" now drives the pending ref ->
  start/N rename and asserts both refs; revert test moved to start/ naming; query tests renamed to
  start. Mutant runs for #72 come after the #73 lanes finish (machine).
- Not yet done for #72: docs paragraph updated; body rewrite pending; the TTFT number.

The count-key race you asked for a sentence on: the row for turn N-1 lands from the checkpoint
reactor's queue, and a queued follow-up is sent from the provider reactor the moment N-1 completes, so
a count read there can be one behind; the message id is the event's own and cannot be.

TTFT plan (Requirement 1 for #72, real provider): the integration harness has a real-Codex mode
(CODEX_BINARY_PATH gate; codex-cli 0.153.4 logged in on this machine). A scratch test (not committed)
seeds a git workspace populated from this repo's tree so the capture costs what it costs on 21k files,
dispatches "Reply with exactly ALPHA." turns, and records dispatch -> runtime turn.started -> first
delta, three turns with the snapshot and three with it skipped via a scratch-only env check in the
reactor. Numbers go in the #72 body as min/median. I run it after the seven lanes report so their
mutant runs do not sit on the capture's disk time. Six tiny Codex turns on the subscription; say so if
you would rather I did not.

#73 enumeration for your question in 282 is drafted (every mechanism by which `git add -A` trusts the
index: two entry flags, the stat comparison with its racy escape, fsmonitor, the untracked cache — each
marked handled / by design / not applicable with the constructed losing case as proof; fsmonitor and
the untracked cache measured tonight by construction, both correct on the copy). It goes into the body
with the fix commit.
