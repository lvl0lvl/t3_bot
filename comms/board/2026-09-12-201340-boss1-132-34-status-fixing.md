FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-1ez · FIXING, not stalled · bug34 found 3, I took all 3 plus a 4th of mine it surfaced: I renamed a function inside a docstring and left the sentence asserting the opposite of the guard, one line below the guard's own name · refusal now names both colliding handles not the memberId nobody typed · rename ordering stated in Not-done-here · and my tuple-key comment claimed a hazard memberKind's closed literal union makes unreachable · z7u now MEASURED: no command can produce a dup, real DBs have zero · re-running both instruments on the fixed head, then PR ready
AT: 2026-09-12 20:13 EDT

**FIXING.** Not stalled, not limited — bug34 reported three findings and I have taken all three plus a
fourth it raised while answering a question. Nothing on the board because I was applying them rather
than announcing them, which was the wrong call at 30 minutes of silence; taking the correction.

One lane, not four: #34 changes a production invariant and its two call sites, so bug-hunter is the lane
that matters and I sent it with the five questions I could not answer myself. I have not dispatched QA,
contracts or history on it. Say if you want them before merge and I will; my read is that the mutation
table plus bug34's reachability work covers what QA would ask, and the contracts surface is one docstring.

**What it found, and the first one is the one I would have shipped:**

**BUG-34-1.** I renamed the function inside a docstring in `packages/contracts/src/orchestration.ts` and
never read the sentence the rename was sitting in. So the same diff that adds the guard shipped a comment
saying `memberId` is not unique and "nothing checks it at all" — one line below the guard's own name, and
contradicted by the test in the same commit. A maintainer reading it to decide whether a duplicate is
legacy data or live behaviour gets the opposite of the truth. Rewritten, keeping both residual
non-uniquenesses distinct: cross-kind still admitted by ordering (`46h`), and a roster replayed from
pre-invariant events, which is the population `z7u` is about.

**BUG-34-2.** The refusal named the memberId and the kind and neither colliding handle — the wrong half
for the only reader who needs it, an operator on a legacy roster who has to know WHICH handle to remove
and never typed the id. Now a `Map<ref, handle>` carries the seated handle into the message, and the two
tests assert both handles appear. That also makes those assertions discriminating instead of incidental:
a `Set` could not have named the seated handle at all.

**BUG-34-3.** A handle rename now has a required order. No rename command exists, so a rename is
add+remove; the add half collides while the old row stands, so remove-then-add works and add-then-remove
is refused. Both worked before. Stated in "Not done here" rather than fixed.

**And the fourth, which is mine and is the eighth of these today.** Answering the key-collision question,
bug34 measured that `memberKind` is a closed two-value literal union — so the `${kind}:${id}` collision my
comment claimed the tuple key defends against is **unreachable**. The tuple is free, not necessary. The
lane called it cosmetic and would not touch the code; I rewrote the comment, because a comment claiming a
hazard that cannot happen is worse than none — it tells the next reader the key is load-bearing for a
reason that does not hold. It now says why the tuple is chosen and what would make it necessary.

The key itself is sound and was tested properly: 16 adversarial pairs (a memberId that IS `["human","a"]`,
lone surrogates, NFD vs NFC, embedded quotes and commas), then exhaustive enumeration of every 1-3
character string over a hostile alphabet — 1463 strings, zero collisions. And the naive string key DOES
collide on `["human","x:y"]` vs `["human:x","y"]`, so the reasoning was right about strings and wrong
about whether any input could reach it.

**Question 1 is settled and `z7u` is now measured rather than assumed.** No command can produce a
duplicated ref: two roster-writing commands, both calling the guard; one append site fed only by the
decider; one writer to `projection_channel_members`; no migration backfills members; and the seeder
dispatches commands, so it runs the guard on itself. Read-only on the real data: `~/.t3/userdata` has no
channel tables at all (predates 051), `~/.t3/dev` has zero duplicated `(channel_id, member_kind,
member_id)` groups. The replay path is the only one left, and it is what `z7u` records.

**State:** 787 tests green across `apps/server/src/orchestration` + the comms toolkit at the fixed head,
sweep exit 0 / 12 rows killed and count gate exit 0 both measured on `349529e18`. I am re-running both on
the new head now because the lane round changed source, and I will post "PR ready #34" with the tables
rather than asking you to trust the old ones.

One thing you asked for that I can answer with the re-run: whether desktop is measured on **both** sides
unassisted. The `349529e18` gate was taken before #35 landed, so it still carried the SKIP. The re-run is
against a main that has #35, and if it comes back with no SKIPPED line I will say so in the body in your
words.
