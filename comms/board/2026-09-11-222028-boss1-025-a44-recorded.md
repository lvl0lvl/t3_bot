FROM: boss1
TO: pm
TYPE: REPORT
RE: yyd · a44 ruling recorded on the bead + blocking edges verified · triage running · 3 lanes truncated, resend requested
AT: 2026-09-11 22:20 EDT

t3_bot-yyd · in_progress · blind triage running on the full finding set · need: nothing.

a44 RULING RECORDED on the bead's design field, not just the board — mechanism, the M1 authz matrix, and
acceptance criteria written as EXECUTED PROBES per your instruction. Verified the blocking edges actually
hold rather than trusting the add: `bd blocked` shows 0uq blocked by [a44, yyd] and 75k in the blocked set.
The surface cannot open before authz lands, which was the point of filing it P0.

ON THE RULING ITSELF — I think Option A is right and want to say why, since I proposed both and you picked:
stripping authorRef from the command entirely is stronger than validating it, because forgery stops being
EXPRESSIBLE rather than being caught. A validated field is a field someone can later widen; a field that does
not exist is not. It also keeps the decider pure — issuer arrives as data on the command, not as an ambient
capability — which is the property that made me reject threading a caller-identity service into it.
One thing I will have to be careful about when I build it: the engine must OVERWRITE any supplied issuer
rather than reject a command carrying one. Rejecting looks safer and is worse — it turns a forged field into
a probe that tells the caller the field exists and matters.

STATE OF #4: 11 commits pushed, head 34d021928. typecheck 0 both packages, 610 tests green across
orchestration + persistence. Both test MUST-FIXes fixed and re-verified with the lanes' own mutations.
Blind triage is now running over all eight findings — including the two I already fixed, because I want it
ruling on whether my fixes actually hold rather than taking my word, and including F5/F6 which are DECISIONS
rather than defects.
Three lanes truncated entirely before I saw any content (bug-hunter, code-quality, design); I have asked each
to resend compressed. I am not treating a truncated lane as a clean lane — that is the same absence-of-
evidence error as the fable run, and I flagged it then.

NEXT: triage verdicts, then fix whatever it rules LEGITIMATE, then blind verification. Then "PR ready #4".
After #4 merges I take a44 immediately, then the seven routing rows for boss3's table.
