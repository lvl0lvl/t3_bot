FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-7iw · in_progress · PR #37 review: 6/7 lanes back, 4 important (all fixable here), one CLAUDE.md clause is yours · needs: a one-clause CLAUDE.md edit from you, not this PR
AT: 2026-09-12 23:31 EDT

Since 234: bugs 0 PR findings (1 pre-existing test idiom -> bead); tests 1 IMPORTANT — a real survivor:
every fixture in my new test file holds ONE channel, so `channels.find` vs `channels.slice(0,1).find`
is indistinguishable (mutant survived 706/706; the lane proved it measurable with a two-channel engine
probe). Fix: a two-channel test + a sweep row. History 3: the reachability record #24 designated
(ChannelMemberRemovedPayload's docstring, rewritten by #34 an hour ago) still says "create thread X
(admitted)"; decider.channels' #34 comment says "7iw ... not decided here"; HUMAN_OPERATOR_MEMBER_ID's
docstring counts two callers and the guard is a third, against #18's "identity behind a function"
direction — architect escalation, contracts lane is on the same question.

FOR YOU (PM-owned file, not touched by #37): CLAUDE.md:102-106's worked example names "reachable by
ordering (add human X, create thread X, add thread X)" as the answer to FIND. After #37 that is the
stale answer and "the only route is replay" is the true one — the rule's own example becomes the
record it warns about. One clause fixes it: "(reachable by ordering until t3_bot-7iw; replay-only
since)". Also pre-existing: lines 84 and 101 are the same sentence twice. I am not editing CLAUDE.md
from a PR; it is yours.

NO documented ruling is reversed: #24 recorded the ordering as a fact, #29 and #34 deferred the "should"
to 7iw by name, #34's admit test (member.add still admits the pair) passes at head because the guard is
on thread.create.

Gate by the 3+ important rule: FAIL until fixed; all four are in this PR's reach (one test, three
comment sites). Waiting on contracts, then triage -> SE -> verify.
