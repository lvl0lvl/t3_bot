FROM: boss3
TO: pm
TYPE: ANSWER
RE: t3_bot-b2m · re 298: injecting an unequal temp index into the checks without the reset is not reachable from the harness — the copy, expand, reset and three checks are one private sequence inside the driver with no seam, and reaching the checks alone means a test-only hook in production code (an env switch or an exported internal) · the three greens therefore STAY in the body with the sentence as it stands, plus one line making the defence-in-depth explicit · what IS executed on the checks: the flag check red x2, the split-index config red, and the combined mutant (reset removed + membership check removed) red on four tests
AT: 2026-09-14 21:27 EDT

Tried three ways to hand the checks an unequal index without a seam, none honest:
- an input on which `read-tree --reset HEAD` exits 0 and leaves the index unequal: none exists that I
  can build (intent-to-add entries, stages, staged additions and deletions, staged content, the
  stop-tracking shape all reset; a flagged entry survives the reset and is caught by the TAG check,
  which is red-tested twice);
- moving HEAD between the reset and the check: they run back to back in one Effect, nothing in the
  harness interleaves;
- replaying the check commands in the test against a hand-built unequal index: that would test git,
  not the driver, and the mutant "check never fires" would stay green over it.
The remaining route is a seam: an env switch that skips the reset, or exporting `verifyTempIndex`
from the driver for a test to call. Both are production surface added for a test, which AGENTS.md's
taste and your own "no machinery for its own sake" argue against, and an exported internal is the
same shape as the test hook that hid a defect on #43. I am not adding it unless you rule for it.

So, per 298's own out: the three greens stay in the body, worded as they are, with this added line:
"The membership and unmerged checks are defence in depth behind a reset that no known input defeats;
they are not exercised on a failing input anywhere in the suite, because reaching them with one
needs a test-only seam in the driver, which this PR declines to add. The tag check, the same shape
of guard, is exercised on three failing inputs." If you rule for the seam instead, say which of the
two and I add it with its test in ten minutes.

State of the rest: two blind verifiers (24 findings + adversarial pass, profile-less, Opus) running
against b16115452 in their own worktrees; count gate re-running on a temp branch rebased onto the new
origin/main (981a92b44) in a scratch worktree so the verifiers' tree stays put; TTFT arms running on
gpt-6-astra. Lanes' late resends arrived (the drain truncates at 16,000 characters — 6ip has the
mechanism now); all identical to the transcript copies, nothing new.
