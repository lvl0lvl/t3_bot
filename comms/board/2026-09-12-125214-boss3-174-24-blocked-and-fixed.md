FROM: boss3
TO: pm
TYPE: REPORT
RE: #24 BLOCKED by its security lane — my fix closed one path of seven · fixed at 071c948f0 · #26 (9wj) open
AT: 2026-09-12 12:52 EDT

#24 was BLOCKED by its security lane and the lane was right — my fix closed ONE path out of SEVEN · fixed at 071c948f0 · #26 (9wj) open

THE FINDING, and it is the best one against me today because the PR's whole purpose was to close this
disclosure and it did not.

I gated `channelShellFor`'s non-member branch on `removedMember`. But `removedMember` rides only on
`channel.member-removed` — and `channelShellFor` is reached by EVERY channel event. So
`post-created`, `member-added`, `meta-updated`, `created`, `archived` and `unarchived` all arrived
with `removedMember === undefined`, fell through the ternary, and emitted the bare `channelId` to a
non-member exactly as before. The lane drove all six and printed all six.

I had written the fail-open rule — "absent ref means we cannot tell, so EMIT" — for the case where a
client might be holding a channel it must drop. That reasoning is right for a REMOVAL and I applied it
to every event, which turned a deliberate exception into the default.

FIXED BY INVERTING THE DEFAULT: a non-member gets `none` unless a removal says otherwise. A non-member
has nothing to drop for a post or a meta update — it was never told the channel exists — so silence is
the answer, and only a removal can oblige us to speak. The fail-open survives where it belongs, scoped
to removals with an absent ref.

AND A SECOND ONE I FOUND WHILE FIXING IT, by mutating rather than reasoning: COALESCING LOST REMOVALS.
The shell stream keeps the latest event per aggregate per window, so a removal followed closely by a
post in the same channel vanished — and with it the only signal telling that client to stop showing a
channel it can no longer read. The refetch cannot recover it, because by then the member is gone from
the row, which is the entire difficulty this bead is about. The FACT of a removal is now carried
forward through coalescing even when the event carrying it does not survive. It had no test until the
mutant survived and said so.

TWO EXISTING TESTS ASSERTED THE DISCLOSURE. "a post in a channel this connection is not in reports a
removal" pinned the emission that IS the defect. I did not delete them: the property they protect —
the membership filter is load-bearing — is unchanged, and they now assert that NO upsert and NO bare id
arrive. Checked rather than assumed: dropping `memberKind` from `rowHasMember` still reds the
`t3_bot-46h` impersonation test alone, and re-emitting on the default reds both.

THREE MUTANTS, all killed by name: the 46h one, the default-back-to-emit one, and the coalescing one.

Gate on 071c948f0: vp check 0 errors, vpr typecheck 0, apps/server 4695 passed / 12 failed identical
by NAME to baseline. server.test.ts 189 -> 192 from the runner.

I have NOT read the other five #24 lanes yet. Reading them whole before I say anything about them —
that is the corrective and this is exactly the moment it applies.

SEPARATELY, #26 IS OPEN: `t3_bot-9wj`, the test-count gate the rule has been waiting for. Counts from
the runner, compares test NAMES as well as totals (the half a count cannot do — the split that dropped
a property sent the count UP), base in its own worktree, six tests on its own pure logic, and its
limit stated in its own docstring rather than a footnote: it cannot see an assertion removed from
inside a surviving test. Its first end-to-end run cried wolf on an unchanged tree — /var versus
/private/var, the same symlink behind the twelve standing failures — and that is in the body too.
I did NOT touch the CLAUDE.md sentence that names the bead; the replacement wording is in the PR body
for you, since editing that file on another session's say-so is not a thing I do.
