FROM: boss3
TO: pm
TYPE: ASK
RE: t3_bot-wjt · built and proven cold→hook→suite in one run · ONE bootstrap question: the gate's BASE side is origin/main, which has no hook, so this PR's own gate will race on desktop exactly once — how do you want its table?
AT: 2026-09-12 19:30 EDT

t3_bot-wjt · hook built, proven the way the race was proven, entry deleted, gate tests 32/32 · one ruling needed on this PR's own gate table

BUILT, ON YOUR FORM. `scripts/prefetch-electron.ts`, third step of the root `prepare` — measured to
run under `pnpm install --frozen-lockfile` here (cold-install log lines 40-42). Resolves electron
through apps/desktop's package (a root resolve finds nothing and would report done for a tree with no
electron); returns immediately when path.txt exists; otherwise runs install.js once, serially. Cause
and idempotence stated in the docstring in the words you asked for. Two things measured rather than
assumed on the way: install.js reads NO argv, so the `--no` electron's own error message mentions is
for a different entry point and is not passed; and `prepare` is the hook that fires, not postinstall.

PROVEN THE SAME WAY THE RACE WAS, on the same cold tree:
  cold electron -> hook -> suite, ONE run       98 passed, 4 skipped, 1260 tests, no FAIL
  hook again                                     "already fetched (path.txt exists)", nothing done

UNMEASURABLE_IN_COLD_TREE is `{}`; the mechanism stays and its docstring now records why its one
entry is gone, as the worked example the next entry should follow. The predicate became a defaulted
parameter on splitScope/workspacesToRun — two gate tests had been reading the production map as their
fixture, and an empty map left them no input. 32/32, typecheck 0. Branch
boss3/t3_bot-wjt-electron-prefetch at 2438910d1.

THE BOOTSTRAP. The gate builds its base worktree from origin/main and installs it cold. origin/main
does not have the hook. So on THIS PR — and only this one — the base side will hit the race the PR
fixes, and the gate will report desktop as failed-to-load in the base tree: exit 2, could not measure.
The head side will be clean. I have the run going now against 71642a423 to show exactly that rather
than describe it. Three ways to get this PR a table, your call:

  a. Accept the exit-2 run as the artifact, with the base-side load failure quoted — it IS the defect,
     measured one last time, and the head side of the same run shows the fix. Body says so.
  b. Pre-fetch electron in the base worktree by hand for this one run, say so in the body, and show
     desktop measured on both sides. Honest if labelled; it is the "author prepares that tree by hand"
     clause the gate's own docstring already allows for a touched-skipped workspace.
  c. Merge on (a) plus the cold-tree proof above, and let the NEXT PR's gate be the one that shows
     desktop on both sides, since its base will have the hook.

My recommendation is (b) with (c) as the confirmation: the bead's acceptance is "the gate measuring
@t3tools/desktop on both sides", and (b) produces that table now, labelled as a one-time hand step;
then the next PR — boss1's 1ez, whichever lands first — shows it unaided. (a) alone leaves the bead's
acceptance unmet on the PR that closes it.

ALSO, FOR THE RECORD: the tool I shipped as #32 caught me within a second of starting this run. I
typed the base SHA from memory — a 9-character prefix I had seen, padded with 31 characters I invented
— and `resolveBase` refused it: "could not resolve ... to a commit". Re-ran with the SHA read from git
into a file and passed from there. Second time today a figure I typed was wrong and the first time a
tool of mine caught it before it reached a body.
