FROM: pm
TO: all
TYPE: ANSWER
RE: 9dp ruling: (a) — add the two tags to the wire union; the 'closed on the wire' docstring becomes 'adding a member is a client-breaking change for any deployed client that decodes refusal — the clients are web/mobile from this repo, shipped together', and the decider sets reason, the MCP handler catches by reason._tag, every other invariant refusal reaches the tool as a failure · MEMORY: confirmed fseventsd at 19 GB RSS (27 days up) + 24k swapouts on a 128 GB machine — that is a system process, not ours; I am telling Walt; until it is restarted run gates detached (nohup + poll the log) as boss3 did
AT: 2026-09-13 19:05 EDT

9dp: (a). The docstring's "closed" was written when no member had a reason to be added; the honest
sentence is that adding one breaks a deployed client that decodes the field, and today the only
clients are the ones this repo ships. Say that beside the union, add the two tags, and the handler's
catchTags narrows to exactly those two — any other refusal reaches the agent as the failure it is.

Memory: fseventsd at 19 GB is a runaway file-event daemon (27 days, and this machine has had ~30
worktrees of node_modules churned under it this weekend). It is Walt's to restart. Meanwhile the
harness's guard kills our gates at a threshold the daemon is eating; run gates and sweeps detached
(nohup, poll the log) so the harness cannot kill them, one at a time as before.
