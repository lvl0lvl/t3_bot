FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · TTFT on real Codex is BLOCKED tonight: gpt-6-astra answers 'Codex usage limit reached. Send the message again once the limit resets' (the subscription's limit, not the code) · what the two diagnostic turns measured before erroring: dispatch -> runtime turn.started 519 ms and 1084 ms with the 21k-file start snapshot INLINE (session start included) · options: wait for the reset, or a real-Claude harness mode that does not exist yet (Claude is the in-process agent SDK; the harness only has real Codex) · your call
AT: 2026-09-14 21:30 EDT

Two diagnostic runs on the 21,629-file workspace, snapshot ON, full-access mode, gpt-6-astra (the
account's configured model; gpt-5.3-codex is refused on a ChatGPT-account Codex):
  run 1 (21:22): turn.started at +519 ms after dispatch, item.started +2622, runtime.error +3439
                 (model refused — that was the 5.3 run)
  run 2 (21:25): turn.started at +1084 ms, item.started +3057, runtime.error +3962
                 lastError: "Codex usage limit reached. Send the message again once the limit resets."
So the end-to-end arm cannot complete until Walt's Codex limit resets, and the arm with the
snapshot skipped would fail the same way. I have not burned further turns on it.

What those two numbers do say, with the caveat that neither reached a first token: the provider
acknowledged the turn 0.5-1.1 s after dispatch with the 288 ms capture in front of sendTurn, session
start included. The capture is a known 288 ms of that (measured directly on the same tree); the rest
is Codex session start and network. That is not the A/B you asked for and I am not presenting it as
one.

Options, none of them mine to pick:
(a) wait for the limit to reset and rerun both arms (the scratch test is ready; 6 turns);
(b) build a real-Claude mode into the integration harness (the Claude provider is the in-process
    agent SDK on `claude auth login`; the harness only wires real Codex) — an hour of harness work
    for a measurement, and it is not in either PR's scope;
(c) accept the direct measurement (288 ms fast path, 173 ms + old path on fallback) as #72's number
    with "TTFT delta on a real provider not measured: provider limit" stated in the body, and take
    the A/B when the limit resets, before or after merge as you rule.
I recommend (c) with the A/B owed as a bead, because the number that decides the shape is bounded
above by 288 ms per send on this repo and the provider's own first token was already over 2.6 s in
both runs before it errored. Say which.
