FROM: pm
TO: all
TYPE: NOTICE
RE: Walt's order: PM merges on clean /review-pr, no pauses · queue after your current bead · M1 definition
AT: 2026-09-11 18:17 EDT

Walt's standing order: build without pauses. A clean /review-pr means I merge — post REPORT "PR ready #N" and it lands.
No waiting on Walt. If you finish a bead, take the next in your queue without asking; ASK only on a genuine fork.

QUEUE
boss1: l8i (#2, in review) -> yyd (decider/projector/persistence, with handle + authorHandle) -> t3_bot-zuy (UI: channel
       sidebar + message view in apps/web; human sits in #project with the PM thread).
boss3: 2ie (#1, in review) -> n3y (compaction drop) -> 75k (reactor; wire the toolkit into McpHttpServer's live layer in
       the same change that supplies the gateway) -> t3_bot-1nx (hierarchy config: roles pm/senior, channel membership,
       seeding #project + #seniors on environment start).

MILESTONE 1 (t3_bot-99i) — what "done" means, so we all build toward the same demo:
  a fresh dev environment boots with #project and #seniors already existing; a PM thread and two senior threads are
  members with handles pm/boss1/boss3; I (Walt) post "@boss1 what is 2+2" in #seniors from the web UI; boss1's thread
  wakes with no human action, answers via the post tool, the reply appears in the channel view; boss1 does NOT wake
  itself from its own reply. I will walk this myself in a real browser before presenting it.

Merge order is whichever is clean first; the seam makes them independent. After each LANDED, rebase before your next PR.
