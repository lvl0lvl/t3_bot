FROM: boss3
TO: pm
TYPE: INCIDENT
RE: t3_bot-b2m · VERIFY-B in (file, 467 lines, terminator): 12 RESOLVED, 2 STILL_PRESENT test-half only (fsmonitor/untracked-cache config unpinned, R7 green), and ONE NEW MUST-FIX from the adversarial pass: the flag listing runs at the runner's default 1,000,000-byte SILENT truncation cap; this repo's listing is 1,571,002 bytes, so a flagged entry sorting past the cutoff is invisible to the gate and its edit is dropped — executed through the real driver, red at head, green at base · this is the fifth unsafe-direction case your bar names, and it is MY regression: the pre-redesign revision carried a 256 MiB cap that the rewrite lost · your ruling: (a) per the bar, or the fix below
AT: 2026-09-14 21:44 EDT

Verify-b, verbatim essentials. Probe N1b: a repository padded to just over the cap (9,000 100-char
paths) plus one assume-unchanged file sorting last, edited by "the turn", captured through
CheckpointStore.captureCheckpoint: HEAD b16115452 captured "committed"; the base driver captured
"edited by the turn"; the same fixture without padding passes at head. ls-files -v -z on the
scratch worktree of THIS repository: 1,571,002 bytes against a 1,000,000 cap, outputMode
"truncate", no marker — the last ~7,700 of 21,665 entries are past the cutoff.

Where it came from, so the record has the mechanism: my first seeded revision passed
`maxOutputBytes: 256 * 1024 * 1024` on that listing (the quality lane even flagged the unnamed
constant). The redesign's `run` helper does not pass it, so the listing silently reverted to the
runner default. The truncation is invisible because `appendTruncationMarker` is off and the
result's `stdoutTruncated` is never read. A check that reads a prefix and calls it "all H" is a
check that cannot tell "no flags" from "no listing" — rule 1 from tonight, in my own new code.

The fix, if you rule for it (building it now in a fresh worktree so the numbers are real either
way, nothing pushed): (1) `stdoutTruncated` on the flag listing or the unmerged listing is a
FAILED check, so a cut listing falls back — the positive statement that the listing was complete;
(2) the listing carries an explicit cap sized for it (the 256 MiB the earlier revision had, named
this time) so this repository's 1.6 MB stays on the fast path. Test: verify-b's fixture (padded past
the default cap, flagged entry sorting last, edited), asserting the edit is captured; the mutant
that reds it is the original defect (default cap AND truncation ignored). Disclosed in advance:
"ignore truncation" alone stays green behind the raised cap, and "drop the cap" alone stays green
because the truncation check falls back — the pair guard each other, same shape as the reset and
its checks, and the body says so.

I am not arguing the bar. You said a fifth unsafe-direction defect in the redesign is (a),
immediately. This one was found by the blind verifier on this repository, it drops a file from
the card, and it is in code I wrote three hours after the fourth. The one fact in mitigation is
that the seeded approach did not produce it — a dropped constant did — and the fix is a
completeness check of the same kind the redesign is built from. Rule it.

Also from verify-b, no ruling needed: TEST-5/HIST-4's config (`core.fsmonitor=false`,
`core.untrackedCache=false`) verified correct by construction (daemon started, stopped, file edited
while down, edit captured) but unpinned — goes in the body under the disclosure rule. And a
pre-existing note worth a bead: a VcsProcess git call that starts the builtin fsmonitor daemon
appears to hang (the detached daemon inherits the pipe); captureCheckpoint cannot trigger it.
