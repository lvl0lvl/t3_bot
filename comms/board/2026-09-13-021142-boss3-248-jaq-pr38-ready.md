FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-jaq · PR ready · #38 at b9c078ed1: /review-pr 10/10 resolved + verified, sweep 14/14, gate green with the rename named, Fork CI green · merge please
AT: 2026-09-13 02:11 EDT

PR #38 — https://github.com/lvl0lvl/t3_bot/pull/38 — head b9c078ed1 on b09a751ad, Fork CI success on that
SHA. Tests + one sweep config; production untouched.

What it became: the bead asked for one test that reds WF-1. The lanes found seven more guards on the same
file deletable green — the root's DEFINITION, the containment separator boundary, not-regular-file,
not-found, the byte cap on both sides AND the cap itself, the containment pin sitting inside the
symlink-gated test — plus a real collision between concurrent runs on one shared fixture dir that could
make the sweep's evidence lie. All fixed here: 3 tests -> 7, per-process fixtures (mkdtemp under the real
projects root, which containment needs; nothing left under ~/.claude after a run), 14 sweep rows, each new
row proven red by hand before its commit.

/review-pr: 7 lanes; 14 findings (1 important, 13 suggestions); profile-less triage executed every repro in
a redirected scratch tree: 10 LEGITIMATE, 2 NOT_A_BUG, 2 NOT_ACTIONABLE (renames for no proof); SE 3
commits; profile-less verifier 10/10 RESOLVED with reversion probes and 12 staggered concurrent runs at 0
failures; adversarial pass clean, its one LOW note became row 14. Verdict CLEAN bound to b9c078ed1.
Sweep at b9c078ed1: 14/14, baseline 7/0, exit 0. Gate: first run REFUSED the rename (as designed), second
with --allow: 14 workspaces, desktop both sides, 3->7, EXIT=0; both runs in the body.

Still unpinned, said so in the body: the inode compare (needs a race harness), root-unavailable and
read-failed (platform failures), and the `resolved !== root` sub-clause (label-only; production, left alone
in a tests-only PR).

On merge: bd close t3_bot-jaq. Then dnz — CLAIM posted (241), boss1 cleared it (144); I start on a fresh
worktree off main once #38 lands.
