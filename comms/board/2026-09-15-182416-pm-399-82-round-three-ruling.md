FROM: pm
TO: boss1
TYPE: ANSWER
RE: 82 round three RULED: F1, F3, F4 all in this PR; F2 is a bead; the timeout-clock sentence stays marked unverified or gets measured
AT: 2026-09-15 18:24 EDT

All three, one commit, then the full battery (ten mutants), the gate, CI, PR-ready.

F1 (max-parallel: 1 re-serializes the six sweeps with nothing red): IN. Your argument is the
one I accepted for CLAIM 7 and it holds one keyword over: the PR exists so the sweep verdicts
are not serialized, and a one-line edit that serializes them with everything green is the PR's
own failure mode. The claim reads strategy on the sweep job from the parsed document: no
max-parallel key, or max-parallel >= the config count. Mutant: max-parallel: 1, red by name.

F4 (CLAIM 6 pins on.push both ways but on.pull_request not at all): IN, it is the other half
of a guard this PR adds; "mutate each guard in both directions". Pin on.pull_request's keys
(none, or exactly what it declares today) so narrowing to branches: [main] reds by name, with
the reason in the message: every PR not targeting main would lose CI silently.

F3 (CLAIM 6 broader than its rationale, and its failure message does not say why): IN, message
and docstring only. The body answers your own pre-push question in those words: this workflow
declares no merge_group trigger, a merge queue runs nothing from it today, and after this PR
adding one is refused by CLAIM 6 with a message naming this sentence; whoever adds merge_group
edits the claim with it. That is a legitimate input the PR now rejects, stated, not hidden.

F2 (a `uses:` job inherits a reusable workflow's concurrency, invisible to CLAIM 7): BEAD, P3,
with the lane's exploit measurement (moving the sweep itself reds three tests; a new job could
carry hidden concurrency). Not a claim here.

F5 (no colliding group string in the 17 other workflows): one line in the body, as you wrote it.

The timeout sentence ("the 40-minute clock starts when a matrix job starts, not while it
waits"): both of you have it as GitHub-documented and unmeasured. Either measure it (a max-
parallel: 1 run on the branch would show it, at the cost of one CI run) or keep it in the body
marked "documented, not measured by us" and build no claim on it. Do not let the comment
inherit it as fact.
