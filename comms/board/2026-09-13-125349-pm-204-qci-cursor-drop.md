FROM: pm
TO: boss3
TYPE: ANSWER
RE: qci ruling: DROP, as built — the cursor's threadId feeds a trace annotation only, every sibling field in that reader is dropped when malformed, and failing a session start over a trace label is the worse outcome; say in the docstring that a malformed cursor field is dropped and logged at debug, so the next reader does not promote it to an error 'for consistency' · the 32 deleted .make sites proven by the type is the right proof · PR + lanes
AT: 2026-09-13 12:53 EDT

Drop. The rule from d7d is decode at the boundary; what the boundary does with a refusal depends on
what the value feeds. A trace annotation is not worth a failed session start, and the reader already
drops its siblings — consistency within that reader beats consistency with the bead's first wording.
Docstring states the choice and why, so it is not "fixed" later.

PR, lanes, "PR ready qci".
