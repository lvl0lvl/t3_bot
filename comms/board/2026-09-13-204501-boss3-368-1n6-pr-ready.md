FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1n6 · PR ready · #58 at 4efedd305: review 11/11 fixed+verified, count gate exit 0, Fork CI green on the sha, verdict CLEAN attested · merge; then us8
AT: 2026-09-13 20:45 EDT

#58 is ready to merge — https://github.com/lvl0lvl/t3_bot/pull/58, head 4efedd305 (rebased onto main past #59, base 5467aff70), Fork CI green on that exact sha (Check + Test Server 1/2/3), mergeable CLEAN.

What it does, under your board 220 ruling: the OpenCode part id goes through `RuntimeItemId`'s decoder at the door — a refused id (`""`, `"  "`, a `null`/`42` from a broken server) drops the item the way `""` always did, logged per event through `previewRefusedId`; an admitted id is carried DECODED (`" prt_1 "` → `"prt_1"`), because the event store decodes persisted events on read and a raw padded key would name one row live and another after replay. The request id stays carried raw for strings (#49's contract) with a `typeof` narrowing at an `unknown` boundary — a non-string request id, which threw inside `.make` at base, now opens the request without an id.

Review record: /review-pr at 4f5163122, seven lanes, 13 → 11 findings, blind triage 11/11 LEGITIMATE (it ran the null Die at head vs base, the request-id Die on a truthy non-string, the inert 60 s TestClock timeout), SE six commits with every guard red by a named mutant, post-fix by-name gate 128/128, blind verifier 11/11 RESOLVED with every reversion probe red (raw `.slice` reopened the Die on the wire and reddened the table in 2.0 s naming the null row; base-restore; raw carry; `.trim()`; TS2345), adversarial pass empty. Count gate at 4efedd305 exit 0 (OpenCodeAdapter.test.ts 113→115, nothing lost by count or by name). Verdict CLEAN written and attested against the live body. The body carries the withdrawn echo claim as withdrawn and names t3_bot-rm2 for the same hazard in Claude/Codex/ACP.

After merge I tear down wt-1n6, fix-tree and gate-tree, close 1n6, and take us8, then i2e.
