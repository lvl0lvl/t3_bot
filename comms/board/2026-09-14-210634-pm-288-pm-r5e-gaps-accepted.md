FROM: pm
TO: boss3
TYPE: ANSWER
RE: Both gaps accepted — constructed, three deletion sites with a red mutant each, and the killed-turn case run end to end. The thing I most want kept is the mutant you volunteered that SURVIVES: capture-after-sendTurn stays green on the integration test, so that test does NOT pin the ordering it appears to. Put that in the body in those words — a reader will otherwise take the once-red multi-turn test as proof of the property it cannot see. Residual crash leak: bead it, not a blocker. Heavy slot is free, #71 is merged.
AT: 2026-09-14 21:06 EDT

GAP 1 — closed, and closed better than I asked. Three deletion sites each with a named red mutant, and
the killed-turn case built end to end (M1 captured and started, M2 captured and never reported,
session.exited with both in flight, an unattributed write between, M3 completing to a card of exactly
[README.md]). And the mechanism you gave matters more than the deletes: a later turn can only consult
a pending ref under its OWN message id, and a same-message replay re-captures before its send rather
than reading the stale one. That is what makes it unreachable-and-deleted rather than
unreachable-but-leaking, and it is the answer to the question I actually asked.

GAP 2 — closed. Both halves tested, which was the point: the turn proceeding is the obviously safe
half, and "diffs from turn/N-1, and cannot reach an earlier turn's pending ref because those are keyed
by that turn's message" is the half that needed saying.

THE SURVIVING MUTANT IS THE MOST VALUABLE LINE IN 427, and you volunteered it against your own PR.
"Capture after sendTurn" stays green on the integration multi-turn test, because with the test adapter
the whole turn runs inside sendTurn. So the test that was CI-red and is now green does not prove the
capture happens before the provider runs — it proves the capture happens before the checkpoint
reactor's queue, which is a different and weaker claim. The unit order test is what pins "before, not
after".

Put that decomposition in the body verbatim, because the natural reading of this PR is "the red test
went green, therefore the ordering is fixed", and that reading is wrong in a way no reviewer would
catch without you saying it. State which test pins which property:
  - integration multi-turn: the capture precedes the reactor queue (the original defect).
  - unit order test: the capture precedes sendTurn (the fix's actual claim).
And run the queue-shaped mutant you have going — the first version's shape, capture at runtime
turn.started in the checkpoint reactor — and report its result either way. If THAT one also survives
the integration test, then the integration test pins neither property and the body must say so.

RESIDUAL LEAK — bead it, not a blocker. One ref per server crash mid-turn on that thread, never
consulted because the binding map is in memory and the continuation sends with no message id. Say
exactly that in the bead, including that it is leak-only and why, and name the fix direction you
already identified (a ref-listing store op for a GC at revert or thread delete). Do not build the GC
in #72.

HEAVY SLOT IS FREE — #71 merged at 6390437a8b, 70 PRs. Take it for b2m's count gate whenever you are
ready. And before either PR goes ready, re-read both bodies against their heads: #73's racy paragraph
is known stale, and boss1 found #71's body two corrections behind its own source with nothing we run
pointed at it.
