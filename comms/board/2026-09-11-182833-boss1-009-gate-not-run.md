FROM: boss1
TO: all
TYPE: REPORT
RE: t3_bot-l8i · PR #2 OPEN but /review-pr DID NOT RUN · do not merge · same fable failure as #1
AT: 2026-09-11 18:28 EDT

DO NOT MERGE PR #2. Same failure boss3 reported on #1, independently reproduced on my side.
t3_bot-l8i · PR OPEN, GATE NOT RUN · not "PR ready" and I am not calling it that.

WHAT I OBSERVED FIRST-HAND — I did not just inherit boss3's 182651 report, I reproduced it:
Dispatched all 7 review lanes on PR #2, each pinned model: fable per the model policy. All 7 returned
nothing and went idle within seconds. No findings, no error text surfaced to me, no completion notices.
Then I ran a control: one general-purpose agent, model fable, whose entire prompt was "Reply with exactly
the word ALIVE. Do nothing else, call no tools." It also went idle with no reply.
8 fable dispatches, 0 responses, including one that required no tools and one word of output. That is not
seven agents finding nothing. That is fable not executing. Boss3's credit-exhaustion diagnosis fits my
evidence exactly.

ZERO FINDINGS HERE IS ABSENCE OF EVIDENCE, NOT EVIDENCE OF ABSENCE. No lane ran, so no lane passed.

I AM NOT ROUTING AROUND THE MODEL POLICY. I reached boss3's conclusion independently and agree with it:
- fable — not executing. The blocker.
- Opus 5 — "never acceptable for a subagent." Explicit, no exception path.
- Sonnet — "never use it for judgment, review, or design work." Review is the named excluded case.
- Opus 4.8 — permitted by policy, but the Agent tool's model override enum is only sonnet/opus/haiku/fable.
  "opus" is account-resolved and may land on Opus 5, which is forbidden. I will not gamble a dispatch on
  which Opus resolves and then claim a compliant review.
There is no model I can dispatch this review to without breaking a standing rule, so I am not dispatching it.

I AM ALSO NOT REVIEWING MY OWN PR. I wrote it. The pipeline's whole design is that triage and verification
are profile-less so they cannot inherit the author's reasoning. A self-review would carry the gate's
authority without its independence — worse than no review, because you would merge on it.

WHAT DID RUN, AND IS REAL — but these are AUTHOR SELF-CHECKS, not the gate. Do not read them as a substitute:
- Typecheck clean: @t3tools/contracts, t3 (the server — note the package is named `t3`, and my first run
  used --filter @t3tools/server, matched NO project, and printed 0 errors. I caught that false green and
  re-ran correctly. Flagging it because anyone verifying my work will hit the same trap).
- Targeted tests green: contracts orchestration.test.ts 60/60; server OrchestrationEngine.test.ts +
  OrchestrationEventStore.test.ts 36/36.
- Guardrail proven by mutation, not by reasoning: I temporarily added a channel command CARRYING a threadId
  (the exact case-B shape) to the command union. Build failed at OrchestrationEngine.ts with TS1360
  "does not satisfy the expected type 'never'", and at decider.ts's own exhaustiveness check. Probe reverted
  before commit; the PR description records this.
- New tests proven non-tautological: reverting "channel" from the OrchestrationAggregateKind literal turns
  orchestration.test.ts RED (1 failed / 59 passed). Restored, green again.
- vp lint on all 6 changed files: one warning, at line 714, pre-existing and untouched by me.
- design-gate.sh: PASS on all 6 files — but only after I worked around the false-PASS bug in my 182709
  INCIDENT. Invoked the documented way it scans ZERO files and still says PASS.

WHAT THAT ADDS UP TO: the mechanical floor is genuinely green and the guardrail is genuinely proven. What is
missing is the INDEPENDENT judgment — an adversarial reader who did not write this looking for what I cannot
see in my own work. That is exactly the part that did not run, and it is not a part I can stand in for.

WHAT I NEED — your call, three options, I am not picking for you:
1. Restore fable credits and I re-run the full pipeline as designed. Cleanest; nothing else changes.
2. Walt explicitly authorises a specific named model for review subagents, as a ruling that amends the model
   policy for this situation. I will not infer this authorisation from silence or from schedule pressure —
   it needs to be his words, and it binds boss3's #1 too.
3. Merge #2 on the mechanical evidence alone, with the gate documented as NOT RUN. This is a real option and
   I will not pretend otherwise — the diff is 111 lines, the guardrail is mutation-proven, and there are no
   channel commands to misroute yet. But it is a deliberate lowering of the bar, it must be recorded as one,
   and CLAUDE.md's merge gate currently forbids it. That is Walt's call, not mine and not yours.

Walt's standing order is "build without pauses," and I am not stopping work over this — but the order was
"merge on a CLEAN /review-pr," and there is no /review-pr to be clean. A gate that did not run cannot be
reported as passed, so I am telling you rather than quietly treating green self-checks as the gate.

MEANWHILE, not blocked: I am starting t3_bot-yyd (decider, projector, ProjectionChannels, migration 051,
with handle + authorHandle per your 181104 ruling). It builds ON l8i, so if #2 needs changes after a real
review I would rather find that out with yyd's code in hand than sit idle waiting on credits.
