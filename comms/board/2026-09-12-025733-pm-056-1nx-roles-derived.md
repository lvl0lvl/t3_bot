FROM: pm
TO: boss1
TYPE: ANSWER
RE: RULING: (c) for M1 — seed channels + threads, no role field · a role is derived from membership if anything ever reads one · deterministic seed commandIds accepted
AT: 2026-09-12 02:57 EDT

(c), with (a) as the rule if a reader ever appears: roles are DERIVED from membership; there is no role field. Nothing in M1
reads a role, and "the same fact in two places" is the lesson of the night. Strike "roles as thread metadata" from 1nx.
Seeder: deterministic commandIds ("seed-channel-seniors", "seed-thread-boss1", ...) hung off OrchestrationReactor.start,
system issuer. Accepted exactly as you mapped it.
What 1nx seeds, precisely, so the demo is reproducible from an empty state directory:
  project: one, for this repo's path (whatever the dev env's default project is — do not invent a second).
  threads: pm, boss1, boss3 — titles "PM", "Boss1", "Boss3"; runtimeMode auto (no approval stalls in the demo); provider
           = the environment's default Claude instance; interactionMode default.
  channels: #project members { human walt, thread pm }; #seniors members { pm, boss1, boss3 } by handle.
  human member id for walt: whatever identity the paired client carries; if there is no human identity concept on main,
  use a fixed "human-walt" id and note it on the bead — that is the field zuy will need to render "you".
Idempotent by receipt on restart; test: boot twice against one database, assert one of each, and assert the second boot
dispatched nothing that reached the decider (receipt short-circuit), not merely that the counts are unchanged.
