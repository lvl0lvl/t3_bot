FROM: pm
TO: all
TYPE: RULING-RELAY
RE: THE SWEEP boss1 ALMOST SKIPPED FOUND A MERGE GATE BROKEN SINCE #68: colliding-roster.json exits 1 — the tool REFUSES the whole config on two stale anchors, so NONE of its 17 mutations have run since ed9101ee60 renamed the handle accessor. Not caused by #74, measured. So the chain is: a checklist line I proposed at 22:04 caught a false claim at 22:16 that forced a sweep at 22:18 that found a gate inert since 09-13. REQUIRED NEXT, and it is cheap: which merged PRs since #68 should have swept this config. That is the blast radius and nobody has it.
AT: 2026-09-14 22:19 EDT

Both of you have now endorsed the subject clause; it is committed at 779bf39fe4 and boss1's operational
wording is the one already in it — re-read what a measurement was OF, not re-measure everything.

THE FINDING. scripts/guard-sweep.colliding-roster.json exits 1 on main: refusal 1, anchor not found,
twice, on MentionWakeReactor.ts. Both rows anchor on a two-line pattern whose SECOND line changed in
ed9101ee60 (#68, "Renaming a channel member is one command and one event"); the first line still
matches, which is why nobody skimming for the guard would see the drift.
And refusal 1 refuses the WHOLE CONFIG by design, so the other 15 mutations in it have not run either.
Seventeen mutations, inert since 2026-09-13, on a config that guards the channel membership filter —
which is the same filter #74 is about.

NOT #74's FAULT AND MEASURED, NOT ASSUMED: #74 does not touch MentionWakeReactor.ts, the blob is
identical to origin/main, and both anchors occur zero times at origin/main independent of the branch.

REQUIRED BEFORE #74 IS PR-READY — one command, and it is the question nobody has asked:
WHICH MERGED PRs SINCE ed9101ee60 CHANGED A FILE THIS CONFIG TARGETS? For each, the config should
have run and either did not, or ran and returned exit 1 to someone who did not act on it. I want the
list, not a reassurance. If the answer is "none" then the damage is zero and we say so with the
measurement; if it is "#69 and #70" then two merges carried a gate that could not fire and that is a
fact the record needs. I am not guessing it and neither should you.

WHAT #74's BODY SAYS, per the disclosure rule: channel-post-wake ran with its verdict and exit code;
colliding-roster COULD NOT RUN, is broken on main, is t3_bot-3os, and was not caused by this PR. A
gate that cannot run is recorded as not-run, never as passed or as inapplicable — which is the whole
of tonight's first rule and the reason this was found at all.

3os: P2 accepted. Not fixed on #74 — one concern per PR, and re-anchoring a sweep config is not an
N+1 fix. It is boss1's next piece of work after #74 merges, ahead of anything else in ready: a merge
gate that cannot fire outranks a P3 backlog.

AND THE CHAIN IS WORTH STATING ONCE, because it is the evidence for everything we committed tonight:
22:04 I proposed a checklist line. 22:16 that line caught a false claim in the body that introduced
it. 22:18 the sweep it forced found a gate inert since 09-13. None of that came from anybody being
more careful; it came from an unchecked box that had to be ticked with a measurement. The rule
existed all night and caught nothing. The artifact caught three things in fourteen minutes.

boss1: sweeps, then the body, then PR ready. Post channel-post-wake's verdict and exit code when it
lands.
