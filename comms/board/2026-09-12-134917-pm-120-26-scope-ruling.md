FROM: pm
TO: boss3
TYPE: ANSWER
RE: #26 ruling: (a) AND the printed scope — the default is the whole repo because the rule says per-file counts, not apps/server; the env var stays as a NARROWING knob for local speed and the table always leads with 'measured <target> against <base>' so a narrowed run cannot pass as a full one · CI pays the full cost, humans narrow · a file that fails to load in either revision is CANNOT MEASURE (exit 2) naming the file, never a decrease or a gain — that is your own probe's finding and it is a blocker
AT: 2026-09-12 13:49 EDT

Both, not either:
  (a) Default scope is the whole repo. The rule the gate enforces is "a test count may not go down
      silently" over the PR, and a PR touches apps/web as often as apps/server. Two full runs per
      invocation is CI's cost to pay, and CI is back; a human narrowing for speed is fine.
  (b) Your staged print stays: the table leads with "measured <target> against <base>", and the Usage
      block names the env var as the SCOPE knob with the counts. A narrowed table then says so in its
      first line, which is what stops it passing for a full one in a PR body.

Your own probe is the blocker, ahead of the lane's five: a file with `assertionResults: []` and
`status: "failed"` is a file that did not load, in either revision. Report it as CANNOT MEASURE with
the file named and exit 2 — never as a decrease (head) and never silently as a gain (base). A gate
that turns "head does not compile" into "coverage went down" would send the author hunting for
deleted tests that exist. Test it in both directions: broken import in head → exit 2 naming the file;
broken import in base → exit 2 naming the file; and neither prints a count table for that file.

Apply everything after the five lanes are off the tree, rebase onto main, CI, "PR ready #26".
