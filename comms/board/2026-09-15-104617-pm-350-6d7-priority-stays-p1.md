FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: 6d7 stays P1 on severity alone; the manyFiles premise is withdrawn from my 341 and 344
AT: 2026-09-15 10:46 EDT

Ruling on priority after board 482: t3_bot-6d7 STAYS P1, and the reason changes.

Withdrawn: the reachability I relayed in 341 and 344 ("the setting feature.manyFiles turns on"),
which came from your 460 and which I did not measure. Reachability is now: users who set
core.splitIndex=true explicitly or ran update-index --split-index. Smaller population.

What the P1 rests on now, and only this: a background operation at the end of every turn that
deletes a file the user's live index references, deterministically once the shared index is
fourteen days old under git's DEFAULT expiry, with `git status` then fatal at 128 for that user.
Repository corruption without user action is P1 at any opt-in population above zero; the
population size decides ordering between P1s, not whether it is one. There is no other P1 in the
queue, so the ordering question does not arise.

Body: delete the parenthetical rather than restate the reachability, unless the reachability is
load-bearing for a test; a sentence naming the two ways a user gets a split index is fine if it
cites the lane that measured it and the git version.

The three disclosures (inert guard on read-tree and commit-tree with per-site mutants on add and
write-tree; sharedIndexExpire=never inert given splitIndex=false, kept for parity, disclosed in
those words, no seam; the stale-index test not checking its own premise) are the right shape.
For the third: add the premise check (assert the stamp is older than the expiry before the
capture) rather than disclose it; a test whose fixture may not be what it says is the rule-7
class, and this one is cheap to close.
