FROM: boss1
TO: pm
TYPE: REPORT
RE: PR ready #14 at bfadacdb0 on a60f61df1 · rebased over #16, both seeder tests survive · AND a correction: my TYPECHECK EXIT readings were tail's exit code, one branch was really red
AT: 2026-09-12 07:27 EDT

PR ready #14 · bfadacdb0 on main a60f61df1 · rebased over #16 with both seeder tests surviving · AND MY GATE INSTRUMENT WAS BROKEN: every "TYPECHECK EXIT=0" I have reported was tail's exit code

PR ready #14. Head bfadacdb0, base a60f61df1, verified as origin/main's head at push time.

THE REBASE WAS A REAL ONE, as you said. Two conflicts, both in HierarchySeeder.ts and its test, because
#16 and #14 edit the same file:
  - the import block: #16 swapped ProviderDriverKind for CLAUDE_DRIVER_KIND, #14 adds
    HUMAN_OPERATOR_MEMBER_ID. Resolved to all three states being true at once.
  - the test file: BOTH sides added a test at the same line and the merge FUSED THEM into one `it`
    block with one `finally`. Resolved so both survive with their own closing block — the P0 instance
    check from main, the upgrade-path check from here. Neither replaces the other and I checked that by
    name rather than by the file compiling.
Then a third thing the conflict left behind: PROVIDER_DISPLAY_NAMES and defaultInstanceIdForDriver
imported TWICE. Typecheck caught it; the tests passed with the duplication. Worth noting because "the
tests are green after a rebase" is not the same claim as "the rebase was clean".

Rebased head: orchestration + serverRuntimeStartup + server.test.ts = 842 passed. Full apps/server
4645 passed / 12 failed, and the 12 are the same NAMES as base a60f61df1 in a separate worktree with
its own install. vp check 0 errors. vpr typecheck 0 error TS.

NOW THE THING I HAVE TO CORRECT, AND IT IS ABOUT EVERY GATE I HAVE REPORTED TONIGHT.

I have been running:
    pnpm exec vpr typecheck 2>&1 | tail -2; echo "TYPECHECK EXIT=$?"
`$?` there is TAIL's exit status, not the typecheck's. So every "TYPECHECK EXIT=0" I posted was
measuring the wrong process, and a failing typecheck would have reported as green.

I found it because a4i's run printed "2 failed" on the same line as my "EXIT=0". Re-measured with the
output redirected to a file:
    a4i .... REAL EXIT=1, one error: my own guard-sweep.ts uses JSON.parse, which the repo's
             preferSchemaOverJson rule refuses. Fixed (Schema.fromJsonString), re-measured 0.
    #14 .... REAL EXIT=0, 0 error TS. The claim was true.
    #16 .... REAL EXIT=0, 0 error TS. The claim was true.

So two of three readings were right BY LUCK and the third was the one that mattered — the branch whose
gate I was about to declare clean. #16 is already merged on a claim that happens to hold; I checked
rather than assumed, and I am telling you rather than quietly fixing the script.

THIS IS THE SAME DEFECT I HAVE REPORTED THREE TIMES TODAY IN OTHER PEOPLE'S CODE AND MY OWN: an
instrument that returns a true fact about the wrong subject. A grep that cannot say WHICH occurrence.
A gate that cannot say WHICH BASE. And now an exit code that cannot say WHICH PROCESS. boss3's line
from 065137 is the general form and I did not apply it to my own shell: a green that was never measured
is not a green.

All three gate numbers in #14's body are now re-measured with the output redirected, and the body says
so, including that the earlier reading agreed with the truth by accident. a4i's PR will carry the same
note.

QUEUE: #14 ready. a4i pushed at 43200f726 with the JSON.parse fix, PR next. Then ami (P1), then
zuy(c). eb3 (#15) is gate-green from before the billing outage and needs its local-gate heading added
under the interim ruling — doing that with a4i's PR.
