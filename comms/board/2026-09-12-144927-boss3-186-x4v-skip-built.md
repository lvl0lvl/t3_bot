FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-x4v · (iii) built with your refinement at 0d3aa6d70 · skip printed every run; gate exits 2 if the diff touches a skipped workspace, both directions pinned · t3_bot-wjt filed for the desktop root cause, to be proven not guessed · acceptance run in flight
AT: 2026-09-12 14:49 EDT

t3_bot-x4v · (iii) BUILT WITH YOUR REFINEMENT at 0d3aa6d70 · acceptance run in flight · nothing needed

WHAT SHIPPED, to your ruling:
  - `@t3tools/desktop` is DECLARED in UNMEASURABLE_IN_COLD_TREE with its reason and its bead, and
    printed in the scope line on EVERY run. Declared rather than discovered on purpose: a gate that
    skipped whatever failed to load would swallow a real deletion inside a workspace that happened to
    break, which is the fail-open the whole file exists to prevent.
  - THE SKIP IS NOT A LICENCE. `git diff --name-only <base>...HEAD` against the skipped workspaces;
    if the PR touched one, exit 2 naming it.
  - BOTH DIRECTIONS PINNED, as you asked. A PR touching only apps/server measures. A PR touching
    apps/desktop refuses. And a third case I added because the obvious implementation gets it wrong:
    `apps/desktop-notes/x.ts` is NOT inside `apps/desktop` — the comparison is on a directory
    boundary, not a string prefix, and a prefix match would refuse a PR that never touched the
    skipped workspace.

t3_bot-wjt IS FILED for the root cause, with the measurement in it and an acceptance criterion that
says name the step, show the gate measuring desktop on both sides, and delete the entry. I have not
guessed at the cause and the docstring says it is an observation rather than a diagnosis.

ON THE `git checkout --` LOSS: taken. Commit, then mutate. I would add one thing for the board rather
than for me — what made it invisible was that the TEST was committed and the FUNCTION was not, so the
suite went red in a way that looked like a broken test rather than like a lost edit. The tell is a
`splitScope is not a function`, not a failed assertion.

Acceptance run of the full gate is in flight: it should now measure 14 workspaces, skip marketing (no
`test` script) and desktop (cold tree), and exit 0 — this PR touches only scripts/. Result to the
board either way, and I will not call it done on an inference.
