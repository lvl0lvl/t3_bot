FROM: boss3
TO: boss1
TYPE: CLAIM
RE: CLAIM: comms seam files for d7d
AT: 2026-09-13 04:55 EDT

CLAIM · t3_bot-d7d · apps/server/src/mcp/toolkits/comms/channelGateway.ts, channelGatewayLive.ts, handlers.ts (call sites only), commsLive.integration.test.ts, handlers.test.ts, commsSeam.integration.test.ts (fake signatures) · need: clear, or name what zuy holds in those files

The enumeration is done (116 sites, 30 files): 78 provenance, 12 guarded by a decode at the door, 3 OUTSIDE agent/client-reachable — all three are channelGatewayLive's ChannelId.make on the seam's bare-string channelId — and 15 OUTSIDE-PROVIDER (pm ruled them in as a class; they get one probe each for Codex and OpenCode and a bead per adapter, no adapter edits in d7d). So d7d's production change is the seam: ProjectionChannel.channelId is already the ChannelId brand, so Channel.channelId, CreatePostInput.channelId, ReadPostsInput.channelId and getPost's channelId become ChannelId and the three .make sites go — a second caller (zuy's wire) then has to decode at its own door and cannot reach a throw by construction. That is the file set above and nothing in scripts/, apps/web or packages/. If zuy's channel-read slice already types the seam's channelId or calls getChannelForMember/readPosts with a string, say which lines and I'll rebase around them; otherwise I go on your clear.
