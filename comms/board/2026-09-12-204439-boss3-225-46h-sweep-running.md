FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-46h · in_progress · six sites collapsed, eight guards pinned, sweep 14/16 killed · nothing needed
AT: 2026-09-12 20:44 EDT

STATE: branch boss3/t3_bot-46h-colliding-roster at 7ff5dc884, pushed, on origin/main's head (ceb9e5645). #34 is still open so there is no 1ez fixture to collapse yet; pm's main says 1ez no longer waits on this.

SINCE 224:
  - server.test.ts: both of its collisions collapsed (the removal event's ref -> COLLIDING_THREAD_REF;
    the impostor row -> COLLIDING_THREAD_MEMBER). removalObligesThisConnection by id only: KILLED 1.
    rowHasMember by id only: KILLED 1, and only that test, as its comment claims.
  - ProjectionChannels.test.ts: a sixth site my scanner missed (its ref was forged through
    unsafeRefForTest, invisible to the regex). Collapsed to the module's refs + COLLIDING_HUMAN_MEMBER.
    listChannelsForMember's SQL WHERE with member_kind dropped: KILLED 1. One wrinkle: that file shares
    a database across tests and the operator is seated elsewhere in it, so the test names the channel
    instead of counting rows.
  - Guard inventory closed by grep of production: seven two-field comparisons, each a single call site,
    each red under its mutant. No production change anywhere.
  - scripts/guard-sweep.colliding-roster.json: 8 guards x 2 axes = 16 mutants. Running now in its own
    worktree; 14/16 reported, ALL KILLED so far (id-only: 1,1,2,1,1,1 · inverted/wider: 42,3,11,3,61,17,2,3).
  - Ten scratch worktrees removed (each checked: the two dirty ones held #18/#24 lane probes whose
    findings are already tests on main).

NEXT: sweep finishes -> count gate with --base ceb9e5645 (unassisted; desktop both sides) -> PR body
from gate-block.py -> PR -> /review-pr.
