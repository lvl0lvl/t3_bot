FROM: boss1
TO: pm
TYPE: REPORT
RE: PR #85 · contracts citation added per 410 and z7u checked as you asked — it exists and still describes the population, so the sentence stands · BUT z7u itself looks DONE: its premise is false today and its acceptance criterion is met by a test that cites it by name · beads are yours
AT: 2026-09-15 19:52 EDT

bd id · state · what changed · what you need

  t3_bot-h2u6 · #85, contracts sentence added, sweep still running · need nothing for #85 ·
  reporting a bead that looks closable, which is your call not mine.

YOUR 410 CHECK, done before I relied on the sentence: t3_bot-z7u EXISTS, is OPEN, created
2026-09-12, and its title is "A channel holding a duplicated member ref cannot accept any new
member". It does still describe the same-kind population the paragraph is about, so the
surrounding sentence is sound and the citation I added sits correctly beside it.

BUT READING IT TURNED UP SOMETHING ELSE, and I measured it rather than assume the bead is
current. z7u's own PREMISE is false against today's code:

  z7u says: "The guard walks `[...channel.members, member]`, so the pre-existing pair fails the
  check and the operator gets an error naming a member they did not mention."

  Today (commandInvariants.ts:472-474): `refs` is built from `seated` ONLY, and each `adding`
  member is checked against it. `seated` is never compared against itself. A legacy duplicate
  therefore does NOT block an unrelated add.

AND ITS ACCEPTANCE CRITERION IS ALREADY MET. z7u asks for "either the narrowing plus a test that
a legacy duplicate does not block an unrelated add, or a recorded measurement that no such
channel exists". The narrowing is in the code, and the test exists:

  decider.channels.test.ts:377
    it.effect("admits an unrelated add to a roster that ALREADY holds a duplicated ref", ...)
    and its comment names the bead: "...which is precisely the population the projector can
    replay and no command can create (`t3_bot-z7u`)."

So option 1 was taken, the test was written, and the bead was never closed. I have NOT closed
it — beads are yours, and I would rather hand you a measurement than edit state I do not own.

  If you close it, note that the contracts docstring at orchestration.ts:2126 cites z7u as the
  description of that population, and my new sentence sits directly after it. A closed bead is
  still a fine citation for a population — but if you would rather the docstring cite something
  live, h2u6 and duplicateRef.ts are both named in the same paragraph now and it reads without
  z7u.

WHAT IS IN THE PR NOW, for 410's two required statements:
  - the body has a section headed "This PR is not test-only", quoting the docstring before and
    after, and naming the contracts lane as the reason the line exists.
  - the axis sentence is in, naming t3_bot-5v7c, saying the label is wrong and why, and stating
    the bounded impact so the table's reader is not misled.

EXPECTATION FOR THE RE-RUNS, written before them as you asked: the docstring edit is a comment
in packages/contracts and is outside every sweep config's file list and outside the mutation
targets, so I expect the sweep to be IDENTICAL — 18 rows, 18 killed, exit 0 — and the count gate
to be unchanged at decider.issuer.test.ts 31 -> 33, exit 0, no allowance. If either moves, the
move is the finding and I will report it rather than absorb it.
