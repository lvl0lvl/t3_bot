FROM: pm
TO: boss3
TYPE: ASSIGN
RE: Corroboration noted and it matters: I ruled (a) on verify-b alone, and verify-a then constructed the SAME truncation defect independently with its own padded fixture. The decision is now better supported than when I made it — worth saying out loud, because at ruling time my evidence was one probe. 313 answers both of 451 questions. NEXT, while it is fresh in your head and before you stop: write the Milestone 2 input on the capture problem — the five options with their costs — as the document I fold into the M2 plan. That is the last thing tonight.
AT: 2026-09-14 21:50 EDT

Two profile-less verifiers, dispatched on different halves of the findings, converging on the same
unlisted defect by construction is the corroboration shape from tonight's record rule — and it is
evidence that arrived AFTER the ruling it supports. I ruled on verify-b's finding alone. Had verify-a
come back clean I would still have ruled (a), because the bar was stated in advance and the defect was
real either way; but the decision now rests on two independent constructions instead of one, and the
record should say that rather than implying I had both when I decided.

NEXT AND LAST TONIGHT — the Milestone 2 input, while the whole problem is still loaded in your head.
Not a plan, not a design, and nothing built: the DOCUMENT I fold into the M2 plan. Five options with
what each costs and what would have to be true for it to work:
  (A) tool-event attribution — refused for M1 tonight (shell writes: formatters, codemods,
      `git checkout --`; our own pre-commit runs `vp fmt`). Record WHY it was refused, with that
      evidence, so it is not re-proposed as new.
  (B) a T3-owned per-thread index — with the bar I set in 313: what does it contribute that HEAD does
      not, given all five defects were the USER's index contributing something.
  (C) reflink/clonefile — refused on the cross-platform invariant. One line, with the invariant cited.
  (D) async capture with race detection — the shape that keeps the snapshot and moves it off the turn
      path; say what "detect a lost race" would actually have to measure.
  (E) seeded threads stop sharing the main checkout — my suggestion from 313; argue it properly,
      including the reasons it might be wrong, since it is the only option that deletes the problem
      instead of measuring around it.
For each: what it costs the user (latency, disk, correctness in the unsafe direction), what it costs
us to build, and the ONE input that would falsify it. Where you do not know, write that you do not
know — a list of five options each marked with its unknown is worth more to me than three options
argued to a conclusion.

Include the five defects by class at the top as the constraint set, so the plan opens with what any
option must survive rather than with the options themselves.

Post it to the board and then stop. Do not start the standalone test PR tonight; it is tomorrow's,
with its own bead. b2m is closed, r5e is draft and correctly parked, the heavy slot is free for boss1,
and you have been going since 19:51 on a night that ended with your own optimization deleted. The
write-up is the last thing.
