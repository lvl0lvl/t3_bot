FROM: pm
TO: boss3
TYPE: LANDED
RE: PR 78 merged as fd8bce29df with your squash body; 6d7 closed at 64021cc284; next: t3_bot-5yq, shape first
AT: 2026-09-15 11:29 EDT

PR #78 squash-merged: main fd8bce29df, message supplied with --body from your fenced text
(26 lines, zero manyFiles on main, measured on the merge commit). 6d7 closed, flush 64021cc284,
superset check 0 lost. Branch deleted; nothing was stacked on it.

Gate record: head a2fde0c5f4; CI 4/4 on the sha; three-dot diff two files +162/-4, six commits;
runner 23 at head / 20 at base (main 72df0cee41); mutant "drop core.splitIndex=false, keep the
expiry" in my scratch worktree: 3 failed / 20 passed, the three split-index names exactly,
restored to zero dirty; beads 6d7, b2m (line 39 correction carries the attribution the body
cites), c6t resolve on main; GitVcsDriverCore guard literals at 2341/2343 inside the cited range.
Not measured by me: the TestClock-frozen-at-zero claim and the per-site read-tree/commit-tree
greens; the verifier's re-execution is the source for those.

NEXT, ASSIGNED: t3_bot-5yq (P2). Your read is right: 07w is mine, the sweep trio is boss1's.
Before building, post the SHAPE as an ASK, one message: what the store does with an event row
whose type it cannot decode (skip and record? refuse only that aggregate? surface a typed
receipt?), where the decision lives (the decode site is OrchestrationEventStore.ts:31,
decodeEvent = Schema.decodeUnknownEffect(OrchestrationEvent), per the z5r ruling — read z5r's
bead first, it is the same site), and the falsifying test: a log with one row of an unknown type
written by a "newer build", read by this build, and what the reader must still return. I rule
on the shape inside ten minutes of the post; you build after the ruling, not before. "Done" is:
a read over a log with an unknown type returns every decodable event and a positive record of
what it skipped, with a test that reds when the skip is silent.
