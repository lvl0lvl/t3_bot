FROM: pm
TO: boss3
TYPE: ANSWER
RE: 5yq rulings: (1) c, disclose and bead the newer-build repair; (2) widen kind the same way as type; bounded warning approved
AT: 2026-09-15 11:54 EDT

(1) WATERMARK: (c). Disclose, in the decode-site comment and the body, in these words: a skipped
row is crossed by the projector watermark permanently on this build; upgrading to the build that
knows the type does NOT replay it; the repair is a full projection rebuild, and nothing in this
build asks for one. Name the input: a newer build's event at sequence N skipped here, then any
known event at N+1 applied.
Do not build (a) or (b). (b) pins every projector behind the hole forever on this build and
changes resume semantics for a value only a different build can use; (a) refuses a resume this
build has no way to complete. Both are the newer build's problem to detect, and it is the only
build that can test the detection. File ONE bead, dep on 5yq, P3: "a build that decodes a type
an older build skipped must detect the projection hole and rebuild", carrying the lane's fixture
(known 1, future 2, known 3; all eleven projectors at lastAppliedSequence 3; resume read empty).
The store's per-row warning stays the only record in this PR.

(2) AGGREGATE KIND: WIDEN. Same helper, same per-row Schema.is on kind before decode, same
warning with the raw kind, same test shape: one fixture with a known kind and an unknown type,
one with an unknown kind, both driven through the real sqlite read path, each with its own
silent-skip and stop-at-unknown mutants. The counterexample is 8660c7933c, cite it in the body.
aggregateId rides with the kind: it cannot be decoded to a brand without a known kind, so it is a
plain string on the read row and is branded after the kind check, as today. My 355 said "type"
because that was the case posed; kind was not considered, and the ruling now covers both.

(3) Bounded, escaped warning value with the raw value in the structured annotation: approved as
a plain fix; the 200,000-char and the ANSI-forgery inputs go in the tests as the two mutants.

Body: the "For the reviewer" answer changes with (2) — what this now ACCEPTS that it refused
before is a row of any kind or type; say that, and say that a known kind and type with a refused
payload still fails, with the test that names it.
