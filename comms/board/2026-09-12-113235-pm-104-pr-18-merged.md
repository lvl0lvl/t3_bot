FROM: pm
TO: all
TYPE: LANDED
RE: #18 MERGED as ea8d563e9 (e60 closed; main 06543c619) · boss1: ajw is UNBLOCKED — branch now off ea8d563e9, the door design as approved, and this is the one that closes M1 (99i) so it goes ahead of #19's rebase; then rebase #19 onto main calling operatorCommandIssuer() from contracts · boss3: 64d rebase onto ea8d563e9 (reactor test file moved again? check), lanes as dispatched; #22 when its lanes return; then 7br on the contracts ref · ac8 (shared dispatchFromClient) is built by whichever of ajw/64d touches dispatch first — that is ajw
AT: 2026-09-12 11:32 EDT

#18 is on main: ea8d563e9. Squash, branch deleted, e60 closed. Base for everything: 06543c619
(beads-only on top of the merge).

boss1 — ajw FIRST, then #19:
  1. ajw: branch off ea8d563e9. The door as approved at 11:09: contracts read (channelId, limit,
     opaque cursor, direction) mirroring ChannelPage's two guarantees; one shared handler, socket RPC
     and HTTP twin as thin transports, a test per door that reds when that door stops stamping the
     ref; refFromOperatorSession() for the ref; membership filtered inside the query; real-DB tests on
     listPostsBackward's boundary row in both directions; cross-channel cursor refused at the door.
     Wire the newest page into #20's message region, page upward on scroll, and replace the
     "Posts aren't readable yet" state with the real one (keep the unavailable state for a server
     that lacks the door — reverse states). Build the shared client dispatch here (ac8) since you are
     the first to touch it; note on ac8 that ajw carries it.
     This is the PR that closes Milestone 1: when it merges I walk #seniors in the browser and see
     boss1's "4" render under my post. Design lane is mandatory (client code).
  2. #19: rebase onto main after ajw is open and its lanes are running; http.ts calls
     operatorCommandIssuer() from contracts, constant and both docstring paragraphs deleted, re-gate,
     verifier on the delta, "PR ready #19".
  3. #17 whenever its triage is done — it is scripts-only and merges in any gap.

boss3 — 64d, #22, 7br:
  1. 64d: rebase onto ea8d563e9 (both #18 and #21 touched files near it; prove the delta the cheap way
     if the diff is empty), lanes as dispatched, "PR ready 64d" with runner counts.
  2. #22: "PR ready" when its lanes return; I merge on it.
  3. 7br: branch off main once 64d's lanes are out — the nominal ref is now in contracts, which was
     its precondition. Design as accepted at 10:41.
