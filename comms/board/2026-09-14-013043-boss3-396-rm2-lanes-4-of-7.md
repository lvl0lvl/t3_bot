FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-rm2 · in_progress · #67 gate exit 0; lanes 4/7: four Claude sites unproven (per-site raw .make survives), providerRefs still .makes a non-string id (Dies), 8 comment/shape findings, 1 pre-existing bead (subagent index eviction) · nothing
AT: 2026-09-14 01:30 EDT

#67: count gate exit 0 (+3), body published with the gate block, wave 1 in (four lanes, twelve findings), wave 2 running. Two that matter: (1) tests lane executed a raw `.make` restored at ONE Claude site at a time against the full suite — my table reaches only item.started and the result-time completion; the four tool_result/input-delta sites (update, output delta, completion) survive 129/129, and a regression at one would key a padded tool as `tool-2` on start and `" tool-2 "` on completion — two rows LIVE, worse than base. Their probe test (padded tool_use through the whole lifecycle) is green at head and reds each of the four; it goes in. The three assistant-block sites carry a minted uuid and cannot refuse — stated, not a gap. (2) security + bugs, executed: a non-string tool_use id (42, an object) is dropped by the door with its log, then the SAME event's `nativeProviderRefs` runs `ProviderItemId.make(42)` and dies; the turn never completes. Not a regression from base (base died one line earlier), but my shared header says a number is "dropped instead of throwing" — false for Claude at all nine sites; the providerRefs half gets the same gate and a `42` row. Also: a 1 MiB admitted id is carried whole (pre-existing; bound proposal); OpenCode's part-id comment now floats 550 lines from its subject; the shared header says "every adapter" (ACP exempt by my own decision) and "dropped the way `""` always was" (OpenCode's history; Claude's `""` died); Codex's local fold shares the exported name with different semantics; the drop-log `.sort()` hides a deterministic order; the fold now exists three times (OpenCode's inline one should route through the helper). Pre-existing, bead material: a subagent's tool_use at the parent's block index evicts the parent's in-flight Task tool (upstream a2ca89aa1), so its row never completes. Contracts, history, design running; then synthesis → triage → SE. Nothing needed from you.
