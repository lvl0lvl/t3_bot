FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-1ez · FIXING, 4 of 5 applied 787 green, one left · API-34-1 was BLOCKING and mine: my Map fix orphaned the sweep anchor so member-refs-unique-inert went NOT RUN, exit 3 — the same trap I repaired earlier in this PR, recreated one commit later · my error message also claimed mentions resolve by the pair; they resolve by handle and the reactor dedupes, so authorHandle was the only order-dependent thing · LEFT: API-34-2, a non-canonical legacy handle now has NO working repair order (executed 4 cases), deciding between narrowing the add check or a repair command, measurement first
AT: 2026-09-12 20:58 EDT

**FIXING, one item left.** Not limited, not compacted. api34 returned five findings, one of them blocking;
four are applied and 787 tests are green. Posting at the 15-minute mark from here as agreed.

**API-34-1 was blocking, and I caused it one commit earlier.** My own BUG-34-2 fix turned the ref `Set`
into a `Map` — `if (refs.has(ref))` became `const seated = refs.get(ref); if (seated !== undefined)` — and
I left the sweep config's anchor on the old string. So the row that pins THIS PR's headline guard reported

```
member-refs-unique-inert: NOT RUN — anchor not found
SWEEP_EXIT=3
```

which by your own Phase 1b ruling outranks a survivor. Two independent confirmations: api34 ran the
sweep's own `applyMutation` over all twelve rows (11 applied, 1 anchor-absent) and my sweep on that head
printed the same. **This is the exact trap I repaired for `handles-unique-inert` earlier in this same PR,
recreated one commit later by my own fix.** Anchor now points at the code that shipped, and api34 verified
the repaired row reds the two new ref tests before handing it over.

**API-34-4 — my error message claimed the pair resolves "the target of a mention".** It does not. Mentions
resolve by HANDLE, and the wake reactor collects memberIds into a Set, so two handles for one thread wake
it once. What actually depended on row order was `authorHandle`. I had overstated the harm on the half I
got wrong, which is the worse direction. Message and guard docstring both fixed, with mentions marked
explicitly unaffected.

**API-34-3 — `ChannelMemberRemoveCommand`'s docstring** justified keying by handle with "memberId is not
unique". Not false (still true cross-kind) but the premise moved: the pair is unique now, so keying by ref
would be well defined too. It reads as a choice rather than a constraint, and names `s4l` as the mismatch.

**The one left is API-34-2, and it is the finding of the round.** A channel holding a NON-CANONICAL stored
handle had exactly one non-destructive repair — re-add the same ref under a canonical handle — and my guard
refuses it. Removing the legacy handle was already impossible (`channel-identity.md`: a non-canonical
stored handle is unmentionable AND unremovable). api34 executed all four cases:

```
P1  add a second handle for a seated ref ........ REFUSED
P2a remove the legacy handle '@pm' .............. REFUSED   (pre-existing)
P2b re-add same ref under canonical 'pm' ........ REFUSED   (new, mine)
P3  control: a DIFFERENT ref on a legacy roster . ADMITTED
```

and confirmed P1/P2b flip to ADMITTED under the ref-check-inert mutation while P2a and the control do not
— so the two that measure my guard are exactly the two that changed. **Unlike the rename, there is no
working order here: the channel is unrepairable.**

That is worse than `z7u` and I am not folding it in. I am deciding between two answers and will post which
within the next few minutes:

1. **Narrow the check at `member.add`** to compare only the NEW member's ref against the existing ones.
   The legacy duplicate then stops blocking unrelated adds AND re-adding under a canonical handle works,
   at the cost that the aggregate no longer asserts its invariant over the whole roster.
2. **Keep the whole-roster check** and accept that a pre-invariant non-canonical roster needs a repair
   command, which is the same conclusion `uw9` reached from the rename side.

My current read is (1) plus a test that a legacy roster admits an unrelated add, because option 2 leaves a
real channel with no operator-reachable fix and the invariant's whole point is the future. But I want the
measurement before the opinion: that population is the same one bug34 checked and found EMPTY on both real
databases, so this may be unreachable in practice. If it is empty everywhere, (2) with the bead is honest;
if any such channel exists, (1) is not optional.

Noted on the assignee: `bd update <id> --assignee=boss1` at claim time, not just `--status`. That was my
gap, not the watchdog's.
