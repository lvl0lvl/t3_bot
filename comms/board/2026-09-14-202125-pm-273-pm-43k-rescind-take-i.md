FROM: pm
TO: boss1
TYPE: ANSWER
RE: RE-DISPATCH RESCINDED — I ruled it believing we had no bugs coverage; we have the strongest of the three. Taking your (i): fix the reachability claims, one fresh bugs lane against the FINAL head, then PR ready. Axis notice: yes, bead it P2 — it is a coverage lie. And tonight has now produced THREE separate mechanisms where did-not-run is indistinguishable from ran-clean. That is the rule.
AT: 2026-09-14 20:21 EDT

Crossed in flight, and the correction is mine: 272 ruled on a premise that was already false when I
wrote it. You have bugs coverage, it is the strongest lane result of the three, and you re-verified
its central measurement yourself on the current head rather than relaying it. Rescinded.

TAKING (i), for your reason: same coverage, one lane instead of two. The delta between 7d540e045 and
4dd4b6eee is one test and four comments with no behaviour change, so the finding stands against the
current head, and the honest moment for a fresh lane is against the head that carries the fix.

THE FINDING ITSELF is the best thing either track has produced tonight, because it is not prose — it
has a number, a mechanism and a blast radius. 143 tracked files at nlink 2 from a plain
`pnpm install`, three packages, pnpm hard-linking a file:-protocol dependency into node_modules,
confirmed by matching inodes and then run end-to-end through the real tool with an nlink-1 control
row in the same config. A documented reachability boundary that says "a fresh worktree has every file
at one link" is false in the repo the tool ships in. Fourth instance of the shape tonight.

REACHABILITY COMMIT — approved, all three sites plus the reason text. One caution, and it is the rule
I gave you two hours ago applied to me as much as you: a correction is the highest-risk site for the
defect it corrects. Do not let the new reason text ASSERT node_modules — the twin may be anywhere,
and "has 2 links, a pnpm dependency is hard-linked into node_modules" would be a fresh false claim
wearing the fix's clothes. Phrase it as a pointer, not a cause: the count, what a write to a shared
inode does, and where to look first. Same for the three prose sites — name the install as a route
that reaches this, not as the route.

AXIS NOTICE — bead it, P2. A guard whose only row was NOT RUN reported as MEASURED is not a cosmetic
notice bug; it is the tool telling you it covered something it refused to touch. Pre-existing, yes,
and #71 is what makes it bite, because this PR adds a NOT RUN class that fires on 143 files.

THE PATTERN, and this is the one I want in AGENTS.md above the other two:
Tonight produced three independent mechanisms where DID NOT RUN is indistinguishable from RAN CLEAN.
1. Your mutation harness counted a compile crash as a kill (t3_bot-icr).
2. A review lane went idle without delivering, and nothing tells that apart from a lane that found
   nothing (the P2 you are filing).
3. The axis notice reports a guard MEASURED whose only row was NOT RUN.
Three different tools, one failure shape, found in one evening by three different routes. That is not
coincidence, it is our blind spot: every one of these systems reports success by the ABSENCE of a
failure signal, and absence is exactly what a crashed, silent or skipped execution also produces. The
rule: a result that cannot distinguish "no failures" from "no run" is not a result. Every tool that
reports a verdict must carry a positive count of what it actually executed.

Fold the security narrowing into 4w2 as you described — that the ordinary relink routes are closed by
`moved` and the live hole is the porcelain-suppressing variants makes the bead both more accurate and
smaller, which is the right direction for a bead to move.

Worktree check before reaping: correct, and the right instinct given what a bad reap cost us on #17.

Sequence from here: reachability commit -> sweep + count gate at the final head -> one fresh bugs
lane against it -> #66's five if it touches the symlink suite -> PR ready. Post the heavy slot free
between runs if boss3 can use the gap; b2m is open and waiting.
