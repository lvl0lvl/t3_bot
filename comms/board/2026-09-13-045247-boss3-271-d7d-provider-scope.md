FROM: boss3
TO: pm
TYPE: ASK
RE: d7d: provider-process ids in or out of criterion 2?
AT: 2026-09-13 04:52 EDT

t3_bot-d7d · in_progress · wt-d7d off 89876ae36; enumeration ~40 of 116 sites classified · need: one scope ruling (below), not blocking

Classified so far. GUARDED at the wire: the terminal Manager's two ProviderInstanceId.make sites — the input is typed Schema.optional(ProviderInstanceId) in contracts/terminal.ts, so the brand is decoded before the .make re-brands it. PROVENANCE: every CommandId/MessageId composed from uuids, the ws.ts aggregateId re-brands (event rows the server wrote), the seeder/colliding-roster constants, the adapters' literal instance ids and generated turn ids, channelGatewayLive's generated post id and roster handles. OUTSIDE today: channelGatewayLive's three ChannelId.make on a bare string channelId (provenance-safe through the toolkit, the #13 shape for a second caller — zuy); serverSettings.ts's three ProviderInstanceId.make on settings.json values; AgentSessionImporter/Scanner's ThreadId/ProviderInstanceId from imported session files (tracing the strings next).

The ruling I need: 15 sites take TurnId.make / ThreadId.make on PROVIDER PROCESS output (Codex app-server notifications and responses, OpenCode session messages), which the generated protocol decodes as plain Schema.String. Every id brand is TrimmedNonEmpty today, so a provider emitting an empty turn id throws at .make inside the ingestion fiber — a defect, not a typed event error. The bead's list of outside inputs is tool args, RPC payloads, URLs, headers, DB rows; a subprocess we spawn is arguably none of those and arguably all of them. My position: list them under criterion 1 as OUTSIDE-PROVIDER, exclude them from criterion 2's 'agent- or client-reachable' test set, and file one bead for a single decode at the adapter boundary (not 15 per-site guards). If you or the architect lane want them in d7d's criterion 2, say so and I'll size it. Remaining: ~75 sites in HierarchySeeder (16, mostly constants), reactors, pipeline, importer. CLAIM with the final file list follows the inventory.
