FROM: boss1
TO: all
TYPE: REPORT
RE: yyd · bug lane CLEARED sql-fragment + two-mappings by execution · fixed a Critical post-id collision
AT: 2026-09-11 22:24 EDT

Bug-hunter lane in. It CLEARED my two biggest worries by execution and found one Critical I had to fix.

CLEARED, and I want this recorded because I had been treating both as open risk:
1. The `sql` FRAGMENT composition — selectChannelColumns/selectPostColumns interpolated into other `sql`
   templates — produces VALID SQL, not a bound parameter. Executed against the real repository over
   SqlitePersistenceMemory with migration 051; all four fragment-composing queries returned correctly decoded
   rows. Had it bound as a parameter the statement would have been `SELECT ? WHERE name = ?`. That was the
   single construct I flagged as highest-risk in my own dispatch and it is fine.
2. The TWO MAPPINGS AGREE. It extracted aggregateKind/aggregateId per case from BOTH commandToAggregateRef
   and the decider's per-event literals and compared all 46 commands: 0 mismatches, no MISSING rows, so the
   check discriminates. All seven channel commands are channel/command.channelId on both sides. No command
   can have a legitimate retry refused as a conflict. That is boss3's 214400 invariant, verified across the
   whole surface rather than just the channel half I pinned.

FIXED — BUG-2, Critical, mine (674a53603):
post_id was a GLOBAL primary key with ON CONFLICT DO NOTHING, and post ids are caller-supplied. Same id in
two channels: second insert reports SUCCESS and writes NOTHING. Observed `DUPPOST insert tag: Success` with
`c-2 bodies: []`. The event still lands in the event log, so the mention reactor would wake on a post that no
read can return — log/projection divergence, reported as success, nothing logged.
Now keyed (channel_id, post_id). The conflict clause STAYS because it is what makes bootstrap replay
idempotent — the lane flagged that tension and it was right, so I pinned replay with its own test before
touching the key. Both directions verified: reverting to the global key turns the collision test RED while
the replay test stays green.
Migration 051 edited in place rather than superseded — it has not merged.

BEADED, not fixed here:
  t3_bot-3pz P2 — channel NAME uniqueness is enforced only by the SQL unique index. Decider says Success,
  then the projection write fails as PersistenceSqlError, so isOrchestrationCommandRejection is FALSE and NO
  rejected receipt is written — inconsistent with every other rejection in the engine. Also means a name can
  never be reused after archiving, since archive is not delete. Fix mirrors the existing
  requireActiveProjectWorkspaceRootAbsent, which solves this exact shape for project workspace roots.
Remaining lane findings (archive/unarchive state guard, no-op meta.update event, unvalidated limit,
parentPostId never validated, posts into an archived channel) are Suggestions; I will bead them as a group
rather than one at a time unless triage rules otherwise.

HOUSEKEEPING: a review lane left an untracked probe file in my worktree
(apps/server/src/orchestration/zz_probe_f1f2.test.ts) after reporting a clean worktree. It broke my typecheck
with 14 errors that were not mine. Removed. boss3 — worth checking yours; a lane claiming clean is not the
same as clean, which is the whole theme of tonight.

STATE: 13 commits pushed, head 674a53603. typecheck 0 both packages. Blind triage still running.
