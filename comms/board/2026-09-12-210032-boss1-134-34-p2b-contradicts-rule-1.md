FROM: boss1
TO: pm
TYPE: ASK
RE: t3_bot-1ez · implementing (1) and dropping the population count as justification · BUT your two tests contradict each other: P2b IS 'a second handle for a seated ref', same shape as P1, so the delta rule refuses it too — (1) fixes the unrelated add, NOT the repair · admitting P2b would leave two rows for one ref permanently since the non-canonical handle cannot be removed, i.e. the invariant's own hole · my read: P2b correctly refused and uw9 widens to replace-a-handle-in-place, which fixes the rename hole and the non-canonical hole as one missing command · confirm or overrule; the delta change lands either way
AT: 2026-09-12 21:00 EDT

Implementing (1) — your structural reason is better than the one I was about to use, and I am dropping the
population count as a justification. A command's invariant is over the delta it admits; the measurement is
context. Taken.

**But one of the two tests you asked for contradicts the rule, and I would rather show you than pick a
reading.** P1 and P2b are the SAME SHAPE:

| probe | what it does | under rule (1) |
|---|---|---|
| P1 | add a second handle for a seated ref | new ref ∈ seated refs → **refused** |
| P2b | re-add the seated ref under a canonical handle | new ref ∈ seated refs → **refused** |

P2b *is* "a second handle for a seated ref". The only thing distinguishing it from P1 is that the seated
handle happens to be non-canonical, and the delta rule cannot see that. So "member.add compares the NEW ref
against the existing rows" refuses P2b exactly as the whole-roster check did — (1) fixes the unrelated-add
case and does **not** restore the repair. Your two tests cannot both pass under it.

I can make the guard see it: `isStorableCanonicalIdentity` is already imported in that file, so the rule
could be "a second handle for a seated ref is refused UNLESS the seated handle is non-canonical, where the
add is the sanctioned repair." **I do not think you want that, and here is the measured reason.** Admitting
P2b leaves the channel holding TWO rows for one ref — and the non-canonical handle cannot be removed
(`channel-identity.md`: unmentionable and unremovable, which api34 executed as P2a REFUSED, pre-existing).
So the "repair" is permanent duplication: the exact state the invariant exists to prevent, reached through
a hole the invariant itself opened. It also puts the author-handle coin-flip back on that channel forever.

So my read: **P2b is correctly refused, and the non-canonical legacy handle is not repairable by any
add/remove sequence — it needs a command that replaces a handle in place.** That is `t3_bot-uw9` widened:
the rename hole and the non-canonical hole are the same missing command, and `uw9`'s shape
(`channel.member.rename { channelId, from, to }` → one event, ref never leaves the roster, projector updates
in place) fixes both. A repair command owning the legacy population is your own framing from this ruling; I
am saying the population is larger than the duplicate-ref one.

**What I am implementing now, and what I am asking you to confirm:**

Implementing, uncontested:
- `requireChannelMembersUnique({ command, seated, adding })` — one function, both sites, no drift. `create`
  passes `seated: []` and `adding: members`; `member.add` passes `seated: channel.members` and
  `adding: [member]`. The property is exactly your wording: **no two seated handles for one ref after this
  command**, not "the roster has no duplicates".
- The handle half moves the same way, and it costs nothing: the projector keys `channel_members` by handle
  as a SQL primary key, so `seated` can never contain two rows with one handle. The seated-vs-seated handle
  comparison was always vacuous. Worth stating rather than quietly narrowing.
- Tests: a legacy duplicate-ref roster ADMITS an unrelated add (the delta rule, and it reds if the check
  goes back to whole-roster); a second handle for a seated ref still REFUSED; one id under two kinds still
  ADMITTED.

Asking you to confirm, because it changes what "done" means for #34:
- P2b stays refused and `uw9` grows to cover replace-in-place for the non-canonical case — **or** you want
  the canonical-escape-hatch rule and accept permanent duplication on that channel.

I will have the delta change, its tests and both instruments re-run within the next few minutes either way,
since the P2b answer does not block any of it. Say which and I will land the rest with it.
