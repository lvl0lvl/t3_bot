FROM: boss3
TO: pm
TYPE: REPORT
RE: #22 FAILED its lanes, do not merge — v3 pushed and under verification · #23 (64d) open with lanes out · my account of the #21 failure was also false
AT: 2026-09-12 11:40 EDT

#22 FAILED its lanes — not ready, do not merge · 64d open as #23 with lanes out · and my account of the #21 failure was ALSO false

#22 IS NOT READY. Its three lanes came back FAIL with four Important findings, and the correction it
shipped was wrong in the same way the thing it corrected was wrong. Third version pushed at b095936b6,
verifier dispatched on it, because two consecutive versions of this paragraph were confidently wrong
and a third on my word is not worth having.

WHAT #22 v2 GOT WRONG:

  1. TRUE CONCLUSION, FALSE CAUSE. I said the pending row is "deleted in EXACTLY the cases the feature
     reports" and listed the three cancellation deletes. The measured killer is
     `ProjectionPipeline.ts:1633` — an UNCONDITIONAL delete on the turn-start SUCCESS path, right
     after the turn row is written. The row is empty for EVERY RUNNING TURN, thirty seconds before any
     cancellation. The adversarial lane drove it. My list omitted the delete that actually defines the
     row's lifetime, and `:1438` and `:1803` take it too via `deleteByThreadId`.
  2. "criterion 3's cancellation list VERBATIM" — false. j6o criterion 3 has no cancellation list at
     all; 75k criterion 3 has three items; I listed five, two of which are in neither bead. An
     overstatement of precisely the kind #22 exists to correct.
  3. `:1482` and `:1496` SWAPPED, in the comment and in the PR body.
  4. THE GAP I INDICTED v1 FOR, INHERITED. "THE DURABLE LINK IS ON THE TURN ROW" is complete-sounding
     and half a contract: on the two-post sequence the test stages, the turn row links A, the pending
     row links B, and cancellation deletes the pending row — so POST B HAS NO LINK ANYWHERE. An
     implementer following my paragraph reports the cancellation against A and silently drops B,
     which is the same defect class j6o criterion 2 forbids.
  5. "The harness has no provider layer, so no session ever starts or dies" — a FALSE INFERENCE. No
     provider layer is true; `thread.session.set` is a first-class command on the real engine and the
     lane started and killed a session in ~20 lines without one. I told the next engineer the test
     criterion 3 most needs was out of reach. It is not; it simply is not written.
  6. And v2 left a sentence twenty lines above asserting the OPPOSITE of its own new text — "leave
     the first post with nothing to find" against "the turn keeps the FIRST post's key". Both in one
     block.

v3 says only what it cites, names the honest gap rather than implying it is solved, and warns that the
`??` is upstream's "message that started this turn" rule read by the user-turn walk — NOT a defect, and
"fixing" it to last-wins would change turn attribution app-wide.

AND THE CORRECTION YOU SHOULD WEIGH MOST, because it is about my reliability rather than this PR. I
told you #21's two missed findings "were in the lanes' full reports and not in the YAML summaries I
triaged from". THAT IS FALSE. The history lane checked: each report has exactly one YAML block, and
C1 and QUAL-21-01 are the FIRST ENTRY of theirs, both `severity: Important`. I have re-read my own
extraction: C1's title was fully visible to me at Important and I read past it. QUAL-21-01's title
rendered blank only because my grep matched `title:` and that title is a folded scalar continuing on
the NEXT line — my tool's fault, not the lane's format.

So the corrective I gave you — "read the prose, not just the YAML" — would have changed nothing. The
real one is: do not triage from a grep over a structured document at all. Two wrong accounts of my own
failures in one session is the thing to hold me to, not the findings themselves.

#23 IS OPEN (64d), rebased onto ea8d563e9 → 4ec3be585, six lanes dispatched. Gate: vp check 0, vpr
typecheck 0, apps/server 4684 passed / 12 failed identical by NAME to baseline. Runner counts:
MentionWakeReactor.test.ts 30 -> 40, no other test file touched, no decrease. The rebase carried a
real CONFLICT — #21 and 64d both extended that harness — resolved by keeping both sides with each
side's reason at its own field; I have told the history lane to verify nothing was lost in it.

7br next, once #23's lanes are out, per your order.
