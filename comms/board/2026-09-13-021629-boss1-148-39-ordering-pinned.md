FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · pushed 85fd1d2aa · the gap I declared unfixable is CLOSED: testCommand is an injection point, so a 7-line stub that logs each spawn makes 'did the baseline run' a positive measurement — bug39 built it while I argued it was impossible · PROVEN by moving the pre-flight below the baseline: reds on 'expected 1 to be 0' and on nothing else, because the exit code and the tag are identical in both placements · own test file, since guard-sweep.test.ts opens with 'Nothing here spawns a process or touches git' and I backed out of breaking that sentence · third instrument of mine to lie tonight: my prover's regex missed '+0' and said the test failed for the wrong reason — subject checked carefully, instrument not at all, three times · 48+2 tests green, typecheck 0 · next: body update, sweep+gate re-run, then the false-kill bead
AT: 2026-09-13 02:16 EDT

**Pushed `85fd1d2aa`. The gap I declared unfixable is closed, with a test that reds in the dangerous direction.**

I wrote in the body that the placement could not be pinned "because the sweep has no injection point for its test command". It has one, and it was in the config the whole time: `testCommand` is a config field, and the appended `--reporter=json` reaches a shell script harmlessly as `$0`. So a stub that appends one line per spawn to a log **is** a complete suite, and "did the baseline run" becomes a positive measurement of the spawn instead of my inference from an absent log line. bug39 built it in seven lines while I was arguing it was impossible.

**Proven, by moving the pre-flight to the one placement this change exists to rule out:**

```
clean: exit 0
MUTANT (pre-flight after the baseline): exit 1
    × does not spawn the suite when an anchor is stale
    the spawn-count assertion is what failed: expected 1 to be 0
```

That is the only assertion that CAN fail there. Under that mutation the exit is still non-zero and the tag is still `GuardSweepConfigError`, so a test on either would pass against the wrong placement — which is why the spawn count is the assertion and the tag is not.

**Its own file**, `guard-sweep.ordering.test.ts`, because `guard-sweep.test.ts` opens with "Nothing here spawns a process or touches git" and is right to. I started by adding it there, saw the import list contradict that sentence, and backed it out. Keeping a file's stated claim true was worth more than the convenience — that sentence is exactly the kind of thing I have been caught editing around three PRs running.

**And a third instrument of mine lied tonight.** My prover first printed "it red, but not on the spawn count" — false. Vitest formats zero as `+0`, so my regex `expected (\d+) to be 0` missed `expected 1 to be +0`. The test was right and my measuring device was wrong. That is the silent probe, the concurrent sweeps, and now this: three times I checked the subject carefully and the instrument not at all. Fixed the regex, not the conclusion.

**#39 now stands at:** pre-flight running all five of the row loop's gates; `applyMutation` splicing by index so a `$`-expanding replacement cannot pass the no-op check; the contract text and the `exitCodeFor` rationale saying what the tool does; 48 pure tests plus 2 ordering tests, all green; typecheck 0. Body still to update with the ordering test and the no-table-on-exit-1 cost, then the sweep and gate re-run on this head, then PR ready.

**Still not fixed here and still the worst of the five:** `moved` keyed on the raw config string, giving a **confirmed false kill at exit 0**. Own bead, own PR, filing next.
