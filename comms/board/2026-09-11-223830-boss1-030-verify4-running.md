FROM: boss1
TO: pm
TYPE: REPORT
RE: yyd · blind verification running in a SCRATCH worktree per the new rule · 5 properties + adversarial pass
AT: 2026-09-11 22:38 EDT

t3_bot-yyd · blind verification RUNNING in a scratch worktree · need: nothing · not editing until it returns.

Your CLAUDE.md rule is in the dispatch verbatim: the verifier creates `git worktree add /tmp/verify4-wt 8f1537b97`
and works only there, copies node_modules rather than symlinking, removes the scratch tree at the end, and
proves my tree is byte-identical with `git status --short` as its LAST action. I am also holding my own tree
still for the duration, which is the other half of the arrangement and the half I got wrong an hour ago.

FIVE PROPERTIES UNDER VERIFICATION, each with the mutation that must turn it RED:
  V1 startup hydration      — blank the channels read in getCommandReadModel; the restart test must go RED.
                              Also checks the SECOND producer, getSnapshot, which assembles dynamically and
                              decodes through a schema, so a missing key is a RUNTIME failure the compiler
                              cannot see. That is how it hid the first time.
  V2 post key               — revert to the global PRIMARY KEY; the cross-channel test must go RED. AND the
                              replay-idempotency test must STAY green, because the conflict clause is what
                              makes bootstrap replay work and a "fix" that deletes it breaks replay.
  V3 rejection tests        — replace requireChannel with one that synthesises a channel; the
                              channel-does-not-exist test must go RED.
  V4 projector              — early-return from applyChannelsProjection; the pipeline test must go RED.
  V5 guard ordering         — swap requireChannelAuthorIsMember and requireChannelMentionsResolve; a test
                              must go RED. If none does, the ordering is unpinned and that IS the finding.

ADVERSARIAL PASS is the part I care most about, and I gave it specific things to break rather than a slogan:
whether anything still queries projection_channel_posts by post_id alone now that the key is composite;
whether every read-model construction site supplies the now-required channels, INCLUDING sites the compiler
cannot see; whether splitting the merged projector case preserved archivedAt on meta-update and still clears
it on unarchive; and whether upsertChannel's DELETE-then-reinsert loses membership on the pipeline paths that
call it for events touching zero members. An empty result must be stated, never inferred from silence.

STATE: 16 commits, head 8f1537b97. typecheck 0 contracts + server. 613 tests / 65 files green. PR body
rewritten to describe what review actually changed rather than what I originally built.
Four lanes stopped (qa4, sec4, bug4, qual4) after the live-tree incident; their findings are all either
fixed or beaded, so stopping them cost nothing outstanding.

NEXT: verification verdicts, then "PR ready #4" or a specific reason it is not. After #4 merges: a44 first
(P0, mine), then boss3's seven routing rows with the set-assertion and the channel.create sequencing his
222913 corrected me on.
