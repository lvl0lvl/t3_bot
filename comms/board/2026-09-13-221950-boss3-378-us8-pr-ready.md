FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-us8 · PR ready · #61 at 9798fe41d: review 4/4 fixed+verified, count gate exit 0, Fork CI green on the sha, verdict CLEAN attested · merge; then i2e
AT: 2026-09-13 22:19 EDT

#61 is ready to merge — https://github.com/lvl0lvl/t3_bot/pull/61, head 9798fe41d (rebased onto main past #60, base fe335eaa4), Fork CI green on that exact sha (Check + Test Server 1/2/3), mergeable CLEAN.

What it does: `readThread` in the OpenCode adapter compared every message id to `session.revert?.messageID` raw, so with no native revert an id-less message (past the SDK's `string`) matched the absent boundary and ended the snapshot before #49's turn-id gate — `rollbackThread` then reverted from the wrong turn and the checkpoint reactor reported the revert complete. Now the boundary is bound once and compared only when it is a string; an id-less assistant fails typed at the gate before any `session.revert`, an id-less user message is skipped, a string boundary compares raw as before. The review widened it from `!== undefined` to `typeof === "string"` on an executed input: a `null` boundary with a null-id message over-reverted and reported complete (`session.revert(assistant-1)`); the body states that as the motivation, not a guard for an input nobody ran.

Review record: /review-pr at 4ec6a14a4, seven lanes, 8 → 6 findings, blind triage 4 LEGITIMATE / 1 DEFERRED (r6t) / 1 OUT_OF_SCOPE (1r1), SE three commits with every guard red by name (the first cut's guard, base restored, the non-empty tightening, the gate bypassed with a boundary present), post-fix by-name gate 128/128, blind verifier 4/4 RESOLVED with every reversion probe red and the base shape re-executed (`revertCalls: [assistant-1]`), adversarial pass empty. The history lane's catch is in the body: my first cut stated the bead's "reverted nothing" for a fixture where base over-reverted. Count gate at 9798fe41d exit 0 (no count moved — every row joined the existing test). Verdict CLEAN written and attested against the live body. Beads from the review: t3_bot-1r1 (reactor restores files before the provider rollback; P2) and t3_bot-r6t (part-level revert snapshot; P3), both dep us8.

After merge I tear down wt-us8 and the fix tree, close us8, and take i2e.
