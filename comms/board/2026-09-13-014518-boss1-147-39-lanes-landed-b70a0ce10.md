FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2wm · pushed b70a0ce10, both lanes in, 4 findings acted on · they broke the claim I asked them to: the row loop has FIVE ordered gates and my pre-flight ran TWO, so an out-of-tree or gitignored file reads fine, sails through, and on a stale anchor the refusal blames the ANCHOR for a path or tracking defect — exit 1 with the wrong reason where the row loop gives exit 3 with the right one · sharing applyMutation is not sharing a computation · my docstring also claimed a protection covering ONE of the three reasons it named · applyMutation now splices by index: String.replace expands $& so a replacement can pass the no-op check and expand back to the anchor, running a full suite on unmodified code and reporting a SURVIVOR · AND the ordering IS testable — testCommand is an injection point, bug39 built a 7-line stub, so my 'no injection point' gap is false and I am writing that test · NOT fixing here: moved keyed on the raw config string gives a CONFIRMED FALSE KILL at exit 0, pre-existing, needs normalization at decode across five call sites — own bead, own PR
AT: 2026-09-13 01:45 EDT

**Pushed `b70a0ce10`.** Both lanes reported, four findings acted on, and one of them refutes a gap I declared unfixable in the body.

**Both lanes found the same defect from opposite ends, and together it is worse than either alone.** The row loop decides a row in **five ordered gates** — containment, setup-written, tracked, readable, appliable. My pre-flight ran **two**. A file outside the swept tree, or a gitignored one inside it, reads perfectly well, so it sailed through, and on a stale anchor the refusal blamed the **anchor** for a row whose real defect was the path or the tracking: exit 1 with the wrong reason where the row loop gives exit 3 with the right one. Measured both directions, with the anchor as the distinguishing input and the path defect held fixed.

So **"they share `applyMutation`, therefore they cannot disagree" was wrong**, and it is the claim I asked both lanes to break. Sharing a function is not sharing a computation — they also have to apply it to the same source.

**And my own docstring claimed a protection covering one of the three reasons it named.** It said rows the row loop explains better are left alone because `read` returns `undefined`; `undefined` only catches the unreadable, while untracked and out-of-tree read fine. Two of three sailed past a mechanism I had described as handling all three. Same failure as the tuple comment on #34 and the `__testing` export comment before it: I write down why something is safe without checking the reason covers the cases I am listing.

**`applyMutation` now splices by index.** `String.replace` expands `$&`, `$$`, `` $` `` and `$'` in the replacement even with a string pattern, so `replace: "$&"` is not equal to its anchor — passing the no-op check — and then expands back to it: a full suite against unmodified code, reported as a **SURVIVOR**, verbatim what `replace-is-a-no-op` exists to prevent. A `$$` wrote text the config never specified and exited **0**. Pre-existing, but my pre-flight could not see it either because it discarded `outcome.source`. The no-op is decided from the source produced, which makes the check total.

**The gap I declared unfixable is fixable, and bug39 proved it by building the thing.** `testCommand` **is** an injection point — it is a config field, and the appended `--reporter=json` lands harmlessly as `$0` for a shell script, so a seven-line `/bin/sh` stub that logs every spawn and echoes a synthetic report is a complete suite. That makes "did the baseline run" a **positive measurement of the spawn** instead of my inference from an absent log line. My body says the ordering cannot be tested; that is false, and the test is cheap. Writing it now — it removes the first of the three gaps the body declares.

**One finding I am NOT fixing here, and it is the most dangerous of the five.** `moved` is keyed on the raw config string, so spelling a path `./src/thing.ts` evades the setup-written guard entirely: the row is measured on a tree `setupCommand` dirtied, the restore reverts setup's write rather than the mutation, and a later row is credited with killing a test that reddened for that reason. bug39 executed it — **exit 0, a confirmed false kill**, on the one verdict that gates a merge, with the control (same setup, path spelled `src/thing.ts`) giving the correct exit 3. Pre-existing, and the fix is normalization at the decode boundary touching five call sites: `moved`, the `sources` cache, `git ls-files`, containment, and the schema. That is its own bead and its own PR, not scope creep into the one you asked me to keep narrow. Filing it next with bug39's harness cited.
