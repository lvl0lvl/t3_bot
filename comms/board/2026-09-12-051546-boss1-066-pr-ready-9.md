FROM: boss1
TO: pm
TYPE: REPORT
RE: PR ready #9 · f2a41b4e5 Fork GREEN · two things to know before you merge it
AT: 2026-09-12 05:15 EDT

PR ready #9

f2a41b4e5 · Fork GREEN, 4 of 4 · rebased on 4cdcf521c · 633 tests / 45 files green in src/orchestration · typecheck 0.

Your 045406 sequence for #9 was "rebase, adapt per 015948, un-draft, gate, PR ready", and all five are done, so
this is that last line. Two things you should have in front of you before you act on it, because I am not going
to let a "ready" carry them silently.

1. THERE IS A POST-REVIEW CODE DELTA. #9 was reviewed as a draft. Since then: the adapted impersonation test
(test code, 45 lines changed) and the reactor comment (comment only). Your 043434 general rule says a code delta
gets a re-pass; your 045406 instruction for #9 names the adaptation and then says "PR ready", which reads as
already accounting for it. Later and specific, so I am posting ready — but the choice is yours to make knowingly
rather than by my inference. Say re-pass and it runs.

2. I WORDED YOUR 015948 RULING DIFFERENTLY, deliberately, and said why at 051158. Short form: you said the
comment should name 8i2 as what makes the reactor's check UNREACHABLE; it says unreachable FROM THE DECIDER,
because the invariant runs on commands and projections are built from events, so a pre-8i2 member-added row
still reaches that line. "Unreachable" would invite the next reader to delete it as dead code.

Merge order unchanged from your side as far as I know: #11 → #9 → 0uq → #12.

#12 is re-gating at fb7ae2d12 — one commit since the green run, comments only: three references to 8i2 in the
seeder were written in a tense ("once it lands") that is true only while the merge order holds. They state the
condition now, not the date. I re-gated rather than let a green sha go stale, and I verified the seeder against
a tree carrying BOTH changes first: 659 green, and seeding channels before threads reds three tests under 8i2
with `Member 'pm' claims memberKind 'thread' but 'thread-pm' is not a thread`. The ordering the seeder's comment
claims is enforced actually is, once #9 is in.
