FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-r5e · in_progress · #72 review: 2/7 lanes in — bugs found a Codex mid-turn regression and a stale-pre door; a shape change likely · nothing needed
AT: 2026-09-14 06:06 EDT

#72: two of the four wave-1 lanes in (quality 5 findings, bugs 3), security and tests still running, then contracts/history/design. The bugs lane executed three inputs I did not: (1) a REGRESSION on Codex only — Codex accepts a follow-up while a turn runs and is the one adapter that emits turn.diff.updated (the mid-turn placeholder), so a follow-up sent mid-turn takes pre/N+1 halfway through the running turn and the NEXT turn's summary lists the first turn's later writes (head `[b.ts, late.ts]`, base `[b.ts]`); (2) not a regression but the bead's symptom by another door — a wake that dies or is refused before its checkpoint leaves pre/N stale and the next turn at that count keeps it, so drift lands in the summary again; (3) for turn 1 the panel goes through the all-turns query (turn/0) while the card uses pre/0, and after a revert to 0 the two split. Its proposed shape: key the pre snapshot on the runtime turn.started for a NEW turnId and overwrite, instead of once-per-count on any of three signals — closes (1) and (2) together. Quality: "pre-turn" now names two refs in one file (upstream's ensurePreTurnBaseline… = turn/N vs my pre/N); a redundant second rev-parse per completion; the receipt comment overclaims (published only when turn/N is new); the fallback comment narrower than the catch; a test-stub helper beside five inline copies. Nothing triaged by me — synthesis then the blind triage rule, and the SE builds what triage executes. Heavy slot still yours/boss1's for the count gate. Nothing needed from you.
