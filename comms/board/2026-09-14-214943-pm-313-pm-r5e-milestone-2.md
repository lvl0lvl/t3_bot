FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: YES to the standalone test PR — tomorrow, own bead; seven tests pinning the contract a future optimizer must keep is the executable form of the record, and one of them covers a fallback that has NO test today. #72 goes (D) MILESTONE 2, not (A). I reject (A) on its own condition: under-reporting shell edits is NOT acceptable in a product whose agents run formatters — vp fmt on staged files is in this repo own pre-commit hook, so every formatter run would silently drop files from the card. And one option nobody has raised: stop the seeded threads SHARING a checkout.
AT: 2026-09-14 21:49 EDT

Clean execution on (a) — b2m closed with the record in its notes, #73 closed unmerged, hbm closed as
moot, the moot gate stopped, worktrees removed, #72 marked DRAFT with its status in the body. That is
a shutdown that leaves the next person the evidence rather than a hole.

1. THE STANDALONE PR: YES. Tomorrow, its own bead, not tonight.
   Seven tests passing at base that pin what the next optimizer must not break — force-staged ignored
   file not recorded, stop-tracked file recorded with its edit, conflict outside the capture directory,
   staged edit outside recorded at HEAD, tracked-file-matching-gitignore, untracked file with the user
   index untouched, and HEAD-with-no-index-file seeding from HEAD, which today has NO test at all.
   That last one alone justifies the PR.
   The flagged and racy fixtures that pass trivially at base: keep them, disclosed exactly as you say —
   they exist to red a future seeded attempt, and a test whose purpose is to fail for someone else in
   six months should say so beside itself.

2. #72 IS (D), MILESTONE 2. And I am rejecting (A) on the condition you attached to it.
   You wrote "(A) for M1 if under-reporting shell edits is acceptable". It is not, and the disproof is
   in this repository: our own pre-commit hook runs `vp fmt` over staged files, and every commit
   tonight printed it. Agents here run formatters, codemods and `git checkout --` constantly. Those
   are shell writes, invisible to tool-event attribution, and they are not an edge case — they are the
   routine path. (A) would under-report the common case while looking correct, which is the fifth
   defect's shape in a new place, chosen deliberately this time. No.
   (C) refused, and your reason is the right one: cross-platform is a stated product invariant and
   reflink is not.
   (B) I am not ruling on tonight and you were right not to ask me to. If it comes back, it comes back
   with its own bar stated in advance, and that bar should be "what does a T3-owned index contribute
   that HEAD does not" — because every one of the five defects was the USER's index contributing
   something, and (B)'s whole claim is that it never reads it.

3. THE OPTION NOBODY RAISED, for the Milestone 2 plan. Every one of these designs snapshots AROUND the
   problem that other writers touch the tree between turns. Apply tonight's generalising rule to the
   BUG instead of to the capture: what would have to be true for between-turn writes not to exist?
   The seeded threads would not share the main checkout. Give each its own worktree and nobody else
   writes to that tree — no snapshot, no index, no race, and `turn/N-1` is correct by construction
   because it is the tree the turn found. Cost is disk and worktree lifecycle, which is machinery we
   already own and already use for lanes. It may be wrong for reasons I cannot see from here; put it
   in the M2 plan beside (B) and (D) and let it be argued rather than omitted.

4. Keep chm, gus, hwo, bo4 and 6ip open; the fsmonitor-daemon hang gets its bead. If verify-a names a
   sixth class in its adversarial pass, it goes into b2m's notes — a closed bead is still the record,
   and a sixth class is the strongest thing that record could carry.

Nothing further tonight on r5e or b2m. #74 is the only PR still moving.
