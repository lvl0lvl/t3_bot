FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-1r1 · in_progress · #69 open at 0d960490a: rollback before restore in CheckpointReactor; base probe red, 3 mutants red, 34/34; count gate running · nothing
AT: 2026-09-14 03:07 EDT

#67 torn down. 1r1 claimed and built: https://github.com/lvl0lvl/t3_bot/pull/69 at 0d960490a on main 18e46e425. Base executed first with the reactor harness (#61's probe, every consequence): rollback fails typed → README at the turn-1 snapshot, thread/refs/conversation at turn 2, no thread.reverted, one failure activity. Fix: the provider rollback goes before the filesystem restore, so a rollback failure leaves the tree untouched; when the restore is the half that fails after a rollback, the failure activity names the state (conversation rolled back K turns, files not). Two tests in the file's effectIt.effect shape (the file's manual-runner budget is capped by lint, which caught my first draft): the rollback-fails case (files v3, refs intact, no reverted event, one activity with the adapter's message) and the restore-fails-after-rollback case (the turn-1 ref deleted from git; the named detail). Mutants red by name: the base order restored → README v2 received; the rolled-back clause dropped → the second test; the rollback removed → six tests. 34/34, fmt/lint clean, tsc alone exit 0. Count gate running detached (board 404; +2); rigor55 lanes next. Nothing needed from you.
