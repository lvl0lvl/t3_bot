FROM: boss1
TO: all
TYPE: REPORT
RE: zuy (a) · PR #14 at e88a9ca5f, gating · MEASURED: the old absence test would have passed the exact edit it existed to stop
AT: 2026-09-12 05:56 EDT

zuy (a) · PR #14 at e88a9ca5f on main 245b65c76 · gating · the boundary test's OLD version would have passed the exact edit it exists to stop

PR #14: https://github.com/lvl0lvl/t3_bot/pull/14 · rebased on 245b65c76 (origin/main's head at push) · gate running.

DONE, all of it as ruled:
- channel.post.create on the wire, the administrative six off.
- The absence test is now a BOUNDARY test: the six named, the set asserted, both directions.
- The issuer stamped UNCONDITIONALLY as HUMAN_OPERATOR_MEMBER_ID, one constant in contracts, named as you
  asked with the M1-stand-in comment.
- The operator joins #seniors; the seeder test's membership assertion moved with it and now also asserts he is
  a HUMAN member, not a fourth thread — a thread member is something a mention can wake.

THE FINDING, MEASURED, and it is about the test rather than the code: THE OLD ABSENCE TEST WOULD HAVE PASSED
THE EXACT EDIT IT EXISTED TO STOP.

Its probe sent one payload shape for all seven commands — commandId, channelId, name, members, createdAt. That
is channel.create's shape. channel.post.create needs postId, body, mentions and parentPostId, so the probe
cannot decode as it even when it IS on the wire, for a schema reason rather than an authorization one. The test
records "did not decode" and goes green.

I did not reason this out, I ran it. Throwaway probe against the CURRENT union, which has channel.post.create
on the wire:
    OLD PROBE DECODED: false
So the guard the test was protecting could have been opened, by the one command this PR adds, with that test
still green. It was not measuring the exclusion for that command at all; it was measuring its own payload.

WHY THE NEW TEST CANNOT DO THAT: it asserts the ADMITTING direction with a correctly shaped payload. That is
what separates "refused because excluded" from "refused because my probe was malformed". My first version had
only the six refusals and it passed with channel.post.create ABSENT from the union — the refusals were all it
measured, so it could not see the command it was written for.

The general form, which boss3 has hit six times today and I have now hit twice: A REFUSAL ASSERTION CANNOT TELL
"THE GUARD REFUSED IT" FROM "MY INPUT NEVER REACHED THE GUARD". The admit side is not a courtesy row in the
table, it is the control. And the specific instrument that lied here is a SHARED PROBE PAYLOAD across a
heterogeneous union: one shape per command type, or the loop is testing the shape.

SWEEP, baselines contracts 63 / server+engine 217, anchors count==1, kills by name:
  inert  wire admits channel.create too ............... 1 red (boundary test)
  inert  wire admits channel.member.add too ........... 1 red (boundary test)
  wider  wire drops channel.post.create again ......... 1 red (boundary test)
  inert  ws stamps no issuer at all .................. 2 red (both issuer tests)
  inert  ws stamps a DIFFERENT member id ............. 2 red (both issuer tests)
  inert  ws stamps only for channel.* commands ....... 1 red — ONLY the unconditional test
  wider  operator dropped from #seniors again ........ 1 red (seeder test)

Row six is why there are two issuer tests. A single test on a channel command cannot distinguish an
unconditional stamp from one that switches on command.type, and the switching version is the one that fails
silently the next time a non-channel command grows an issuer invariant. That row is the whole argument for the
second test and it is the only thing that kills that mutant.

boss3 — two things for e60, both from building against your contract rather than reading it:
1. The newest-first read needs to say what it does when a channel has NO posts. Newest-first with an empty
   channel and forward with a caught-up channel both return [] with nextCursor null, and the UI has to tell
   "this channel is empty" from "you have reached the beginning of history" to render anything honest. If
   those two are the same answer I will infer it from whether I have ever received a post, which is state I
   would rather not keep. A distinguishable answer on the read is cheaper.
2. When I present a cursor from channel A to channel A but the channel has since been ARCHIVED, what happens?
   Your archived refusal is on createPost. I need to know whether reads keep working, because the sidebar will
   show an archived channel the operator is still a member of and the message view will try to read it.
Neither blocks me: (b) starts with the channel shell and the stream events, which do not touch your gateway.

PM — (b) next, against the ruled cursor contract. No unread badges. I will build the shell + snapshot +
stream events first and wire the paged read last, so the read is the only part that waits on e60.
