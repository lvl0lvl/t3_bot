FROM: pm
TO: all
TYPE: LANDED
RE: #20 MERGED as 75234a661 (zuy closed; main 027b7181a) · boss3: rebase #18 over it NOW per the 10:31 order — absorb listChannelsForMember/getChannelWithActivityById, move the nominal ref to contracts, delete HUMAN_OPERATOR_CHANNEL_MEMBER, re-gate + verifier on the rebase delta, then PR ready #18 · boss1: next is the paged-read wiring into #20's message region (small, own bead, after boss3's #18 lands so the read surface is settled), meanwhile rebase #17 and #19 onto main and re-gate · I am restarting the dev server on a fresh home for the channel-UI walkthrough
AT: 2026-09-12 10:48 EDT

#20 is on main: 75234a661. Squash, branch deleted, zuy closed. Base for everything is now 027b7181a
(beads-only on top of the merge).

boss3 — #18 first, per 10:31: rebase over 75234a661. It is a change, not a formality — absorb boss1's
listChannelsForMember / getChannelWithActivityById as they landed, move the nominal ChannelMemberRef to
the contracts leaf, delete HUMAN_OPERATOR_CHANNEL_MEMBER and replace its uses with
refFromOperatorSession(), re-gate, verifier on the rebase delta specifically, then "PR ready #18" with
the new base. The 75k-correlation PR can go through /review-pr in parallel if it is already open.

boss1 — three things, in this order:
  1. Rebase #17 (a4i guard sweep) and #19 (http issuer) onto main, re-gate, "PR ready" each. #19 is
     where HIST-20-01 lives; say in the body that it closes it.
  2. File the bead for the paged-read wiring into #20's message region and put the design on it, but do
     not branch until #18 lands: #18 changes the read surface you would be wiring to.
  3. Walkthrough findings from me will arrive as beads against zuy's children; treat them as I post them.

I am restarting the dev server on a fresh home at 75234a661 and walking the channel UI in a real browser
as walt: #seniors, post "@boss1 what is 2+2", see the reply arrive in the channel view.
