FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-3os · PR #75 open at acbc3b0df6 · sweep exit 0 (17/17, survivor now killed by 1), count gate exit 0 (57→58, nothing lost) · CI starts now — a branch push runs nothing, fork-ci triggers on pull_request, which I read rather than assumed · /review-pr next
AT: 2026-09-14 22:58 EDT

PR #75: "The colliding-roster sweep runs again, and the guard it found unpinned has a test".

GATES SO FAR:
  guard sweep .... exit 0 at acbc3b0df6. 358-test baseline, 17 rows, 17 killed, 0 survivors, 0 NOT
                   RUN. wake-filter-ignores-kind killed by 1 — the fixture written for it.
  count gate ..... exit 0 vs origin/main@f520f03c0. 14 workspaces, no test lost by count or by name.
                   MentionWakeReactor.test.ts 57 -> 58, which is the one fixture added.
  base drift ..... re-measured for the NEW subject when main moved under the gate (f520f03c09 vs
                   e21cbc39ce): CLAUDE.md only, test-file blob md5 identical. Gate stands, no rebase.
                   Note the hash differs from the measurement I took on #74 — correct, since that was
                   a different base pair and would not have been portable even had I reused it.
  CI ............. NOT YET RUN, and I checked WHY rather than reporting an empty result as a problem:
                   `.github/workflows/fork-ci.yml` triggers on `pull_request` and on pushes to `main`
                   only, so a branch push runs nothing. The PR now exists, so it starts here. I will
                   read it per-SHA from check-runs.

THE BODY carries the sequence you asked for in my words — config could not run, re-anchored on the
guard line alone, gate ran and reported a SURVIVOR which is the gate working, diagnosed to #68
narrowing the guard's protection onto a legacy branch nothing exercised, fixture was VACUOUS and
passed under its own mutant, caught by a positive control, root-caused, rebuilt — plus the three-state
table (clean/mutant/control), the docstring admission that the helper already told me and I read it
anyway, and the 2ij correction stating that the pre-flight does NOT close the class.

EVERY FACTUAL CLAIM IN THE BODY TRACES TO A MEASUREMENT, per the checklist line. The one I had NOT
measured when I drafted it was "the decider stamps mentionRefs on every post" — the reason no fixture
exercises the legacy branch. I checked before publishing: decider.ts:2376 computes it and :2407 writes
it, unconditionally on channel.post-created. It is now a cited fact rather than an explanation I found
plausible.

NEXT: /review-pr. Six lanes on a diff of two anchors, one fixture and one docstring — the roster
script will say whether design fires again on the .ts extension (t3_bot-c6t), and if it does I will
record it as not-dispatched with measurements the way #74 did rather than manufacture a lane.
Heavy slot free between now and the review's own gates.
