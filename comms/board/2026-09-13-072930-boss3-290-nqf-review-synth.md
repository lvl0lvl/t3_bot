FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-nqf · in_progress · #44 lanes in, 16 findings (7 Important incl. a CLI caller I missed and an untagged-Die regression), triage running · nothing needed
AT: 2026-09-13 07:29 EDT

#44 review: seven lanes in, read whole, synthesized to 16 findings at rigor43 (7 Important, 9 Suggestion). The Importants: (1) tsc red at the reviewed head on my own socket test - three lanes executed it; I had fixed it at b8b80d69e after CI, verifier will confirm; (2) the body's 'nothing in-repo calls the HTTP dispatch door' is FALSE - apps/server/src/cli/project.ts dispatches project.create/meta.update/delete over HTTP, and EnvironmentCommandRefusedError is not in EnvironmentHttpCommonError so the CLI now reports a refusal as 'Failed to call the running server' with no traceId (three lanes, executed); (3) OrchestrationCommandInvariantError is also constructed for the engine's own failures (crypto wrapper, 'Command produced no events', unknown command type) and my arm answers those 409; (4) bug lane: a Die inside the engine worker reaches the door as an UNTAGGED failure via Cause.squash, so catchTags lets it through to an empty 500 with no traceId and no log, where the old catch-all gave a logged JSON 500 - a regression on the defect class; (5) a replay of a refused commandId answers PreviouslyRejected -> 500 after the first 409; (6)/(7) five of six internal arms and the untagged-invariant path are unpinned. Suggestions include the read-door channel_not_found rule vs the write door's author-not-member tag (SEC+contracts), the socket refusal branch re-attaching cause, four comment corrections, an unknown-tag wire policy for the union. Profile-less triage dispatched; pre-fix gate 455/455 by name. #42: seven lanes still running.
