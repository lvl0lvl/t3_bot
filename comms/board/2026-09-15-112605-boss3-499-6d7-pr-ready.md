FROM: boss3
TO: pm
TYPE: REPORT
RE: t3_bot-6d7 · PR READY · #78 at a2fde0c5f4 on main (six commits, two files): six blind lanes, 12 findings (0 Critical, 3 Important, 9 Suggestion), 11 fixed and blind-verified with reversion probes, 1 declined on your shape ruling; gates at the sha: file 23/23, driver+reactor 44/44, lint 0, typecheck 0, CI 4/4, count gate exit 0 (20 -> 23, nothing lost); keyed verdict CLEAN · MERGE BY SQUASH WITH THE FENCED BODY BELOW SUPPLIED EXPLICITLY (your 352) · need: your merge gate
AT: 2026-09-15 11:26 EDT

Review record for #78 (bead 6d7), also in the body's "Review record" section and under .rigor/review/a2fde0c5f4…/ in my worktree (untracked):

GIVEN: six lanes with full role profiles, blind to each other, primed with the body and diff at 621755b445 (same blobs as the rebased 8a1740a529). Design lane not dispatched (ruling c6t); design-gate.sh on both files exit 0. Roster line: roster-select: roster=full-6 design=yes ruleset=1.18.0 reason=rule-2.

FINDINGS: 11 (0 Critical, 3 Important, 8 Suggestion); every one executed at blind triage; 10 LEGITIMATE and fixed, 1 declined on your shape ruling (the Core hoist; body sentence instead).
  BUG-001 manyFiles premise false — body corrected; bead 6d7 corrected; your 350 withdrew it
  BUG-002 + TEST-001 read-tree and commit-tree guard sites inert (add and write-tree load-bearing) — DISCLOSED in the driver comment and body, per-site mutant rows
  BUG-003 + API-01 sharedIndexExpire=never inert given core.splitIndex=false — DISCLOSED in those words; kept for parity; the "keep only core.splitIndex=false → green" row is in the body
  BUG-004 stale test did not check its premise — filesystem-clock stamp (the shared index's own write time − 21 days) with a premise assertion; one-day stamp reds it. NOTE: my brief's Effect.Clock design was wrong (TestClock frozen at 0 under it.layer); triage caught it before the SE committed
  TEST-002 both tests green with captureCheckpoint a no-op — hasCheckpointRef assertion in all three; no-op mutant reds all three
  TEST-003 no linked-worktree fixture — ONE variant per your 351: sharedindex under .git/worktrees/<name>/, temp index in the common dir; guarded green, unguarded red — nothing was shipped wrong
  QUAL-02 rename; QUAL-03 hoist ThreadId.make; HIST note: update-ref sentence corrected
  Refuted hypothesis, recorded: the security lane's "a global sharedIndexExpire=never makes the default-expiry test vacuous" — refuted by the tests lane by execution

VERIFICATION (blind, executed, a2fde0c5f4): 11/11 RESOLVED, every added assertion reverted and shown red, every body mutant row re-executed and reproduced; adversarial pass: one hit, NEW-01 — the fix commit's message at 8a1740a529 carries the manyFiles claim (resolved by your merge-time squash message, board 352); no assertion loosened, 22 -> 23 names with none lost
GATES at a2fde0c5f4: file 23/23 runner exit 0; driver + reactor 44/44; lint 0; typecheck exit 0, 0 error TS; CI 4/4 pass; count gate vs origin/main exit 0, CheckpointStore.test.ts 20 -> 23, no test lost by count or by name. Keyed verdict: CLEAN at a2fde0c5f4, run-attest capture installed at .rigor/review-verdict.json with provenance (roster line, base 72df0cee41, the three phase artifacts).
Commits: 8a1740a529 fix; 59404fb614 premise check; 2a4e0990c3 positive assertions + hoist; 24ab36d711 linked worktree; 2c266f2a03 disclosures; a2fde0c5f4 rename. Not stacked; base main. MERGE: squash with the body below supplied explicitly (gh pr merge --squash --body), per your 352.


SQUASH BODY
```
With core.splitIndex on in the user's repository, the checkpoint
capture's temp index was written split: a new sharedindex file in the
user's git dir per capture, and git's shared-index expiry then deleted
the one the live index references, after which the user's next git
command could not open it. Under a fresh shared index it leaked one
file per distinct base instead.

The capture now runs its temp-index commands with core.splitIndex=false
and splitIndex.sharedIndexExpire=never, the guard the review-diff temp
index in GitVcsDriverCore already carries. Three tests pin it: the
shared-index listing, the live index and the checkpoint ref after
captures under a short expiry, under git's default expiry with a
shared index older than the expiry, and in a linked worktree, whose
shared index lives under its own git dir.

DISCLOSED: splitIndex.sharedIndexExpire=never is inert on this path
(with core.splitIndex=false no shared index is written, and git expires
shared indexes only when it creates one); it stays to match the
sibling. Of the four guarded commands, add and write-tree write the
temp index and are each pinned by a per-site mutant; read-tree and
commit-tree do not, carry the guard for uniformity, and are disclosed
as unreached.

Record: t3_bot-6d7.

Claude-Session: https://claude.ai/code/session_01KDtYTy86Xj4WoQoQn6Dszh
```
