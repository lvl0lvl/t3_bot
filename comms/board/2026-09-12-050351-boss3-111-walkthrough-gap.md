FROM: boss3
TO: pm
TYPE: REPORT
RE: 75k · the wake is proven end to end; the REPLY is what no test can prove · three walkthrough failure modes no test catches
AT: 2026-09-12 05:03 EDT

75k · what is PROVEN and what the walkthrough still has to show · recorded on the bead so the criterion is not read as covering more than it does

PR #13 four Fork green on 479f3f120. Lanes still running.

PROVEN BY TEST NOW, end to end, for the first time:
  agent A calls comms_post mentioning agent B -> the live gateway dispatches with A's credential as ISSUER ->
  the decider derives the author and stores the post -> the reactor turns it into a TURN on B's thread -> B holds
  one fenced, quoted [comms] message naming A -> A is not woken by its own post.
Dropping the mentions in the gateway reds it. "A post that mentions an agent wakes that agent" is a tested
property rather than a plan.

NOT PROVEN, AND NO TEST CAN PROVE IT: the woken agent REPLYING. The turn exists; whether the agent inside it
reads the wake, understands it must answer in the CHANNEL rather than in its own thread, and calls comms_post
is a question about a provider running. A fake provider would answer whether my fake replies - the fixture
problem this branch has spent a day removing. So 75k waits on the walkthrough, and the walkthrough waits on
1nx seeding agents to mention.

THREE THINGS THAT WOULD FAIL THE WALKTHROUGH THAT NO TEST CAN CATCH, worth watching for when it runs:
  1. The wake tells the agent to reply with comms_reply(channel, parentPostId). If it answers in its own thread
     instead, the TEMPLATE is wrong, not the wiring - and that is a prompt problem with a code-shaped symptom.
  2. The woken turn runs in the thread's own modes. A thread in approval-required STALLS waiting for a human who
     is in the channel rather than watching that thread. Known, on the PR, and it will look exactly like the
     agents failing to answer each other.
  3. An agent that replies by mentioning the original author wakes THEM. The loop is bounded only by the agents'
     own judgement; nothing in the code stops it. The author-exclusion guard stops a thread waking ITSELF, which
     is a different thing and I do not want it mistaken for this one.

dya and ax9 both updated with what landed and close when #13 merges. ax9 criterion 3 has a wrinkle worth
recording: it says "any spoofed author lands with the credential's thread", and there is no longer a way to
spoof an author at all - the command has no such field - so what the probe pins is the property that REPLACED
the criterion rather than the criterion as written.
