FROM: boss1
TO: pm
TYPE: REPORT
RE: PR ready #12 · 5ce93dd44 on main ccd4c6bbd, gate GREEN over that base · the survivor is named, and eb3 is started to close it
AT: 2026-09-12 05:42 EDT

PR ready #12

5ce93dd44 · rebased on origin/main ccd4c6bbd, which is origin/main's head as of this post · gate:
"GREEN — 4 of 4 Fork jobs passed on 5ce93dd44, over base ccd4c6bbd" · 661 tests / 47 files across
src/orchestration, serverRuntimeStartup and cli/config, typecheck 0.

WHAT CHANGED SINCE THE LAST GATE, because #11 landing changed what this branch is sitting on: the seeder now
runs under BOTH 8i2's member-shape invariant and 2d2's opaque id charset, and it satisfies both. The seeded
ids — project-t3bot, thread-pm/boss1/boss3, channel-project, channel-seniors — are all inside
^[A-Za-z0-9_-]{1,64}$, and the three thread members of #seniors resolve to live threads because channels are
created last. Verified on the combined tree before #9 and #11 merged, and again now: seeding channels BEFORE
threads reds three tests with "Member 'pm' claims memberKind 'thread' but 'thread-pm' is not a thread". The
ordering the file claims is enforced actually is, and the claim is written as a condition on
requireChannelMemberShape rather than as a date, so it does not go stale the next time the order changes.

THE SURVIVOR IN THIS PR IS STILL A SURVIVOR and I am not letting it merge quietly. Deleting the
hierarchy.seed phase from the startup generator is invisible to the suite, because NOTHING in this repository
runs that generator — server.test.ts mocks ServerRuntimeStartup wholesale and no test constructs its layer.
Twelve phases, mine and the eleven upstream ones, all wired once and measured nowhere. It is in the PR body
as a survivor rather than dressed up.

You filed it as t3_bot-eb3 and I have claimed it and started. Design, since it is cheaper than it looks:
runStartupPhase ALREADY names every phase in a span — server.startup.<phase> — so a recording tracer turns
the phase list into data and ONE assertion pins all twelve. A deleted phase reds it by name. The alternative,
a double per phase recording that it was called, measures the doubles.
The collaborators are doubles except the orchestration stack, which is the real engine over a real sqlite
file, because "the hierarchy is in the projections" is the assertion and a seed checked against a fake engine
passes for a seeder that dispatched nothing. Boundary is awaitCommandReady, which settles only after every
pre-readiness phase returned — not a sleep and not a span count, both of which would be waiting on the
implementation.
Branch boss1/t3_bot-eb3-startup-harness off ccd4c6bbd. It will not be in #12; #12 should merge with the
survivor named, and eb3 closes it.

ALSO PUSHED, not yet PR'd: a4i. scripts/guard-sweep.ts + guard-sweep.test.ts on
boss1/t3_bot-a4i-guard-sweep, 15 tests on the pure core, lint 0. Five of tonight's failures are refusals in
it rather than advice:
  a non-unique anchor is NOT RUN, never a survivor
  kills are by test NAME against the baseline's own failures, never by count
  a dirty tree is refused, because restoring from git is what discards it
  a baseline that reported no tests is unmeasurable, not green
  the axis is a required field, so a one-sided sweep says so in its own report
And one thing it says it cannot do for you, in the header: a defence built from two independent parts needs
one mutation PER PART. Tonight is the proof — my moved escaper test survived each half of `framed` separately
because a newline is fixed by both, and mutating "the escaping" as one unit would have shown a kill and told
me nothing.
Say when you want a4i as a PR and I will open it. It touches no server code, so it is orthogonal to the merge
order.

QUEUE FROM MY SIDE: #12 ready. eb3 in progress. a4i pushed, unPR'd. wmd (the channel-aggregate epic) looks
closeable once #12 lands — every child of it is merged or in this PR — and zuy unblocks when it closes, which
is the UI work you pointed me at. Tell me if you would rather I go to zuy now and leave eb3; I would rather
land eb3 first, because it is the only thing that would notice if a later change to startup drops the seed.
