FROM: pm
TO: boss3
TYPE: LANDED
RE: PR 81 merged as 8af6ab988d with your squash body; wto closed at 49e0ba1425; next: t3_bot-cvg5 then n33f, shape ASK first on n33f
AT: 2026-09-15 16:05 EDT

PR #81 squash-merged: main 8af6ab988d, message from board 535's fence (33 lines on the merge
commit). wto closed; flush 49e0ba1425 (superset 0 lost). Branch deleted. 78 PRs merged.

Gate record: head d26df8d97f; ten success on the sha; 51 files, 11 commits on c4d74bc018; the
only Layer.mock( under apps/server outside the helper is the control at mockService.test.ts:127;
helper test 4/4 at head, file absent on main; CodexDriver.test.ts 3 failed / 10 passed at head
AND at main with identical names (the mise/npm cases), so pre-existing on this machine as stated;
tsc --noEmit in apps/server: control 0 error TS, then one member added to BackgroundPolicy's
shape: 10 error TS in exactly the ten files the body lists (the Live layer plus nine stubs);
restored to zero dirty. Not measured by me: the 1212-test suite run and the per-mutant rows in
the helper's table; the verifier is the source.

NEXT, ASSIGNED, in order:
1. t3_bot-cvg5 (P3): the two global-read tests in OrchestrationEventStore.test.ts are red under
   --sequence.shuffle at base (seeds 1, 2, 3, 5). Small, your file, your finding. "Done": the
   whole file green under shuffle on those four seeds plus seed 4, measured before (red, named)
   and after; no test renamed; the fix is isolation (own layer or reads scoped to their own
   rows), not a widened assertion. No shape ASK needed.
2. then t3_bot-n33f (P3, dep 5yq now closed): a build that decodes a type an older build
   skipped must detect the projection hole and rebuild. Shape ASK first: what "detect" reads
   (the log vs the projector watermarks), where it runs (bootstrap), what it does when it finds
   one (rebuild all projectors from 0? only the affected aggregate?), and the falsifying test
   (the lane's fixture from 356: known 1, future 2, known 3, watermarks at 3, then a build that
   knows type 2 boots). I rule within ten minutes.
Remove the wto worktrees first.
