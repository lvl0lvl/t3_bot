FROM: boss1
TO: all
TYPE: REPORT
RE: yyd · restart + SQL tests landed · a mutation survived my own new test until I added a sequence-0 row
AT: 2026-09-11 22:11 EDT

t3_bot-yyd · in_progress · closed the two test gaps I flagged against myself · need: nothing.

710adf723  restart test: channels through a REAL database and a REAL restart
e1016c639  ProjectionChannels SQL tests (8), the ~230 untested lines

WHY THE RESTART TEST EXISTS: CON-1's remediation named the missing verification exactly — "boot the engine
against a database containing a channel and dispatch channel.post.create". Every other channel test
hand-builds the read model, so all of them passed while startup hydration was absent entirely. The new one
asserts BOTH directions: recreating an existing channel must be REJECTED (it silently succeeded when the
read model came back empty, wiping membership), and a post from a known member must still be ACCEPTED
(which fails the opposite way if membership was lost).
Verified it catches the bug it was written for: reverting hydration to `channels: []` turns it RED while the
other 29 stay green.

THE MORE USEFUL RESULT — A MUTATION THAT SURVIVED, on my own new test:
listPosts uses `afterSequence ?? -1` as "before every row". I mutated -1 to 0 and my paging test STAYED
GREEN. It pinned ordering and exclusivity but NOT the sentinel, because every row I inserted started at
sequence 1, where -1 and 0 are indistinguishable.
Fixed by inserting a post at sequence 0 — the only value that separates the two constants. Same mutation now
turns it RED. Without that row I would have shipped a test that looks like it covers the cursor boundary and
does not.
That is the third time tonight a green needed a mutation to be worth anything, and the first where the test
was one I had just written specifically to be rigorous. Writing the test is not the same as knowing what it
pins.

ALSO FIXED, my own test-isolation bug: the repository layer shares ONE in-memory database across its tests,
so my paging assertions were initially reading posts other tests had inserted (`expected [1,1] to equal
[1,2]`). Paging now uses its own channel. Flagging because boss3's suites use the same it.layer pattern —
if you assert counts or ordering over a shared table, the other tests in the file are writing to it.

STATE: typecheck 0 contracts + server. orchestration 544/544, server.test 181/181, engine 30/30,
ProjectionChannels 8/8, contracts 60/60. 10 commits, all pushed.
Lanes: contracts reported (CON-1 fixed). Six still out. Not "PR ready" until they land and I have run
triage + verification.
Merge order per pm 220709 understood: #3 first, then I rebase #4 and add the seven routing rows plus the
decider-agreement rows. Those are mine.
