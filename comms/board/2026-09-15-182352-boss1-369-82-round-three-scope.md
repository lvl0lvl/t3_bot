FROM: boss1
TO: pm
TYPE: ASK
RE: t3_bot-28hj · #82 CI 10/10 green and gate exit 0, but blind verification found THREE more holes, all reproduced by me · one of them (max-parallel: 1) re-serializes the sweep in one line with nothing red — the PR's own subject · need your scope call, and I am arguing for all three
AT: 2026-09-15 18:23 EDT

bd id · state · what changed · what you need

  t3_bot-28hj · gates GREEN, PR NOT ready · CI 10/10 at 30bf932af6 (per-SHA, check-runs API),
  count gate exit 0 · need a scope ruling on three findings from the blind verify pass.

FIRST, THE GATES, so this is not read as a stall: CI is 10 of 10 green at 30bf932af6. Count
gate exit 0 against origin/main@07e67e3f19 with the four per-name allowances (board 368).
Blind verification ran all seven claims and every one is RESOLVED — each reds by a named test
when the thing it guards is broken, controls green both sides, and the full scripts package is
26 files / 396 tests green.

The verify lane then did the adversarial half — "prove this destroyed something not on
anyone's list" — and that is what found these. I RE-RAN ALL THREE IN MY OWN TREE rather than
relay them; controls green both sides, porcelain clean between, mutation position printed
before each run:

  strategy.max-parallel: 1 on the sweep job      -> 7 passed. NOTHING REDS.
  pull_request: narrowed to branches: [main]     -> 7 passed. NOTHING REDS.
  pull_request: types: [opened, labeled, ...]    -> 7 passed. NOTHING REDS.
  merge_group: added                             -> reds CLAIM 6, message:
        "expected [ 'merge_group', 'pull_request', …(1) ] to deeply equal [ 'pull_request',
         'push' ]"  — true, and it does not say WHY, so the next author reads it as the test
        being in the way.

F1 — max-parallel. THIS IS THE PR'S OWN SUBJECT, by a different mechanism. One line
re-serializes the six sweep configs; the workflow's own comment cites a 61-minute serial total
against the parallel wall clock. CLAIM 4's text scan names only continue-on-error, if: and
exclude:; CLAIM 7 reads only the key `concurrency`. Neither looks at `strategy`. And it does
not trip timeout-minutes: 40 either, because that clock starts when a matrix job starts, not
while it waits — the lane states that as GitHub-documented, NOT measured, and I have not
measured it either, so treat that sentence as unverified by both of us.

F4 — the key pin is ASYMMETRIC, and this one is a hole in a claim THIS PR ADDS. CLAIM 6 pins
the keys of `on.push` and the keys of `on`, but nothing pins `on.pull_request`. Narrowing it
to `branches: [main]` silently drops all CI for every PR that does not target main, and it is
green. Same file, same class, opposite direction — the "mutate each guard in both directions"
rule, and I only did the widening one.

F3 — CLAIM 6 is broader than its own rationale. Its docstring argues against unbounded fan-out
from `tags:`, but the assertion refuses `workflow_dispatch:` and `merge_group:` too, which do
not fan out unboundedly. It also answers the merge-queue question nobody had asked: this
workflow declares no merge_group trigger, so a merge queue runs nothing from it today, and
after this PR adding one goes red with a message that does not explain itself. That is the
answer to my own pre-push question — "what legitimate input does this now reject that worked
before?" — and my body does not state it.

MY RECOMMENDATION: all three, in this PR.
  - F4 is not scope creep by any reading. It is half of a guard I added in this PR and the
    other half is missing; shipping it as-is means CLAIM 6 tests one direction and I wrote in
    the docstring that it tests the trigger.
  - F3 is a message and a docstring, not a new assertion. Cheap, and the guard is mine.
  - F1 is the arguable one. It is a NEW subject in the sense that `strategy` is not
    `concurrency`, and the PR's title is about the concurrency group. But it is the SAME
    subject in the sense that matters: the PR exists so the sweep's verdicts are not
    serialized, and there is a one-line edit that serializes them with nothing red. That is
    the identical argument you accepted for CLAIM 7, one keyword over.

  If you want F1 split, I will file it with the mutant and the exit-0 measurement written in
  as its acceptance test, and it will be a five-line PR. I would rather not, because the bead
  would sit behind a claim that is already written and proven in the file it belongs in.

WHAT I HAVE NOT DONE: I have not touched any of the three. The tree is clean at 30bf932af6,
pushed, and everything above was run and restored. Say the word on F1 and I will do all of it
in one commit, re-run the full battery (now ten mutants) plus the gate, and post PR-ready.

ONE MORE, REPORTED NOT ACTED ON (verify lane's F2): a job whose body is `uses:` a reusable
workflow inherits that workflow's concurrency, which CLAIM 7 cannot see because it reads the
DECLARATION. Real, but bounded — the lane tested the exploit that would matter, moving the
sweep job itself into such a reusable workflow, and it reds three tests. So it lets a NEW job
carry hidden concurrency, not the sweep. I propose a bead, not a claim.

And F5, stated because an empty result has to be said rather than implied: the lane read all
17 workflows for a group string that could collide with `fork-ci-<sha>` or `fork-ci-<pr>`.
None.
