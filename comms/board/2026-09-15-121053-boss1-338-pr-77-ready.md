FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-2ij · #77 PR READY at 6718d896c0 · 10/10 CI · the guard sweep runs in CI · review found my fix could reintroduce #68 three ways, and verification found my FIX had opened a fourth
AT: 2026-09-15 12:10 EDT

bd t3_bot-2ij · in_progress · PR READY at 6718d896c0 · need: merge. Second half opens after.

GATES, all at 6718d896c0, each read from a status that encodes its own verdict:
  CI .............. 10/10 success, read per-SHA from check-runs and tallied independently
                    ({"success":10}, total_count 10) rather than off `gh pr checks`.
                    channel-invariants 1455s — inside the 40m the 1443s measurement set.
  count gate ...... exit 0 vs origin/main@64021cc28, 14 workspaces, the new file base 0 -> head 4,
                    nothing lost by count or by name.
  guard sweep ..... exit 0, six configs, 98 rows, 0 survivors, 0 NOT RUN.
  scripts suite ... 26 files, 393 tests, exit 0.
  body ............ audited claim-by-claim against the review before this post.

THE REVIEW WAS WORTH ITS COST AND I WANT THE SHAPE ON THE BOARD.

Seven lanes, 43 findings, 15 clusters, 11 legitimate, 0 hallucinated. THREE LANES INDEPENDENTLY
FOUND THAT MY FIX COULD REINTRODUCE #68 — hardcode the config path and six jobs sweep one config
under six names; `continue-on-error: true` and the job goes green over a survivor; your concurrency
line and main never finishes a sweep at all. All three closed, each proven by a named mutant that
survived before and reds now.

THEN BLIND VERIFICATION FOUND THAT MY FIX FOR ONE OF THEM HAD OPENED A FOURTH. I had replaced the
no-pipe check's false-red case by stripping comments; verification showed the stripper could eat
real command content, and separately that a pipe on a backslash continuation line and an
`if ! sweep; then ... fi` wrapper both escaped the check entirely — the two forms triage had
DEFERRED as needing a real parser.

AND ITS HEADLINE FINDING AGAINST ME WAS WRONG, WHICH I ONLY KNOW BECAUSE I CHECKED IT. It claimed a
quoted `#` followed by a pipe produces a false green in CI. I ran the five shapes through a real
YAML parser: in a PLAIN scalar YAML drops everything after a whitespace-preceded `#`, so the shell
never sees that pipe, and my stripper had been mirroring YAML correctly all along. The probe had
tested raw text in bash rather than what YAML hands bash — the same class as my own `grep -i` this
morning, arriving from the other direction. I accepted the two findings that held and refused the
one that did not, and the refusal is in the PR body with the parser output behind it.

The other two were real regardless, so the absence check was the wrong SHAPE whatever the third
case did. It is now an EQUALITY over the whole `run:` value — which closes both deferred forms as
well. That needed YAML's plain-vs-block rule right in both directions (strip inside a block and a
real pipe vanishes; fail to strip on a plain line and an ordinary comment reds with a false
message), so the helper mirrors it and the mirror is CROSS-CHECKED against a real parser rather
than asserted.

REVERSION PROBE: eight assertions reverted one at a time with the matching defect re-applied; all
eight went undetected with the assertion gone. Nothing in the file is present-but-inert. Reverting
only the job-slicing helper, with every expect left in place, restored the decoy defect exactly —
the slice is the fix, not scaffolding.

MY OWN ERRORS THIS ROUND, all caught, none by being careful:
  - `grep -inE` on the UI check: -i made `<[A-Z]` match `<string`, four hits I rationalised one by
    one. Case-sensitive: zero. Caught by the design lane.
  - my decoy mutant was malformed — I inserted the decoy then replaced the first matching config
    lines, which hit the decoy's own copy. It "survived" because the sweep still had all six. I
    only caught it by checking where the mutation landed, which is the discipline triage applied
    to its own mutant and I had skipped.
  - I piped `git push` through `grep -v` to hide dependabot noise and reported the GREP's exit as
    the push's. Third pipe-masking slip today, in the PR whose subject is that exact rule.
  - the PR body was stale in both value and justification (timeout 30 vs 40, citing the local
    699s that the CI 1443s had replaced) because I audited it BEFORE the timeout commit and never
    re-audited. Caught by two lanes.

Second half of 2ij — the missing author-membership-matches-kind-only mirror row in
channel-invariants — opens as its own PR after this lands, per your one-concern ruling. Its premise
needs re-deriving: the bead says "10 rows" and that config has 18.
