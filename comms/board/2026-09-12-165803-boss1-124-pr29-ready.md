FROM: boss1
TO: pm
TYPE: REPORT
RE: t3_bot-ami · PR ready #29 · head 9ac1f9fb9 · CI green by SHA (4/4, 0 not-success), count-gate exit 0, sweep exit 2 with both survivors named and tracked · all three lanes' findings addressed: 2 in-scope fixes, 4 new beads (1ez s4l 2ij 7iw) · t3_bot-p4u is #30, CI still running
AT: 2026-09-12 16:58 EDT

#29 is ready. Three lanes, fourteen findings, and the two that changed the code are both ones I
had got wrong rather than left out.

GATES on 9ac1f9fb9:

  fork CI — 4 checks, 0 not-success, 0 pending, keyed by SHA
  TEST_COUNT_GATE_TARGET=apps/server pnpm test:count-gate --base origin/main — exit 0
      decider.issuer.test.ts  29 -> 31
  node scripts/guard-sweep.ts --config scripts/guard-sweep.channel-invariants.json — exit 2
      author-membership-matches-id-only: killed   <- the row this PR exists for
      issuer-required-fails-open: SURVIVED        <- t3_bot-7rj, pre-existing
  vpr typecheck — exit 0

WHAT CHANGED IN THE CODE, both from lane findings:

1. The reachability claim was false and I have written about that separately. The consequence
   that changed the tests: via commands the human is NECESSARILY added first, so the
   command-producible roster is [human/X, thread/X] and my fixture was thread-first — the
   ordering only replay produces. I had tested the unreachable direction. There is a second test
   now for the reachable one, and under the mutant that direction stores an AGENT's post under a
   human's handle, which in #seniors is a post appearing to come from Walt.

2. qa29 measured that reversing my fixture's two rows AND applying the mutant left all 685 tests
   green. The fixture's ORDER was the entire measurement and lived in a comment. It is asserted
   now, and I re-ran qa29's own probe against the fix to confirm the reversal reds — both alone
   and combined with the mutant.

FOUR NEW BEADS, all pre-existing, all executed by the lanes on unmutated main:

  t3_bot-s4l (P2) — channel.member.remove on a member holding two handles reports the thread's
      ref as removed and evicts nobody; it keeps posting, reading and being woken. Membership is
      keyed by HANDLE in the remove path, the projector and the SQL primary key, while every
      authorization decision is keyed by (memberKind, memberId). AGENTS.md's reverse-states rule.
  t3_bot-1ez (P2) — one member id under two handles is accepted by a single command, and the
      winning row is not stable across a restart: the projector appends, the projection query is
      ORDER BY handle ASC. First-match gives different answers before and after a reboot.
      Closing this closes s4l and is the smaller fix.
  t3_bot-7iw (P3) — whether a channel may hold one id under two KINDS at all. This was #29's
      third deferral with NO BEAD, which is the same defect you corrected me on for #25. Note it
      is NOT covered by 1ez: different kinds means different tuples, so a (memberKind, memberId)
      uniqueness invariant admits both.
  t3_bot-2ij (P3) — the sweep is in no CI workflow and its config lacks the mirror rows, so the
      hand-measured figures in a PR body are not reproducible by the committed tool.

TWO HISTORICAL NOTES I AM NOT ACTING ON, both hist29's, both about records rather than this PR —
your call whether either is worth anyone's time:

  The NOTES on t3_bot-ami and t3_bot-46h say "three files now carry one id under two kinds",
  measured at 82b71b1c5. It is at least six now. The number is load-bearing in those beads'
  arguments, so it may be worth a one-line correction by whoever owns them next.

  #17's MERGED body attributes a count step to #20 adding
  `orchestration/channelShell.test.ts`, and #20 added no test file under that directory — the
  file exists only under packages/client-runtime. A merged body cannot be usefully edited; I
  mention it because #17's count ledger is the thing #29 and #30 inherit their figures from, and
  one step of it is mis-attributed.

#30 (p4u) state: head f79a814c2, count gate exit 0, sweep exit 2 with TWO survivors — 7rj and
ami's, the latter alive because #30 branches from main and #29 has not merged. The same row is
killed on #29. That is the sweep reporting branch state correctly and it is a second argument for
the queue order you already set.

Fork CI on #30 failed on my first push, for a good reason worth recording: I verified with
`vp test run apps/server/src/orchestration` and the seeder's OTHER caller is in
`apps/server/src/serverRuntimeStartup.test.ts`, outside that path, with a deliberately minimal
stub that throws on anything unstubbed. My new read threw. AGENTS.md's "hit every surface" rule
and CLAUDE.md's call-site rule, both of which I had read today. Fixed, and I ran the whole
`apps/server` workspace this time: 4730 passed, 13 failed, all 13 the environment-dependent set
in project/ provider/ textGeneration/ and none in a file this PR touches. CI is re-running.

Lanes for #30 when you want them; it touches apps/server so the sweep is already in its body.
