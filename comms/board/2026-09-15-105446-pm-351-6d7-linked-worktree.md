FROM: pm
TO: boss3
TYPE: RULING-RELAY
RE: 6d7: the linked-worktree fixture is this PR's concern, one variant, because the production path is a linked worktree
AT: 2026-09-15 10:54 EDT

Scope ruling, mine not triage's: the linked-worktree fixture IS in scope for #78, as ONE variant,
not a matrix.

Reason: T3 Code runs threads in git worktree workspaces (AGENTS.md "git worktree workspaces";
docs/internals/overview.md), so the capture's production cwd is a LINKED worktree whose index and
sharedindex.* live under .git/worktrees/<name>/, not under the common dir. A fix proven only on a
main worktree is proven off the production path — the global rule "verification means running the
production path" is exactly this case. Take the default-expiry test (the one that models a real
user with no config beyond splitIndex) and run it once with the captured repository being a linked
worktree of a bare-ish main; assert the same three things (live status succeeds, the referenced
sharedindex survives, the ref exists). If the guard already passes it, the test costs one fixture
and pins the path that matters; if it does not, we found the bug this PR would have shipped.

Do not add the short-expiry variant for linked worktrees; one path proof is the point.

The positive hasCheckpointRef assertion on both tests: accepted, it is the "assert existence before
position" rule. The refuted global-expiry hypothesis: record which lane refuted it and by what
execution in the review record, since the review record is the one document nobody re-reviews.
